# Requirements Archive: v1.2 Emotional Resource Pack Import & Export

**Archived:** 2026-03-18  
**Status:** SHIPPED

For current requirements, start a new milestone and create a fresh `.planning/REQUIREMENTS.md`.

---

# REQUIREMENTS

## Milestone v1.2: Emotional Resource Pack Import & Export

**Goal:** Let teachers and admins exchange structured emotional training resources through JSON and Excel workbook formats without breaking the current local-first resource architecture.

## Completed Requirements

### Pack Format & Export

- [x] **PACK-01**: Admin can export one or more `emotion_scene` / `care_scene` resources as a standard JSON pack that preserves `resourceType`, base resource fields, tags, and normalized `meta_data`.
- [x] **PACK-02**: Admin can download and export an Excel workbook format for emotional resource packs so schools can review or author packs offline without editing database records.

### Import Parsing & Validation

- [x] **IMPT-01**: Admin can upload a JSON pack and preview per-resource validation results before any records are written.
- [x] **IMPT-02**: Admin can upload an Excel workbook for `emotion_scene` and `care_scene`, and the system converts workbook sheets into the current `src/types/emotional.ts` metadata contract.
- [x] **IMPT-03**: System blocks invalid resources from persistence and shows teacher-readable row/resource errors that point to the failing field or nested item.
- [x] **IMPT-04**: Admin can choose a duplicate handling strategy for matching `resourceType + sceneCode` imports (`skip`, `update existing`, or `import as copy`).

### Integration & Compatibility

- [x] **INTEG-01**: Imported emotional resources are persisted through the existing `ResourceAPI` / `sys_training_resource.meta_data` path with no schema migration.
- [x] **INTEG-02**: Newly imported resources appear in current Resource Center editors, emotional scene selector pages, and training runtime without additional repair steps.
- [x] **INTEG-03**: Exported demo/custom emotional resources can complete a JSON round-trip and an Excel round-trip without losing required metadata fields.

## Traceability

| Requirement | Phase | Final Status |
|-------------|-------|--------------|
| PACK-01 | Phase 9 | Completed |
| PACK-02 | Phase 9 | Completed |
| IMPT-01 | Phase 9 | Completed |
| IMPT-02 | Phase 9 | Completed |
| IMPT-03 | Phase 9 | Completed |
| IMPT-04 | Phase 9 | Completed |
| INTEG-01 | Phase 9 | Completed |
| INTEG-02 | Phase 9 | Completed |
| INTEG-03 | Phase 9 | Completed |

## Deferred / Future Follow-ups

- **PACK-03**: Resource packs can bundle local image assets instead of only exchanging metadata and image URL references.
- **PACK-04**: Resource pack tooling expands from `emotional` into a generic cross-module import/export framework.
- **PACK-05**: Schools can merge packs with richer conflict resolution, change history, and audit logs.

## Notes

- v1.2 deliberately stayed inside the current emotional module surface area.
- v1.2 did not expand into schema rewrite, import-job tables, cloud sync, or binary asset packaging.
