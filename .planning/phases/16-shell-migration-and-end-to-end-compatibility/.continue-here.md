---
phase: 16-shell-migration-and-end-to-end-compatibility
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-30T10:51:58.5997100+09:00
---

<current_state>
Formal Phase 16 plan work is already complete and milestone v1.6 remains locally closed.
Current paused work happened after closeout: training-record hard-cut cleanup was tightened further, a main-process atomic-write queue was added, a few console warnings were cleaned, and resource-center redesign requirements were analyzed but not yet implemented.

There is no newer formal phase directory yet. Treat this handoff as a post-closeout continuation note attached to the latest active phase directory for GSD compatibility.
</current_state>

<completed_work>

- Task 16-01-01: Done previously
- Task 16-01-02: Done previously
- Task 16-01-03: Done previously
- Post-closeout follow-up:
  - removed unused legacy record pages (`src/views/games/TrainingRecords.vue`, `src/views/games/SensoryTrainingRecords.vue`, `src/views/emotional/Records.vue`)
  - redirected `/emotional/records` into unified training records
  - changed unified emotional record detail jump to `/emotional/session-summary` with `trainingRecordId`
  - hard-cut active record list queries to require `entry_code`
  - added `scripts/hard-cut-training-records.cjs`
  - serialized `save-database-atomic` by database path in `electron/main.mjs`
  - fixed `src/database/init.ts` raw-db table inspection warning
  - fixed `src/views/admin/ClassManagement.vue` invalid `ElTag` warning
  - verified `node scripts/hard-cut-training-records.cjs --dry-run`
  - verified `npm run type-check`
- Post-closeout analysis:
  - training-resource business-group redesign requirements were decomposed
  - teaching-material delivery/import redesign requirements were decomposed
</completed_work>

<remaining_work>

- No remaining implementation work inside formal Phase 16 tasks
- Next unimplemented slice:
  - redesign resource-center training resources around UI business groups while keeping true `moduleCode`
  - stop old teaching-material seed auto-repopulation
  - clear legacy teaching-material data
  - rebuild teaching-material batch import for external customer file delivery + CSV
</remaining_work>

<decisions_made>

- Post-closeout training-record cleanup should stay hard-cut; do not revive old module-based compatibility UI
- Active record list flows should only read entry-backed rows
- `emotion_scene / care_scene` can be shown as `情绪行为 / 游戏` in resource-center UI, but top-level authorization and real `moduleCode` must remain unchanged
- Teaching materials should remain a separate library from training resources
- Teaching-material files should not be shipped inside the Electron install directory
- Customer teaching-material import should be based on separately delivered files + CSV
</decisions_made>

<blockers>
- No active blocker on the current committed code
- Before deleting old teaching-material rows, `init.ts` -> `resource-data.ts` seed injection must be disabled or the old 211 rows will come back on re-init
</blockers>

<context>
The current repository has moved beyond the formal v1.6 emotional closeout. The latest meaningful work is repository-level cleanup plus prep for the next slice. The right next move is not to reopen emotional engine refactor work; it is to start a new focused implementation pass for resource center.

Think in two layers:
1. training resources = `sys_training_resource` + UI business grouping
2. teaching materials = separate externally imported library with authorization-aware dimensions
</context>

<next_action>
Start with disabling old teaching-material seed auto-repopulation in `src/database/init.ts` / `src/database/resource-data.ts`, then redesign teaching-material import around external file delivery + CSV before touching the visible resource-center filters.
</next_action>
