# 审计过程与命令记录

审计基线 Commit：`9c44ae62ce0989b4c78fc30fafeb02004bb358b9`  
审计分支：`main`  
工作区状态：脏工作区  
审计时间：`2026-07-17 11:58:37 +08:00`  
是否包含未提交内容：是

## 一、执行边界

- 只读取源码、文档、配置、测试与 Git 元数据。
- 未修改业务源码、配置文件或数据库结构；未运行会改写业务数据的性能脚本。
- 未安装依赖，未访问生产系统，未上传源码到外部服务。
- 仅在 `doc/tendering/source-audit-v1.0/` 新建本任务要求的 10 个 Markdown 文件。
- 未创建 commit，未 push，未删除或移动原有文件。

## 二、基线与扫描范围

| 项目 | 结果 |
|---|---|
| 工作目录/仓库根 | `E:\VSC\H5\SIC-ADS` |
| Branch | `main` |
| HEAD | `9c44ae62ce0989b4c78fc30fafeb02004bb358b9` |
| 最近提交时间 | `2026-07-17T00:05:16+08:00` |
| Stash | 无 |
| 工作区 | 存在修改、删除和未跟踪文件 |
| 主应用 | 单一 Electron/Vue 应用，非 workspace/monorepo |
| 扫描文件 | 约 1,594 个非生成文件 |
| 主要范围 | 根文档、package/lock、src、electron、scripts、tests、public、数据库 schema/迁移、路由、Store、报告、文件与构建配置 |
| 默认排除 | node_modules、dist、build、release、coverage、缓存、.git、大型媒体二进制、第三方库源码 |

未提交内容主要集中于 AI 智能体/技能库、AI 数据 API 与界面，以及部分生活自理和资源相关工作。相关证据在账本标记为 `WORKTREE`；后续按采购人明确要求，AI 助手已采用该工作区证据进入公开参数。

## 三、主要只读命令

以下为命令类别与代表性命令；重复的定向 `rg` 和 `Get-Content` 不逐行展开，以免日志失去可读性。

```powershell
Get-Content -Raw AGENTS.md
Get-Content -Raw docs/planning/2026-03-23-scgp-context-bootstrap.md
Get-Content -Raw README.md
Get-Content -Raw E:\Downloads\Codex_教育信息化招投标专家版Prompt_V1.0.md
git status --short --branch
git stash list
git log --oneline -5
git rev-parse HEAD
git show -s --format=%cI HEAD
rg --files
rg -n "register.*Driver|driverRegistry|DriverRegistry|registerDriver" src
rg -n "TODO|FIXME|DRAFT|简化|模拟|占位|未实现|待实现" src
rg -n "exportTraining|xlsx|CSV|downloadReport|window.print|Packer.toBlob" src/views src/utils
rg -n "roles:|effectiveEntitlements|accessEntitlementsAnyOf" src
rg -n "saveDatabaseAtomic|SQLWrapper|resource://|backup|restore" src electron
```

合规依据使用 Tavily CLI 仅检索公开官方网页，未发送仓库源码：

```powershell
tvly search "中华人民共和国政府采购法实施条例 官方" ...
tvly search "政府采购需求管理办法 财库 2021 22号 官方" ...
tvly search "公平竞争审查条例 国务院令 783号 官方" ...
tvly search "个人信息保护法 未成年人 敏感个人信息 官方" ...
tvly search "未成年人网络保护条例 国务院令 766号 官方" ...
```

## 四、验证结果

| 检查 | 命令 | 结果 |
|---|---|---|
| TypeScript/Vue 类型检查 | `npm run type-check` | 通过，约 27.2 秒 |
| Web 生产构建 | `npm run build:web` | 通过，约 44.1 秒；4,338 modules transformed |
| 主源码契约测试 | `node --test "scripts/tests/*.test.mjs"` | 167 项：166 通过，1 失败 |
| 生活自理退出流 | `npx jiti tests/self-care-task-exit-flow.test.ts` | 通过 |
| 资源文件引用 | `npx jiti tests/resource-file-refs.test.ts` | 通过 |
| 加密字节处理 | `npx jiti tests/crypto-bytes.test.ts` | 通过 |
| entitlement 目录 | `npx jiti tests/entitlement-catalog.test.ts` | 失败：测试期望 `placeholder`，当前实现为 `active` |

主测试唯一失败项为 `plan-list-task-training.test.mjs`：测试仍用旧的静态模板正则匹配 `TASK_TRAINING_RESOURCE_TYPE`，当前页面改为动态选项。源码中仍存在任务资源选择与启动链，但正式验收应执行 UI 用例 AT-10，不能将该失败忽略为“全部测试通过”。

Web 构建的非阻断警告：

- 部分模块同时被静态和动态导入，无法按预期拆分 chunk。
- 若干输出 chunk 超过默认 500 kB 警告阈值。
- 这些警告不等于正式性能结论；仓库仍无正式容量和响应时间压测证据。

## 五、未执行的检查

| 检查 | 未执行原因 |
|---|---|
| Electron 安装器构建/签名 | 本次任务以源码审计为主，成本较高且无签名环境；不影响 Web/typecheck 证据 |
| 安装、卸载、覆盖升级、回滚 | 需要隔离终端、发布物与升级服务器 |
| 自动更新真实下载 | 当前默认源安全性待复核，避免访问或改变外部状态 |
| 正式性能基准 | 仓库性能脚本可能写入/清理测试数据库，且不是正式压测环境 |
| 备份恢复实操 | 会改写测试数据库和资源目录；本轮只做只读审计 |
| 全量评估金标准回归 | 缺少采购人确认的合法题库、常模与金标准附件 |
| 摄像头/音频硬件兼容 | 需要目标硬件和现场环境 |
| 第三方安全/渗透测试 | 超出只读源码审计范围 |
| AI 真实提供方调用 | 本轮审计时缺少获授权测试密钥，且涉及外部数据与费用；后续采购人明确要求不将该项作为 AI 进入公开参数的前置条件 |

## 六、过程异常与纠正

1. 首次 Windows 定向搜索使用 `src/database/*.ts` 通配路径，触发路径语法错误；随后改为目录级 `rg` 与 `--glob`，没有文件写入。
2. 一次读取猜测了不存在的 `src/utils/training-game-registry.ts`；随后通过 `rg --files`/符号搜索定位真实文件，没有副作用。
3. 首轮 Tavily JSON 输出因 Windows GBK 无法编码个别 Unicode 字符失败；随后仅为该只读命令设置 `PYTHONIOENCODING=utf-8` 后成功。
4. 并行测试批次因预期断言失败使外层命令提前报告失败；随后将关键检查分别执行并记录每项真实结果。

按任务边界，上述过程问题未写入仓库既有学习或配置文件，只在本审计日志记录。

## 七、生成文件

1. `00_执行摘要.md`
2. `01_仓库与架构审计.md`
3. `02_产品能力清单.md`
4. `03_源码证据账本.md`
5. `04_软件技术参数及功能要求_公开候选版.md`
6. `05_参数—证据—验收追踪矩阵.md`
7. `06_待确认与功能缺口.md`
8. `07_招标合规与排他性风险审查.md`
9. `08_履约验收测试方案.md`
10. `09_审计过程与命令记录.md`

## 八、源码不修改声明

未修改业务源码；未修改数据库；未修改项目配置；未创建 commit；未 push。构建命令生成/刷新了默认忽略的 `dist/` 产物，未将其纳入审计输出。工作区仍包含任务开始前已有的未提交内容，本任务未尝试清理、覆盖或暂存这些内容。
