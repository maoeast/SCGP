为了让 Claude Code 完美消化这些文件，建议您**先将这 4 个文件放入您项目根目录下的一个临时文件夹中**（例如新建一个名为 `_docs` 或 `temp_assets` 的文件夹）。

然后，我们需要在之前的 5 个 Prompt 中，**在特定的时机**指示 Claude 去读取对应的文件。以下是为您调整后的**“喂饭级”实操步骤**：

### 📍 准备工作
将这 4 个文件放入项目根目录的 `_docs/` 文件夹下。

---

### 🚀 注入时机与更新后的 Prompt 指令

#### 第一步：全局扫描 (不读文件，只摸底)
> "Claude，请阅读我提供的 PRD V3 文档。在写代码前，请先全局扫描现有的项目结构，找出数据库定义文件、打分组件和报告组件。告诉我你打算如何通过增加 `scale_type = 'fine_motor'` 来复用它们。"

#### 第二步：数据库扩展与数据导入 (读取 CSV 和 TXT)
*时机：当 Claude 确认了项目结构后，让它处理数据库。*
> "很好。现在进行数据库扩展。
> 1. 请确保 `assessment_records` 表兼容 `is_auto_filled` 字段。
> 2. **请读取文件 `_docs/学前儿童精细动作评估量表.csv` 和 `_docs/学前儿童精细动作评估单项评语专家建议数据.txt`**。
> 3. 请编写一个初始化脚本（或无损迁移函数），将这些文件中的 87 道题目数据、领域信息以及对应的单项专家建议，插入到现有的 `items` 题库表中，并标记 `scale_type = 'fine_motor'`。"

#### 第三步：封装计分引擎 (读取 计分引擎.js)
*时机：数据库搞定后，开始写核心算法。*
> "接下来，我们要创建精细动作的专属计分引擎。
> **请读取文件 `_docs/学前儿童精细动作评估自动计分引擎.js`**。
> 请参考这个文件里的原生 JS 逻辑，在现有的工具目录下（如 `src/utils` 或 `src/engines`）创建一个 TypeScript 文件 `FineMotorEngine.ts`。
> 要求：
> 1. 将文件里的逻辑转化为强类型的 TS 类或函数。
> 2. 补全 Basal（连续3题2分）和 Ceiling（连续3题0分）的自动跳题和 `is_auto_filled` 标记算法。
> 3. 确保暴露出的方法签名与现有系统（如 CBCL）的引擎风格保持一致。"

#### 第四步：修改前端打分 UI (不读文件)
*时机：引擎写好后，让前端接上引擎。*
> "现在，请修改现有的打分界面组件（如 `AssessmentView.vue`）。
> 当 `scale_type === 'fine_motor'` 时，显示【2分 掌握】、【1分 部分】、【0分 未掌握】三个按钮。
> 接入刚才写的 `FineMotorEngine`，当用户打分触发了 Basal 或 Ceiling 自动跳题时，复用现有的 Toast 组件给用户提示。"

#### 第五步：生成报告与解析 (读取 报告解释生成引擎.js)
*时机：打分界面搞定后，处理最后的报告呈现。*
> "最后一步，处理报告渲染。
> **请读取文件 `_docs/学前儿童精细动作评估报告解释生成引擎.js`**。
> 1. 请将这个文件转化为项目中的配置文件（如 `src/config/FineMotorReportConfig.ts`）。
> 2. 修改现有的报告组件（如 `ReportView.vue`），当识别到 `fine_motor` 时，调用 `FineMotorEngine` 计算出得分率和状态（age_appropriate/emerging/delayed）。
> 3. 根据计算出的状态，从 `FineMotorReportConfig.ts` 中提取对应的评语（summary, expert_advice）进行渲染。
> 4. 在报告底部，渲染出引擎提取到的《近期 IEP 训练目标》列表。"

---

