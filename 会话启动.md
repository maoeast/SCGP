# 会话启动：最小现场恢复（Session Bootstrap）

> 用法：新会话第一句把本文件作为指令粘贴。
> 「本次交接」区块由上一会话执行 `会话结束.md` 时自动覆写；下方流程为固定步骤，不要手改。
> 当前阶段只允许读取文件和检查 Git 状态，不允许恢复 `stash`、不允许修改文件、不允许运行构建或测试。
> 规则冲突时以 `AGENTS.md` 为准；本文件只规定启动动作。

<!-- HANDOFF:START -->
## 本次交接（自动生成，请勿手编）

- 项目：SCGP / 星愿能力发展平台
- 当前任务：报告导出做实（自定义页眉 / 学生头像 / HTML 导出 / 默认格式）+ 报告页性别修复——已随 1.1.4 第四次同版本覆盖发布上包
- 当前状态：4 笔提交已推送（8744922 报告链 / 1af5abd 看板与手册链 / ecf1141 发版整理 / 39cee49 发版记录），HEAD = origin/main；verify:core 全绿、release:verify 8/8、IP 源 sha512 逐字节一致
- 下一步唯一原子动作：修复 HTTPS 更新源——从服务商控制台续发免费 DV 证书，替换 `/etc/nginx/ssl/maohedong.top/www.maohedong.top.pem|.key`，然后 `sudo nginx -t && sudo systemctl reload nginx`
- 验证命令：在服务器上执行 `curl -s https://maohedong.top/scgp/win/latest.yml`（应输出 version: 1.1.4）
- 相关文件：electron/handlers/update.js、/etc/nginx/ssl/maohedong.top/、src/utils/report-export.ts
- 注意：①HTTPS 主源证书已于 2026-09-20 23:59:59 GMT 过期，当前仅 IP 源 `http://124.220.104.199/scgp/win` 可用；②1.1.4 为同版本覆盖发布，已装 1.1.4 的机器收不到自动更新
- 生成时间：2026-09-21
<!-- HANDOFF:END -->

---

## 步骤一：按顺序读取

1. `AGENTS.md`（代理规则唯一来源；`CLAUDE.md` 是它的 `@AGENTS.md` 引用，已自动加载）
2. `.continue-here.md`
3. `PROJECT_CONTEXT.md`
4. 如果 `.continue-here.md` 提到了额外必读文件，再按其中顺序补读

要求：

- 顶部「本次交接」已给出当前任务与下一步，先按它定位，再读上述文件补全细节
- 如果 `.continue-here.md` 已足够，不要主动扩读大型历史文档（PRD、差距分析、技术规范等按需再读，导航见 `docs/INDEX.md`）
- 当前阶段目标只是恢复现场，不是开始执行

## 步骤二：检查当前 Git 现场

执行：

- `git stash list`
- `git status --short --branch`
- `git log --oneline -5`
- 输出当前分支名

注意：

- 默认只列出 `stash`，不要自动 `pop`
- 即使 `stash` 名称看起来熟悉，也不要自动恢复
- 存在 `wip(<主题>)` stash 时，先说明内容，等我确认是否恢复

## 步骤三：输出启动确认摘要

固定用下面格式：

```text
- 当前任务：
- 当前状态：
- 下一步唯一原子动作：
- 相关文件：
- 是否存在 stash：
- 当前分支：
- 最近提交：
- Repo 状态：clean / dirty / stashed
```

## 步骤四：遇到以下情况先停止并说明

- `.continue-here.md` 或顶部「本次交接」与当前 Git 状态明显冲突
- 存在你无法判断用途的 `stash`
- 工作区有与当前任务直接冲突的未提交改动

完成以上检查后停止，等待我下一步指令。不要提前开始干活。
