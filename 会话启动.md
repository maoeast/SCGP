# 会话启动：最小现场恢复（Session Bootstrap）

> 用法：新会话第一句把本文件作为指令粘贴。
> 「本次交接」区块由上一会话执行 `会话结束.md` 时自动覆写；下方流程为固定步骤，不要手改。
> 当前阶段只允许读取文件和检查 Git 状态，不允许恢复 `stash`、不允许修改文件、不允许运行构建或测试。
> 规则冲突时以 `AGENTS.md` 为准；本文件只规定启动动作。

<!-- HANDOFF:START -->
## 本次交接（自动生成，请勿手编）

- 项目：SCGP / 星愿能力发展平台
- 当前任务：认知发展（cognitive）模块游戏数为 0 → K03_PATTERN_NEXT（模式补全，P0 首样板）已实现 + type-check/build:web/实机 UAT 全通过。
- 当前状态：工作区 dirty（K03 全链路代码 + PRD §7 第 0 条决策固化 + size 差异调优，本地未提交）；commit `037245e` 已 push；handoff commit `cec4a9e` 本地领先 1 待 push。
- 下一步唯一原子动作：待用户决定是否提交/push；提交后复制 K03 节奏推 P0 其余游戏（K01/K04/K05）。
- 验证命令：`npm run type-check` / `npm run build:web`（均已通过）
- 相关文件：`PatternNextGame.vue`（含 SIZES=[0.5,0.85,1.15]）、`cognitive-games-api.ts`、`PatternNextPage.vue`、`GameContainer.vue`（moduleCode dispatch）、`custom-game-registry.ts`、`router/index.ts`
- 注意：库表方案 B（cognitive 走 training_session，source_table 合成源 `cognitive_game_inline`，不建表）；定位 B（`summary_payload` 透传 `actual_params`）；GameContainer 按 moduleCode dispatch 防 emotional 串台；IEP 报告消费端 4 处改动留后续（数据已留好）；非 size 规律候选 size 固定 0.85 是有意设计。
- 生成时间：2026-07-19
<!-- HANDOFF:END -->

---

## 步骤一：按顺序读取

1. `AGENTS.md`（代理规则唯一来源；`CLAUDE.md` 是它的 `@AGENTS.md` 引用，已自动加载）
2. `.continue-here.md`
3. `PROJECT_CONTEXT.md`
4. 如果 `.continue-here.md` 提到了额外必读文件，再按其中顺序补读

要求：

- 顶部「本次交接」已给出当前任务与下一步，先按它定位，再读上述文件补全细节
- 如果 `.continue-here.md` 已足够，不要主动扩读大型历史文档（PRD、差距分析、技术规范等按需再读，导航见 `docs/INDEX.md`）
- 当前阶段目标只是恢复现场，不是开始执行

## 步骤二：检查当前 Git 现场

执行：

- `git stash list`
- `git status --short --branch`
- `git log --oneline -5`
- 输出当前分支名

注意：

- 默认只列出 `stash`，不要自动 `pop`
- 即使 `stash` 名称看起来熟悉，也不要自动恢复
- 存在 `wip(<主题>)` stash 时，先说明内容，等我确认是否恢复

## 步骤三：输出启动确认摘要

固定用下面格式：

```text
- 当前任务：
- 当前状态：
- 下一步唯一原子动作：
- 相关文件：
- 是否存在 stash：
- 当前分支：
- 最近提交：
- Repo 状态：clean / dirty / stashed
```

## 步骤四：遇到以下情况先停止并说明

- `.continue-here.md` 或顶部「本次交接」与当前 Git 状态明显冲突
- 存在你无法判断用途的 `stash`
- 工作区有与当前任务直接冲突的未提交改动

完成以上检查后停止，等待我下一步指令。不要提前开始干活。
