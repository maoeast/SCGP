# 会话启动：最小现场恢复（Session Bootstrap）

> 用法：新会话第一句把本文件作为指令粘贴。
> 「本次交接」区块由上一会话执行 `会话结束.md` 时自动覆写；下方流程为固定步骤，不要手改。
> 当前阶段只允许读取文件和检查 Git 状态，不允许恢复 `stash`、不允许修改文件、不允许运行构建或测试。
> 规则冲突时以 `AGENTS.md` 为准；本文件只规定启动动作。

<!-- HANDOFF:START -->
## 本次交接（自动生成，请勿手编）

- 项目：SCGP / 星愿能力发展平台
- 当前任务：C12/C13 阻塞期间另起 T01 资源/备份覆盖证据闭环与 T02 生产调试信息清理两个独立技术债批次，均已代码级完成且未混入 closeout 主线。
- 当前状态：工作区 dirty 且未提交；`npm run verify:release` 已通过（`verify:core` 78/78 + TS 契约通过，`build:web` sanitizer checked 276 files, sanitized 1）；`git diff --check` 干净。
- 下一步唯一原子动作：复查当前 T01/T02 dirty diff 边界；如用户确认提交，用精确 `git add` 提交这些已验证文件，不要 `git add .`。
- 验证命令：`npm run verify:release`
- 相关文件：`package.json`、`vite.config.ts`、`electron/main.mjs`、`scripts/sanitize-production-build.mjs`、`scripts/tests/production-debug-boundary.test.mjs`、`scripts/tests/dev-route-production-boundary.test.mjs`、`scripts/tests/backup-table-coverage.test.mjs`、`scripts/tests/backup-restore-result.test.mjs`、`scripts/tests/resource-reconcile.test.mjs`、`src/utils/resource-reconcile.ts`、`src/utils/resource-reconcile-core.ts`
- 注意：C12/C13 仍被正式 HTTPS 更新目录 URL/写入凭据、Windows 签名证书、Windows 验收机阻塞；本轮禁止把这些技术债批次混进 closeout 发布门禁结论。
- 生成时间：2026-07-18
<!-- HANDOFF:END -->

---

## 步骤一：按顺序读取

1. `AGENTS.md`（代理规则唯一来源；`CLAUDE.md` 是它的 `@AGENTS.md` 引用，已自动加载）
2. `.continue-here.md`
3. `PROJECT_CONTEXT.md`
4. 如果 `.continue-here.md` 提到了额外必读文件，再按其中顺序补读

要求：

- 顶部「本次交接」已给出当前任务与下一步，先按它定位，再读上述文件补全细节
- 如果 `.continue-here.md` 已足够，不要主动扩读大型历史文档（PRD、差距分析、技术规范等按需再读，导航见 `docs/INDEX.md`）
- 当前阶段目标只是恢复现场，不是开始执行

## 步骤二：检查当前 Git 现场

执行：

- `git stash list`
- `git status --short --branch`
- `git log --oneline -5`
- 输出当前分支名

注意：

- 默认只列出 `stash`，不要自动 `pop`
- 即使 `stash` 名称看起来熟悉，也不要自动恢复
- 存在 `wip(<主题>)` stash 时，先说明内容，等我确认是否恢复

## 步骤三：输出启动确认摘要

固定用下面格式：

```text
- 当前任务：
- 当前状态：
- 下一步唯一原子动作：
- 相关文件：
- 是否存在 stash：
- 当前分支：
- 最近提交：
- Repo 状态：clean / dirty / stashed
```

## 步骤四：遇到以下情况先停止并说明

- `.continue-here.md` 或顶部「本次交接」与当前 Git 状态明显冲突
- 存在你无法判断用途的 `stash`
- 工作区有与当前任务直接冲突的未提交改动

完成以上检查后停止，等待我下一步指令。不要提前开始干活。
