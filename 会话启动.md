# 会话启动：最小现场恢复（Session Bootstrap）

> 用法：新会话第一句把本文件作为指令粘贴。
> 「本次交接」区块由上一会话执行 `会话结束.md` 时自动覆写；下方流程为固定步骤，不要手改。
> 当前阶段只允许读取文件和检查 Git 状态，不允许恢复 `stash`、不允许修改文件、不允许运行构建或测试。
> 规则冲突时以 `AGENTS.md` 为准；本文件只规定启动动作。

<!-- HANDOFF:START -->
## 本次交接（自动生成，请勿手编）

- 项目：SCGP / 星愿能力发展平台
- 当前任务：AI 智能体 Phase 5C（技能库整页 CRUD + references 按需勾选注入）待启动
- 当前状态：Phase 1-5 全完成并推送 origin/main（`96a2d18` feat + `792ab4f` handoff + `52dbfa8` glob fix）；Phase 5 = 按 agent 挂载技能（5A 工具过滤 + 5B 知识技能注入 systemPrompt），type-check 全绿、真机已验；工作区仅剩未跟踪的 `src/data/skills/家校沟通话术官/`（用户拷的外部技能，待定是否提交）
- 下一步唯一原子动作：进 plan mode，为「技能库整页 CRUD」（查看/编辑知识技能正文、启停、新建自定义知识技能）出实施方案；references 按需勾选注入作子项
- 验证命令：`npm run type-check`
- 相关文件：`C:\Users\maoea\.claude\plans\jolly-gathering-book.md`（§不做=Phase 5C 范围）、`src/views/system/AiAgentConfig.vue`、`src/database/ai-api.ts`、`src/data/skills/index.ts`
- 注意：知识技能绑定复用 `ai_agent_skill` 表（不分 kind），UI 用单 `skillIds` + `el-option-group` 分组；知识种子用 INSERT OR IGNORE + UPDATE 保留 id（非 OR REPLACE）
- 生成时间：2026-07-17
<!-- HANDOFF:END -->

---

## 步骤一：按顺序读取

1. `AGENTS.md`（代理规则唯一来源；`CLAUDE.md` 是它的 `@AGENTS.md` 引用，已自动加载）
2. `.continue-here.md`
3. `PROJECT_CONTEXT.md`
4. 如果 `.continue-here.md` 提到了额外必读文件，再按其中顺序补读

要求：

- 顶部「本次交接」已给出当前任务与下一步，先按它定位，再读上述文件补全细节
- 如果 `.continue-here.md` 已足够，不要主动扩读大型历史文档（PRD、差距分析、技术规范等按需再读，导航见 `docs/INDEX.md`）
- 当前阶段目标只是恢复现场，不是开始执行

## 步骤二：检查当前 Git 现场

执行：

- `git stash list`
- `git status --short --branch`
- `git log --oneline -5`
- 输出当前分支名

注意：

- 默认只列出 `stash`，不要自动 `pop`
- 即使 `stash` 名称看起来熟悉，也不要自动恢复
- 存在 `wip(<主题>)` stash 时，先说明内容，等我确认是否恢复

## 步骤三：输出启动确认摘要

固定用下面格式：

```text
- 当前任务：
- 当前状态：
- 下一步唯一原子动作：
- 相关文件：
- 是否存在 stash：
- 当前分支：
- 最近提交：
- Repo 状态：clean / dirty / stashed
```

## 步骤四：遇到以下情况先停止并说明

- `.continue-here.md` 或顶部「本次交接」与当前 Git 状态明显冲突
- 存在你无法判断用途的 `stash`
- 工作区有与当前任务直接冲突的未提交改动

完成以上检查后停止，等待我下一步指令。不要提前开始干活。
