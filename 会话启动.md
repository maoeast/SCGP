# 会话启动：最小现场恢复（Session Bootstrap）

> 用法：新会话第一句把本文件作为指令粘贴。
> 「本次交接」区块由上一会话执行 `会话结束.md` 时自动覆写；下方流程为固定步骤，不要手改。
> 当前阶段只允许读取文件和检查 Git 状态，不允许恢复 `stash`、不允许修改文件、不允许运行构建或测试。
> 规则冲突时以 `AGENTS.md` 为准；本文件只规定启动动作。

<!-- HANDOFF:START -->
## 本次交接（自动生成，请勿手编）

- 项目：SCGP / 星愿能力发展平台
- 当前任务：用户手册 v1.3 全量同步 + 质量看板双修 + 附录瘦身 + 截图专项 217 张 + 特校营销文案 v2.0——全部完成并推送 origin/main（cb1d4ce，9 提交）
- 当前状态：全部验证绿（type-check 0 error / test:core:node 132 测 125 过 7 存量败 / 手册 validate 全绿 18 章 217 图 / docx+PDF 已重出）；工作树 clean
- 下一步唯一原子动作：用户真机验收——重启应用看「系统管理 → 评估质量」标签与 PEP-3 报告页，翻阅新版手册 docx（217 图）
- 验证命令：npm run type-check
- 相关文件：docs/user-manual/SCGP-星愿能力发展平台用户使用手册.md、docs/user-manual/SCGP-用户手册截图采集与维护基线.md、docs/marketing/2026-09-10-scgp-special-school-marketing.md
- 注意：①「AI 解读」按钮实测 15/18（CSIRS/Conners PSQ/TRS 无），validate 门禁已固化两条精确子串，对外表述不得写「18 项均提供 AI 解读」；② test:core:node 7 个失败用例为存量历史问题（登录主题/生成报告入口/DevTools/AI 工具绑定，与本轮改动无关）
- 生成时间：2026-09-11
<!-- HANDOFF:END -->

---

## 步骤一：按顺序读取

1. `AGENTS.md`（代理规则唯一来源；`CLAUDE.md` 是它的 `@AGENTS.md` 引用，已自动加载）
2. `.continue-here.md`
3. `PROJECT_CONTEXT.md`
4. 如果 `.continue-here.md` 提到了额外必读文件，再按其中顺序补读

要求：

- 顶部「本次交接」已给出当前任务与下一步，先按它定位，再读上述文件补全细节
- 如果 `.continue-here.md` 已足够，不要主动扩读大型历史文档（PRD、差距分析、技术规范等按需再读，导航见 `docs/INDEX.md`）
- 当前阶段目标只是恢复现场，不是开始执行

## 步骤二：检查当前 Git 现场

执行：

- `git stash list`
- `git status --short --branch`
- `git log --oneline -5`
- 输出当前分支名

注意：

- 默认只列出 `stash`，不要自动 `pop`
- 即使 `stash` 名称看起来熟悉，也不要自动恢复
- 存在 `wip(<主题>)` stash 时，先说明内容，等我确认是否恢复

## 步骤三：输出启动确认摘要

固定用下面格式：

```text
- 当前任务：
- 当前状态：
- 下一步唯一原子动作：
- 相关文件：
- 是否存在 stash：
- 当前分支：
- 最近提交：
- Repo 状态：clean / dirty / stashed
```

## 步骤四：遇到以下情况先停止并说明

- `.continue-here.md` 或顶部「本次交接」与当前 Git 状态明显冲突
- 存在你无法判断用途的 `stash`
- 工作区有与当前任务直接冲突的未提交改动

完成以上检查后停止，等待我下一步指令。不要提前开始干活。
