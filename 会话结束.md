# 会话结束：最小可恢复交接（Minimal Handoff）

> 用法：会话收尾时把本文件作为指令交给代理执行。目标是让下一会话最低成本恢复现场，而不是写长总结。
> 规则冲突时以 `AGENTS.md` 为准；本文件只规定交接动作。

## 全局原则

- 精简 > 完整
- 可恢复 > 可读
- 原子 > 模糊
- 只写下次必须信息
- 禁止长段回顾、重复历史、全量列举

---

## 步骤一：更新 `.continue-here.md`（全量覆写）

只保留以下结构：

```md
# Current State
- 任务：<精确一句话>
- 已完成：<具体到文件 / 行为 / 验证结果>
- 产出物：<文件路径或提交号>

# Next Action
- <下一步唯一原子动作>

# Blockers
- <真实阻塞；没有则 None>

# Key Decisions
- <最多 3 条，只保留影响后续实现的决策>

# Relevant Files
- <下一步真正会打开的最小文件集>
```

要求：

- `Next Action` 必须小到新会话开头可以立刻执行
- 不要把"下一阶段目标"写成 `Next Action`
- `Relevant Files` 只留下一步真的会打开的文件

---

## 步骤二：条件更新 `PROJECT_CONTEXT.md`

只有满足任一条件才更新：

- 架构变化
- 技术选型变化
- 新增全局约束
- 修正了会影响后续理解的全局事实

不满足则明确输出：

```text
PROJECT_CONTEXT.md 无需更新
```

更新规则：

- 只追加，不重写历史
- 每条尽量短
- 只写会影响后续会话判断的事实

---

## 步骤三：Git 分类处理

先执行：

```bash
git status --short --branch
```

然后逐项分类：

- 本轮任务且完整、已验证：可提交
- 本轮任务但未完成：默认不自动 `stash`，优先保留工作区并通过 `.continue-here.md` 交接
- 无关改动：明确跳过原因

规则（与 `AGENTS.md` §4 / §5 一致）：

- 禁止 `git add .`，只允许 `git add` 精确文件
- 只有"本轮任务完整且已验证"**且用户明确要求提交**时才 `commit`
- 在 `main` 上提交前先开分支
- 只有用户明确要求时才 `push`；`push` 失败保留本地 `commit`，停止，不重试
- 如必须 `stash`，说明原因和用途，命名 `wip(<主题>)`

提交格式（沿用仓库 conventional commits 习惯）：

```text
type(scope): 简述本次变更
- Done: <核心完成项>
- Next: <下一步动作>
- Risk: <有则填，无则省略该行>
```

输出：

- 是否提交
- `commit hash`（如有）
- 是否 `stash`
- Repo 状态：`clean / dirty / stashed`

---

## 步骤四：更新 `会话启动.md` 的「本次交接」区块（自动联动）

全量覆写 `会话启动.md` 中 `<!-- HANDOFF:START -->` 与 `<!-- HANDOFF:END -->` 两个标记之间的内容（**保留标记本身和文件其余部分不动**），填入：

```md
## 本次交接（自动生成，请勿手编）

- 项目：SCGP / 星愿能力发展平台
- 当前任务：<一句话>
- 当前状态：<已完成 / 产出物，一句话>
- 下一步唯一原子动作：<原子动作>
- 验证命令：<下一步做完后应运行的第一条验证命令>
- 相关文件：<下一步真正会打开的最小文件集>
- 注意：<最多 2 条真实约束>
- 生成时间：<本次会话结束日期，如 2026-07-10>
```

要求：

- 只覆写两个 `HANDOFF` 标记之间的内容，不要改动 `会话启动.md` 的固定流程部分
- `下一步唯一原子动作` 必须小到新会话开头可立刻执行，不要写成阶段目标
- `验证命令` 必须是真实会运行的第一条命令，不要写泛泛"自测"。SCGP 常用：
  - 默认类型检查：`npm run type-check`
  - emotional 专题：`npm run type-check:emotional`
  - Web 构建：`npm run build:web`
  - 授权目录：`npx jiti tests/entitlement-catalog.test.ts`
  - 训练路由访问：`node --test scripts/tests/training-route-access.test.mjs`
- 本步骤的 `当前任务 / 下一步 / 相关文件` 要与步骤一写入 `.continue-here.md` 的内容一致，避免两处漂移

完成后，下一会话只需粘贴 `会话启动.md` 即可带着最新交接直接开干。

---

## 步骤五：最终四行摘要

```text
Done: <完成项>
Blocker: <阻塞项 / None>
Next: <唯一原子动作>
Repo: <clean / dirty / stashed>
```
