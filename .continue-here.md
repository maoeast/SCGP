# Current State
- 任务：AI 智能体 Phase 3 图片上传 vision 已完成提交；下一步 Phase 4 文档上传（新会话执行）
- 已完成：Phase 1（多 provider，`1548186`）+ Phase 2（function calling，`affb747`）+ Phase 3（图片 vision + 附件生命周期，`074431e`）。Phase 3 主链真机已验（豆包看图回答）；type-check 全绿；本地 main ahead 4（未 push）
- 产出物：commit `074431e`（12 文件 +456/-52，含新建 `src/utils/ai-attachment-manager.ts`）；plan `purring-riding-pie.md`（Phase 1-5）；Phase 3 细化 plan `graceful-orbiting-dusk.md`

# Next Action
- 启动 Phase 4 文档上传：读 `purring-riding-pie.md` §Phase 4 + 现有 AI 子系统代码（ai.mjs/ai.ts/AiAssistant.vue/main.mjs/preload.mjs），先确认新依赖 `pdf-parse`+`mammoth`（纯 JS、零原生依赖，AGENTS §5 红线）后给实施计划

# Blockers
- Phase 4 新依赖 pdf-parse/mammoth 待用户确认（零原生依赖红线）
- Phase 3 防御项待补验：多轮「最近1轮带图」重发、附件 GC 回归、deleteSession 清文件、vision+FC 同请求
- plan 仅规划到 Phase 5；用户提到的「Phase 6」未定义，待确认（可能笔误或拟扩展）

# Key Decisions
- AI 子系统：明文 Key 只在 Main，渲染端做 tool 循环 + 拼多模态/技能；Main 是 dumb transport（Phase 1-3 已定，Phase 4/5 沿用）
- 附件/文档落 `userData/resources/uploaded/` 自动进 A4 备份 + GC；新引用源必须纳入 `resource-reconcile.collectReferencedPaths`，否则被当孤儿删（Phase 4 文档附件同模式）
- 多模态/FC 按 provider 能力位开关；豆包走 Chat Completions + image_url（非 Responses API）

# Relevant Files
- `C:\Users\maoea\.claude\plans\purring-riding-pie.md`（§Phase 4/5）
- `electron/main.mjs` + `electron/preload.mjs`（Phase 4 新 extract-document-text IPC）
- `electron/handlers/ai.mjs`（多模态透传，Phase 4 文档文本复用）
- `src/stores/ai.ts` + `src/features/ai/components/AiAssistant.vue`（文档 UI + sendChat 扩展）
- `src/utils/ai-attachment-manager.ts`（Phase 3 附件范式，Phase 4 文档可仿）
