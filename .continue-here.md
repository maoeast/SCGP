# Current State
- 任务：继续 `Wave B / life-skills / L04_SET_TABLE` 主视觉重做；SVG 舞台骨架已落地并接入页面。
- 已完成：
  - `src/components/emotional/games/SetTableStageArt.vue` 已实现餐桌、餐位 ghost anchor、碗/勺/杯/筷/餐巾 SVG 资产、主题配色、点击热区和完成态 sparkles。
  - `src/components/emotional/games/SetTableGame.vue` 已在 `ready / playing / finished` 三态接入 `SetTableStageArt`，保留原有选物、提示、完成 `performanceData` 链路。
  - `npm run type-check` 已通过。
- 产出物：
  - `src/components/emotional/games/SetTableStageArt.vue`
  - `src/components/emotional/games/SetTableGame.vue`

# Next Action
- 修改 `src/components/emotional/games/SetTableGame.vue` 的 `set-table-game__items` 区块，去掉餐具按钮里的 emoji 主展示，改为统一 SVG 缩略资产。

# Blockers
- None

# Key Decisions
- 只重做 `L04_SET_TABLE` 视觉层：不改路由、落库和记录摘要链路。
- `L04_SET_TABLE` 继续走 `CSS + SVG` 平面场景方案：保留 ghost anchor 机制，只升级正式桌面与餐具资产。
- 工作树存在大量无关改动：后续只处理 `SetTable` 相关文件，不扩散到其他路径。

# Relevant Files
- `src/components/emotional/games/SetTableGame.vue`
- `src/components/emotional/games/SetTableStageArt.vue`
