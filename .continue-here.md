# Current State
- 任务：基于 `docs/planning/MediaPipe-打泡泡.md` 完成 MediaPipe Hands 体感游戏“打泡泡”，并修复“打泡泡”“木块磁贴拼图”顶部按钮重叠回归。
- 已完成：新增 `BubblePopGame.vue` 与 `bubble-pop-game.ts`；接入 `GamePlay.vue`、`GameLobby.vue`、`GamePreviewCard.vue`、`sensory-game-seed.ts`、`TaskID.HAND_BUBBLE_POP`；删除旧 `GestureGardenGame.vue` 接入。
- 已完成：保留 `HandCameraLayer.vue` 相机层；复用 `hand-game-gestures.ts` 手势点位；泡泡生成、碰撞、同帧去重、模式/难度、计分、错误命中惩罚均在可复用运行时中实现。
- 已完成：木块磁贴拼图抽出 `wood-block-puzzle.ts` 与共享 `WoodenShapeBlock`；`GameGrid.vue` 形状模式复用共享木块外观。
- 已完成：修复打泡泡顶部栏按钮消失/返回无效问题，移除 Teleport 方案，改为组件内三栏顶栏；修复木块拼图难度栏被返回按钮遮挡问题。
- 产出物：`src/components/games/hand/BubblePopGame.vue`
- 产出物：`src/components/games/hand/bubble-pop-game.ts`
- 产出物：`src/components/games/hand/wood-block-puzzle.ts`
- 产出物：`src/components/games/shared/WoodenShapeBlock.vue`
- 产出物：`src/components/games/shared/wooden-shape-block.ts`
- 验证：`node --test scripts/tests/sensory-hand-gestures.test.mjs` 通过。
- 验证：`node --test scripts/tests/sensory-hand-games.test.mjs` 通过。
- 验证：`npm run build-only` 通过；仅有既有 Vite chunk/dynamic import warning。

# Next Action
- 执行 `npm run type-check`，确认是否仍仅剩既有 `vite.config.ts` 类型问题。

# Blockers
- None

# Key Decisions
- 泡泡顶栏不使用 Teleport：避免相机初始化、路由切换和动画帧更新交错时触发 Vue 空目标节点 patch 错误。
- `HandCameraLayer` 只保留相机/手势点位层：泡泡和木块的状态推进、碰撞、计分、布局逻辑放在可复用模块。
- 不恢复“森林手势魔法屋”：该旧实现已从当前手势游戏主线移除。

# Relevant Files
- `src/components/games/hand/BubblePopGame.vue`
- `src/components/games/hand/bubble-pop-game.ts`
- `src/components/games/hand/WoodBlockPuzzleGame.vue`
- `src/components/games/hand/wood-block-puzzle.ts`
- `src/components/games/shared/WoodenShapeBlock.vue`
- `src/components/games/shared/wooden-shape-block.ts`
- `src/views/games/GamePlay.vue`
- `scripts/tests/sensory-hand-games.test.mjs`
- `scripts/tests/sensory-hand-gestures.test.mjs`
- `vite.config.ts`
