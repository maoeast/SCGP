# Current State
- 任务：游戏训练 IEP 报告闭环扩展（Phase 1：Tier1 精细动作/生活自理 F03/L03/L05），来源 `docs/plans/2026-07-07-game-iep-extension-plan.md`。
- 已完成：新建归一化层 `src/utils/game-performance-normalizer.ts`；`GameContainer.vue` 的 `runSocialIepChain` 泛化为 `runModuleIepChain`，触发守卫改为按 `trainingEntryCode ∈ {social-communication, fine-motor, life-skills}` 放行；`IEPGenerator` 新增 `generateFineMotorReport`/`generateLifeSkillsReport`；`IEPReport.vue` 按 `raw_data.gameCode` 前缀 S/F/L 路由。`npm run type-check` 全程 exit 0；归一化层 25 条断言全过。已提交 `7ea4de3`。
- 产出物：commit `7ea4de3`（4 文件：`game-performance-normalizer.ts` / `iep-generator.ts` / `GameContainer.vue` / `IEPReport.vue`）

# Next Action
- `npm run electron:dev` 冷启动，分别通关 F03/L03/L05 各一次，核对：①`training_records` 新行（module_code 分别为 sensory/life_skills/life_skills、task_id=null、raw_data.gameCode 存在）②`report_record` 新行 report_type='iep' ③自动跳 IEPReport 展示专属段 ④缺指标统计卡隐藏 ⑤可导 Word。

# Blockers
- Phase 1 真机 E2E 未跑（本环境无法驱动交互式 Electron 游戏 + 查 DB）。
- Phase 2（Tier2 F04/L01/L02/L04）待用户决定是否推进。

# Key Decisions
- 触发守卫按 `trainingEntryCode` 放行而非 `moduleCode`：EMOTIONAL 同时含 G(情绪调节) 与 C(安抚教具)，必须排除二者，故只能用 entry 白名单 `{social-communication, fine-motor, life-skills}`。
- 归一化层 `normalizeGameMetrics` 是 chain/generator/report 三处唯一提取入口；Phase 2/3 只在 `GAME_EXTRACTION_RULES` 加 gameCode→规则行，不改主体逻辑。
- `IEPReport.vue` 按 `raw_data.gameCode` 前缀 S/F/L 路由：`getTrainingRecord` SELECT 不含 `module_code` 列，且 fine-motor 的 `module_code` 实为 `'sensory'`（与经典感官同列），无法用 module_code 区分。
- 工作区脏改动（`compileCareSceneImmersive.ts`、`SceneSelector.vue`、`docs/references/*`、`package.json`、`AGENTS.*` 等）属另一条线（情绪沉浸式收尾 + 杂项），非本任务，不要并入本任务提交。

# Relevant Files
- `src/utils/game-performance-normalizer.ts`
- `src/views/emotional/games/GameContainer.vue`
- `src/utils/iep-generator.ts`
- `src/views/games/IEPReport.vue`
