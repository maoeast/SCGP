# Current State
- 任务：A4「资源文件生命周期 + 备份收口」Phase 2 待开（Phase 1 已完成并推送）。
- 已完成：Phase 1 止血——删/替即清物理文件（`hardDeleteResource`/`updateResource` 钩入 + 跨表引用计数==0 防误删共享）+ 统一托管路径工具（`resource-file-refs.ts`/`resource-file-service.ts`，TM file-manager 复用）+ AI 候选 regenerate 清理 + 删 link A 死代码（`ResourceUpload.vue`/`SAVE_ASSET` IPC）。type-check ✅、单测 ✅。
- 产出物：提交 `30e4ad2`（已 push origin/main）；新增 `src/utils/resource-file-refs.ts`、`resource-file-service.ts`、`tests/resource-file-refs.test.ts`。

# Next Action
- Phase 2 起步：先与用户确认 zip 实现方式（新增 `fflate` 依赖 vs 手写 store-only zip 无依赖，计划 §7 P1），确认后按计划 §4 实施 backup v3.0（备份纳入 `userData/resources` 托管文件）。

# Blockers
- §7 P1：`fflate` 依赖需用户一句话确认（Phase 2 第一步即遇；备选手写 store-only zip）。
- Phase 1 真机验证未跑（硬删 / 替换 / AI 重生成 / 软删恢复 四项，计划 §3.7）。

# Key Decisions
- 托管路径规则：托管（可删 / 进备份）= `uploaded/` + `teaching-materials/`；预置（永不删）= `docs/images/videos/audio/`。备份与 GC 共用（见 `resource-file-refs.ts`）。
- §3.6 link B（`resource-manager.ts`）有活引用（`PlanList.vue handlePreviewResource→openWithSystem`）未删，另起子任务；§3.5 候选清理用 regenerate 时机（apply 时机破坏同批次改选）。
- 软删 `deleteResource` 不动文件（可恢复语义）；跨表引用计数==0 才删物理文件。

# Relevant Files
- `docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md`（§4 Phase 2 步骤、§7 P1 风险）
- `src/utils/backup.ts`、`src/utils/crypto.ts`、`electron/main.mjs`、`electron/preload.mjs`、`src/types/electron.d.ts`
- `src/utils/resource-file-service.ts`（Phase 2 复用 resolveAbsolutePath/deleteManagedFile）
