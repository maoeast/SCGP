# Current State
- 任务：A4 Phase 2 — 备份纳入物理文件 zip 归档（备份版本 2.0→3.0）
- 已完成：AI 生图功能整体移除并提交（`94836a9`，type-check ✅，5 文件 +3/-515，未 push）；Phase 1 已 push（`30e4ad2`+`ab3b0d7`）
- 产出物：commit `94836a9`；plan `docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md` §4

# Next Action
- Phase 2 §4.1：`src/utils/crypto.ts` 新增 `encryptBytes(bytes: Uint8Array, key?): string` / `decryptBytes(payload: string, key?): Uint8Array | null`（`CryptoJS.lib.WordArray.create(bytes)` → AES → `toString()`；解密逆操作，纯 crypto-js 无新依赖）→ `npm run type-check`

# Blockers
- Phase 2 §7 P1：`fflate` 依赖待用户确认（备选手写 store-only zip，不引原生依赖）；该决策在 §4.2 IPC 前必须定，§4.1 不受影响

# Key Decisions
- 托管路径规则（全局约束）：托管 = `uploaded/` + `teaching-materials/`（可删 / 进备份）；预置 = `docs/` / `images/` / `videos/` / `audio/`（永不删）；唯一可写根 = `userData/resources`
- Phase 2 计划见 plan §4；备份载体保持单 `.dat`（加密 zip 串作为 BackupData 字段），`BACKUP_VERSION` 2.0→3.0 保留 2.0/1.0 兼容
- §3.6 link B（`resource-manager.ts` 废弃 getAppPath）有活引用（`PlanList.vue openWithSystem`）未删，另起子任务；软删 `deleteResource` 不动文件

# Relevant Files
- `src/utils/crypto.ts`（§4.1 改动点）
- `docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md`（§4.1 / §4.2 / §7 P1）
