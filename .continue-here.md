# Current State
- 任务：A4 资源文件生命周期。Phase 1 已完成推送（`30e4ad2` + `ab3b0d7`）。**新确认首要任务：移除「情绪场景 / 表达关心」编辑器里未用的 AI 生图功能**（用户确认是忘删的功能），完成后 Phase 2 顺延。
- 已完成（Phase 1，已 push）：删/替即清物理文件（hardDelete/updateResource 钩入 + 跨表引用计数）+ 托管路径工具（resource-file-refs/resource-file-service）+ 删 link A 死代码。type-check ✅ 单测 ✅。

# Next Action
- **移除 AI 生图功能**（用户已确认范围；功能自包含，无其他消费者——已 grep 核实）：
  1. 删 `src/services/scene-image-generation.ts`（整文件）
  2. `electron/main.mjs`：删 `ai:generate-scene-images` IPC（~750-793）+ `generateGeminiSceneImage`（~289-331）+ `getGeminiApiKey`（~271）+ `GEMINI_IMAGE_MODEL`（~269）——Gemini 生图基建只为场景图服务
  3. `EmotionSceneEditor.vue` / `CareExpressionEditor.vue`：删「AI 生成」按钮、候选网格、`handleGenerateImages`/`applyGeneratedCandidate`/`generatedCandidates`/`isGeneratingImages`、scene-image-generation import；**同时删本会话 §3.5 加的 `purgeAbandonedSceneCandidates` import + 调用**
  4. `src/utils/resource-file-service.ts`：删 `purgeAbandonedSceneCandidates` + `ManagedFileRef`（§3.5 随功能移除）；保留 resolveAbsolutePath/deleteManagedFile/getManagedRoot + 路径助手（Phase 2/3 复用）
  5. 顺手删灰的「上传占位」按钮（同为死 UI）
  - **保留**：「场景图片 URL」手动输入框 + `imageUrl` 字段（资源仍可有场景图，手动填 resource:// 或预置路径）
  - type-check → 更新 backlog/handoff（§3.5 标"随 AI 生图移除废弃"）→ commit `refactor(resource): 移除情绪/关心场景未用的 AI 生图功能`
- 移除完成后 → **Phase 2**（备份纳入物理文件 zip 归档 v3.0，计划 §4；遇 §7 P1 fflate 依赖停下问用户）。

# Blockers
- Phase 2 §7 P1：`fflate` 依赖待用户确认（备选手写 store-only zip）。
- Phase 1 真机验证未跑（硬删无 UI 入口，建议跳过等 GC；替换/AI/软删恢复入口在「资源中心→训练资源」tab）。

# Key Decisions
- 托管路径规则（全局约束）：托管 = uploaded/ + teaching-materials/；预置 = docs/images/videos/audio/（永不删）。
- AI 生图功能确认移除（非禁用）：link D 孤儿源从根消除，比 §3.5 regenerate 清理更干净 → §3.5 随之废弃。
- §3.6 link B（resource-manager.ts）有活引用（PlanList.vue openWithSystem）未删，另起子任务；软删 deleteResource 不动文件（可恢复语义）。
- ⚠️ Write 工具会把内容里 `\uXXXX` 解释成字面控制字节——写文件避免 `\u` 转义，或事后 perl 修。

# Relevant Files
- 移除 AI 生图：`src/services/scene-image-generation.ts`、`electron/main.mjs`（Gemini 块 ~269-331、750-793）、`src/views/resource-center/editors/EmotionSceneEditor.vue`、`CareExpressionEditor.vue`、`src/utils/resource-file-service.ts`
- Phase 2：`docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md`（§4/§7 P1）、`src/utils/backup.ts`、`src/utils/crypto.ts`
