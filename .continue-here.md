# Current State
- 任务：继续 SCGP 授权能力包拆分，下一阶段从训练入口链路推进到评估入口与启动链路的 entitlement 口径统一。
- 已完成：`src/features/entitlements/entitlement-catalog.ts` 已落地影子映射层；`src/stores/auth.ts` 已新增 `effectiveEntitlements` / `hasEntitlementAccess()`；`src/utils/training-entry.ts` 与 `src/utils/equipment-training-entry.ts` 已补 `requiredEntitlement`；游戏训练 / 器材训练 / 训练记录的入口列表、菜单、聚合入口与相关路由守卫已优先按 entitlement 判权。
- 产出物：`src/features/entitlements/entitlement-catalog.ts`、`src/stores/auth.ts`、`src/utils/access-visibility.ts`、`src/utils/training-entry.ts`、`src/utils/equipment-training-entry.ts`、`src/router/index.ts`

# Next Action（必须原子级——第一步可执行的单一动作）
- 修改 `src/features/assessment/assessment-scale-catalog.ts`，为量表定义新增 entitlement 访问口径（在保留现有 `accessModulesAnyOf` 兼容前提下补 `accessEntitlementsAnyOf` 或等价字段）。

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 授权口径解耦：`moduleCode` 继续表示数据归属，`requiredEntitlement` / entitlement 映射表示访问控制。
- 训练入口守卫优先 entitlement：`/games/*`、`/equipment/*`、`/training-records/*` 先按 entitlement 放行，再回落旧 `moduleCode`。
- 未知授权默认拒绝：未知 code 不进入 `effectiveEntitlements`，仅在 DEV 记录诊断信息。

# Relevant Files（最小集——仅列下一步会用到的）
- `src/features/assessment/assessment-scale-catalog.ts`
- `src/router/index.ts`
- `src/stores/auth.ts`
- `src/features/entitlements/entitlement-catalog.ts`
- `src/utils/training-entry.ts`
