# Current State
- 任务：继续自理训练模块阶段 1，补齐 `task_training` 平台主链后续合同层。
- 已完成：新增 `src/features/self-care/task-training-contract.ts`，定义 `task_training` 资源类型、`life_skills` 授权模块、`life-skills` 入口、复用 `game` 展示主链的常量，以及资源元数据、步骤、执行结果 TypeScript 类型。
- 已完成：新增 `scripts/tests/task-training-contract.test.mjs`，验证合同常量与类型夹具。
- 产出物：`src/features/self-care/task-training-contract.ts`
- 产出物：`scripts/tests/task-training-contract.test.mjs`

# Next Action（必须原子级——第一步可执行的单一动作）
- 修改 `src/utils/resource-center-business.ts`，将 `task_training`、`life-skills`、`game` 相关硬编码替换为 `task-training-contract.ts` 导出的合同常量。

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 自理训练继续挂 `life_skills` 授权：当前授权体系只识别既有业务模块代码。
- `task_training` 继续复用训练资源的 `game` 展示主链：阶段 1 目标仍是可见、可选、可启动，不提前扩展独立展示主链。
- `task_training` 步骤结构以 `meta_data.steps[]` 为一期事实源：不恢复旧 `task_step` 表作为新主链。

# Relevant Files（最小集——仅列下一步会用到的）
- `src/utils/resource-center-business.ts`
- `src/features/self-care/task-training-contract.ts`
