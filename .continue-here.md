# Current State
- 任务：在 `emotion_scene` 与 `care_scene` 的全屏沉浸式训练中新增教师圈画模式，支持隐藏前景 UI 并直接在场景画面上做绿色荧光标注。
- 已完成：
  - `src/components/training/TrainingLayout.vue`：接入共享教师圈画模式状态；支持双击非按钮/非工具栏区域切换；圈画模式下隐藏沉浸式前景 UI。
  - `src/components/training/TeacherAnnotationLayer.vue`：新增共享圈画图层；支持右下角悬浮开关进入、左键拖动画绿色荧光线、触摸拖动圈画、清空标注、退出时自动清空。
  - 已按实机反馈收口交互：悬浮开关移到右下角；未开启时显示笔图标，开启后按钮文案为“退出”。
- 产出物：
  - `src/components/training/TrainingLayout.vue`
  - `src/components/training/TeacherAnnotationLayer.vue`

# Next Action
- 在 Electron 中打开 `care_scene` 沉浸式页面，回归验证右下角开关、双击空白切换、左键圈画、清空和退出按钮互不冲突。

# Blockers
- None

# Key Decisions
- 圈画能力收口到 `TrainingLayout`：`emotion_scene` 与 `care_scene` 共用一套沉浸式壳层，避免分别补两套交互。
- 进入方式保留两种：双击空白/背景区域和右下角悬浮笔开关都可进入圈画模式，避免只依赖单一入口。
- 圈画模式隐藏前景 UI 且退出自动清空：教师可专注圈画背景重点，同时避免回到训练界面后残留标记干扰孩子。

# Relevant Files
- `src/components/training/TrainingLayout.vue`
- `src/components/training/TeacherAnnotationLayer.vue`
