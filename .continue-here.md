# Current State
- 任务：A4 资源文件生命周期。Phase 1 已 push（`30e4ad2` + `ab3b0d7`）。**AI 生图功能移除已完成**（本会话，commit 待提交），link D 孤儿源从根消除。下一步 Phase 2 顺延。
- 已完成（Phase 1，已 push）：删/替即清物理文件（hardDelete/updateResource 钩入 + 跨表引用计数）+ 托管路径工具（resource-file-refs/resource-file-service）+ 删 link A 死代码。type-check ✅ 单测 ✅。
- 已完成（AI 生图移除，本会话）：删 `scene-image-generation.ts`（整文件）+ `main.mjs` Gemini 生图基建（GEMINI_IMAGE_MODEL / getGeminiApiKey / generateGeminiSceneImage / sanitizeFileSegment / mimeTypeToExtension + `ai:generate-scene-images` IPC）+ 两 editor AI 按钮/候选网格/handlers + §3.5 helper（`purgeAbandonedSceneCandidates` / `ManagedFileRef`）；保留 `imageUrl` 手动输入。type-check ✅。diff 5 文件 +3/-515。

# Next Action
- **Phase 2**（备份纳入物理文件 zip 归档 v3.0，计划 `docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md` §4）：
  1. §4.1 `src/utils/crypto.ts` 加 `encryptBytes` / `decryptBytes`（纯 crypto-js，无新依赖）
  2. §4.2 `main.mjs` + `preload.mjs` + `electron.d.ts` 加 `pack-resource-archive` / `unpack-resource-archive` / `walk-dir` IPC
  3. **遇 §7 P1 `fflate` 依赖停下问用户**（备选手写 store-only zip，不引原生依赖）
  4. §4.3/§4.4 `backup.ts` export/import 钩入 `resourceArchive`，`BACKUP_VERSION` 2.0→3.0（保留 2.0/1.0 兼容）
  5. type-check + 真机验证（导出→清库清 resources→恢复→图片复显）
- 顺带技术债（可选）：§3.6 link B（`resource-manager.ts` 废弃 getAppPath）有活引用（`PlanList.vue openWithSystem`）未删，另起子任务。

# Blockers
- Phase 2 §7 P1：`fflate` 依赖待用户确认（备选手写 store-only zip）。

# Key Decisions
- 托管路径规则（全局约束）：托管 = uploaded/ + teaching-materials/；预置 = docs/images/videos/audio/（永不删）。
- AI 生图功能确认移除（非禁用）：link D 孤儿源从根消除，比 §3.5 regenerate 清理更干净 → §3.5 随之废弃。
- §3.6 link B（resource-manager.ts）有活引用（PlanList.vue openWithSystem）未删，另起子任务；软删 deleteResource 不动文件（可恢复语义）。
- ⚠️ Write 工具会把内容里 `\uXXXX` 解释成字面控制字节——写文件避免 `\u` 转义，或事后 perl 修。

# Relevant Files
- Phase 2：`docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md`（§4/§7 P1）、`src/utils/backup.ts`、`src/utils/crypto.ts`、`electron/main.mjs`、`electron/preload.mjs`、`src/types/electron.d.ts`
- 已移除（参考，勿改）：`src/services/scene-image-generation.ts`（已删）、两 editor 的 AI 代码、`resource-file-service.ts` §3.5 helper
