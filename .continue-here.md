---
status: active
last_updated: 2026-04-08
scope: emotional module — emotion_scene + care_scene 顶层场景图路径收口已完成，待运行时验证
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**情绪行为模块 `emotion_scene` 和 `care_scene` 的顶层场景图路径收口均已完成，两条资源链已统一到同一套预置资源策略。**

- **已完成**:
  - `emotion_scene` 80 条场景：
    - canonical seed: `docs/references/current-emotion-scenes-export.json`
    - 逻辑路径: `images/emotional-scenes/<file>`
    - 预置资源目录: `assets/resources/images/emotional-scenes/`
  - `care_scene` 60 条场景：
    - canonical seed: `care_scenes_database.json`（已修复为合法 JSON 数组）
    - 逻辑路径: `images/care-scenes/<file>`
    - 预置资源目录: `assets/resources/images/care-scenes/`
  - 共享预置资源解析层: `src/utils/preset-resource.ts`
    - 已包含 `/assets/care_scenes/` → `images/care-scenes/` 旧路径映射
  - 一次性修复脚本: `scripts/fix-care-scenes-json.mjs`（可删除）
  - 已验证：
    - `emotion_scene`: 80/80 imageUrl 全部为 `images/emotional-scenes/...`
    - `care_scene`: 60/60 imageUrl 全部为 `images/care-scenes/...`
    - 旧 `/assets/...` 引用已归零
    - 60 张 care-scene 文件已复制到 `assets/resources/images/care-scenes/`
    - 60/60 imageUrl → 本地文件逐条命中，缺失数为 0
    - type-check 通过（报错均为已知的非本轮引入问题）

- **当前仍未完成**:
  - `care_scene` 训练页运行时验证：尚未在 Electron 或 dev server 中确认顶层场景图能否稳定显示
  - `care_scene` AI 配图端到端联调仍然未跑完

## 下一步

**第一个动作**: 启动 dev server 或 Electron 联调，验证 `care_scene` 训练页是否能稳定显示顶层场景图。具体验证路径：
1. `npm run dev` 或 `npm run electron:dev`
2. 进入情绪模块 → 关怀场景选择 → 确认场景卡片图片正常加载
3. 进入资源中心 → 确认 care_scene 资源封面图正常加载

## 遇到的问题 / 阻塞 / 风险

1. **仓库级 type-check 仍被旧问题阻塞**：
   - `src/components/emotional/games/EnergyBallGame.vue`
   - `src/components/emotional/games/VisualSupportOverlay.vue`
   - `src/components/login/StarfieldTunnel.vue`
   - `src/composables/useEmotionDetector.ts`
   - 这些错误不是本轮引入，但仍然阻塞"本轮代码全仓绿灯"验证
2. **`docs/` 当前只是构建期 seed 来源，不是安装包运行时依赖**
   - 真正运行时依赖的是打包进安装包的 `assets/resources`

## 本次对话做出的关键决策及原因

1. **`care_scene` 60 条 JSON 修复策略：用状态机提取所有顶层对象，重建为合法数组**
   - 原因：原文件是多个 JSON 数组/对象拼接的残损结构，60 条虽全部存在但无法被标准 JSON.parse 解析

2. **`care_scene` imageUrl 统一改为 `images/care-scenes/<local-file>.png`**
   - 原因：与 `emotion_scene` 的 `images/emotional-scenes/` 模式一致，复用同一套 preset-resource 解析链

3. **在 `preset-resource.ts` 新增 `/assets/care_scenes/` 旧路径映射**
   - 原因：已持久化到 DB 的旧路径记录仍需在运行时正确解析，不能假设所有用户都是全新 DB

4. **本轮仍不提交 `assets/care_scenes/` 原始素材目录到主线**
   - 原因：该目录是 AI 生成素材的原始副本，运行时只消费 `assets/resources/images/care-scenes/` 中的规范化副本
