# Current State
- 任务：本次会话完成首页看板两个面板的口径重做（智能特教助理 / 本周异常预警）并已提交；**下一任务改为首页看板下半部分 UI 布局重排**（三面板纵向堆叠、观感差）
- 已完成：评估面板 18 量表覆盖修复 + 评估缺口/优先建议（提交 78d2952）；异常面板扫描切统一训练主表 + 修提示依赖死规则 + 补信号 + 严重度排序（提交 f4b0ff6，10 文件）；`npm run verify:release` 全绿（type-check 0 error、node 140/140、jiti 全过含 10+7 场景、build:web 40.9s）
- 产出物：提交 `f4b0ff6`（本轮）/ `78d2952` / `9fb0418`；口径真源文档 `docs/planning/2026-09-21-首页智能特教助理-评估缺口与干预建议.md`、`docs/planning/2026-09-21-首页本周异常预警-判定口径.md`

# Next Action
- 读 `src/views/Dashboard.vue` 下半部分（`今日训练日程` / `本周异常预警` / `智能特教助理` 三个 `article.dashboard-surface` 面板的模板 + `.dashboard-board__stack` / `.alert-panel` 相关 CSS），输出当前布局结构图 + 2–3 个候选排布方案（含取舍与截图影响），等用户选型后再改代码

# Blockers
- None

# Dead Ends
- 纯 node 下 jiti 无法加载 `src/services/assessment-score-adapters.ts`（import 链里 `init.ts` 直接 `import ... from '*.sql'` → ERR_UNKNOWN_FILE_EXTENSION）→ 真库复算脚本改为从适配器源码正则解析各量表等级列
- `git` 工具 push 传 `ref: main` 报 "'main' does not appear to be a git repository"；须传 `ref: origin main`（remote+branch 空格分隔）

# Key Decisions
- 两个面板判定口径各有独立真源文档（评估缺口面板：明显偏弱 > 尚无评估 > 偏弱需关注 > 缺口 ≥3 域 > 超期；异常面板：统一主表 `training_session` + 四信号 + 严重度排序）；**改布局不得动判定口径**
- 显示层已统一：两面板均只显示前 4 条 + 超量「查看全部」弹窗 + 条目可点进学生详情（共享 `dashboard-insight-*` 样式类）；指标卡数字与面板同源
- 待办后续（未排期）：异常阈值升到 5 / 改相对基线（见专题档 §5）；`PROMPT_LEVEL_LABELS` 与 `equipment/Records.vue` 同名表下一轮统一

# Relevant Files
- src/views/Dashboard.vue（本次改动目标：三面板模板 + `.dashboard-board__stack` / `.alert-panel` 样式）
- src/database/dashboard-api.ts（面板数据接口，只读参考，勿改口径）
- docs/planning/2026-09-21-首页本周异常预警-判定口径.md、docs/planning/2026-09-21-首页智能特教助理-评估缺口与干预建议.md（口径真源）
- scripts/manual/capture-user-manual-screenshots.mjs（首页族截图 S008/S010 依赖 `.dashboard-hero` 与面板文案，改布局后需复核截图契约）
