# Current State
- 任务：侧栏「记录与系统」重排 + 登录默认主题改静蓝 + 登录背景降级链保留。三项均已实现并验证。
- 侧栏顺序：训练记录 → 报告生成 → 资源管理 → 系统管理（`src/views/Layout.vue` records-and-system 固定顺序；训练记录/资源管理为 `moduleManaged`，必须经 ModuleRegistry 准入 + 统一授权过滤）。
- 资源命名收口：feature/路由标题/页面标题均为「资源管理」（`module-registry.ts`、`router/index.ts`、`admin/ResourceCenter.vue`）。
- 默认主题：`DEFAULT_LOGIN_THEME_VARIANT='calm-blue'`、`DEFAULT_LOGIN_PRIMARY_COLOR='#4FB3BF'`；DB 初始化（init.ts×2、sqljs-init.ts）、mock-data、SystemSettings 初始值、登录组件 CSS 兜底与 variant 默认值全部同步。已持久化配置不被覆盖。
- 降级链：GalaxyBackground.vue 视频 → 图片 → StarfieldTunnel 程序化背景，新增契约测试锁定。
- 测试：新增 `scripts/tests/sidebar-menu-contract.test.mjs`（已入 `test:core:node`），扩展 `login-theme-background.test.mjs`（默认值 + 降级链）。相关测试 12/12、`npm run type-check`、`npm run build:web` 均通过。

# Next Action
- 等待用户验收；确认后小步提交（模块注册表/路由/Layout/资源页为一组，主题默认为一组，测试为一组）。

# Blockers
- Electron 实机登录背景视频播放（解码/autoplay/resource:// 运行时回退）未验证——MP4 结构可解析性已 ffprobe 验证，文件存在性有测试断言。
- 既有失败与本任务无关（HEAD 上同样失败）：`module-registry-consistency`（social/cognitive 期望 experimental 但实际 active）、`production-debug-boundary`（DevTools surface）、AI 悬浮入口组件测试。

# Key Decisions
- custom 主题初始调色板保留 `#E6B93C`（那是自定义初始色，不是默认主题）；warm-glow 仍为可选主题，其预设全部保留。
- `login_theme_backgrounds` JSON 中按变体存储的 warm-glow 媒体配置是合法数据，未改动。
- 固定菜单条目不绕过授权：`moduleManaged` 仅要求注册表提供入口，entitlement/角色过滤仍在排序前统一执行。

# Relevant Files
- `src/views/Layout.vue`、`src/core/module-registry.ts`、`src/router/index.ts`、`src/views/admin/ResourceCenter.vue`
- `src/utils/login-theme.ts`、`src/database/{init,mock-data,sqljs-init}.ts`、`src/views/system/SystemSettings.vue`
- `src/components/login/{GalaxyBackground,StarfieldTunnel,PrimaryButton,InputField,LoginCard}.vue`
- `scripts/tests/{sidebar-menu-contract,login-theme-background}.test.mjs`、`package.json`
