# Current State
- 任务：学生与当前账号头像功能已实现并推送，待 Electron 实机 UAT。
- 已完成：学生/个人资料共用 `AvatarPicker`；`user.avatar_path` 已覆盖 schema、迁移、API、Pinia、localStorage 与顶栏；预置头像存 canonical 路径，上传/拍照存 Canvas 压缩 Data URL；`type-check`、头像契约、`verify:release`、Windows 打包均通过。
- 产出物：`b3d3af2`、`488199a`、`src/components/common/AvatarPicker.vue`、`assets/resources/images/user-avatars/`。

# Next Action
- 在 `release/win-unpacked` 启动 Electron，完成一次个人资料头像保存后重启并确认头像仍正确显示。

# Blockers
- None

# Key Decisions
- 预置头像 DB 仅存 `images/user-avatars/...`，显示时经 `resolvePresetResourceUrl()`。
- 上传与拍照只存 Canvas 压缩 Data URL，不接入托管文件生命周期。
- 学生与个人资料分别限制为各自的 6 张预置头像，复用同一选择器状态机。

# Relevant Files
- `src/views/Profile.vue`
- `src/components/common/AvatarPicker.vue`
- `release/win-unpacked/`
