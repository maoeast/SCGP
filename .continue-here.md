# Current State
- 任务：完善 AI 智能体流式响应与最近用户消息编辑链路
- 已完成：响应头超时与流式 idle watchdog 已拆分；最近一条无附件用户消息支持编辑并替换后重新生成；数据库事务、附件清理、真实消息 ID 与契约测试已补齐
- 验证：`npm run type-check`、`npm run test:core:node`（84/84）、`npm run build:web`、`git diff --check` 均通过
- 产出物：`electron/handlers/ai.mjs`、`electron/handlers/ai-stream.mjs`、`src/features/ai/components/AiChatTranscript.vue`、`src/features/ai/components/AiAssistant.vue`、`src/stores/ai.ts`、`src/database/ai-api.ts` 及对应契约测试

# Next Action
- 手动验证真实 provider 的流式响应，以及主聊天最近一条纯文本用户消息的编辑、失败保留和重新生成流程

# Blockers
- 自动化 UI 夹具未完成隔离激活流程；不影响静态与核心契约验证，待手动验收

# Key Decisions
- HTTP 请求超时只覆盖等待响应头；响应头到达后不再限制流式响应总时长
- 首个有效内容 token 到达前不启动 idle watchdog；首 token 后连续 60 秒无新数据才超时
- 编辑只允许当前会话最后一条不含附件的用户消息，替换原轮次并截断旧回复
- 历史预览与管理员审计保持只读；激活码仅作为临时测试环境变量使用

# Relevant Files
- electron/handlers/ai.mjs
- electron/handlers/ai-stream.mjs
- src/features/ai/components/AiChatTranscript.vue
- src/features/ai/components/AiAssistant.vue
- src/stores/ai.ts
- src/database/ai-api.ts
- scripts/tests/ai-stream-timeout.test.mjs
- scripts/tests/ai-message-edit-contract.test.mjs
