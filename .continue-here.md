# Current State
- 任务：为 SCGP 规划并落定 A1 方案，将 Self-Care ATS 的“任务训练链”迁入 SCGP，并并入 `life_skills` 主线
- 已完成：已完成迁移方案设计并写入 `docs/planning/2026-05-08-self-care-training-module-a1-migration-design.md`；已锁定入口结构（`/self-care/*`）、数据落位（`sys_training_resource / sys_training_plan / training_records / training_session`）和 4 个迁移阶段
- 产出物：`docs/planning/2026-05-08-self-care-training-module-a1-migration-design.md`

# Next Action（必须原子级——第一步可执行的单一动作）
- 基于 `docs/planning/2026-05-08-self-care-training-module-a1-migration-design.md` 编写详细实现计划，拆成可执行任务清单

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- `自理训练` 作为 SCGP 新顶层入口：需要与 `情绪行为`、`游戏训练`、`器材训练` 同级，但授权继续挂 `life_skills`
- 旧 `task` 不作为新主链保留：统一映射到 `sys_training_resource`，资源类型约定为 `task_training`
- 旧 `train_plan / train_log` 不迁成新主链：训练计划与训练记录统一接入 SCGP 现有 `sys_training_plan / training_records / training_session`

# Relevant Files（最小集——仅列下一步会用到的）
- `docs/planning/2026-05-08-self-care-training-module-a1-migration-design.md`
- `docs/planning/`
- `.continue-here.md`
