# Current State
- 任务：AI 会话学生绑定放宽（v4.2）——老师一个对话窗口连续分析多个学生的真实习惯
- 已完成（未提交）：
  - bindSessionStudent 语义：从「有消息即锁定」改为「整理完可改绑」——活动批次（pending/summarizing）或有未总结消息时拒绝，水位追平后可随时更换
  - 归属安全：批次创建时快照 student_id，改绑不影响已创建批次；改绑前消息归原学生、改绑后归新学生
  - 总结 prompt v2：增加「涉及绑定学生以外的学生信息一律忽略」防串档
  - AI 抽屉绑定 UI：下拉不再锁定；未绑定提示「不选择也能对话（不会记录长期记忆）」
  - 契约测试更新：data-contract（绑定语义）/ governance-contract（UI）/ permission-contract（修复既有过时断言 label= → div.compliance-label）
  - 设计文档 §4.3 同步 v4.2 变更说明
  - 验证：type-check ✅ / memory 契约 29 全过 ✅ / test:core:ts ✅
- 注意：test:core:node 全套 119 中 5 个**既有失败**（login theme 3、social/cognitive experimental 1、DevTools 边界 1），与本次改动无关，未处理

# Next Action
- 手动验收：AI 抽屉选学生 → 发消息 → 换学生（改绑成功）→ 确认原学生记忆与新学生记忆各归各档
- 验收通过后提交推送

# Blockers
- None

# Key Decisions
- v4.2 语义：绑定=「当前对话关注的学生」，可随时更换；记忆归属按改绑时刻划分（批次快照保证）
- 未绑定也能对话（AI 分析走工具不依赖绑定），只是不产生记忆——UI 文案已明示
- 既有 5 个 node 契约失败（login theme / entitlement experimental / devtools）非本次引入，未顺手修（窄范围原则），可另行处理
- 改动文件：src/database/ai-api.ts、src/services/ai-memory.ts、src/features/ai/components/AiAssistant.vue、scripts/tests/ai-memory-{data,governance,permission}-contract.test.mjs、docs/planning/2026-08-05-ai-student-memory-design.md

# Relevant Files
- src/database/ai-api.ts（bindSessionStudent）、src/services/ai-memory.ts（prompt v2）
- src/features/ai/components/AiAssistant.vue（绑定栏）
- scripts/tests/ai-memory-data-contract.test.mjs、ai-memory-governance-contract.test.mjs、ai-memory-permission-contract.test.mjs
- docs/planning/2026-08-05-ai-student-memory-design.md（§4.3）
