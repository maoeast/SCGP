# Current State
- 任务：认知发展模块 Phase 3「综合认知自测·视空间图形匹配绩效题」(cognitive_self) **已实现并提交推送**（main = origin/main，0/0）。
- 已完成：Phase 1(BRIEF) + Phase 2(CRT) + Phase 3(cognitive_self) 全链入库。Phase 3 拆 2 提交：`7dd6516`(数据层 + CognitiveSelfDriver + useTrialTimer 计时引擎 + PerformanceTrialBoard) / `b735071`(cognitive_self_assess 表 + 注册 + 目录 + 维度映射 + 路由 + Report.vue)。type-check ✅、training-route-access 4/4 ✅。
- 产出物：提交 `7dd6516` / `b735071`（已推送 origin）；核心能力为评估主链**首个试次级真反应时绩效题**——`isPerformanceTask` flag 分流渲染 `PerformanceTrialBoard`。

# Next Action
- 用户真机 E2E：认知 tab → 综合认知自测 → 跑 12 题流程，确认 ①图片渲染 ②计时（RT 非零且合理）③落库 ④报告展示正确率/RT/维度。

# Blockers
- 真机 E2E 未跑（type-check 过 ≠ SVG 渲染/计时交互可用）。
- 题库（12 题）与常模均为 DRAFT，需专业心理测量审核 + 本地常模采集。
- `assets/resources/images/raven60/`（420 张瑞文 SPM 图）**已取得授权**，当前仅本地保留（已加入 .gitignore），暂不纳入版本库；如需接入可随时纳入。

# Key Decisions
- 计时自包含在 `PerformanceTrialBoard`（`useTrialTimer` + `watch(question)`），`AssessmentContainer` 不新增 watch——既有 14 量表零回归。
- `report_type` 三处双写（init.ts CHECK + migrate CHECK + needsMigration 谓词），漏任一旧库不迁移。
- 命名约定：report_type 字面量 `cognitive_self`（下划线），URL/组件目录 `cognitive-self`（连字符）。

# Relevant Files
- 真机 E2E 主查：`src/views/assessment/components/PerformanceTrialBoard.vue`、`src/views/assessment/composables/useTrialTimer.ts`、`src/strategies/assessment/CognitiveSelfDriver.ts`、`src/views/assessment/cognitive-self/Report.vue`、`src/database/cognitive-self-data.ts`
