# Current State
- 任务：将软件更新入口移到“系统管理 → 关于”，并把桌面端默认更新源切换到 `https://upadate.hzxckj308.com/scgp/win`
- 已完成：`src/views/System.vue` 已移除“开发者调试”中的更新面板，并在“关于”页签复用 `UpdatePanel`
- 已完成：`electron/preload.mjs` 已修复更新事件桥接，渲染层可收到 `_event + payload`
- 已完成：`electron/handlers/update.js` 已从 GitHub Releases 默认源切到 `generic provider`，默认地址为 `https://upadate.hzxckj308.com/scgp/win`，未配置更新源时返回明确错误
- 已完成：`package.json` 已新增 `build.publish` 自有更新源配置，并将 Windows 安装包名固定为 `scgp-setup-${version}.exe`
- 已完成：新增 `scripts/tests/update-module-contract.test.mjs` 与 `scripts/tests/update-provider-config.test.mjs`，已通过
- 已完成：`release/自有更新站点部署说明.md` 已生成；`release/latest.yml`、`release/scgp-setup-1.0.1.exe`、`release/scgp-setup-1.0.1.exe.blockmap` 已按新域名重新构建
- 产出物：`electron/handlers/update.js`、`electron/preload.mjs`、`src/views/System.vue`、`package.json`、`scripts/tests/update-module-contract.test.mjs`、`scripts/tests/update-provider-config.test.mjs`、`release/自有更新站点部署说明.md`

# Next Action
- 将 `release/latest.yml`、`release/scgp-setup-1.0.1.exe`、`release/scgp-setup-1.0.1.exe.blockmap` 上传到 `https://upadate.hzxckj308.com/scgp/win/`

# Blockers
- None

# Key Decisions
- 更新源改为 `generic provider`：避免继续依赖 GitHub Releases，便于走自有站点分发
- Windows 安装包名固定为 `scgp-setup-${version}.exe`：保证 `latest.yml`、安装包与 `.blockmap` 文件名始终一致
- `release/` 部署文档不纳入 Git：`release/` 已被 `.gitignore` 忽略，文档作为本地交付物保留

# Relevant Files
- release/latest.yml
- release/scgp-setup-1.0.1.exe
- release/scgp-setup-1.0.1.exe.blockmap
- release/自有更新站点部署说明.md
- electron/handlers/update.js
