# Current State
- 任务：游戏训练 IEP 闭环扩展 Phase 2+3（Tier2 F04/L01/L02/L04 + Tier3 F02/F01/F05），来源 docs/plans/2026-07-07-game-iep-extension-plan.md。
- 已完成：归一化层加 accuracyDerived（派生正确率）+ reactionArrayField（数组反应时）+ 7 游戏规则；生成器加对应 7 段评估。npm run type-check exit 0；jiti 断言 Phase2 15条 + Phase3 19条全过后删除。守卫/路由/统计卡/名字映射/通用 summary 均 Phase 1 已就绪，本批未改 GameContainer.vue / IEPReport.vue。
- 产出物：commit 974a69b（已推送 origin/main）。

# Next Action
- 用户在真机 npm run electron:dev，通关新增 7 游戏，核对 training_records + report_record + IEPReport 专属段 + 缺指标卡隐藏。

# Blockers
- 真机 E2E 未跑（本环境无法驱动交互式 Electron 游戏 + 查 DB）。

# Key Decisions
- Tier2 用派生正确率 correct/(correct+wrong)；Tier3 近似指标口径（F02 轨迹精度 / F01 覆盖率 / F05 抑制控制 early_taps）。F05 反应时 window_response_ms 是数组→reactionArrayField 取均值。
- G07_MONSTER 延后为独立任务：属 emotional-regulation 入口（不在 IEP 放行集合），启用需改守卫 + G07 专属白名单（否则 Tier4 G 游戏被一起放进 IEP）+ IEPReport 情绪匹配分支 + 新生成器。
- 字段全对照真实 emit；F04 时长用会话兜底（average_layout_ms 是每关平均非总时长）。

# Relevant Files
- src/utils/game-performance-normalizer.ts
- src/utils/iep-generator.ts
