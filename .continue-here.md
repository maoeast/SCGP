# Current State
- 当前任务：收口并验收 SCGP cross-entry custom games Wave 1 最小目标 `F01_CLOUD_ERASE`
- 已完成到哪一步（具体到函数 / 文件 / 行为）：
  - `src/data/custom-game-registry.ts` 已新增 `F01_CLOUD_ERASE` registry，挂到 `fine-motor` / `ModuleCode.SENSORY` / `/emotional/games/cloud-erase`
  - `src/views/emotional/games/CloudErasePage.vue` 与 `src/components/emotional/games/CloudEraseGame.vue` 已接通最小游戏页，复用擦除玩法并改为云层文案 / `BADGE_CLEAR_SKY`
  - `src/router/index.ts` 已注册 `/emotional/games/cloud-erase`，并给 `EmotionalGameRecordDetail` 标记 `meta.activeMenu = '/training-records'`
  - `src/views/games/GameModuleMenu.vue` 已把 registry-backed custom games 纳入入口游戏数统计，`fine-motor` 首页不再固定显示 `0`
  - `src/database/emotional-games-api.ts#getStudentRecordsByEntry()` 与 `src/views/training-records/components/GameRecordsPanel.vue` 已按 `entryCode` 展示 custom game 训练记录
  - `src/views/emotional/GameRecordDetail.vue` 已按真实 `entry_code` 返回 `/training-records/:entryCode?type=game`
  - `src/views/Layout.vue#isActive()` 已改为 `activeMenu` 排他高亮；`npm run type-check` 已通过
- 当前产出物（代码 / 文件）：
  - `src/data/custom-game-registry.ts`
  - `src/components/emotional/games/CloudEraseGame.vue`
  - `src/views/emotional/games/CloudErasePage.vue`
  - `src/router/index.ts`
  - `src/views/games/GameModuleMenu.vue`
  - `src/database/emotional-games-api.ts`
  - `src/views/training-records/components/GameRecordsPanel.vue`
  - `src/views/emotional/GameRecordDetail.vue`
  - `src/views/Layout.vue`

# Next Action
- 下一步第一个动作（必须原子级，例如：修改 xx 文件的 xx 函数）
  - 刷新 `emotional/game-record` 详情页，验证左侧只高亮“训练记录”；若仍双高亮，先检查 `src/views/Layout.vue#isActive()`

# Blocking / Risks
- `Layout.vue` 的 `activeMenu` 排他高亮修复已过 type-check，但尚未人工刷新复验
- 工作区存在大量 `.codex/**` 无关修改；后续提交必须继续排除

# Key Decisions
- `F01_CLOUD_ERASE` 通过新增 `CloudErasePage / CloudEraseGame` 复用 `WipeSadnessGame` 擦除链；原因：遵守“不做额外重构”
- custom game 继续写入 `game_emotion_records`，通过 `entryCode` 跨入口展示到训练记录；原因：复用现有持久化与详情页链路
- 详情页保留 `/emotional/game-record` 路由，但用 `meta.activeMenu` 归到“训练记录”；原因：最小改动修复侧栏高亮

# Relevant Files
- `src/views/Layout.vue`
- `src/router/index.ts`
- `src/views/emotional/GameRecordDetail.vue`
- `src/views/training-records/components/GameRecordsPanel.vue`
