# Current State
- 任务：SCGP 入门视频自动生成工具链首支样片「管理员：班级与学生管理」v6 已通过流程验收；清晰度和音频体验留待下一会话定位。
- 已完成：`scripts/video/` 的分镜、旁白、隔离录制、字幕合成工具链已落地；5 个场景 dry-run、正式录制与合成均通过，用户确认整体流程可用。
- 产出物：`output/videos/管理员：班级与学生管理.mp4`（1280×800、H.264 + AAC、50.96s）及 `scripts/video/` 工具链。

# Next Action
- 运行 `ffprobe -hide_banner -show_streams "output/videos/管理员：班级与学生管理.mp4"`，确认最终样片的音视频流参数，为清晰度与“试听无声”问题分流。

# Blockers
- None

# Dead Ends
- ffmpeg `gdigrab` 按 Electron 窗口标题录制不可行：开发窗口标题无法稳定匹配，已改为 Playwright 读取窗口 bounds 后按桌面区域录制。
- Electron 强制全屏录制不可行：会采集约 3842×2402 物理像素并造成编码积压，当前固定逻辑窗口 1280×800（物理 1920×1200）后输出 1280×800。

# Key Decisions
- 工具链采用 Playwright Electron + `msedge-tts` + 外部 ffmpeg，不新增产品运行时依赖；Remotion 因商业许可要求排除。
- 当前 v6 先冻结，不在本会话调整清晰度或音频；下一会话先区分 MP4 编码问题与播放器/系统硬件输出问题。
- 录制从演示数据包解压到临时 userData，等待 SPA 路由就绪后注入激活缓存，避免污染正式库。

# Relevant Files
- `output/videos/管理员：班级与学生管理.mp4`
- `scripts/video/record.mjs`
- `scripts/video/compose.mjs`
