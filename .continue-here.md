# Current State
- 本轮已完成并验收：
  - 统一评估容器的“断点续评”主链已落地，不再停留在只有 `saveProgress()`、没有真实恢复的过渡态。
  - 新增 `src/views/assessment/assessment-progress.ts`，统一处理评估进度快照的保存、读取、清除、恢复态解析和旧快照兼容。
  - `src/views/assessment/AssessmentContainer.vue` 已接入真实恢复流程：
    - 保存并恢复 `phase`
    - 保存并恢复 `currentIndex`
    - 保存并恢复 `answers`
    - 保存并恢复 `startTime`
    - 保存并恢复 `metadata`
    - 保存并恢复 `cbclStep`
    - 保存并恢复 `socialFormData`
    - 保存并恢复 `currentPage`
  - `src/strategies/assessment/CBCLDriver.ts` 已新增 `clearSocialData()`，避免 Driver 单例缓存造成“看起来能恢复、实际只是内存残留”的假象。
  - 已补旧版无版本号 `localStorage` 快照兼容，避免之前保存过的进度在升级后直接失效。
- 当前代码现实：
  - 当前接入统一评估容器的 12 个量表都共享这套断点续评主链：
    - `sm`
    - `weefim`
    - `csirs`
    - `conners-psq`
    - `conners-trs`
    - `sdq`
    - `srs2`
    - `cbcl`
    - `cnbsr2016`
    - `fine_motor`
    - `gmfm_88`
    - `tgmd_3`
  - 其中 `sm / fine_motor / cnbsr2016` 继续依赖各自已有的 `metadata` 运行态，容器现在会一并恢复这些状态。
  - `cbcl` 现在不仅恢复行为题答案，还会恢复社会能力表单数据和分页状态，并重新注入 Driver。
  - 本轮没有创建 WIP commit；当前变更仍在工作区中。

# Work Completed
- 新增测试：
  - `scripts/tests/assessment-progress.test.mjs`
- 关键代码改动：
  - `src/views/assessment/assessment-progress.ts`
  - `src/views/assessment/AssessmentContainer.vue`
  - `src/strategies/assessment/CBCLDriver.ts`
- 关键实现点：
  - 初始化时先加载 Driver，再尝试恢复进度；如果用户确认恢复，则直接回填运行态，否则清除旧快照并从欢迎页重新开始。
  - 开始评估、CBCL 社会能力表单提交、分页切换、上一题、跳题/自动补题后的导航、异常保存失败回退，都会再次保存快照。
  - 离开页面时，不再只在 `phase === 'assessing'` 时保存；欢迎页和 CBCL 社会能力表单阶段也会保存。

# Verification
- 已运行并通过：
  - `npm run type-check`
  - `node scripts/tests/assessment-progress.test.mjs`
  - `node scripts/tests/assessment-select-state.test.mjs`
- 当前尚未完成：
  - Electron 运行时的人工中断恢复 UAT
  - 12 个量表逐项人工点测记录

# Next Action
- 如果下一轮继续这个主题，优先做代表性人工 UAT，顺序如下：
  - `weefim`：验证线性量表恢复
  - `sm`：验证基线/回退/继续评估恢复
  - `cbcl`：验证社会能力表单 -> 行为题 -> 中断 -> 恢复
  - `fine_motor`：验证 basal / ceiling / auto-fill 恢复
  - `cnbsr2016`：验证多能区 month group / auto-fill / domain runtime 恢复
- 如果这 5 组通过，再决定是否对剩余 7 个量表补一轮全量人工点测。

# Blockers
- None
- 唯一剩余的是运行时验收，不是当前代码级阻塞。

# Key Decisions
- 断点续评能力放在统一容器层收口，不为各 Driver 再各自造一套恢复机制。
- 高风险量表的运行态继续复用既有 `metadata`，不额外拆新状态结构：
  - `sm`
  - `fine_motor`
  - `cnbsr2016`
- `cbcl` 的多阶段状态必须进入统一快照，否则“容器能恢复”在该量表上不成立。
- 兼容旧快照，而不是要求所有已中断评估都从头开始。
- 本轮先不提交 WIP commit，避免在用户未明确要求的情况下动 git 历史。

# Files Modified
- `src/views/assessment/AssessmentContainer.vue`
- `src/views/assessment/assessment-progress.ts`
- `src/strategies/assessment/CBCLDriver.ts`
- `scripts/tests/assessment-progress.test.mjs`

# Notes
- 当前工作区还包含与本轮无关的既有变更：
  - `scripts/manual/` 未跟踪文件
- 这些内容本轮未处理，也未回滚。
