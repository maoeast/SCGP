# Current State
- 任务：继续自理训练模块阶段 3，补齐自理任务列表到学生选择的“开始训练”入口闭环
- 已完成：`src/views/self-care/TaskList.vue` 已新增“开始训练”按钮与入口上下文提示；`src/views/self-care/SelectStudent.vue` 已创建并复用 `StudentSelector`，可按 `taskId` 选择学生后回推 `/self-care/tasks`；`src/features/self-care/self-care-routes.ts` 已新增 `/self-care/tasks/:taskId/select-student` 路由；对应测试 `scripts/tests/self-care-task-pages.test.mjs`、`scripts/tests/self-care-routes.test.mjs` 已补齐并通过
- 产出物：`src/views/self-care/TaskList.vue`、`src/views/self-care/SelectStudent.vue`、`src/features/self-care/self-care-routes.ts`、`scripts/tests/self-care-task-pages.test.mjs`、`scripts/tests/self-care-routes.test.mjs`

# Next Action（必须原子级——第一步可执行的单一动作）
- 修改 `src/views/self-care/TaskList.vue`，在读取到 `studentId + resourceId` 入口上下文后渲染“当前训练任务”工作区占位，并明确显示 `meta_data.steps[]` 的任务步骤列表

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 自理训练继续挂 `life_skills` 授权：当前授权体系仍以既有业务模块代码为准
- `task_training` 步骤结构继续以 `sys_training_resource.meta_data.steps[]` 为一期事实源：本期不恢复旧 `task_step` 表
- 阶段 3 当前只闭合“任务列表 -> 选学生 -> 回到自理任务壳页”入口链：不伪造尚未实现的执行页

# Relevant Files（最小集——仅列下一步会用到的）
- `src/views/self-care/TaskList.vue`
- `src/views/self-care/SelectStudent.vue`
- `src/database/self-care-task-api.ts`
- `src/features/self-care/task-training-contract.ts`
