# Current State
- 任务：把评估选择页布局重构计划收口为可执行版本，并为下一会话切到代码实现阶段做交接。
- 已完成：
  - `docs/planning/2026-04-13-assessment-layout-refactor-plan.md` 已重写为可执行计划，补齐了真实 `TRAINING_ENTRY_CODES`、单一量表目录、路由授权收口、直链拦截和验收标准。
  - 已明确本期范围只做评估入口展示与直链授权，不改 `AssessmentContainer`、Driver 评分逻辑、报告页和历史 `report_record.module_code` 语义。
  - 已确认 `PROJECT_CONTEXT.md` 本轮无需更新。
- 产出物：
  - `docs/planning/2026-04-13-assessment-layout-refactor-plan.md`
  - `.continue-here.md`

# Next Action
- 新建 `src/features/assessment/assessment-scale-catalog.ts`，先写 `AssessmentScaleCode`、`AssessmentScaleCatalogItem`、`ASSESSMENT_TABS` 和 `ASSESSMENT_SCALE_CATALOG` 的初始定义。

# Blockers
- None

# Key Decisions
- `/assessment` 视为跨模块公共评估入口：后续实现要去掉当前路由上的 `meta.moduleCode = 'sensory'`。
- Tab id 必须使用真实 training entry code：`sensory-integration`、`emotional-regulation`、`soothing-aids`、`social-communication`、`fine-motor`、`life-skills`。
- UI 授权与展示归属拆开：量表目录用 `entryTabs` 表示展示归属，用 `accessModulesAnyOf` 表示顶层模块授权。

# Relevant Files
- `docs/planning/2026-04-13-assessment-layout-refactor-plan.md`
- `src/views/assessment/AssessmentSelect.vue`
- `src/views/assessment/SelectStudent.vue`
- `src/router/index.ts`
