# Current State
- 任务：统一首页看板、侧边栏、游戏训练/器材训练/训练记录菜单的授权可见性规则
- 已完成：新增 `src/utils/access-visibility.ts`，统一定义全局入口始终可见、模块入口按授权可见
- 已完成：`src/views/Dashboard.vue` 已改为仅显示可见快捷入口；`快速发起评估`、`录入训练记录` 设为全局入口
- 已完成：`src/views/Layout.vue` 已改为侧边栏隐藏未授权模块入口，不再显示锁定态
- 已完成：`src/views/games/GameModuleMenu.vue`、`src/views/equipment/EquipmentMenu.vue`、`src/views/training-records/TrainingRecordsMenu.vue` 已只显示已授权入口卡片
- 已完成：新增 `scripts/tests/access-visibility.test.mjs`，并通过 `node scripts/tests/access-visibility.test.mjs` 与 `npm run type-check`
- 产出物：`src/utils/access-visibility.ts`、`src/views/Dashboard.vue`、`src/views/Layout.vue`、`src/views/games/GameModuleMenu.vue`、`src/views/equipment/EquipmentMenu.vue`、`src/views/training-records/TrainingRecordsMenu.vue`、`scripts/tests/access-visibility.test.mjs`

# Next Action
- 启动 Electron 应用，在仅授权 `sensory` 的环境下先人工点测 `/dashboard`，确认只显示“快速发起评估 / 启动感官游戏 / 录入训练记录”，且不显示“情绪场景训练”

# Blockers
- None

# Key Decisions
- 全局入口保持可点：`能力评估 / 游戏训练 / 器材训练 / 训练记录` 与首页对应全局快捷入口不受单模块授权隐藏
- 模块入口未授权直接隐藏：不再使用“显示但加锁”的口径，避免首页、导航、菜单页体验分裂
- 首页和导航共用同一可见性规则：后续新增入口应继续走 `access-visibility.ts`

# Relevant Files
- src/utils/access-visibility.ts
- src/views/Dashboard.vue
- src/views/Layout.vue
- src/views/games/GameModuleMenu.vue
- src/views/equipment/EquipmentMenu.vue
- src/views/training-records/TrainingRecordsMenu.vue
- scripts/tests/access-visibility.test.mjs
