# Current State
- 任务：将 Air Conductor 儿童视角 UI 调整完成，并把当前开发分支安全合并回 `main`
- 已完成：
  - `src/components/games/pose/AirConductorGame.vue` 已移除右侧儿童无用数据面板
  - `src/components/games/pose/AirConductorGame.vue` 已把主提示区改为儿童向大卡片，并按状态分流布局：正常/离框在右下，完成态居中
  - `src/components/games/pose/AirConductorGame.vue` 已降低左上状态卡视觉权重，避免与主提示区抢焦点
  - `scripts/tests/sensory-hand-games.test.mjs` 已更新 Air Conductor UI 结构断言，覆盖右下卡片区和完成态居中状态
- 产出物：
  - `src/components/games/pose/AirConductorGame.vue`
  - `scripts/tests/sensory-hand-games.test.mjs`

# Next Action
- 在独立 `main` worktree 中，将 Air Conductor 相关提交按依赖顺序摘取到 `main`，运行验证后完成合并

# Blockers
- 当前分支 `codex/mediapipe-tiles` 相对 `main` 混有大量无关提交，不能直接整分支 merge

# Key Decisions
- 保持 `GamePlay -> training_records -> training_session -> report_record` 主链不变：本轮只改 UI 呈现
- 视频镜像、pose overlay、粒子、尾迹、波纹、节拍轨迹继续保持同一镜像坐标系
- 不直接合并整个 `codex/mediapipe-tiles`：改为在 `main` 上只整合 Air Conductor 相关提交

# Relevant Files
- `src/components/games/pose/AirConductorGame.vue`
- `scripts/tests/sensory-hand-games.test.mjs`
- `.continue-here.md`
