# Current State
- 任务：评估→IEP→器材推荐→训练计划 四者联动（推荐引擎叠加）—— 代码全部落地，CSIRS 真机 E2E 已通过，WeeFIM 刚修复待真机验证。
- 已完成：6 阶段全落地；`src/features/recommendation/`（types/ability-taxonomy/scale-dimension-mapping/equipment-entitlement/domain-tag-keywords/recommendation-engine/plan-generator + components/RecommendationDrawer.vue）+ `src/stores/recommendation.ts`；init.ts 加 source/source_assessment_id 列（safeAddColumn）+ migrateTrainingPlanSourceColumns；plan-api.ts createPlan 写两列；api.ts ReportAPI.getReportRecordByAssessId；App.vue 挂抽屉；CompleteDialog.vue 加「器材推荐」按钮；AssessmentContainer 传 assessmentId/scaleName；PlanList.vue「由评估生成」徽标+回链。
- 验证：type-check exit 0；build:web 成功；jiti 维度映射断言（12 量表 53 dim，已删）；引擎 smoke（CSIRS+WeeFIM+entitlement 过滤，已删）；**CSIRS 真机 E2E 用户确认通过**（出推荐+生成计划）；WeeFIM 仅 smoke 通过，真机未跑。
- 弱势信号修复：CSIRS dimension 只写 standardScore(T)、WeeFIM 只写 averageScore，均无 severity → 引擎 `SCORE_WEAKNESS_RULES` 兜底（CSIRS T<40w/<30d；WeeFIM avg<3w/<1.5d）。
- 产出物：上述文件 + `docs/plans/2026-07-09-assessment-recommendation-engine-plan.md`（未提交，全在工作区）。
- WeeFIM 等级命名统一（2026-07-09）：真机跑 93 分发现完成弹窗文案矛盾（等级「极轻度依赖」vs 总结「极度弱、几乎完全依赖」）。根因=半迁移：driver 早是 7 级 FIM 官方命名，但数据层 `weefim-data.ts` + 报告页仍是 6 级旧命名，`generateFeedback` 的 `recommendation.find` 永远 miss → 兜底数组末位「极度弱」文案（任何分数都中招，与得分无关）。已把 `weefimLevels`/`weefimRecommendations`/`getWeeFIMLevelDescription` 升 7 级（区间对齐 driver），`Report.vue` 四张等级 map 删 6 级条目并补全原本缺失的「极轻度依赖」档（90-107 段，classMap 用 level-5），`WeeFIMDriver.ts:10` 注释校正。type-check exit 0，真机待验。死导出链（reportExporter/exportUtils/simpleReportPrinter）+ 反向解析工具（parser/mapper/example/test-weefim-levels）+ weefim-data 的 getWeeFIMLevelDescription/inferScoresFromAssessmentText/generateAssessmentFromRecommendations 已清理（4f43be2，-2941 行）；reportTemplates 的 WeeFIM 死模板（generateWeeFIMReport 等）成孤儿 export 留存（SM 边界交织未拆），calculateWeeFIMScores 保留（_archived 依赖）。

# Next Action
- 真机 `npm run electron:dev` 验 WeeFIM：① 跑 93 分附近，完成弹窗等级栏「极轻度依赖」与总结句文案一致、不再出现「极度弱」；② 报告页等级卡/描述/徽章 class/说明均显示 7 级命名、无裸字符串；③ 若要验器材推荐，需跑一份**低分** WeeFIM（各项打 1-2 分触发弱势）——93 分独立性高、无弱势是预期不出推荐（非 bug）。

# Blockers
- WeeFIM 真机未验文案矛盾是否消除（type-check exit 0，但按规约不得声称运行时已验证）。
- commit/push 待用户确认（当前工作区 dirty，未提交）。

# Key Decisions
- entitlement 硬过滤按【器材 category】（非 moduleCode）映射唯一 entitlement；fine-motor 器材 moduleCode=sensory，按 moduleCode 会误判 sensory_integration。禁用模块级 canAccessModuleByEntitlements。
- 入口放 CompleteDialog（全量表共享完成弹窗，持真实 ScoreResult），非 Report.vue×12（report_record 不存 ScoreResult，维度数据在各量表专用表）。
- schema 加列用 safeAddColumn（非表重建 idiom）：两列 nullable 无 CHECK，表重建对 sys_training_plan(FK+索引) 风险更高。
- 弱势兜底=字段+方向感知 SCORE_WEAKNESS_RULES；新缺陷型量表只需加一行。

# Relevant Files
- `src/features/recommendation/recommendation-engine.ts`（SCORE_WEAKNESS_RULES：调阈值/加量表在此）
- `docs/plans/2026-07-09-assessment-recommendation-engine-plan.md`（计划主真源）
- `src/database/weefim-data.ts`（WeeFIM 等级命名真源：weefimLevels/weefimRecommendations/getWeeFIMLevelDescription，已 7 级）
- `src/views/assessment/weefim/Report.vue`（报告页四张等级 map：levelTextMap/descMap/classMap/explanations，已删 6 级补极轻度依赖）
- `src/strategies/assessment/WeeFIMDriver.ts`（generateFeedback 第123行 find + getLevel 7 级真源）
