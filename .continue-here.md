# Current State
- 任务：C12 自动更新链 HTTPS 切换已完整闭环（代码 + 服务器部署 + 真机验证）。
- 已完成：
  - `electron/handlers/update.js` 默认更新源切 `https://maohedong.top/scgp/win`；`LEGACY_UPDATE_URLS` 收纳 hzxckj 与旧 http IP，老用户升级无感迁移到 https。
  - `package.json` publish 切 HTTPS，版本升至 1.0.7；契约测试 `update-provider-config` 同步。
  - 1.0.7 Windows 安装包已打包（未签名，`cscInfo=null`，SmartScreen 蓝屏为已知取舍）并部署到服务器 `/home/lighthouse/scgp/win/`（HTTPS 主源 + HTTP 旧源同物理目录，两源 `latest.yml` 均返回 `version: 1.0.7`）。
  - 真机验证：1.0.6 → 1.0.7 自动升级通过；1.0.7 查更新走 https 显示「已是最新版本」。
  - 新增 `release-deploy` skill（`.claude/skills/release-deploy/SKILL.md`）固化打包→校验→上传→部署→验证两源流程 + 踩坑库；`scripts/release-verify.mjs` + `npm run release:verify` 校验 latest.yml 完整性（防 latest.yml 被 exe 内容覆盖，曾实际发生）。
- 提交：`d4b86fd feat(update)` + `64f6604 chore(release)` 已在本地 main，**ahead origin/main 2，未 push**；工作区 clean。

# Next Action
- 等用户授权 push 这两个 commit 到 `origin/main`。
- C12 已无阻塞。可选下一步：C13（发布终验，若仍适用）、T03（系统设置-开发者调试加更新地址 UI 入口，需 https-only / dev-only 安全设计）、或 AGENTS 列出的优先技术债（备份/恢复全 schema 覆盖、资源文件生命周期、资源收藏、评估入口硬编码、命名残留、开发/迁移工具混入主路由）。

# Blockers
- 无代码阻塞。push 待用户明确授权。
- 历史 C12 外部阻塞均已解除或按决策绕过：HTTPS 源用户自有（`maohedong.top` + DigiCert/Let's Encrypt DV）；签名证书决策不购买；验收机就位并真机通过。

# Key Decisions
- 不购买 Windows 代码签名证书；SmartScreen 蓝屏为已知取舍，安装时用户点「更多信息 → 仍要运行」。
- 自动更新源用 generic provider 自托管：HTTPS 主源 + HTTP 旧源 IP 给 1.0.x 老用户当跳板，代码 `LEGACY_UPDATE_URLS` 自动迁移到 https。
- 服务器部署走 tailscale + `openclaw.pem`（登 `ubuntu` 用户，免密 sudo），不走公网密码。开发机有代理（fake-ip）污染外部 HTTPS/DNS 探测，外部验证必须在服务器或干净设备。
- 开发机 tailnet 节点 `asus-dong`，服务器 `vm-0-8-ubuntu`（`100.114.108.86`），私钥 `E:/Downloads/openclaw.pem`。

# Relevant Files
- `electron/handlers/update.js`（更新源 URL + LEGACY_UPDATE_URLS 迁移）
- `package.json`（version、build.publish、release:verify）
- `scripts/tests/update-provider-config.test.mjs`
- `.claude/skills/release-deploy/SKILL.md`
- `scripts/release-verify.mjs`
