# Current State
- 任务：授权链口径收口已完成，当前需要把后续续接时对授权链的理解统一到同一套表述上，避免再回退到旧的“模块授权直判”心智模型。
- 已完成：
  - `effectiveEntitlements` 已成为前端授权事实来源。
  - `allowedModules` / 许可证 `am` 已降级为“原始授权 code”语义，保留兼容与调试用途。
  - 旧 code `sensory / emotional / social / life_skills / cognitive` 仍会兼容展开为新的授权能力包。
  - 路由拦截、菜单可见性、训练入口判权与模块访问都已切到 `entitlement-first`。
  - `hasModuleAccess()` 已改为由 `effectiveEntitlements` 反推，不再直接依赖原始 `allowedModules`。
  - `System.vue`、`Dashboard.vue`、路由提示、README、手册等用户可见口径已统一为“能力包授权”，开发态只保留“原始授权 code”诊断信息。
  - 本轮验证已通过：`npx jiti tests/entitlement-catalog.test.ts`、`node --test scripts/tests/training-route-access.test.mjs`、`npm run type-check`。

# Next Action
- 如果下一轮继续做授权相关工作，优先做“零散残留清理”或“新入口接入时沿用 entitlement-first 约束”的核查，不要再把 `allowedModules` 当作最终访问判权事实来源。

# Blockers
- None

# Key Decisions
- 用户可见授权口径统一使用“能力包授权”。
- `allowedModules` 继续保留为原始许可证载荷和兼容调试层，不删除，但不再作为最终授权事实来源。
- 菜单级授权缺少显式 `entry/module` 参数时，不允许默认回退到 `sensory`。
- 本专题只处理授权链相关文件，不混入现有大量图片与更新配置脏改动。

# Relevant Files
- `src/features/entitlements/entitlement-catalog.ts`
- `src/stores/auth.ts`
- `src/router/index.ts`
- `src/utils/training-entry.ts`
- `src/utils/training-route-access.ts`
- `src/views/System.vue`
- `src/views/Dashboard.vue`
- `tests/entitlement-catalog.test.ts`
- `scripts/tests/training-route-access.test.mjs`
