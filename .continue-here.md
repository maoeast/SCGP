# Current State
- 任务：认知发展（cognitive）模块 P0 游戏包——K03/K01/K04/K05 均已交付，P0 四个游戏全部完成。
- 已完成：
  - K05_NUMBER_SENSE 数感小铺全链路（number-sense-data.ts + NumberSenseGame.vue + NumberSensePage.vue + registry + router），双模式（按数取物 / 多少比较），三级难度，纯点选交互。
  - K04_ODD_ONE_OUT 落库契约 bug 修复：buildPerformanceData 字段从 accuracy/avg_response_time_ms/顶层 trials 对齐为 accuracy_ratio/average_response_ms/嵌套 actual_params（K03 权威契约）。
  - type-check ✅  type-check:emotional ✅  build:web ✅（built in 1m 3s）。

# Next Action
- P0 实测校准：安排 3–5 名不同诊断类型儿童对 P0 四个游戏（K01/K03/K04/K05）小范围试跑，据实测锁定各难度级数值后再推 P1。
- P1 下一步：K02_MISSING_ITEM（少了什么）→ K09_ECHO_SEQ（序列复现）→ K10_STORY_ORDER（因果排序）。

# Blockers
- None。P0 四游戏全链路已通过验证，等待实测反馈后再定 P1 参数。

# Key Decisions
- 库表方案 B：cognitive 走 training_session，`source_table='cognitive_game_inline'`，actual_params 显式透传 summary_payload；GameContainer 按 moduleCode dispatch。
- PRD §7 第 0 条定位 = B（IEP 级可追踪）：每个认知游戏 performanceData 必须含 actual_params。
- K05 交互：纯点选（无拖拽），模式切换做进游戏内 HUD 分段控件（不依赖 GameContainer 设置面板）。
- 落库契约权威源：K03_PATTERN_NEXT 的 buildPerformanceData（accuracy_ratio / average_response_ms / 嵌套 actual_params），其余 K 系列组件必须与之对齐。

# Relevant Files
- `docs/planning/2026-07-19-cognitive-games-prd.md`（P1 游戏 PRD §4 K02/K09/K10）
- `src/components/emotional/games/PatternNextGame.vue`（K03，落库契约权威样板）
- `src/data/number-sense-data.ts`（K05 出题器）
- `src/components/emotional/games/NumberSenseGame.vue`（K05 游戏组件）
- `src/database/cognitive-games-api.ts`（K 系列公共 API，只认 accuracy_ratio/average_response_ms/actual_params）
