# Current State
- 任务：评估质量追踪（Phase 1-3）与 ATEC 真机修复、AI 适配、模型配置对话框重构——全部完成并已提交
- 已完成：
  - `84cb587` Phase 1：17 量表加 total_duration/avg_response_time/quality_note + 幂等迁移 + 报告用时卡 + 温和提示
  - `ac687ae` ATEC 真机三连修：BaseDriver require→ESM import、report_record CHECK 约束补 abc/atec（旧库自动迁移）、atec Report.vue db.exec→db.all、devtools 同类修复
  - `39ecde4` Phase 2 质量看板（/system/quality，AssessmentQualityAPI 17 表聚合）+ Phase 3 随机作答检测（detectRandomPattern 三信号，仅 crt/cognitive_self，检出追加 '+suspicious' 入库不弹窗）
  - `ab70953` ABC/ATEC 接入 AI 纵向趋势（assessment-score-adapters 注册 15 量表，get_assessment_trend 可用）
  - `3e98981` AiAgentConfig 新增模型对话框重构：顶部快捷拉取工具栏 + 选中自动派生编号（冲突 _2 去重）+ Retiring 过滤
- 产出物：见上述 commit；测试 tests/assessment-quality.test.ts、tests/assessment-random-pattern.test.ts、tests/assessment-score-adapter.test.ts（13 场景）

# Next Action
- 真机冒烟：重启应用（触发 report_record 约束迁移）→ 跑一次 ATEC → 完成无报错 → 报告页底部有时用卡 → admin 打开「评估质量看板」看 ATEC 记录 → 新开 AI 会话问 ATEC 四分量表得分（应返回真实分数）

# Blockers
- None

# Dead Ends
- BaseDriver.saveQualityMetrics 用 CJS require('@/database/init')：ESM 渲染进程抛 ReferenceError 且被 try/catch 吞成 warn → 质量指标从未入库，直到真机日志暴露；必须顶部 ESM import
- atec/Report.vue 用 db.exec(sql, params) 按 sql.js 原生形态读 rows[0].values[0]：SQLWrapper.exec 是 DDL 专用无参无返回 → 恒 undefined 崩溃；查询一律 db.all/get（ABC 报告页同款坑，复制粘贴代码）

# Key Decisions
- 宽松质控：只记录不打扰——quality_note 只入库不进报告页（不做价值判断）；CompleteDialog 仅平均 <2s/题才温和提示；随机作答检出只标记不弹窗，看板是唯一出口
- cognitive_self_assess 的 avg_response_time 是"真反应时 ms"语义（NOT NULL，Driver INSERT 时写），质量列只补 total_duration/quality_note，写入跳过覆盖（saveQualityMetrics skipAvgResponseTime）
- 新增量表 7 处同步清单已存长期记忆（catalog/建表+质量列/report_record 约束×3/api 联合类型+resolveModuleCode/AI 适配器+description/SCALE_DOMAIN_MAP/Driver+Report）

# Relevant Files
- src/views/assessment/atec/Report.vue（冒烟看用时卡）
- src/views/system/AssessmentQualityBoard.vue（看板）
- src/services/assessment-score-adapters.ts（AI 取分）
- src/database/migrate-report-constraints.ts（旧库约束迁移）
