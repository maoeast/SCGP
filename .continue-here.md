# Current State
- 任务：修复“用户登录后退出到登录页，用户名/密码输入框偶发鼠标点击无响应，需要 `Ctrl+R` 或等待一段时间后才能恢复”的问题。
- 已完成：
  - 将 `src/views/Layout.vue` 的退出登录确认从原生 `window.confirm()` 切换为 Element Plus `ElMessageBox.confirm`，避免 Electron 下原生确认框导致窗口焦点和交互状态异常。
  - 在 `src/views/Login.vue`、`src/components/login/LoginCard.vue`、`src/components/login/InputField.vue` 增加登录页输入框焦点恢复链路，页面挂载后会自动重试聚焦用户名输入框，覆盖退出后短时间内的点击失效窗口期。
  - 新增 `src/utils/auth-ui.ts`，收口退出确认和登录页焦点恢复的公共逻辑。
  - 新增回归脚本 `scripts/tests/auth-ui.test.mjs`，锁定“退出确认链路”和“登录页焦点恢复重试”两个关键约束。
  - 已完成人工验收，用户确认“登录 -> 退出 -> 立即点击用户名/密码框”路径通过。

# Next Action
- 无。当前问题已修复并验收通过，等待下一个任务。

# Blockers
- None

# Key Decisions
- 根因判断收口为“退出确认框导致的 Electron 焦点/交互状态异常”与“登录页缺少主动焦点恢复”，而不是账号校验或输入组件本身逻辑错误。
- 本次不做额外重构，只对退出链路和登录页交互恢复做最小修复。
- 不引入新的测试框架，沿用仓库现有 `node + jiti` 轻量脚本方式补回归验证。

# Verification
- `node scripts/tests/auth-ui.test.mjs`
- `npm run type-check`
- 手工验证：用户已确认问题修复生效

# Relevant Files
- `src/views/Layout.vue`
- `src/views/Login.vue`
- `src/components/login/LoginCard.vue`
- `src/components/login/InputField.vue`
- `src/utils/auth-ui.ts`
- `scripts/tests/auth-ui.test.mjs`
