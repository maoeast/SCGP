---
status: active
last_updated: 今天，2026-04-12
scope: 量表评估链逻辑核对与修复 —— 已修复 S-M / FMDA 导航逻辑，并补回 TGMD-3 / GMFM-88 缺失反馈配置与参考资料
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**本次会话（2026-04-12）当前已推进到：S-M 与 FMDA 初步实测正常，随后继续修复了 TGMD-3 完成评估崩溃，并恢复了 TGMD-3 / GMFM-88 后续排查要用的参考资料。**

- **S-M 量表**
  - 修复问题：`6岁2个月` 从第 4 阶段开始时，当前阶段全部通过后错误跳回第 3 阶段。
  - 已完成修复：
    - 新增 `src/strategies/assessment/sm-logic.ts`，把 `连续10项通过建立基线 / 连续10项不通过终止` 抽成纯逻辑实现。
    - `src/strategies/assessment/SMDriver.ts` 已切到新的连续 10 项判定，不再用“整阶段全通过”代替基线规则。
    - `src/views/assessment/AssessmentContainer.vue` 已把 `起始阶段` 与 `当前阶段` 分开显示，避免误导排查。
  - 当前验证结果：
    - `6岁2个月 -> 第4阶段 -> 连续全通过` 不再回跳到第 3 阶段。
    - 新增 `scripts/verify-sm-driver.mjs`，脚本验证已通过。

- **FMDA / 小肌肉功能发展评估量表**
  - 修复问题：以 `陈明峰 6岁2个月` 为例，从第 9 题开始持续选“掌握”，评到约 17 题后提前结束，并给出“显著迟缓”。
  - 已完成修复：
    - `src/strategies/assessment/FineMotorDriver.ts`
    - 各领域起始题现在会向前夹紧，保证起点后至少还能容纳 `3` 题 basal streak。
    - 若当前领域起点已靠近末尾且无法继续向高题建立 basal，不再直接 `complete` 整份量表，而是回退到更低月龄项目继续找 basal，或只结束当前领域后进入下一领域。
  - 当前验证结果：
    - `6岁2个月` 全部选择“掌握”时，不会在约 17 题提前结束。
    - 驱动级验证中可正常走完整份题库并得到 `发展适龄 / age_appropriate`。
    - 新增 `scripts/verify-fine-motor-driver.mjs`，脚本验证已通过。

- **TGMD-3 / GMFM-88 反馈配置缺失修复**
  - 用户实测复现问题：
    - `TGMD-3` 所有题目选满分后，最后 1 题点击完成评估，`AssessmentContainer` 报错 `Cannot read properties of undefined (reading 'total_score_rules')`。
  - 根因确认：
    - `src/strategies/assessment/Tgmd3Driver.ts` 与 `src/strategies/assessment/Gmfm88Driver.ts` 依赖 `ASSESSMENT_LIBRARY.tgmd_3 / gmfm_88`。
    - 当前 `src/config/feedbackConfig.js` 主线版本缺少这两个量表的配置块。
    - 继续追查后确认：相关完整内容不在 `d9109d398082b1002acf41c864104ea8ae9bc795`，而是藏在 `stash@{4}` 的 `feedbackConfig edits` 中。
  - 已完成修复：
    - 已将 `TGMD-3` 与 `GMFM-88` 的反馈配置补回 `src/config/feedbackConfig.js`。
    - 已从 `stash@{4}` 恢复后续排查所需参考资料：
      - `docs/references/GMFM/GMFM-88 计分逻辑.md`
      - `docs/references/GMFM/gmfm88_scoring_engine.html`
      - `docs/references/TGMD-3/TGMD-3 测试技能、器材与路线.xlsx`
      - `docs/references/TGMD-3/TGMD-3上海市常模数据.md`
      - `docs/references/TGMD-3/tgmd3_scoring_engine.html`
      - `docs/references/TGMD-3/大肌肉动作发展测验上海市常模的建立.pdf`
    - 已排除 `TGMD-3` 目录里的临时锁文件 `.~*.xlsx`，未恢复无意义临时件。
    - 本轮修复已单独提交：
      - `b850d35 fix(assessment): restore TGMD-3 and GMFM references`

- **本轮代码级验证 / 状态确认**
  - `scripts/verify-sm-driver.mjs` 通过
  - `scripts/verify-fine-motor-driver.mjs` 通过
  - `npm run type-check` 通过
  - 运行环境仍应优先使用：`/home/DONG/.config/nvm/versions/node/v24.14.0/bin/node`
  - 当前分支状态：
    - `main` 相对 `origin/main` 超前 `1` 个 commit（`b850d35`）

## 下一步

**第一个动作**：

在真实 Electron 应用里，先复验 `TGMD-3` 的原始报错场景：

1. `TGMD-3`：所有题目选满分，完成最后 1 题后点击完成评估，确认：
   - 不再报 `total_score_rules` 相关错误
   - 评估结果能正常保存
   - 报告页能正常展示总体评语与分维度反馈

**紧接着要做的验证**：

1. `GMFM-88`：实际完成一份评估，确认报告页评语和分区反馈恢复正常，不再出现“有 Driver 无 feedback config”的空洞状态。
2. 回头补做 `S-M` 与 `FMDA` 的 Electron 实机点击复验，确认 UI 跳转、落库与报告页仍与前一轮驱动修复保持一致。
3. 如继续出现“Driver 已接入但 config 未进主线”的问题，再考虑给评估 Driver 增加缺配置 fail-fast / 明确报错保护。

## 遇到的问题 / 阻塞 / 风险

1. **TGMD-3 / GMFM-88 这次修的是“配置缺失”而不是“已完成实机验证”**
   - 当前只确认 `feedbackConfig.js` 配置已补回、类型检查通过。
   - 真实 Electron 中的完成评估、保存、报告页渲染仍需立即复验。

2. **仓库仍保留多个历史 stash，不能盲目 `stash pop`**
   - 已审计确认：`stash@{4}` 里确实藏过真实主线依赖。
   - 但 `stash@{0}` / `stash@{1}` / `stash@{3}` 主要是旧快照、实验分支或文档归档，直接恢复会把当前主线拉回旧状态。

3. **FMDA 当前整体等级仍采用全量题目 mastery rate 判定**
   - 这次误判的直接根因已修复，当前“全掌握”场景已恢复正确。
   - 但如果后续用户对 FMDA 的总评口径有进一步业务要求，需要重新确认“是否应按全量 88 题 / 领域闭环题 / 年龄段预期题”中的哪一种算总体等级。

4. **当前评估 Driver 普遍默认配置存在，防御性不足**
   - `Tgmd3Driver` / `Gmfm88Driver` 直接读取 `ASSESSMENT_LIBRARY` 子项，没有先对缺项做显式保护。
   - 如果后续再出现“代码已提交、配置未提交”的情况，仍可能以运行时异常的形式暴露。

## 本次对话做出的关键决策及原因

1. **S-M 改为纯逻辑模块承载基线/上限规则**
   - 原先 `SMDriver` 把“整阶段全通过”误当成基线成立条件，规则口径错误，且不利于复验。
   - 因此把连续 10 项逻辑拆到 `sm-logic.ts`，让导航与粗分都基于同一套规则，减少再次回归。

2. **FMDA 起点必须保证 basal streak 可达**
   - 某些领域按“年龄最近题”直接起评时，后面剩余题量不足 3 题，天然无法建立 basal。
   - 所以起点选择不再只看年龄距离，还必须满足最小可评估窗口。

3. **FMDA 起点靠近领域末尾时，不允许直接结束整份量表**
   - 旧逻辑在当前领域没有更高题可做时，直接返回 `complete`，这是导致“17题提前结束”的根因。
   - 新逻辑改为优先向低题回退建立 basal；实在无法回退，也只结束当前领域并继续下一领域，避免整份量表被错误截断。

4. **TGMD-3 / GMFM-88 缺失配置以 `stash@{4}` 为准恢复，而不是强行从用户提供 commit 回找**
   - 追查后确认 `d9109d398082b1002acf41c864104ea8ae9bc795` 本身并不包含这两段 `feedbackConfig.js` 内容。
   - `stash@{4}` 明确带有 `feedbackConfig edits`，且实际保存了缺失配置与相关参考资料，因此以它为恢复源更接近真实丢失前状态。

5. **只定向恢复 `docs/references/GMFM` 与 `docs/references/TGMD-3`，不恢复整个 stash**
   - 目标是补回后续评估排查确实还要用的参考资料。
   - 避免把 `stash@{4}` 里与当前主线无关的其他历史内容重新混入工作树。

6. **将配置修复与参考资料恢复打成单独 commit**
   - 这样后续如果继续追踪 `TGMD-3 / GMFM-88` 评估链问题，可以直接从 `b850d35` 追溯“缺失配置恢复”这一节点。
   - 也避免本轮修复再次停留在 stash / local changes 中，重演同类问题。
