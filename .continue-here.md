# Current State
- 任务：AI 智能体细化一期 Phase 1（多 provider 抽象）已完成
- 已完成：`ai_provider` 表 + 老库 `deepseek_*` 迁移 + doubao 种子（火山方舟，vision=1/thinking=0/enabled=0，model 留空）；provider CRUD + `getProviderConfig` 读 active 行；`ai.mjs` thinking 按 `supportsThinking` 条件化 + 文案去硬编码；`ai.ts`/`AiAgentConfig.vue` provider 选择器 + 能力位 tag + per-provider 启用。type-check 全绿；真机 DeepSeek 侧绿（老库 Key 迁移 + 流式不回归）。
- 产出物：提交 `1548186`（本地 main，未推送）

# Next Action
- Phase 2 function calling 骨架：新建 `src/services/ai-tool-loop.ts` + `ai-tools.ts`（渲染端 tool 循环），`ai.mjs` 透传 `tools` + 解析 `tool_calls`（首版非流式），首批工具 `list_students`/`get_assessment`/`list_training_sessions`/`get_ai_usage`

# Blockers
- 豆包连接/流式真机验证待火山方舟 Key + 推理接入点 ID `ep-xxx`（后补，非代码阻塞）

# Key Decisions
- provider 抽象：`ai_provider` 表 + 能力位（provider 级），全局（active/budget/总开关）留 `system_config` KV；`getProviderConfig` = active 行 + 全局 KV
- `thinking` 按 `supportsThinking` 条件化（豆包不加）；豆包走 `/chat/completions`，model 填接入点 ID `ep-xxx`，未用 `/responses`
- Phase 2 tool 执行循环放渲染进程（Main 只透传 tools + 解析 tool_calls），零数据层改造

# Relevant Files
- `C:\Users\maoea\.claude\plans\purring-riding-pie.md`（plan §Phase 2）
- `electron/handlers/ai.mjs`（透传 tools + 解析 tool_calls）
- `src/stores/ai.ts`（sendChat 改 tool 循环，注入点 ~308-315）
- `src/database/api.ts`（StudentAPI ~648/670/712、TrainingSessionAPI.listSessions ~3996 等首批工具复用）
