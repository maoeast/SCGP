# Current State
- 任务：报告导出做实（页眉/头像/HTML 导出/默认格式）+ 报告页性别修复 + 报告设置持久化修复——已提交（`8744922` 报告链 / `1af5abd` 看板与手册链）；1.1.4 同版本覆盖发布执行中
- 已完成：报告导出统一分发层（report-export + export-html + export-word 页眉/头像）、SystemSettings 三项设置变更即写库并立即刷盘（修 `saveToStorage` 空操作）、19 页换 exportReport、性别修复（CSIRS 硬编码「未知」→真实性别；ABC/ATEC/视知觉/SDQ/儿心Ⅱ 补性别栏；Conners 英文归一中文）、新库种子补设置默认行
- 验证：`npm run verify:core` 全绿（type-check 0 error、node 155/155、jiti 11 项含 report-export 7 组）

# Next Action
- 1.1.4 同版本覆盖发布：build:web → electron-builder → release:verify → scp 上传 → sudo 部署 → 双源 curl 验证（release-deploy skill 流程）
- 发布完成后更新本文件 Release 记录并提交

# Blockers
- None

# Dead Ends
- edit 批量调用把目录当默认 path → EISDIR 拒绝；edits 数组每条 entry 必须显式写文件路径
- git 工具 push 传 `ref: main` 报 "'main' does not appear to be a git repository"；须传 `ref: origin main`（remote+branch 空格分隔）
- `db.saveToStorage` 在 SQLWrapper 上不存在（只有 `saveNow`）——任何 `if (db.saveToStorage)` 写法都是静默空操作

# Key Decisions
- T26 拍板（2026-09-21）：①页眉真实注入 Word Header/HTML 页眉条（空值不渲染）②头像嵌入（开关/无头像/加载失败三级静默降级）③默认格式 word/html（存量 'pdf' 读取层映射 word，DB 不动）④AI 链固定 Word ⑤单按钮「导出报告」按默认格式导出
- 性别修复拍板（2026-09-21）：新建共享 `formatGenderLabel` 统一中文化（男/女、male/female、M/F → 中文，空值「未填写」）
- 1.1.4 同版本覆盖发布（2026-09-21 第四次）：已装 1.1.4 的机器收不到自动更新（electron-updater 同版本语义），仅新装/手动下载生效——用户明确选择保持 1.1.4
- T26 advisor 遗留（不影响验收）：equipment 器材 IEP 导出仍固定 Word 仅获页眉（补齐需拍板）；docxExporter 四个死函数（约 470 行）待专项清理

# Relevant Files
- src/utils/report-export.ts（分发层）+ export-html.ts（HTML 引擎）+ export-word.ts（Word 引擎扩展）
- src/utils/student-display.ts（formatGenderLabel）+ assessment-word-builders.ts（各量表 meta）
- src/views/system/SystemSettings.vue（报告设置三项 + 即时持久化）
- tests/report-export.test.ts、scripts/tests/assessment-word-builders.test.mjs（守护）
- docs/CHANGELOG.md [2026-09-21] 条目（本轮完整记录）
