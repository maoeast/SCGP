# Current State
- 任务：C07「首次发送前隐私告知门禁」已实现并通过 `npm run type-check`（EXIT=0）。
- 已完成：
  - `AIApi.isPrivacyAcked(userId)` / `acknowledgePrivacy(userId)`：复用 `system_config` KV（key=`ai:privacy_ack:user:<userId>`，值=ISO 时间戳），零 schema 改动，随备份走。
  - `sendChat` 入口在所有前置校验通过、`ensureDb` 之后、`createSession`/落盘副作用之前，加 `ElMessageBox.confirm` 门禁；确认→`acknowledgePrivacy` 后继续，取消→静默 `return {ok:false}`（不弹错误）。
  - 告知文案 `AI_PRIVACY_NOTICE_HTML` 枚举 5 类外发内容（输入文本 / 图片 vision / 文档抽取文本 / 智能体挂载的专业知识技能 / AI 工具查询结果含学生·评估·训练·资源数据）。
  - `AiAssistant.vue` 的 3 个发送入口（send / generateReport / sendStarterPrompt）经 `sendChat` 统一覆盖，组件层无需改。

# Next Action
- 在系统设置「AI 智能体」面板加「重置 AI 隐私告知确认」入口：告知文案已承诺"可在系统设置重置"，但目前无该入口，需闭环；重置应清除当前用户（或全局）的 `ai:privacy_ack:user:*` KV，使下次发送重新触发告知。

# Blockers
- None

# Key Decisions
- 隐私确认按「每个登录用户首次」触发，记忆存 DB `system_config`（key=`ai:privacy_ack:user:<userId>`），随备份走，换账号/新教师登录会重新触发。
- UI 用 `ElMessageBox.confirm`（HTML 静态文案，`dangerouslyUseHTMLString`，无用户输入注入风险）；取消静默中止。
- 本批不做字段级脱敏、不做 tool result 最小化、不做重置入口（重置入口留作下一步闭环）。

# Relevant Files
- `src/database/ai-api.ts`（`isPrivacyAcked` / `acknowledgePrivacy`，紧邻 `getConfig/setConfig`）
- `src/stores/ai.ts`（`sendChat` 门禁 gate + 顶部 `AI_PRIVACY_NOTICE_HTML` + `ElMessageBox` import）
- `src/features/ai/components/AiAssistant.vue`（无需改，仅作入口参考）
