# Current State
- 任务：插队修复系统管理数据恢复外键失败与 Electron 联调端口清理失败；原 `Wave B / life-skills / L04_SET_TABLE` 主视觉任务尚未恢复执行。
- 已完成：
  - `src/utils/backup.ts` 已在 `BackupManager.importData()` 的恢复事务中补齐 `sm_question / sm_norm / sm_raw_to_sq / weefim_category / weefim_question` 静态字典后再做外键校验。
  - `scripts/electron-dev-start.js` 已修复 macOS/Linux 下从 `lsof` 输出提取 PID 的逻辑，不再把整行进程信息拼进 `kill -9`。
  - 用户已确认“数据备份后恢复正常”和“联调启动正常”。
- 产出物：
  - `src/utils/backup.ts`
  - `scripts/electron-dev-start.js`

# Next Action
- 新建 `src/components/emotional/games/SetTableStageArt.vue`，先实现餐桌、餐位锚点和餐具资产的 SVG 舞台骨架。

# Blockers
- None

# Key Decisions
- 备份恢复修复只改恢复链路：不改 `System.vue` 和评估持久化链。
- 联调启动修复只改 `scripts/electron-dev-start.js` 的端口检测 / PID 清理逻辑：不改 Electron 主进程加载链。
- 修完插队 bug 后，下一步回到 `Wave B`：继续 `L04_SET_TABLE` 主视觉重做。

# Relevant Files
- `src/utils/backup.ts`
- `scripts/electron-dev-start.js`
- `src/components/emotional/games/SetTableGame.vue`
- `src/components/emotional/games/MoodMeterStageArt.vue`
- `src/components/emotional/games/prototype-game-shared.css`
