# Current State
- 任务：补齐交付态“按模块激活授权”链路，确保激活码能真正授权 `sensory / emotional / social / cognitive / life_skills`，而不是只在运行时做半成品拦截。
- 已完成：新增 `src/utils/password-security.ts`；`src/database/api.ts`、`src/database/init.ts`、`src/database/sqljs-init.ts` 已切到 `Web Crypto API + PBKDF2-SHA256`；`src/views/Login.vue` + `src/components/login/LoginCard.vue` 已把管理员紧急恢复入口收口为 `DEV` 环境可见且重置为 `admin123`；`npm run type-check` 已通过。
- 产出物：`src/utils/password-security.ts`、`src/database/api.ts`、`src/database/init.ts`、`src/database/sqljs-init.ts`、`src/views/Login.vue`、`src/components/login/LoginCard.vue`

# Next Action
- 修改 `license-generator-dist/generate-license.js` 的 `generateTrialLicense()`，先为试用许可证写入 `am` 授权模块数组，并默认填入 5 个业务模块。

# Blockers
- None

# Key Decisions
- 交付态模块授权以激活码内的 `am` 授权模块数组为准：运行时消费链已存在，当前缺口在发码工具。
- 顶层授权模块固定为 `sensory`、`emotional`、`social`、`cognitive`、`life_skills`：不要把内部 training entry 当成收费授权模块。
- 管理员 `admin123` 紧急恢复仅保留为 `DEV` 调试入口：生产构建不可见。

# Relevant Files
- `license-generator-dist/generate-license.js`
- `src/utils/license-manager.ts`
- `src/utils/activation-manager.ts`
- `src/stores/auth.ts`
- `src/views/ActivationAdmin.vue`
