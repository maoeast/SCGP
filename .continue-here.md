# Current State
- 任务：AI 学生级长期记忆 M3（权限）/ M4（审核 UI）已完成，待提交
- 已完成（工作区未提交）：
  - M3 实时权限：ai-api.canAccessStudentMemory（admin 全量 / teacher 同班 JOIN sys_class_teachers，
    每次现算不存快照）+ getMemoryConfirmerNames（确认教师名映射）+ store listStudentMemories 越权返回 []
  - M4 学校级开关：getMemoryEnabled/setMemoryEnabled（system_config KV 'ai:memory_enabled'，默认关）
    + store loadAll 加载 + setter 持久化
  - M4 审核 UI：新增 StudentMemoryPanel.vue（学生详情第 4 tab「AI 记忆」）：
    pending 确认/拒绝、confirmed 置顶/标记关键（须填依据入审计）/软删除、确认来源展示、
    非服务团队空态；StudentDetail 加 tab + 指标卡（pending 计数）
  - M4 会话绑定：AiChatSession.studentId + listSessions/listAllSessions 映射
  - 测试：ai-memory-permission-contract.test.mjs（7 场景）
  - 验证：type-check / AI 契约 80 条（剩 1 条既有失败 AiAssistantFloatingButton 与本次无关）/ build:web 全过
- 产出物：5 文件改动（ai-api/stores/StudentDetail + 1 新组件 + 1 新测试）

# Next Action
- 用户确认后提交（建议：feat(ai): 学生级长期记忆 M3/M4 权限与审核 UI（v4.1 §8-§10 落地））

# Blockers
- None（M5 测试待启动：全链路契约 + 生命周期/配额/过期/补偿/恢复；以及 AI 抽屉会话绑定选择器 UI 未做
  ——目前绑定走库级 API，UI 入口待补）

# Key Decisions
- 权限 = 服务团队共享（同班教师 + admin），实时计算不存快照（v4.1 §9）
- 开关存 system_config KV（默认关），复用既有 getConfig/setConfig 原子 upsert
- 记忆面板作为学生详情独立 tab（lazy），权限在 store 层 + 组件双层门控
- StudentDetail 指标卡显示 pending 计数（顶部可见待确认数）

# Relevant Files
- src/database/ai-api.ts（canAccessStudentMemory / getMemoryConfirmerNames / 开关 KV / 会话 studentId）
- src/stores/ai.ts（listStudentMemories 门控 / 开关加载与 setter / 透出）
- src/views/student-detail/components/StudentMemoryPanel.vue（新增审核 UI）
- src/views/StudentDetail.vue（AI 记忆 tab + 指标卡）
- scripts/tests/ai-memory-permission-contract.test.mjs（新增契约）
- docs/planning/2026-08-05-ai-student-memory-design.md（v4.1，§11-§13 为 M5 参考）
