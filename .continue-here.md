---
status: active
last_updated: 今天，2026-04-12
scope: care_scene 沉浸式正式入口恢复与旧双栏链路清理已完成
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**本轮已完成 `care_scene` 正式入口修复：`main` 现在重新回到“默认直接进入沉浸式训练”的产品状态，旧左右双栏链路已从活跃代码中清理掉。**

- **本轮已经落地的代码变更**
  1. **恢复正式 cutover 路由**：
     - `/emotional/care-expression` 现在直接进入 `CareExpressionImmersiveTraining`
     - `/emotional/care-expression/immersive` 仅保留兼容重定向，不再是独立产品路径
  2. **移除选择页旧预览开关**：
     - `SceneSelector` 已删除 `immersivePreview` / 模式切换
     - 不再存在“默认走旧双栏，手动切沉浸式预览”的入口逻辑
  3. **补齐沉浸式训练前置校验**：
     - `care_scene` 进入训练前现在显式校验 `studentId` 与 `resourceId`
     - 返回选择页时会清理遗留的 `resourceId / sceneCode / immersivePreview / mode` 查询参数
  4. **清理死代码**：
     - 已删除旧 `CareExpressionTraining.vue`
     - 已删除旧 `compileCareScene.ts`
     - 已删除旧 `useEmotionalTrainingShell.ts`
  5. **验证结果**：
     - `npm run type-check` 已通过
     - 本轮代码提交为：`da0d995 fix(emotional): restore care_scene immersive cutover`

## 遇到的问题 / 阻塞 / 风险

1. **当前阻塞：无**。代码级 cutover 已恢复，工作区也已清理并提交。
2. **运行时验证仍未完成**：
   - 尚未做 Electron 实机 smoke，仍需确认真实入口从“选择学生 -> 选择关心情境 -> 沉浸式训练页”是否完全符合预期。
3. **训练记录持久化仍是当前风险点**：
   - 当前 `compileCareSceneImmersive` 仍使用 `persistence_mode: 'deferred'`
   - `ResultStep` 会因为 `supportsRecordPersistence = false` 而跳过保存
   - `src/database/api.ts` 里的 `EmotionalTrainingRecordAPI` 已存在，但**尚未真正接入当前 `care_scene` runtime**
4. **Git 管理风险**：
   - `stash@{2}` 仍保留一包名称带 `care_scene immersive work` 的旧 stash，其中混有文档、编辑器和其他无关修改
   - 后续不要直接 `git stash pop stash@{2}`，否则可能把不属于当前主线的历史改动一起带回来

## 下一步要做什么

**接下来的第一个动作：启动 Electron，手工跑一轮 `care_scene` 实机 smoke。**

1. 从真实学生选择入口进入 `表达关心`。
2. 确认默认路径直接落到沉浸式训练，而不是旧左右双栏页。
3. 抽查资源加载、步骤推进、返回选择页行为是否正常。
4. 到结果页确认当前是否跳过保存，并据此决定下一步是否把 `EmotionalTrainingRecordAPI` 正式接到 `care_scene` runtime。

## 本次对话的关键决策与原因

1. **不直接 `pop` 整包 stash，只恢复最小 cutover 差异**：
   - 决策：没有直接恢复 `stash@{2}`，而是手工提取 care_scene 正式切换所需的最小差异。
   - 原因：该 stash 混有无关文档、资源中心和其他历史改动，整包恢复风险太高。
2. **以当前代码为准，修正文档口径**：
   - 决策：发现文档已写成“默认已切沉浸式 / 正式记录已接主库”，但 `main` 实际代码并非如此后，优先修回代码并同步修正文档。
   - 原因：仓库规则明确要求“代码是真实现状”，不能把目标态或半落地状态写成已完成现实。
3. **直接删除旧双栏死代码，而不是保留隐藏 fallback**：
   - 决策：在恢复正式 cutover 后，顺手删除旧 `CareExpressionTraining.vue` 及其配套 compile/shell 文件。
   - 原因：这条旧链路已经不再被路由使用，继续保留只会制造误判，并增加以后再次回退或误接入的风险。
