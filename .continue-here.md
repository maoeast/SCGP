---
status: active
last_updated: 2026-04-08
scope: emotional module — 情绪场景训练全屏沉浸式重构 Phase 5 动态答题页已落地，下一步补 Step 5 结算页
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**情绪场景训练全屏沉浸式重构的 Phase 1~5 已推进到仓库：独立原型数据层、核心 Pinia 状态机、TTS / SFX 抽象层、全屏 UI 骨架与引导页、Step 1~4 动态答题页均已落地；当前已能进入沉浸式训练页、自动渲染四个题步的真实选项，并完成正误反馈与步骤推进，下一块明显缺口是 Step 5 结算页。**

- **已完成**:
  - Phase 1 独立原型数据层已落地：
    - `src/db/schema.sql`
    - `src/db/migrateLegacyData.ts`
    - `src/db/useDatabase.ts`
  - Phase 1 真实数据迁移已验证通过：
    - source of truth: `docs/references/current-emotion-scenes-export.json`
    - 结果：
      - `80` scenes
      - `240` clues
      - `320` steps
      - `1040` options
      - `0` hints
  - Phase 2 核心状态机已落地：
    - `src/stores/useTrainingStore.ts`
    - 已接入：
      - `currentStepIndex`
      - `currentStepData`
      - `parsedQuestionText`
      - `recordError()`
      - `recordAnswer()`
      - `nextStep()`
      - `calculateStars()`
      - `saveRecord()`
  - Phase 3 TTS / SFX 抽象层已落地：
    - `src/services/tts/ITTSService.ts`
    - `src/services/tts/EdgeTTSService.ts`
    - `src/services/tts/index.ts`
    - `src/composables/useSound.ts`
  - Phase 4 全屏 UI 骨架已落地：
    - `src/components/training/TrainingLayout.vue`
    - `src/components/training/SceneIntroStep.vue`
    - `src/components/training/TrainingSession.vue`
    - `src/components/training/ExitConfirmDialog.vue`
    - `src/views/emotional/EmotionSceneTraining.vue`
    - `src/views/emotional/SceneSelector.vue`
  - Phase 5 动态答题页已落地：
    - `src/components/training/QuestionPresenter.vue`
    - `src/components/training/ImageOptionCard.vue`
    - `src/components/training/TextOptionBlock.vue`
    - `src/components/training/OptionBoard.vue`
    - `src/components/training/QuestionStep.vue`
    - `src/components/training/training-feedback-sfx.ts`
  - Phase 5 当前已验证通过的运行时行为：
    - `TrainingSession.vue` 已用 `QuestionStep` 替换 Step 1~4 占位
    - `emotion` 题会渲染图片 / 表情卡
    - `reason / need / response` 会渲染文字选项块
    - 点击错误选项会：
      - 锁定 `store.inputLocked`
      - 播放错误提示音
      - 执行红框 + `shake`
      - 调用 `store.recordError(...)`
      - 动画结束后恢复当前题的点击能力
    - 点击正确选项会：
      - 锁定 `store.inputLocked`
      - 播放正确提示音
      - 执行绿框 + 勾选态
      - 调用 `store.recordAnswer(...)`
      - 延时进入下一步 `store.nextStep()`
    - `QuestionPresenter.vue` 已接入 `store.parsedQuestionText`
      - 有 `VITE_EDGE_TTS_ENDPOINT` 时会尝试自动朗读题目
      - 无 endpoint 时会静默降级为不可播报态
    - 每次点击选项后，都会展示数据库 `options.feedback_text`
      - 当前为居中浮动提示气泡
      - 当前行为已改为**常驻显示**
      - 只会在“点击新的选项”或“切换到下一题”时被覆盖 / 清掉
    - 背景遮罩已减轻：
      - 已移除 `TrainingLayout` 与 `SceneIntroStep` 中的 `backdrop-blur`
      - 当前以轻量顶部透明 + 底部加深渐变为主，优先保证孩子看清场景图片

- **当前仍未完成**:
  - Step 5 结算页仍是占位：
    - 当前训练答完四题后仍会落到 “训练完成占位”
    - `calculateStars()` / `saveRecord()` 还没有接到真正的结算 UI
  - TTS 仍未完成真实运行时对接：
    - `EdgeTTS` 只有前端抽象
    - 仓库内没有现成可验证的合成端点
  - 当前答题 SFX 仍是原型级实现：
    - `training-feedback-sfx.ts` 当前使用内置 data URI 短提示音
    - 还没有收口到正式资源文件或统一音频资产管理
  - `hints` 表当前仍只有结构，没有真实 hint 文本
  - `saveRecord()` 仍只写入独立原型库 `training_records`
    - 尚未和当前产品主线记录系统收口
  - 旧情绪训练页仍被原型入口暂时替换：
    - `/emotional/emotion-scene` 当前直接渲染新原型链
    - 这是显式过渡态，不应写成旧主线已正式退场

## 下一步

**第一个动作**: 开始补 Step 5 结算页，先创建真正的结果页组件，把 `calculateStars()`、`saveRecord()`、返回场景选择 / 再来一次等动作接进当前训练链，替换现有 “训练完成占位”。

## 遇到的问题 / 阻塞 / 风险

1. **仓库级全量 `type-check` 仍被历史旧错误阻塞**
   - 位置：
     - `src/components/emotional/games/EnergyBallGame.vue`
     - `src/components/emotional/games/VisualSupportOverlay.vue`
     - `src/composables/useEmotionDetector.ts`
   - 影响：
     - 当前无法用“全仓绿灯”证明本轮 Phase 5 新增代码完全无 TS 问题
   - 现实：
     - 本轮新增训练组件未引入新的已知阻塞点

2. **`EdgeTTS` 当前只有抽象，没有仓库内已验证的后端端点**
   - 当前处理：
     - `QuestionPresenter.vue` 只在存在 `VITE_EDGE_TTS_ENDPOINT` 时尝试播报
     - 否则降级为不可播报态，不阻塞答题流程
   - 风险：
     - 真正接入自动朗读前，仍需确认 Electron / 本地服务的实际接口契约

3. **当前答题 SFX 是原型级内置音**
   - 当前处理：
     - 为避免因仓库中缺少现成反馈音效文件导致页面不可用，先用 data URI 短提示音驱动 `useSound`
   - 风险：
     - 后续如果需要统一风格、音量或资产管理，仍应切换到正式预置音频资源

4. **当前 `feedback_text` 提示气泡已改为常驻**
   - 当前处理：
     - 用户明确要求“直到点击另外一个答案才消失”
     - 当前气泡只会在重新点击选项或切题时被覆盖 / 清理
   - 风险：
     - 如果后续补教师面板、提示层或 TTS 字幕层，需要注意不要和该气泡发生视觉重叠

5. **浏览器侧 `sql.js` 包入口在当前 Vite 环境下仍不可直接依赖**
   - 当前处理：
     - `src/db/useDatabase.ts` 继续使用运行时加载 `sql-wasm.js` 脚本文本的兼容策略
   - 风险：
     - 这是原型链兼容层，后续若升级 `sql.js` / Vite，应优先收口

## 本次对话做出的关键决策及原因

1. **Phase 5 先把 Step 1~4 的真实答题闭环补齐，再谈结算页**
   - 原因：
     - 当前用户最关心的是“孩子能不能真正看到题并完成作答”
     - 先打通题目展示、选项渲染、正误反馈、步骤推进，比先做 Step 5 结果页更关键

2. **`QuestionPresenter.vue` 直接消费 `store.parsedQuestionText`，不再重复拼题干**
   - 原因：
     - `{name}` 替换后的题干应只有一个可信出口
     - 避免 UI 层和未来 TTS 层各自重复处理占位符

3. **正确 / 错误反馈统一严格受 `store.inputLocked` 控制**
   - 原因：
     - 这是防止儿童连续乱点导致状态机错乱的最低要求
     - 选项组件内在点击开始时立即上锁，错误反馈结束后解锁，正确反馈则保持到 `nextStep()`

4. **当前反馈音效先走 `useSound` + 内置 data URI 提示音**
   - 原因：
     - 仓库中当前没有现成可直接复用的答题反馈音频资源
     - 用户明确要求音画同步，本轮需要先保证功能闭环，不应因为缺正式音频资产而卡住 Phase 5

5. **`feedback_text` 不做临时 tooltip，而是做成中央常驻提示气泡**
   - 原因：
     - 数据库里的 `options.feedback_text` 是当前训练链的重要教学语句，不应只做正误颜色提示就丢掉
     - 用户实测明确反馈“时间太短看不清”，因此改为常驻到下一次点击或切题

6. **减轻背景遮罩优先级高于保留毛玻璃质感**
   - 原因：
     - 对儿童训练来说，看清真实场景照片比氛围化模糊更重要
     - 因此移除了 `TrainingLayout` / `SceneIntroStep` 上的 `backdrop-blur`，只保留轻量底部深色渐变保证文字可读

7. **Phase 5 完成后，下一步优先补 Step 5 结算页**
   - 原因：
     - 当前训练链最大的用户可见缺口已经从“不会答题”变成了“答完后没有真正结果页”
     - 先补结算页，才能形成从 intro -> 4 步作答 -> 结果 的完整闭环
