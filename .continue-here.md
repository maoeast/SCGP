# Current State
- 任务：修复训练计划启动链路，使训练计划卡片/详情抽屉可以直接启动 `emotion_scene`、`care_scene`、感统旧游戏和自定义小游戏资源。
- 已完成：
  - `src/utils/training-launch.ts`：`emotion_scene` / `care_scene` 启动会自动从资源元数据补 `sceneCode`。
  - `src/utils/training-launch.ts`：`game` 资源按元数据分流；旧感统游戏补 `taskId` / `mode` 后走 `/games/play`，带 `entryPath` 的自定义小游戏直接跳到对应 `/emotional/games/*` 页面。
  - `src/views/plan/PlanList.vue`：训练计划详情抽屉的资源列表支持整行点击和动作按钮启动；器材显示“开始录入”，文档/视频走预览。
- 产出物：
  - `src/utils/training-launch.ts`
  - `src/views/plan/PlanList.vue`

# Next Action
- 在 Electron 中从训练计划详情抽屉实机点击一个双人合作自定义小游戏（优先“合作造汉堡”），确认是否需要新增“选择搭档”中转流程。

# Blockers
- None

# Key Decisions
- 共享启动逻辑统一收口到 `src/utils/training-launch.ts`：训练计划与首页日程共用，避免在多个页面重复修跳转。
- `game` 资源按元数据分流：带 `entryPath` 的自定义小游戏不再误走 `/games/play`。
- 详情抽屉复用 `handleLaunchTraining()`：仅在确认可启动后关闭抽屉，避免失败时详情信息丢失。

# Relevant Files
- `src/utils/training-launch.ts`
- `src/views/plan/PlanList.vue`
- `src/views/games/GameLobby.vue`
- `src/components/emotional/games/BurgerCoopGame.vue`
