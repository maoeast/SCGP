# 当前任务：评估质量追踪与提醒（Phase 1 已完成，待提交）

## 任务状态
为 17 个评估量表添加答题时间追踪和质量控制功能 —— **代码已完成，验证通过，待用户确认后提交**。

## 已实现（Phase 1 全量交付）

### 1. 数据库层（src/database/init.ts）
- 17 张 `*_assess` 主表 CREATE TABLE 各加 3 列：
  `total_duration INTEGER`（秒，墙钟）、`avg_response_time REAL`（秒/题）、`quality_note TEXT`（'very_fast'/<3s每题 | 'fast'/<5s每题 | NULL）
- 新增 `ensureAssessmentQualityColumns(rawDb)`：对 17 张表幂等 `safeAddColumn` 补列（旧库自动迁移，重复执行安全）
- 挂载点：`initDatabase()` 主路径，`db.run(schemaSQL)` 之后
- **例外**：`cognitive_self_assess` 已有 `avg_response_time`（真反应时 ms 语义，NOT NULL），
  只加 `total_duration` + `quality_note` 两列，避免同名冲突与语义覆盖
- 同步副本：`src/database/schema.sql` 与 `src/database/sqljs-init.ts` 的 sm/weefim 建表语句

### 2. 类型层（src/types/assessment.ts）
- `AssessmentQualityMetrics` 接口 + 阈值常量（3s/5s）
- `computeAssessmentQualityMetrics(startTime, endTime, answeredCount)` 纯函数：
  0 题守卫、时间缺失/NaN/Infinity/end<start 守卫 → 返回 null
- `PersistContext` 增加可选字段 `quality?: AssessmentQualityMetrics`

### 3. Driver 层
- `BaseDriver.saveQualityMetrics(tableName, assessId, context, options?)`：
  INSERT 后 UPDATE 质量列；quality 缺失跳过；写失败只 warn 不阻断主保存
  - `options.skipAvgResponseTime`：cognitive_self 用（其 avg_response_time 是 ms 真反应时，INSERT 时已写）
- 17 个 Driver 的 `persistAssessment` 尾部各加一行调用（表名硬编码传入）

### 4. UI 层
- **AssessmentContainer.vue**：`completeAssessment()` 计算墙钟质量指标 →
  `PersistContext.quality` 下发 Driver + `:quality` 传给 CompleteDialog
- **CompleteDialog.vue**：新增 `quality` prop；仅 `avg > 0 && avg < 2`（秒）时显示
  info 级温和提示「本次评估用时较短，如有需要可重新评估以确保准确性。」
  （不阻断任何按钮；2-3 秒/题只入库标记 very_fast 不弹提示）
- **新组件 `src/views/assessment/components/AssessmentTimingInfo.vue`**：
  客观展示「评估用时 / 平均每题」，**不展示 quality_note**（不做价值判断）；
  两字段全 NULL 时整卡不渲染（旧记录无感）
- **17 个报告页** Report.vue 全部接入该组件（import + 模板尾部）：
  - abc/atec/brief/crt/cognitive-self：`assessData`（SELECT * / API getAssessment 直返）
  - cbcl/sdq/srs2：`assessData`（页面本地 interface 已补 3 个可选字段）
  - cnbsr2016/gmfm-88/tgmd-3/fine-motor/csirs/conners-psq/conners-trs：`assessment`
  - sm/weefim：`reportData`（API 返回展开）
  - cognitive-self 报告不展示 avg（其列是 ms 真反应时，另有专门 UI 展示）

### 5. API 查询列补充（src/database/api.ts）
显式列清单的 `getAssessment` 补 3 列：SMAssessmentAPI、WeeFIMAPI、FineMotorAssessmentAPI、
Cnbsr2016AssessmentAPI、Gmfm88AssessmentAPI、Tgmd3AssessmentAPI
（BRIEF/CRT/CognitiveSelf 本就是 `SELECT *` 无需改；csirs Report 用自己的 `SELECT a.*`）

### 6. 类型补充
- `src/types/csirs.ts` CSIRSAssessment +3 可选字段
- `src/types/sdq.ts` SDQAssessRecord +3
- `src/types/srs2.ts` SRS2AssessRecord +3（+ Report.vue 本地 interface +3）
- `src/types/conners.ts` ConnersPSQAssessment / ConnersTRSAssessment +3
- `src/views/assessment/cbcl/Report.vue` CBCLAssessRecord +3
- `src/views/assessment/gmfm-88/tgmd-3/fine-motor/Report.vue` 本地 interface +3
- `src/features/assessment/cnbsr2016/report-model.ts` Cnbsr2016AssessmentRecord +3

## 验证
- ✅ `npx jiti tests/assessment-quality.test.ts`（新增 8 组用例：very_fast/fast/null 分档、
  边界值 3s/5s、0 题守卫、时间异常守卫、数值正确性、阈值关系）
- ✅ `npm run type-check`
- ✅ `npx jiti tests/assessment-score-adapter.test.ts`、`assessment-profile.test.ts`、`entitlement-catalog.test.ts`（无回归）
- ✅ `npm run build:web`（构建通过）
- ⬜ 真机冒烟：跑一次量表评估 → 报告页看用时卡；旧记录报告页不应出现用时卡

## Phase 2 & 3（未实施，方案见 docs/INDEX 或旧版本文档）
- 管理后台质量统计看板（quality_note 已入库，可按表查询）
- 随机作答模式检测

## Git 状态
- 上一会话修复已提交：`085298b fix(assessment): ABC/ATEC 评估保存、报告加载、报告中心过滤修复`
- 本任务改动未提交（等用户确认）
- 未跟踪无关文件（勿混入提交）：`.agents/`、`thincoder-main.zip`、`docs/references/physical-equipment/aac.html`

## 关键文件
- `src/types/assessment.ts`、`src/database/init.ts`、`src/database/api.ts`
- `src/strategies/assessment/BaseDriver.ts` + 17 个 `*Driver.ts`
- `src/views/assessment/AssessmentContainer.vue`、`components/CompleteDialog.vue`、`components/AssessmentTimingInfo.vue`
- `tests/assessment-quality.test.ts`
