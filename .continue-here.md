---
status: active
last_updated: 2026-04-10
scope: care_scene 阻塞清理 + 仓库文档整理已完成；本地 DB metadata 已核对，仓库级 type-check 已恢复通过，根目录文档已归位并生成 HTML 索引；下一步：做 Electron smoke 并决定默认入口 / 正式记录闭环
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**本次会话已完成两条工作线（2026-04-10）：**

**1. `care_scene` 阻塞清理已落地**：根目录 `care_scenes_database.json` 中的 `name` / `description` / `specificEmotionToken` / `specificEmotionLabel` / `emotionOptions[]` 已接入类型定义、seed 标准化、`sys_training_resource.meta_data` 回填链、资源中心编辑器、资源包导入导出、场景选择页和沉浸式训练运行时。当前代码已不再把 Step 1 情绪识别选项写死为运行时生成，改为直接消费 JSON 作者数据。仓库级 `npm run type-check` 已恢复通过；本地 `/home/DONG/.config/scgp/database.sqlite` 中 64/64 条 `care_scene` 资源已核对为新 schema，未发现 `sceneCode / name / description / specificEmotionToken / specificEmotionLabel / emotionOptions[4] / legacy_source / 主表 description 同步` 缺口。**

**2. 仓库文档整理已落地**：根目录仅保留入口级文档；非入口文档已归位到 `docs/` 对应目录，并新增可直接打开的 HTML 文档目录页，方便后续快速判断每份文档的作用、位置和时间信息。**

**当前仍处于“默认 `care_scene` 入口未切换、正式记录闭环未接入、尚未补 Electron smoke”的阶段。**

- **本轮已完成**:
  - `CareSceneResourceMeta` 已升级为作者态 schema：
    - 新增 `name`
    - 新增 `description`
    - 新增 `specificEmotionToken`
    - 新增 `specificEmotionLabel`
    - 新增结构化 `emotionOptions[]`
  - `src/database/emotional-resource-data.ts` 已改为完整标准化 `care_scenes_database.json`：
    - 不再丢弃上述字段
    - 保留 `receiverName` / `emotionChips` / `comfortTip` 作为兼容/推导字段
    - `EMOTIONAL_RESOURCE_SEED_LEGACY_SOURCE` 已升级到 `emotional_full_seed_2026_04_10`
  - `src/features/emotional/immersive/compileCareSceneImmersive.ts` 已改为：
    - Step 1 直接消费 JSON 的 4 个 `emotionOptions[]`
    - 不再用 `buildEmotionChoiceOptions()` 运行时随机生成基础情绪选项
    - 场景级数据已带入 `specific_emotion_token` / `specific_emotion_label`
  - `src/views/resource-center/editors/CareExpressionEditor.vue` 已支持可视化编辑：
    - 被关心对象称呼
    - 教学目标说明
    - 细分情绪 token / label
    - 色区
    - 4 个情绪识别选项及其反馈文案
  - `src/views/emotional/SceneSelector.vue` 已优先展示：
    - `description`
    - `name`
    - `specificEmotionLabel`
  - `src/utils/emotional-resource-pack.ts` 已升级到 `scgp.emotional-pack.v2`：
    - `care_scene` 新字段已进入 JSON / Excel 导入导出
    - 新增 `care_emotion_options` sheet
  - `scripts/replace-emotional-scenes.mjs` 已适配新 schema，避免后续用脚本回灌时退回旧结构
  - `src/views/resource-center/TrainingResources.vue` 已保证：
    - `care_scene` 的 `metadata.description` 会同步回资源主表 `description`
    - 资源中心编辑时不会出现“主表 description 和 metadata.description 两套口径”
  - 仓库级 `npm run type-check` 已恢复通过：
    - 已修复 `src/components/emotional/games/EnergyBallGame.vue`
    - 已修复 `src/components/emotional/games/VisualSupportOverlay.vue`
    - 已修复 `src/composables/useEmotionDetector.ts`
  - 本地 DB metadata 已完成实机核对：
    - 数据库：`/home/DONG/.config/scgp/database.sqlite`
    - `care_scenes_database.json` 源条数 = 64
    - 本地 `care_scene` 条数 = 64
    - 未发现缺失 `sceneCode` / `name` / `description` / `specificEmotionToken` / `specificEmotionLabel`
    - 未发现 `emotionOptions` 结构异常
    - 未发现主表 `description` 与 `meta_data.description` 不一致
    - 未发现旧 `legacy_source`
  - 根目录文档整理已完成：
    - `GSD指令手册.md` -> `docs/guides/`
    - `SCGP-CNBS-R2016_todo.md` -> `docs/plans/`
    - `care_scenes_schema.md` / `care-scene-description.csv` -> `docs/references/care-scene/`
    - `sql_js_持久化模块标准实现方案_V1.1.md` -> `docs/tech-design/`
    - `tree.txt` / `CLAUDE.md.bak` -> `docs/logs/`
  - HTML 文档索引已生成：
    - `docs/guides/2026-04-10-repo-document-catalog.html`
    - 已覆盖本轮整理涉及的 14 份入口 / 专题文档
    - 已写入文档作用、位置、文件名、Git 创建时间、最近更新时间、大小和行数
  - 文档入口与治理规则已同步更新：
    - `README.md`
    - `docs/INDEX.md`
    - `docs/DOCS_GUIDE.md`
    - `.gitignore` 已放行 `docs/logs/tree.txt` 与 `docs/logs/CLAUDE.md.bak`，避免 `logs` 规则误伤

**当前仍未完成**:
  - `care_scene` 沉浸式结果页仍是”待接入正式记录”：
    - `saveRecord()` 仍只适用于 `emotion_scene` 原型库
    - `care_scene` 当前 persistence mode 仍为 `deferred`
  - 默认 `care_scene` 入口仍未切换到沉浸式链路
  - 本地 `msedge-tts` 依赖仍未安装成功：
    - `electron/handlers/tts.mjs` 加载时仍提示 `Cannot find package 'msedge-tts'`
    - 当前只是”无 TTS 可用”，不是当前 schema 改造的直接阻塞

## 下一步

**第一个动作**: 在 Electron 里重新做一次 `care-expression/select -> immersive -> intro -> 3 steps -> result` smoke，确认当前本地库已对齐的新 schema 在真实界面链路里表现一致，然后再决定是否切默认入口与接正式记录闭环。

关键数据文件位置：
- `care_scenes_database.json` — 作者态源数据（现在是 `care_scene` 唯一真源）
- `src/database/emotional-resource-data.ts` — seed 标准化与 metadata 归一化入口
- `src/features/emotional/immersive/compileCareSceneImmersive.ts` — 沉浸式 runtime 编译入口
- `src/views/resource-center/editors/CareExpressionEditor.vue` — 资源中心编辑器
- `src/utils/emotional-resource-pack.ts` — 导入导出契约
- `src/views/emotional/SceneSelector.vue` — 选择页展示

后续待决定：
1. 接 `care_scene` 正式记录闭环（`saveRecord()` 适配）
2. 完成本地 DB 回填 + Electron 复验后，决定是否切默认入口到沉浸式链路
3. 安装 `msedge-tts` 恢复 TTS 朗读

## 遇到的问题 / 阻塞 / 风险

1. **`care_scene` 结果记录仍未收口到当前产品主线**
   - 当前处理：
     - 沉浸式结果页只展示总结，不落正式记录
   - 风险：
     - 当前只能算“schema 和 runtime 已对齐”，不能算完整交付闭环

2. **默认入口尚未切换**
   - 当前处理：
     - 旧 `EmotionalInteractionEngine` 继续作为兜底
   - 风险：
     - 如果不切入口，新旧 `care_scene` 口径会长期并存

3. **`msedge-tts` 本地依赖缺失**
   - 现象：
      - `electron/handlers/tts.mjs` 动态导入时报 `Cannot find package 'msedge-tts'`
   - 风险：
      - TTS 仍不可用
      - 这会影响沉浸式训练朗读体验，但不是当前 schema 落地的直接阻塞

4. **HTML 文档索引属于人工维护快照**
   - 当前处理：
     - 目录页内容基于本轮实际阅读和 Git 元信息生成
   - 风险：
     - 如果后续继续移动文档但不更新 HTML，目录页会与真实路径逐渐漂移

## 本次对话做出的关键决策及原因

1. **把 `care_scenes_database.json` 定位为 `care_scene` 作者态唯一真源**
   - 原因：
     - 现在根 JSON 已经具备完整业务字典，不应该再让 editor / runtime / seed 继续各自维护一套轻量缩略结构
     - 否则每次保存、导入或回灌都会继续丢字段

2. **保留 `receiverName` / `emotionChips` / `comfortTip`，但降级为兼容/推导字段**
   - 原因：
     - 当前旧运行时和部分旧组件仍会消费这些字段
     - 直接删掉会扩大改动面；先改为从新 schema 推导，能在不打断现有链路的前提下完成对齐

3. **Step 1 情绪识别必须直接消费 JSON 的 `emotionOptions[]`**
   - 原因：
     - 这是 schema 的关键变化之一
     - 如果继续在运行时动态生成基础情绪选项，就会把作者配置好的细分情绪教学设计完全绕开

4. **资源中心主表 `description` 和 metadata.description 必须同步**
   - 原因：
     - 选择页和资源中心列表会直接读主表 `description`
     - 如果只改 metadata，不同步主表，用户会看到两套不同说明文案

5. **本轮不顺手切默认入口，也不顺手接正式记录闭环**
   - 原因：
     - 当前优先目标是先把 schema 对齐和 metadata/UI/runtime 一致性收口
     - 入口切换和记录闭环属于下一阶段决策，应该建立在 DB 回填和 Electron 复验通过之后

6. **根目录只保留入口级文档，其余文档按类型归档到 `docs/`**
   - 原因：
     - 根目录长期堆放专题文档会稀释真正的启动入口
     - `docs/guides / plans / references / tech-design / logs` 已足够承载这些材料

7. **新增 HTML 版本文档目录，而不只补 Markdown 列表**
   - 原因：
     - 用户需要“一打开就知道每份文档大概内容、作用、位置、时间信息”
     - HTML 更适合同时展示摘要、分类、元信息和本地跳转链接
