---
status: active
last_updated: 2026-03-31
scope: training plan module ownership expansion and date-picker standardization are landed; manual runtime verification is pending
---

> Responsibility: the only current handoff file for unfinished work.
> Read when: continuing work from a previous session.
> Not responsible for: replacing `AGENTS.md` repo rules or `PROJECT_CONTEXT.md` global context.

## Session Closeout (2026-03-31)

Current task status:

- training-plan ownership selection is now expanded to the requested option set:
  - `全部模块`
  - `综合计划`
  - `感官训练`
  - `情绪行为`
  - `情绪调节`
  - `社交沟通`
  - `精细动作`
  - `安抚教具`
  - `生活自理`
- training-plan module normalization is now centralized in:
  - `src/utils/training-plan-module.ts`
- legacy stored training-plan values still display and filter correctly through alias normalization:
  - `sensory`
  - `emotional`
  - `social`
  - `life_skills`
- dashboard plan labels were aligned to the same module-label source instead of keeping a separate hard-coded mapping
- date-picker behavior is now standardized for active business pages through:
  - global Element Plus locale: `src/main.ts`
  - shared props/utilities: `src/utils/date-picker.ts`
- active pages updated this round:
  - `src/views/plan/PlanList.vue`
  - `src/views/Reports.vue`
  - `src/components/AddStudentDialog.vue`
  - `src/views/equipment/Records.vue`
  - `src/views/admin/StudentClassAssignment.vue`
  - `src/views/training-records/components/EquipmentRecordsPanel.vue`
  - `src/views/training-records/components/GameRecordsPanel.vue`
- devtools follow-up updated for consistency:
  - `src/views/devtools/ClassManagementTest.vue`
- verification completed this round:
  - `npm run type-check` passes
  - `npm run build:web` passes

Next step:

1. start or relaunch the dev app
2. open `训练计划 -> 新建计划`
3. verify the ownership dropdown shows all requested options and the date-range panel is Chinese
4. then verify date-picker behavior is visually consistent in:
  - `报告中心`
  - `学生管理 -> 添加学生`
  - `学生分班`
  - `训练记录`
  - `器材训练记录`

Problems / blockers / risks:

- no hard blocker in code; type-check and web build both pass
- manual runtime verification is still pending
- `AddStudentDialog.vue` had a previously corrupted template block; the touched area has been repaired and now compiles, but broader historical text-encoding cleanup across unrelated legacy Chinese strings was not part of this turn
- some updated date-pickers still retain matching explicit placeholder / format attributes alongside shared props:
  - behavior is safe now because values match
  - if the shared standard changes later, those templates should be cleaned further so the shared constants remain the single effective source

Key decisions made in this conversation:

- use global `zh-cn` Element Plus locale in `src/main.ts`
  - reason: page-local fixes would keep calendar UI inconsistent and easy to regress
- introduce shared date-picker defaults in `src/utils/date-picker.ts`
  - reason: the repo had several near-duplicate date-picker configurations with inconsistent UX
- normalize training-plan module values instead of running a destructive data migration
  - reason: existing plans and dashboard labels needed to stay compatible while the UI switched to the richer module/business-group set
- keep the training-plan selector and dashboard display on the same label source
  - reason: avoiding duplicated hard-coded label maps reduces future drift

## Current State

Already true:

- active business pages now use Element Plus date pickers with shared defaults and Chinese locale
- `AddStudentDialog.vue` no longer uses native `input[type="date"]`
- training-plan ownership filtering, editing, and display are compatible with both new UI options and old stored codes

Confirmed reality:

- runtime smoke verification is not yet done in the live app
- current worktree includes source changes from this round and should be committed together with the handoff docs

## Remaining Work

- manually verify the updated date-picker UX in the running app
- manually verify one training-plan create/edit flow using the new ownership options
- if any page still shows English calendar chrome at runtime, inspect whether that surface is rendered outside the standard Element Plus app context

## Next Action

Start with:

1. read `AGENTS.md`, bootstrap, `README.md`, `.continue-here.md`
2. launch the app
3. open `训练计划 -> 新建计划`
4. verify the ownership dropdown and Chinese date-range panel there before checking the other pages

## Next Session Read Order

1. `AGENTS.md`
2. `docs/planning/2026-03-23-scgp-context-bootstrap.md`
3. `README.md`
4. `.continue-here.md`
5. `PROJECT_CONTEXT.md` only if broader background is truly needed

## Next Session Prompt

```text
先按 AGENTS.md 做最小上下文启动：
1. 读取 AGENTS.md
2. 读取 docs/planning/2026-03-23-scgp-context-bootstrap.md
3. 读取 README.md
4. 读取 .continue-here.md
5. 只有在需要全局背景时再读 PROJECT_CONTEXT.md

然后先做手工运行验证，不要先写新功能。

当前已完成：
- 训练计划“归属模块”选项已扩展到用户要求的完整集合
- 训练计划模块代码兼容旧值并统一到 `src/utils/training-plan-module.ts`
- Element Plus 日期选择器已切到全局中文 locale
- 活跃业务页日期选择器已统一接入 `src/utils/date-picker.ts`
- `npm run type-check` 和 `npm run build:web` 都已通过

下一步先做：
1. 启动应用
2. 打开 `训练计划 -> 新建计划`
3. 验证归属模块下拉和中文日期面板
4. 再依次验证 报告中心 / 学生管理 / 学生分班 / 训练记录 / 器材训练记录
```
