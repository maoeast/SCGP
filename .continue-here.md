# Current State
- 任务：继续 `Wave B / life-skills / L04_SET_TABLE` 主视觉重做，完成 `SetTableGame` 内所有主展示 emoji 到统一 SVG 缩略资产的替换。
- 已完成：
  - `src/components/emotional/games/SetTableGame.vue` 的 `ready` 预览条、`playing` 物品卡片、完成卡片、徽章弹层已全部切到 SVG 缩略资产。
  - `src/components/emotional/games/SetTableAssetThumbnail.vue` 已新增单件餐具缩略图和 `variant="table"` 完整餐位缩略图。
  - `SetTableGame` 已清理失效的 `emoji / ghostEmoji / position` 旧字段；`npm run type-check` 已通过。
- 产出物：
  - `src/components/emotional/games/SetTableGame.vue`
  - `src/components/emotional/games/SetTableAssetThumbnail.vue`

# Next Action
- 启动 `SetTablePage`，逐一验收三档难度与三套主题下 `ready / playing / finished` 三态的 SVG 缩略资产尺寸、对齐和完成态表现。

# Blockers
- None

# Key Decisions
- 只处理 `L04_SET_TABLE` 视觉层：不改路由、落库和记录摘要链路。
- 缩略资产独立为 `SetTableAssetThumbnail.vue`，避免把卡片和徽章 SVG 重复塞回 `SetTableGame.vue`。
- 工作树存在大量无关改动；后续继续只处理 SetTable 文件。

# Relevant Files
- `src/components/emotional/games/SetTableGame.vue`
- `src/components/emotional/games/SetTableAssetThumbnail.vue`
- `src/components/emotional/games/SetTableStageArt.vue`
