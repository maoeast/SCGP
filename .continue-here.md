# Current State
- `AUDIO_DIFF` 已视觉验收通过，不能改坏。
- `AUDIO_COMMAND` 已完成 UI 重做：参考形状识别的木纹视觉语言，从扁平方案改为木纹风格。
- 已完成的工作：
  - 重写 `GameAudio.vue` 中 `mode === 'command'` 模板，类名从 `game-mode-command` / `audio-sidebar` / `game-tray` 改为 `cmd-stage` / `cmd-sidebar` / `cmd-board`
  - 删除 3 套重复/覆盖样式块（~700 行），替换为 1 套干净木纹样式（~480 行）
  - 删除 `SensoryGameShell.vue` 中 flat override 块，恢复木纹背景
  - `npm run type-check` 通过，`npm run build:web` 通过
- 当前尚未浏览器视觉验收。

# Next Action
- 在浏览器中运行 `npm run dev` 或 `npm run electron:dev`，进入听指令做动作游戏，视觉验收木纹风格 UI 是否满足预期。

# Blockers
- None

# Key Decisions
- 模板类名统一用 `cmd-` 前缀：避免与旧类名残留冲突，也便于后续维护
- 积木 SVG 填色硬编码为 `#dbb878`（木色）：可能与 `getCommandBlockStyle()` 内联样式冲突，需验收时注意
- 1180px 以下改为堆叠布局：侧边栏限制最大高度 40vh

# Relevant Files
- `src/components/games/audio/GameAudio.vue`
- `src/components/games/SensoryGameShell.vue`
