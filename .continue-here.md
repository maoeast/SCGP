# Current State
- 任务：继续自理训练模块阶段 2，确认并实现 `task_training` 任务资源 CRUD 落点。
- 已完成：`src/features/self-care/task-training-contract.ts` 定义 `task_training`、`life_skills`、`life-skills`、`game` 合同常量和任务训练类型。
- 已完成：`src/utils/resource-center-business.ts`、`src/views/resource-center/training-resource-ui.ts`、`src/utils/training-launch.ts`、`src/database/dashboard-api.ts`、`src/views/plan/PlanList.vue`、`src/features/self-care/self-care-routes.ts` 已改用任务训练合同常量。
- 已完成：`scripts/tests/*task-training*.mjs` 与相关资源中心、启动、仪表盘、计划、自理路由测试已覆盖合同常量约束。
- 已完成：`docs/planning/2026-05-08-self-care-training-module-implementation-plan.md` 已显式标注 A1 迁移设计和 ATS 到 SCGP 映射清单均已同步到任务计划。
- 产出物：`44fa45d feat(self-care): 收口 task_training 主链合同常量`，已推送到 `origin/main`。

# Next Action（必须原子级——第一步可执行的单一动作）
- 读取 `src/views/resource-center/TrainingResources.vue`，定位训练资源创建、编辑、保存入口。

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 自理训练继续挂 `life_skills` 授权：当前授权体系只识别既有业务模块代码。
- `task_training` 继续复用训练资源的 `game` 展示主链：阶段 1 目标是可见、可选、可启动，不提前扩展独立展示主链。
- `task_training` 步骤结构以 `meta_data.steps[]` 为一期事实源：不恢复旧 `task_step` 表作为新主链。

# Relevant Files（最小集——仅列下一步会用到的）
- `src/views/resource-center/TrainingResources.vue`
- `src/database/resource-api.ts`
- `src/features/self-care/task-training-contract.ts`
