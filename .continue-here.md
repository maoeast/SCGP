---
status: active
last_updated: 2026-03-31
scope: resource-center smoke verification completed, teaching-material file-category filter landed, and teaching-material runtime verification plus final copy pass pending
---

> 职责：当前唯一有效任务交接，说明这轮在做什么、做到哪、下一步是什么。
> 何时阅读：续接未完成工作时，优先于历史日志与旧计划。
> 不负责：替代 `AGENTS.md` 说明仓库级规则，或替代 `PROJECT_CONTEXT.md` 说明完整全局背景。

## Session Closeout (2026-03-31)

This section supersedes older "Completed in this session" notes below.

Current task status:

- resource-center smoke verification is now completed for the init-duplication repair:
  - app was relaunched successfully in dev mode
  - after relaunch and `Ctrl+R`, `emotion_scene` stayed at `80`
  - after relaunch and `Ctrl+R`, `care_scene` stayed at `60`
  - no refresh-driven re-seed growth reappeared in the current local dev DB
  - sensory `equipment_data` resources still retain ability tags after relaunch and refresh:
    - active sensory equipment resources checked: `63`
    - with ability tags: `63`
    - without ability tags: `0`
- teaching-material second-layer filtering is now landed in code reality:
  - files are now grouped by existing `file_type` into:
    - `video`
    - `image`
    - `document`
    - `other`
  - implementation landed in:
    - `src/utils/resource-center-business.ts`
    - `src/stores/teaching-materials.ts`
    - `src/views/resource-center/TeachingMaterials.vue`
  - the filter bar is now always visible, even when no teaching materials exist yet
  - file-category badges and counts are wired into the teaching-material list view
- current local dev DB still has no imported teaching materials:
  - `teaching_material` row count is currently `0`
  - because of that, the new file-category filter can be seen in zero-state UI, but non-zero category counting still needs real import data for runtime verification
- still already true from previous round:
  - unified training-resource copy workflow remains landed
  - source-of-truth CSV remains `docs/references/resource-copy/2026-03-30-training-resource-copy.csv`
  - generated runtime override file remains `src/data/generated-training-resource-copy.ts`
  - do not edit the generated file directly
- verified this round:
  - `npm run type-check` passes

Next step:

1. first import or upload at least one `video`, one `image`, and one `document` teaching material into the local dev DB
2. then relaunch/open resource center and manually verify:
   - file-category counts update correctly under `全部资料`
   - each business dimension can still be filtered independently
   - second-layer `视频 / 图片 / 文档 / 其他` filtering works inside the selected dimension
3. if teaching-material runtime behavior stays clean, return to the final customer-facing copy review in `docs/references/resource-copy/2026-03-30-training-resource-copy.csv`
4. then run `npm run resource-copy:build`
5. if the current local DB should reflect the latest copy immediately, run `npm run resource-copy:sync -- --yes`

Problems / blockers / risks:

- no hard blocker on the current codebase; type-check passes
- local dev DB is repaired, but any other historical SCGP DB copied from older polluted states may still need explicit dedupe/repair
- the unified copy workflow still covers training resources only; teaching materials are not in that chain
- sensory-equipment copy keys still depend on `src/database/equipment-data.ts` legacy order; do not casually reorder that array
- current local `teaching_material` table is empty, so the new category filter cannot yet be validated against non-zero runtime counts
- runtime verification for the broader resource-center / teaching-material flow is still pending
- teaching-material managed-library / resource-center redesign code is now part of repo reality, but runtime verification is still pending before calling the flow stable
- terminal/display encoding noise still affects some Chinese docs, especially `PROJECT_CONTEXT.md`, so broad text rewrites remain risky

Key decisions made in this conversation:

- treat the resource-center smoke test as an app-lifecycle verification, not only a code read
  - reason: the original duplication bug reproduced on relaunch and `Ctrl+R`, so this round needed restart + refresh confirmation against the live local DB
- derive teaching-material second-layer filtering from existing `file_type`, not a new persisted schema field
  - reason: the file type is already stored, so category grouping can be added without DB migration or duplicate source-of-truth fields
- use four user-facing file categories for teaching materials:
  - `video`
  - `image`
  - `document`
  - `other`
  - reason: this covers the customer requirement while keeping edge-case files visible instead of hidden
- keep the file-category filter visible even when there are zero teaching-material rows
  - reason: with an empty local DB, hiding the filter makes the feature look unimplemented and blocks zero-state verification

## Current State

This file only keeps the current valid handoff.
Continue from code reality, not target-state documents.

Already true:

- top-level authorization still stays on real `moduleCode`, unchanged
- `games / equipment / training-records` menus and routing now use the same 6 internal training entries
- `training_records` and `equipment_training_records` include `entry_code`; new writes persist `entry_code`
- training-record menu/stat panels now count and filter by `entry_code`
- `src/views/equipment/Records.vue` is entry-aware
- runtime system name has already been updated to `星愿能力发展训练系统`
- physical-equipment import script already dedupes by `resourceCode` and can merge duplicate references

Completed in this session:

- hard-cut cleanup for remaining old module-based record/detail flow continued
  - removed unused legacy pages:
    - `src/views/games/TrainingRecords.vue`
    - `src/views/games/SensoryTrainingRecords.vue`
    - `src/views/emotional/Records.vue`
  - `/emotional/records` now redirects into unified training records
  - emotional record detail from unified training records now goes to `/emotional/session-summary` with `trainingRecordId`
- renderer/query side now treats training/equipment record lists as entry-backed active flow only
  - `GameTrainingAPI.getStudentTrainingRecords()` now ignores old rows without `entry_code`
  - `EquipmentTrainingAPI.getStudentRecords()` now ignores old rows without `entry_code`
  - old `task_id -> legacy_id` fallback for game-record display was removed from active list flow
- Electron main-process atomic write path was hardened
  - `save-database-atomic` is now serialized per database path to reduce `.tmp` / rename races
- console warnings investigated and partially cleaned
  - fixed `src/database/init.ts` table-structure check so raw `sql.js` DB no longer assumes `database.get()`
  - fixed `src/views/admin/ClassManagement.vue` invalid `ElTag type=""` warning
- added explicit hard-cut utility:
  - `scripts/hard-cut-training-records.cjs`
  - `package.json` script: `npm run reset:legacy-training-records -- --dry-run|--yes`
- verified:
  - `node scripts/hard-cut-training-records.cjs --dry-run` -> current local dev DB shows 0 legacy entry-less records
  - `npm run type-check` passes

## Confirmed Reality

- current real game-resource coverage is still limited:
  - sensory game resources exist
  - emotional `emotion_scene / care_scene` resources exist
  - `fine-motor / soothing-aids / social-communication / life-skills` still do **not** have real game-resource coverage
- do **not** write current game resources as if all 6 groups are fully delivered
- old disposable record compatibility should stay hard-cut; do not reintroduce module-based inference
- current local dev DB is clean, not the old polluted DB
- the explicit verification student + 2 verification records may still exist unless manually removed/reset later

## Resource-Center Analysis Done

Training resources:

- current training-resource UI now filters by 7 business groups:
  - `感官训练`
  - `情绪行为`
  - `情绪调节`
  - `社交沟通`
  - `精细动作`
  - `生活自理`
  - `安抚教具`
- current training-resource UI now limits display types to:
  - `器材`
  - `游戏`
- important implementation constraint:
  - keep real storage/authorization on true `moduleCode`
  - do **not** promote those UI groups into top-level license modules
  - `emotion_scene / care_scene` are now shown as `情绪行为 / 游戏`, but they are still **not** physically flattened to plain `game`

Teaching materials:

- current teaching-materials flow has moved onto the new managed-library chain:
  - API: `src/database/teaching-materials-api.ts`
  - store: `src/stores/teaching-materials.ts`
  - file manager: `src/utils/teaching-material-file-manager.ts`
  - importer: `src/utils/resource-importer.ts`
  - page: `src/views/resource-center/TeachingMaterials.vue`
- old teaching-material seed auto-repopulation has been disabled in `src/database/resource-data.ts`
- `src/database/init.ts` now creates the new teaching-material tables and clears legacy `resource_meta / teacher_fav` data on init
- teaching materials are now aligned to the customer delivery model:
  - software installed first
  - license activates allowed modules
  - teaching-material files + CSV are delivered separately
  - customer manually batch-imports them later
- materials should still **not** be bundled into Electron install resources because files are large and legal exposure should stay outside the app package
- remaining uncertainty is runtime verification, not schema direction:
  - external file + CSV import still needs real-app smoke testing
  - managed-file open/delete behavior still needs runtime confirmation

## Remaining Work

- do **not** reopen finished training-entry hard-cut work unless a real regression appears
- next real implementation area is customer-facing copy pass plus runtime verification

Training resources:

- complete the final copy-review/edit pass in `docs/references/resource-copy/2026-03-30-training-resource-copy.csv`
- rebuild the runtime override map after copy edits
- sync the current local DB if runtime verification should use the latest copy immediately
- then do a real-app smoke test for the new business-group UI and `equipment / game` display filter
- only patch `TrainingResources.vue` again if runtime verification exposes a concrete regression

Teaching materials:

- second-layer file-category filter is now landed in UI/store, but still needs runtime verification with real imported data
- do a real-app smoke test for external file + CSV import and managed-file opening
- verify legacy `resource_meta / teacher_fav` cleanup behavior on a fresh init or migrated local DB if needed
- only continue schema/importer changes if runtime verification exposes a real gap

## Decisions Made

- old module-based record/detail pages should be removed instead of preserved as compatibility UI
- active training/equipment record flows should only read entry-backed rows
- current hard-cut strategy remains explicit delete/reset, not inference
- main-process atomic DB saves should be serialized per DB path if future save races reappear
- resource-center business grouping should be a UI/domain grouping layer, not a new top-level license model
- teaching materials remain a separate library from training resources
- teaching-material files should not be shipped inside the Electron install resources for customer delivery

## Blockers / Risks

- no active blocker on current committed code
- unified copy workflow currently excludes teaching materials
- current local DB has not yet been updated by `npm run resource-copy:sync -- --yes`
- runtime verification is still missing for the latest resource-center UI changes
- current game-resource reality is still incomplete for several business groups, so a blank group is not automatically a bug
- terminal/doc encoding noise still increases the risk of broad text rewrites

## Next Action

Start with:

1. read `AGENTS.md`, bootstrap, `README.md`, `.continue-here.md`
2. import or upload one teaching-material file for each of `video / image / document`
3. open resource center and verify teaching-material category counts + second-layer filtering with real data
4. if teaching-material runtime behavior is clean, open `docs/references/resource-copy/2026-03-30-training-resource-copy.csv`
5. perform the next final customer-facing copy review/edit pass
6. run `npm run resource-copy:build`
7. if needed for the current local DB, run `npm run resource-copy:sync -- --yes`

## Next Session Read Order

1. `AGENTS.md`
2. `docs/planning/2026-03-23-scgp-context-bootstrap.md`
3. `README.md`
4. `.continue-here.md`
5. `PROJECT_CONTEXT.md` only if broader background is truly needed

## Next Session Prompt

```text
先按 AGENTS.md 做最小上下文启动：
1. 读取 AGENTS.md
2. 读取 docs/planning/2026-03-23-scgp-context-bootstrap.md
3. 读取 README.md
4. 读取 .continue-here.md
5. 如确实需要更大背景，再读取 PROJECT_CONTEXT.md

然后严格以代码现状为准继续当前工作，不要把目标态当现状。

当前真实状态：
- 顶层授权模型仍按真实 moduleCode，不变
- training-record hard-cut 已继续收口，旧 module-based records/detail 旧页已删除
- 统一训练记录里的情绪记录详情现在走 /emotional/session-summary + trainingRecordId
- training/equipment 记录列表当前只读带 entry_code 的 active flow
- main process save-database-atomic 已加 per-db-path 串行化
- npm run type-check 当前已通过
- 当前真实游戏资源仍只覆盖 sensory games + emotional scenes/care scenes，不要把 6 组游戏资源写成已完整交付
- `情绪调节 / 游戏` 空列表 bug 已修复，4 个情绪调节游戏现在会进入 `sys_training_resource`
- 训练资源统一文案工作流已经落地：
  - 主表：`docs/references/resource-copy/2026-03-30-training-resource-copy.csv`
  - 生成：`npm run resource-copy:build`
  - 同步现有 DB：`npm run resource-copy:sync -- --dry-run|--yes`
  - 操作说明：`docs/guides/2026-03-30-training-resource-copy-workflow.md`

接下来优先做最终文案与运行验证：

训练资源统一文案：
- 先改 `docs/references/resource-copy/2026-03-30-training-resource-copy.csv`
- 改完后运行：
  - `npm run resource-copy:build`
  - `npm run type-check`
- 如果当前本地数据库也要同步到最新文案，再运行：
  - `npm run resource-copy:sync -- --yes`
- 不要直接手改 `src/data/generated-training-resource-copy.ts`

训练资源：
- UI 业务分组现在已经落地为：
  - 感官训练
  - 情绪行为
  - 情绪调节
  - 社交沟通
  - 精细动作
  - 生活自理
  - 安抚教具
- UI 展示类型现在只保留：
  - 器材
  - 游戏
- 但顶层授权和真实 moduleCode 不变
- emotion_scene / care_scene 在 UI 中已归到 情绪行为 / 游戏，但底层 resource_type 没有被物理改成 plain game
- 先手工验证：
  - 各业务分组筛选是否正确
  - 情绪资源包按钮是否仍只在情绪相关分组下出现
  - 某些分组为空时要先按真实数据覆盖判断，不要直接当成筛选 bug

教学资料：
- 新 teaching materials 链路已落地，不再按旧 `resource_meta + teacher_fav` 主链继续扩展
- 教学资料在资源中心中现在不只按业务维度筛选，还要支持第二层文件类别筛选：
  - `视频`
  - `图片`
  - `文档`
  - `其他`
- 第二层文件类别当前由已有 `file_type` 推导，不新增持久化字段
- 当前本地 `teaching_material` 表还是 0 条，所以这轮只完成了实现与 zero-state 可见性修正
- 下一步先导入真实教学资料，再验证分类计数和二级筛选
- 客户交付模式是：
  - 先装软件
  - 再激活授权
  - 再单独交付教学资料文件 + CSV
  - 客户手动批量导入
- 教学资料文件不要放进 Electron 安装目录

最后至少跑：
- npm run type-check
```
