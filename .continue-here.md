# Current State
- 任务：销售演示数据包一键导入——已提交 5b467a9；交付终态
- 已完成：
  ① 一键导入演示数据.bat（资产源 scripts/demo-dist-assets/，GBK+CRLF）：自动检测/强杀运行中 SCGP（真实进程名 星愿能力发展训练系统.exe）、自动备份原库到 %APPDATA%\scgp\backups\、替换演示库；wait_exit 循环平铺顶层（块内标签已根治）；两路径真机实测 PASS（含改名 cmd.exe 造同名活进程）
  ② build-demo-dist.mjs：bat 资产守卫+复制进 WORK_DIR，packZip 三件套，README 一键导入章节
  ③ 交付物 output/SCGP演示数据包-20260920.zip（763KB：库+bat+说明）——销售装应用→双击 bat→admin/admin123
- 提交：060fe67（学生档案字段+批量导入）→ 5b467a9（一键导入脚本）
- 评审链：一键导入 R1 超时（两硬发现：exe 名失配/packZip 缺 bat）→ R2-R5 pass；档案字段 R1-R5 pass

# Next Action
- None（任务闭环）。可选后续（非阻塞）：备份时间戳精确到秒（当前分钟级，同分钟重跑覆盖前份）、packZip 日期戳改本地时区（早 8 点前构建 zip 名标前一天）、电话/学籍号格式软校验、新字段回归测试

# Blockers
- None

# Dead Ends
- **bat 文件必须 GBK 编码 + CRLF 行尾**：UTF-8 中文 .bat 在 cmd 下解析错乱（echo 行尾中文被吞导致命令拼接）；PowerShell here-string 写文件默认 LF-only 也炸 cmd；%%a 经 PowerShell/JS 模板串层层降级成 %a/%%%%a 要逐层校对
- **打包应用进程名是 星愿能力发展训练系统.exe（productName）不是 scgp.exe**——tasklist/taskkill 按进程名匹配时以 release/win-unpacked 实证为准
- **execute 里 start 起的假进程会立即死**（工具环境差异）→ 真机进程实验用 bash 或 driver .cmd（内容必须 CRLF+GBK）
- 应用运行中写其库会被防抖回写覆盖 → 写库/替换库前必须确认应用退出（bat 已自动处理）
- getAllStudents/searchStudents 是显式列清单，加表列必须同步补 SELECT

# Key Decisions
- bat 检测/等待循环平铺 goto 结构（不用块内标签），%wait% 非 !wait!（goto 逐行重解析语义）
- bat 以 GBK 资产源入库（scripts/demo-dist-assets/），构建时二进制复制（copyFileSync/CopyFileW 保留 GBK 与源 mtime）
- 自动备份到 %APPDATA%\scgp\backups\database.sqlite.bak-日期-时间，恢复=复制回原位
- 演示学籍号 BigInt 字符串拼接（G24+16 位=19 位）；批量导入从宽校验（仅 trim）

# Relevant Files
- scripts/demo-dist-assets/一键导入演示数据.bat（GBK 资产源）/ scripts/build-demo-dist.mjs（三件套打包）
- output/SCGP演示数据包-20260920.zip（763KB 交付物，不入库）/ output/demo-dist/（WORK_DIR）
- 学生档案链：src/database/{init,api}.ts、src/views/{StudentDetail,Students}.vue、src/utils/student-import.ts
- 备份：C:/Users/maoea/AppData/Roaming/scgp/database.sqlite.bak-20260920-pre-guardian（回退用，先关应用）
