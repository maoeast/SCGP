# Current State
- 任务：cognitive_self 评估链路 UI 重构 + 评估流程相关 bug 修复（用户分批提需求，本轮全部完成）
- 已完成：
  - 答题板重构（PerformanceTrialBoard.vue）：删 board-header，目标卡片化（指导语上移为正式条、视觉加权），左右垂直居中
  - 绩效题合并 Header（AssessmentContainer.vue）：单一信息栏取代"任务总览卡+题目卡"双层结构，左量表名+学生·年龄，右维度Tag+练习Tag+paceTag+n/total迷你进度条
  - 完成弹窗重构（CompleteDialog.vue）：标题去重、评定等级短名化（VERDICT_SHORT 映射）避免整句溢出 Tag、评语卡片化、删误导性"生成中"文案、按钮统一三态（Primary/Default plain/Default）
  - 推荐抽屉 bug 修复（RecommendationDrawer.vue）：封面图改用 resolveResourceItemCoverImage（原直接用 coverImage 原始相对路径导致无法加载）；跳转改 router.push('/training-plan')（原用不存在的路由名 'PlanList' 导致不跳转）
  - 评估前必读文案更新（CognitiveSelfDriver.ts getWelcomeContent）：删文不对题的"给家长的填表大实话"，改为"怎么带孩子做这个测验"+"怎么看这个结果"，补全操作要点（18题/2练习/4难度/超时40s）
  - 品牌标识残留清理：Layout.vue 收缩态 ATS→SCGP；index.html title + Layout.vue 三处兜底"感官综合训练与评估"→"星愿能力发展训练系统"
  - type-check 全程通过；cognitive-self-gate 门禁测试 10/11 过（第11项人工复核）
- 产出物：上述 7 个文件的修改（均在工作区未提交）

# Next Action
- 等待用户验收本轮 UI 重构与 bug 修复（建议真机走一遍：评估前必读→答题→完成弹窗→器材推荐抽屉→生成计划→跳转计划列表）；若需提交，按精确文件提交，注意部分文件混有会话前的 v4 改动

# Blockers
- 工作区 41 个未提交变更（含本轮 7 个 + 会话前既有的 v4 专题/图标/教学资料等改动）；本轮改动与既有改动混在同一文件（Layout.vue / CognitiveSelfDriver.ts），无法干净拆分提交，需用户决定提交策略

# Key Decisions
- 评定等级长句不再塞 el-tag：cognitive_self 的 level 是完整判读句（VERDICT_LABELS），改用 verdict 短名映射（stable→表现稳定 等）放摘要区 Tag，完整句只在评语卡片出现一次
- 推荐抽屉封面图必须经 resolveResourceItemCoverImage 解析（全项目唯一正确入口），禁止直接用 resource.coverImage
- 报告跳转用 path（'/training-plan'）而非路由名，路由 name='TrainingPlan' 不等于组件变量名 PlanList

# Relevant Files
- `src/views/assessment/components/PerformanceTrialBoard.vue`
- `src/views/assessment/AssessmentContainer.vue`
- `src/views/assessment/components/CompleteDialog.vue`
- `src/features/recommendation/components/RecommendationDrawer.vue`
- `src/strategies/assessment/CognitiveSelfDriver.ts`
- `src/views/Layout.vue`
- `index.html`
