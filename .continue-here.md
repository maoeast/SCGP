---
status: active
last_updated: 2026-03-31
scope: training workspace scroll fix and equipment quick-entry duration-minute input are landed, while resource-center runtime verification and final copy pass remain pending
---

> Responsibility: the only current handoff file for unfinished work.
> Read when: continuing work from a previous session.
> Not responsible for: replacing `AGENTS.md` repo rules or `PROJECT_CONTEXT.md` global context.

## Session Closeout (2026-03-31)

This section is the current valid handoff.

Current task status:

- training workspace split-scroll behavior is now landed for the two affected pages:
  - `src/views/equipment/QuickEntry.vue`
  - `src/views/games/GameLobby.vue`
  - shared layout classes landed in `src/assets/layout.css`
  - left resource list and right entry/preview pane now scroll independently inside the workspace
  - `overscroll-behavior: contain` is now part of the pane rule so wheel scrolling stops at pane boundaries
  - the user has manually verified the original right-pane drift problem is resolved
- the layout rule is now documented as current repo guidance:
  - `docs/reports/2026-03-31-training-workspace-layout-scroll-guideline.md`
  - linked from `README.md` and `docs/INDEX.md`
  - summarized in `PROJECT_CONTEXT.md`
- equipment quick-entry duration input is now switched to minute-based entry in UI:
  - file: `src/components/equipment/DataEntryForm.vue`
  - unit shown to users is now minute-based
  - default value is now `3`
  - plus/minus step is now `1` minute
  - manual input remains allowed
  - submit path still converts minutes back to `duration_seconds` for storage, so downstream schema/statistics are unchanged
- broader resource-center work remains where it was before this UI fix:
  - teaching-material second-layer filter is landed
  - current local dev DB still has `0` imported teaching materials
  - runtime verification with real imported teaching-material files is still pending
- verified this round:
  - `npm run type-check` passes

Next step:

1. open equipment quick-entry once in the dev app and manually save a record to confirm the new `3`-minute default, `±1` minute controls, manual input, and record persistence all behave correctly end-to-end
2. if that stays clean, import or upload at least one `video`, one `image`, and one `document` teaching material into the local dev DB
3. relaunch/open resource center and manually verify:
   - file-category counts update correctly under `全部资料`
   - each business dimension can still be filtered independently
   - second-layer `视频 / 图片 / 文档 / 其他` filtering works inside the selected dimension
4. if teaching-material runtime behavior stays clean, return to the final customer-facing copy review in `docs/references/resource-copy/2026-03-30-training-resource-copy.csv`
5. run `npm run resource-copy:build`
6. if the current local DB should reflect the latest copy immediately, run `npm run resource-copy:sync -- --yes`

Problems / blockers / risks:

- no hard blocker on the current codebase; type-check passes
- the minute-based duration input is intentionally UI-only; storage remains `duration_seconds`
  - do not casually rename the field or migrate downstream statistics/report code unless the whole chain is updated together
- local dev DB is repaired, but any other historical SCGP DB copied from older polluted states may still need explicit dedupe/repair
- the unified copy workflow still covers training resources only; teaching materials are not in that chain
- sensory-equipment copy keys still depend on `src/database/equipment-data.ts` legacy order; do not casually reorder that array
- current local `teaching_material` table is empty, so the new category filter cannot yet be validated against non-zero runtime counts
- runtime verification for the broader resource-center / teaching-material flow is still pending
- terminal/display encoding noise still affects some Chinese docs, especially `PROJECT_CONTEXT.md`, so broad text rewrites remain risky

Key decisions made in this conversation:

- fix the training quick-entry / game-lobby scroll issue by correcting scroll ownership, not by forcing the right pane to `position: fixed`
  - reason: the real bug was that the outer route container was scrolling; split panes are more stable than fixed positioning across headers, responsive widths, and future content changes
- standardize this pattern as a reusable workspace layout rule in `src/assets/layout.css`
  - reason: the same bug already appeared in two pages, so the fix should be reusable instead of duplicated page-local CSS
- change equipment quick-entry duration input to minutes in the UI, but keep seconds in storage
  - reason: this matches therapist input expectations without forcing schema/report/statistics churn across the existing record pipeline
- use `3` minutes as the default duration and `1` minute as the step
  - reason: the user explicitly requested minute-scale entry with a sensible default and manual override

## Current State

Already true:

- top-level authorization still stays on real `moduleCode`, unchanged
- `games / equipment / training-records` menus and routing use the same 6 internal training entries
- `training_records` and `equipment_training_records` include `entry_code`; new writes persist `entry_code`
- training-record menu/stat panels count and filter by `entry_code`
- `src/views/equipment/Records.vue` is entry-aware
- runtime system name has already been updated to `星愿能力发展训练系统`
- unified training-resource copy workflow remains landed:
  - source-of-truth CSV: `docs/references/resource-copy/2026-03-30-training-resource-copy.csv`
  - generated runtime override file: `src/data/generated-training-resource-copy.ts`
  - do not edit the generated file directly

Confirmed reality:

- current real game-resource coverage is still limited:
  - sensory game resources exist
  - emotional `emotion_scene / care_scene` resources exist
  - `fine-motor / soothing-aids / social-communication / life-skills` still do not have real game-resource coverage
- do not write current game resources as if all 6 groups are fully delivered
- teaching materials remain a separate managed-library flow and are not bundled into Electron install resources

## Remaining Work

- manually verify the new minute-based duration input by saving one real equipment quick-entry record
- complete teaching-material runtime verification with real imported files
- if the resource-center behavior is clean, finish the final customer-facing copy pass and rebuild the copy map

## Next Action

Start with:

1. read `AGENTS.md`, bootstrap, `README.md`, `.continue-here.md`
2. open equipment quick-entry and save one real record to verify the new `3`-minute default and minute-based input path
3. if that is clean, import or upload one teaching-material file for each of `video / image / document`
4. open resource center and verify teaching-material category counts + second-layer filtering with real data
5. if teaching-material runtime behavior is clean, open `docs/references/resource-copy/2026-03-30-training-resource-copy.csv`
6. perform the next final customer-facing copy review/edit pass
7. run `npm run resource-copy:build`
8. if needed for the current local DB, run `npm run resource-copy:sync -- --yes`

## Next Session Read Order

1. `AGENTS.md`
2. `docs/planning/2026-03-23-scgp-context-bootstrap.md`
3. `README.md`
4. `.continue-here.md`
5. `PROJECT_CONTEXT.md` only if broader background is truly needed

## Next Session Prompt

```text
先按 AGENTS.md 做最小上下文启动：
1. 读取 AGENTS.md
2. 读取 docs/planning/2026-03-23-scgp-context-bootstrap.md
3. 读取 README.md
4. 读取 .continue-here.md
5. 如确实需要更大背景，再读 PROJECT_CONTEXT.md

然后严格以代码现状为准继续当前工作，不要把目标态当现状。

当前最近已完成：
- 器材快速录入和游戏大厅的双栏工作台滚动修复已落地，右侧面板不再因左栏长列表滚轮而被推出视口
- 公共布局类已落在 `src/assets/layout.css`
- 该布局约束已写入 `docs/reports/2026-03-31-training-workspace-layout-scroll-guideline.md`
- 器材快速录入的训练时长输入已改为按“分钟”录入：
  - 默认 `3`
  - 步进 `1`
  - 允许手动输入
  - 提交时仍换算为 `duration_seconds`
- `npm run type-check` 已通过

接下来先做：
1. 在 dev app 中打开 equipment quick-entry
2. 保存一条真实记录，验证分钟输入链路
3. 如果正常，再继续 teaching-material 导入与 resource-center 运行时验证
```
