# Current State
- 任务：继续自理训练模块阶段 1，补齐 `task_training` 的平台主链，当前已完成 `self-care` 路由壳
- 已完成：新增 `src/features/self-care/self-care-routes.ts`，定义 `/self-care` 与 `/self-care/tasks` 路由壳并固定 `meta.moduleCode = life_skills`；新增 `src/views/self-care/TaskList.vue` 作为最小占位页；修改 `src/router/index.ts` 接入 `selfCareRoutes`；修改 `src/views/Layout.vue` 将 `SelfCareTraining` 插入静态菜单顺序；新增 `scripts/tests/self-care-routes.test.mjs` 覆盖路由壳路径与授权绑定
- 产出物：`src/features/self-care/self-care-routes.ts`、`src/views/self-care/TaskList.vue`、`src/router/index.ts`、`src/views/Layout.vue`、`scripts/tests/self-care-routes.test.mjs`

# Next Action（必须原子级——第一步可执行的单一动作）
- 修改 `src/utils/training-launch.ts`，新增 `task_training` 的统一启动映射，先接到现有 `/self-care/tasks` 壳入口

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- `自理训练` 顶层入口继续挂 `life_skills` 授权：当前授权体系只识别现有业务模块代码
- 阶段 1 先只补 `/self-care` 与 `/self-care/tasks` 路由壳：不提前扩到任务 CRUD、执行页或记录详情页
- `task_training` 继续复用训练资源的 `game` 展示主链：本阶段目标仍是“可见 / 可选 / 可启动”

# Relevant Files（最小集——仅列下一步会用到的）
- `src/utils/training-launch.ts`
- `src/features/self-care/self-care-routes.ts`
- `docs/planning/2026-05-08-self-care-training-module-implementation-plan.md`
