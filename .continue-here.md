# Current State
- 任务：C08 开发路由生产隔离已提交（`b9cdf81`）；下一批次 C09 ModuleRegistry 事实修正待新会话开工。
- 已完成：11 个 dev 路由（含 `ActivationAdmin`）隔离到 `src/router/dev-routes.ts`，`import.meta.env.DEV` 三元 + 组件 import 内联使生产构建 tree-shake 移除 chunk；全局守卫改用 `DEV_ROUTE_NAMES`。验证：`type-check` EXIT=0 / 边界测试 + training-route-access 回归 11-11 / `build:web` 39s / `dist/assets` 内容与 chunk 文件名扫描均无命中。
- 产出物：commit `b9cdf81`（C08 代码+测试，本地未 push）

# Next Action
- 读 `src/core/module-registry.ts` 与 `src/types/module.ts`，核实当前已注册模块、`ModuleCode` 全集与 feature 路径声明（不照搬计划文档），再按 `docs/plans/2026-07-18-project-closeout-execution-plan.md` §C09 推进。

# Blockers
- None

# Key Decisions
- C08 dev 路由隔离到 `src/router/dev-routes.ts`（`import.meta.env.DEV` 三元 + 组件动态 import 内联），生产构建 tree-shake 移除 chunk；`DEV_ROUTE_NAMES` 为独立字符串字面量（不 `.map()` 派生）以保 tree-shaking。
- C09 做 registry feature 路径校验时注意：dev 路由（`ModuleDevTools` 等）已在 `dev-routes.ts` DEV-gated、生产不可达，不应进入 registry 正式 feature 路径集合。

# Relevant Files
- `src/core/module-registry.ts`
- `src/types/module.ts`
- `src/utils/training-entry.ts`
- `docs/plans/2026-07-18-project-closeout-execution-plan.md`（§C09）
