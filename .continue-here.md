# Current State
- 任务：路线 D 跨量表学生画像（AI 立体描绘孩子面貌特征）
- 已完成（本轮，工作区未提交）：
  - 新增 get_student_profile AI 工具：聚合学生所有已测标准化量表最近一次评估，按五大发展领域（感觉统合/情绪调节/社交沟通/认知发展/生活自理）分组
  - 新增 assessment-profile.ts 纯函数聚合层（领域映射 SCALE_DOMAIN_MAP + 结论生成 buildScaleConclusion + 领域强弱聚合 + strengthToScore 量化分），可 jiti 单测
  - 扩展 ToolArtifact 联合：新增 ProfileRadarArtifact（profile_radar），领域≥3 时产出雷达图富产物
  - 新增 AiProfileRadar.vue 雷达图组件（echarts radar，50 分=正常基准线），接入 AiChatTranscript 自动渲染
  - 3 个内置 agent（一人一策/成长看得见/稳健训练）挂载 get_student_profile 工具
  - 授权差异与部分测试的处理：工具只聚合有评估记录的量表；未测量表在 untestedScales 透明列出；领域<3 不产图只出文字
- 产出物：9 文件改动（6 源码 + 1 测试新增 + 2 契约测试更新），工作区未提交

# Next Action
- 用户确认后提交（建议：feat(ai): 跨量表学生画像（get_student_profile 工具 + 雷达图 artifact + 3 agent 挂载））
- 实测验证：在 AI 对话问「分析某学生的综合评估画像」，确认回复下方出现雷达图 + AI 文字画像

# Blockers
- None（功能完成，待用户实测确认后提交）

# Key Decisions
- 方案 X：13 量表统一口径结论（scoreNote + level + totalScore 拼装），不调各量表专属规则函数（签名不统一、易碎）；深度建议交给 AI 基于画像数据生成
- 授权差异：不引入工具层授权参数，依赖前端 entitlement 物理隔离（未授权量表测不了 → DB 无记录 → 自动跳过）
- 领域强弱聚合取最差值（weak 优先），保守不掩盖短板；同领域 strong+weak → mixed
- 领域≥3 才产雷达图（多边形至少 3 边）；不足时 AI 只输出文字画像
- get_student_profile 只挂 3 个评估/训练相关 agent，沟通/情绪支持 agent 不加（场景不符）

# Relevant Files
- src/services/assessment-profile.ts（新增，纯函数聚合层）
- src/services/ai-tools.ts（get_student_profile schema + dispatch + ProfileRadarArtifact 类型）
- src/features/ai/components/AiProfileRadar.vue（新增，雷达图组件）
- src/features/ai/components/AiChatTranscript.vue（profile_radar 渲染接入）
- src/data/ai-agent-presets.ts（3 agent 挂载 get_student_profile）
- tests/assessment-profile.test.ts（新增，8 场景纯函数测试）
- scripts/tests/ai-tool-artifact-trend.test.mjs（扩展 4 场景路线 D 契约）
- scripts/tests/ai-builtin-agent-presets-contract.test.mjs（同步 toolCodes 断言）
