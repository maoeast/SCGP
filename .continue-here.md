# Current State
- **v1.1.4 已打包 + 已发布（2026-09-20，同日两次）**：`package.json` version 1.1.3→1.1.4（**工作区未提交**）。① 首版 `release/scgp-setup-1.1.4.exe` 265,996,179 B / sha512 `DiJQTjSH…`；② **ABC 修复后重打（版本不变，同版本覆盖部署）**：265,994,189 B / sha512 `eq7xOrhB…` + blockmap 279,720 + latest.yml 337（releaseDate 09:51:42Z）。两次均 `release:verify` 8/8 PASS、asar 内 `scgpDebugDevtools=false` 复核过；三件套落 `/home/lighthouse/scgp/win/`（root:root 644），服务器 curl 双源均 `version: 1.1.4`，服务器端 exe sha512 与本地逐字节一致
- 同版本覆盖的语义：**已装 1.1.4 的机器不会收到这次修复的自动更新**（electron-updater 同版本视为已是最新）；1.1.3 及更老用户会直接拿到含修复的 1.1.4
- 1.1.4 变更集 = 自 1.1.3（`17f0598`，9-11）以来 31 笔提交：首页看板三段重构 + 锚点修复、AI 记忆批次状态机修复、学生档案字段补全/调班、演示数据 v2 + 一键导入、S-M 报告等级卡、教学资料分页、verify:core 陈旧断言修复
- **ABC 报告维度表 bug 修复 + 同根因收尾（未提交，已进本次 1.1.4 重打包）**：得分列渲染 JSON / 占比 NaN —— 根因①演示数据把维度分写成对象 `{name, rawScore}`（契约是数字）；②报告页硬编码满分 60/48/72/52 实为 GMFM-88 表值（真值 38/34/52/34 = 158）；③演示数据含题库里没有的「生活自理」虚维度并计入总分。改动：`abc-questions.ts`（派生满分 + `normalizeABCDimensionScores`）、`abc/Report.vue`、`seed-demo-data/data.mjs`、新增 `tests/abc-dimension-rows.test.ts`（已入 `test:core:ts`）、`docs/CHANGELOG.md` 一条。同根因收尾：`assessment-score-normalize.ts` flat-number 兜底（AI 趋势不再 NaN）+ `tests/assessment-score-adapter.test.ts` 第 12 组补断言 + 新增存量修正脚本 `scripts/fix-legacy-abc-dimension-scores.ts`（dry-run：15 行待修，真实行不动）。验证 `verify:core` 全绿（132/132 + 6/6）
- 任务：手册截图专项收口——(B) 修复 09-10 重编号遗留的截图错位 + (A) 首页族重拍与文案同步。均已提交：
  - `cb8cbf6` `fix(manual)`: 121 张归位 + 5 张自 git 历史恢复 + approvals 溯源重建（131 files）
  - `3dc179d` `docs(manual)`: 首页族 8 张重拍 + 自动刷新文案（12 files）
  - 本会话另有一笔已提交技术债修复：`1ee5dd1` `fix(tests)` verify:core 陈旧契约断言对齐（6 → 0 fail）
- B 阶段根因：`3e871e6` 的 6 处插行（S080/S081/S176/S177/S190/S210）只重映射了 approvals 元数据、**未同步改物理文件名** → S082 起各段「图注 ↔ 图片内容」整体错位（偏移 +2/+4/+5/+6）；approvals 的 sha 与文件一起漂移，机械校验全绿却掩盖错位（靠开图抽查 5 张才暴露）
- B 阶段规则：新槽位 p 的内容 = 文件 `S(p − k(p))`；旧 n → 新 t 用不动点迭代 `t = n + k(t)`；S148 是 09-10 重拍件占位，其旧内容归位到 S150
- A 阶段：8 张重拍（S001/S002/S007/S008/S009/S010/S011/S168）；run `dashboard-refresh-20260920`（S001/S007/S168）+ `dashboard-refresh-2-20260920`（拆分批）
- 验证：`verify-user-manual-approved-screenshots.mjs` = **217 通过**；`manual:screenshots:check` = 217 场景 / 0 pending；advisor 评审两轮均 pass
- 文档：基线 §2（补「同名异哈希拒绝覆盖」）、§4.1（修复记录）、§4.2（重拍批次与采集纪律）；归档 `docs/archive/manual-screenshot-renumber-20260920/`（映射规则 + 三份一次性脚本）；`docs/CHANGELOG.md` 两条

# Next Action
- 待提交（本轮已攒齐，待用户拍板后提交）：`package.json`（version 1.1.4 + `test:core:ts` 挂新测试）、`src/database/abc-questions.ts`、`src/views/assessment/abc/Report.vue`、`src/services/assessment-score-normalize.ts`、`scripts/seed-demo-data/data.mjs`、`scripts/fix-legacy-abc-dimension-scores.ts`（新）、`tests/abc-dimension-rows.test.ts`（新）、`tests/assessment-score-adapter.test.ts`、`docs/CHANGELOG.md`、`.continue-here.md`；另 `docs/user-manual/…用户使用手册.pdf` 为此前遗留（不进安装包）
- 存量数据遗留（脚本已备，**未落库**）：已入库的 15 行演示 ABC 行总分含已废弃「生活自理」分（学生 10013：88 = 四维 71 + 17）；`npx jiti scripts/fix-legacy-abc-dimension-scores.ts`（dry-run 已验）→ 关应用后加 `--apply` 落库；`output/SCGP演示数据包-20260920.zip` 仍是旧数据，重导前需先用修好的生成器重打
- AI 纵向趋势同根因 **已修**：`assessment-score-normalize.ts` flat-number 模式改走 `toFlatDimensionScore`（对象形状取 `rawScore`，脏值记 null 不产 NaN）——该修复**未进已发布的 1.1.4 安装包**（包在修复前构建），需下一次重打才生效
- 无阻塞待办。未排期可选项（勿顺手做）：
  1. 手册 docx 再生成：`generate-user-manual.mjs --include-approved-screenshots` → `validate-user-manual.mjs`（52MB 产物，gitignore；仅当需要分发版本时跑）
  2. 四组重复图甄别与重拍：S020/S021、S187/S188、S207/S208、S211/S213 字节相同，违反基线 §2「同一张图片不得重复用于多个编号」（继承自修复前）
  3. 测试范式治本专项（30 个 `.mjs` 契约测试为「正则匹配源码文本」，脆弱）

# Blockers
- None

# Dead Ends
- **Windows cmd 下 `git show <sha>^` 的 `^` 会被当转义符吃掉** → 解析成该提交自身，得到「自比自」的假结论；查父提交必须用 `~1`（本会话曾因此误判模型被证伪）
- 截图采集管线：**S007（用户菜单浮层）必须与其它场景分批采集**——同会话会把浮层残留在后续场景画面里（首批 S008 即被污染，已拆分重跑）
- `promote-user-manual-screenshots.mjs` 对「同名但哈希不同」**拒绝覆盖**（`Destination already differs`），重拍落盘前必须先删旧图
- 改名/搬家后若同步重算 approvals 的 sha，机械校验会「全绿掩盖错位」——图与图注的语义一致性没有机器门禁，必须人工抽查

# Key Decisions
- 修复策略取「改名归位」而非整段重采（131 张内容本身没过期，重采会掩盖编号位移这个根因）
- approvals 的 runId 还原为真实采集 run（127 个）；重编号条目下 runId 仅作溯源标签，**approvals 的 sha256 是与落盘图对齐的唯一锚点**；`promote` 必须带 `--ids`，勿全表重放
- 两笔提交分离「技术债修复」与「新特性同步」（用户拍板）；B 先落袋再做 A，保证 A 的重拍形成干净小 diff
- 截图管线仍走「采集 → 人工审批 → promote」三段式，不引入自动审批

# Relevant Files
- 基线（截图单一事实源与维护规则）：`docs/user-manual/SCGP-用户手册截图采集与维护基线.md`（§2 验收标准 / §3 清单 217 / §4.1 修复 / §4.2 重拍）
- 归档：`docs/archive/manual-screenshot-renumber-20260920/README.md`（映射规则、逐项归位表、复现命令、遗留项）
- 管线：`scripts/manual/{user-manual-screenshot-plan.mjs, user-manual-screenshot-scenarios.mjs, capture-user-manual-screenshots.mjs, promote-user-manual-screenshots.mjs, verify-user-manual-approved-screenshots.mjs}`
- 对照源码：`src/views/Dashboard.vue`（自动刷新：`AUTO_REFRESH_INTERVAL_MS = 180_000`、隐藏态跳过、`visibilitychange` 回前台即刷）
