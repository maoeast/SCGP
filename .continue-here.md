# Current State
- 任务：修复“训练计划 > 新建计划 > 资源编排 > 添加资源”弹窗的训练资源筛选链路，并重跑本地 custom game 资源同步。
- 已完成：`src/views/plan/PlanList.vue` 已移除器材下冗余“全部分组/感官训练/...”筛选条；资源弹窗模块筛选已改为 9 项统一口径，类型筛选已收敛为“全部类型/游戏/器材”；`src/views/plan/plan-resource-selector-filter.ts` 已承接统一模块/类型过滤逻辑；`src/data/emotional-game-catalog.ts` 新增 `ALL_CUSTOM_GAME_RESOURCE_SEED`；`src/database/init.ts` 已把 custom game 资源同步从仅情绪调节 5 个扩成全量 25 个；`scripts/resync-custom-game-resources.cjs` 已对本地 `C:\\Users\\maoea\\AppData\\Roaming\\scgp\\database.sqlite` 成功重跑同步，结果为 updated 25 / inserted 0。
- 产出物：`src/views/plan/PlanList.vue`、`src/views/plan/plan-resource-selector-filter.ts`、`src/data/emotional-game-catalog.ts`、`src/database/init.ts`、`scripts/resync-custom-game-resources.cjs`、`tests/plan-resource-selector-filter.test.ts`

# Next Action（必须原子级——第一步可执行的单一动作）
- 启动应用，手动打开“训练计划 > 新建计划 > 资源编排 > 添加资源”，依次验证“器材”下无冗余分组条，且“安抚教具 / 社交沟通 / 精细动作 + 游戏”各自能显示 5 个待选资源。

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 资源弹窗只保留两层主筛选：模块 + 类型：器材下第三层分组条判定为冗余，直接移除。
- `task_training` 在该弹窗的显示类型继续归为“游戏”：与现有 `TASK_TRAINING_RESOURCE_DISPLAY_TYPE = 'game'` 契约保持一致。
- custom game 资源同步改为全量 25 条：空列表根因是数据库历史上只同步了 `emotional-regulation` 5 条，不是单纯前端过滤问题。

# Relevant Files（最小集——仅列下一步会用到的）
- `src/views/plan/PlanList.vue`
- `src/views/plan/plan-resource-selector-filter.ts`
- `scripts/resync-custom-game-resources.cjs`
- `tests/plan-resource-selector-filter.test.ts`
