# Current State
- 任务：评估正常/优秀场景的「能力巩固推荐」文案 + 精选器材 —— 完成、真机验证、提交。
- 已完成：推荐引擎新增 `mode='consolidation'` 模式（`recommendation-engine.ts: generateConsolidationRecommendation`，无弱势时按全部 equipmentSupported 域 + entitlement 硬过滤 + usageCount 降序 + 每域 cap3/默认勾选1）；`store.generate` 弱势为空走巩固分支；`createDraftPlan` 挂载成功后 `incrementUsageCount` 接通 usage_count 死代码；`plan-generator` 巩固兜底目标（`${label}能力巩固与泛化`）；`RecommendationDrawer` 发展概况卡片 + 「能力巩固精选」标题；`CompleteDialog` 按钮动态文案（level∈[优秀,高常,正常]→能力巩固推荐）。type-check exit 0，15 项 smoke 全过，真机 E2E 通过。
- 产出物：6 文件 +234/-39，已提交。

# Next Action
- 本任务已完整收尾，等待用户指定新任务。（可选独立后续：训练记录写入路径也计 usage_count +1，碰训练记录主表，需单独评估。）

# Blockers
- None

# Key Decisions
- usage_count 死代码接通点 = `store.createDraftPlan` 挂载成功后对选中器材 +1（不碰训练记录主表）；初期全 0 退化为 created_at 顺序，随使用累积真实化。
- 巩固精选依据 = usageCount DESC（非标签打分、非人工标记）；弱势模式打分逻辑完全不变（仅补 mode 字段）。
- 入口按钮文案按 `scoreResult.level` 粗引导，与引擎精确模式解耦（极端不一致无害）。

# Relevant Files
- `src/features/recommendation/recommendation-engine.ts`
- `src/stores/recommendation.ts`
