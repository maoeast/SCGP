# Current State
- 任务：继续自理训练模块阶段 2，完成 `task_training` 在资源中心与 `/self-care/tasks` 内部任务库的最小 CRUD 闭环。
- 已完成：`src/features/self-care/task-training-contract.ts` 已补 `task_training` 元数据归一化、步骤校验与编辑器创建函数。
- 已完成：`src/views/resource-center/TrainingResources.vue` 与 `src/views/resource-center/editors/TaskTrainingEditor.vue` 已支持 `task_training` 的创建、编辑、保存，事实源固定为 `meta_data.steps[]`。
- 已完成：`src/database/self-care-task-api.ts` 已封装 `life_skills + task_training` 的任务列表、单条读取、创建、更新、禁用、恢复。
- 已完成：`src/views/self-care/TaskList.vue` 已从占位页改为真实任务列表；`src/views/self-care/TaskEditor.vue` 已支持新建/编辑自理任务。
- 已完成：`src/features/self-care/self-care-routes.ts` 已新增 `/self-care/tasks/new` 与 `/self-care/tasks/:taskId/edit`，授权继续挂 `life_skills`。
- 已完成：相关测试已补齐并通过：`self-care-routes`、`self-care-task-api`、`self-care-task-pages`、`task-training-contract`、`task-training-resource-editor`、`training-resource-ui`、`resource-center-business`、`training-launch`；`npm run type-check` 已通过。
- 产出物：`src/database/self-care-task-api.ts`、`src/views/self-care/TaskList.vue`、`src/views/self-care/TaskEditor.vue`、`src/views/resource-center/editors/TaskTrainingEditor.vue`

# Next Action（必须原子级——第一步可执行的单一动作）
- 创建 `src/views/self-care/SelectStudent.vue`，复用现有学生选择器承接自理任务列表的“开始训练”入口。

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 自理训练继续挂 `life_skills` 授权：当前授权体系只识别既有业务模块代码。
- `task_training` 继续复用训练资源的 `game` 展示主链：当前先保证可见、可选、可启动，不提前拆独立展示体系。
- `task_training` 步骤结构继续以 `sys_training_resource.meta_data.steps[]` 为一期事实源：不恢复旧 `task_step` 表。

# Relevant Files（最小集——仅列下一步会用到的）
- `src/views/self-care/TaskList.vue`
- `src/features/self-care/self-care-routes.ts`
- `src/views/games/SelectStudent.vue`
- `src/database/self-care-task-api.ts`
