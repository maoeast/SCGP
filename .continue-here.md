# Current State
- **当前主线（已完成，真机 E2E 通过 2026-07-09，待提交）**：认知发展（cognitive）器材资源 + 器材训练模块接入 —— 代码全部落地，静态验证 + 真机 E2E 均通过。
  - 6 个决策点已敲定（见下「Key Decisions」）。
  - 已完成：枚举/类型层、CSV 落库、资源归类、训练入口、图片落位、entitlement 激活、teaching_material CHECK 迁移、DEV_MOCK 补 cognitive。
  - 静态验证：`npm run type-check` exit 0；parser jiti 断言 52 条全解析（resourceCode 唯一+形态、16/17 不误去重、顿号标签、seq 1-52 全覆盖）；52 张 webp 已按 resourceCode 改名落位（22 号 25.3K，总 1.9MB）。
  - 改动：18 文件 +124/-9（tracked）+ 新增 `docs/references/physical-equipment/cognitive/cognitive-equipment-draft.csv` + `src/assets/images/physical-equipment/cognitive/*.webp`（52 张）。一次性脚本 `scripts/verify-cognitive-intake.cjs` 已删。
  - **未提交**：用户未要求 commit；改动全在工作区。
  - **真机 E2E 结果（2026-07-09 用户确认通过）**：资源中心认知发展分组+52 条器材资源+图片非占位、器材训练认知发展入口卡片+快速录入落 `equipment_training_records`/`training_session`、教具页认知发展分类。
- 上一条主线：游戏 IEP 闭环扩展 Phase 2+3 已提交推送 974a69b。

# Next Action
- 认知发展接入已完成（真机 E2E 通过）。下一步：提交（待用户确认是否 commit 到 main + push）。
- 既有待办（不阻塞）：IEP Phase 2+3 真机 E2E 仍未跑。

# Blockers
- 无（认知发展接入已完成并真机验证）。
- IEP Phase 2+3 真机 E2E 未跑（既有待办，不阻塞）。

# Key Decisions（6 个待决策点，2026-07-09 敲定）
1. entitlement cognitive：placeholder/lock → **active/hide**（正式开放授权，描述改正式口径）。
2. resourceCode：parser 自动生成 `cognitive-box{xx}-seq{yyy}` + 一次性脚本按序号→resourceCode 改名图（用 parser 实际输出建映射，保证与运行时查图名一致）。
3. 图片转换：用户自行转 webp（已转好 52 张放 G:\SCGP_Rec\认知\images）；我只按 resourceCode 改名落位 + DOMAIN_COLORS。
4. 离线导入：只走 init，不补 cjs（init 每次启动幂等 upsert，老库够用）。
5. 图标配色：`Cpu` / `#13c2c2` teal。
6. 教具页连带：接受（TeachingMaterials 也显示「认知发展」分类，不隔离）。

# 实施中超计划补充（plan 12 坑未列）
- **plan 漏列的穷举映射**（type-check 抓出，已补）：`training-plan-module.ts`（CODES/ORDER/LABELS/ALIASES 加 cognitive-development）、`AssessmentSelect.vue` TAB_THEME_MAP、`GameModuleMenu.vue`/`GameLobby.vue` ENTRY_EMOJIS（cognitive→🧠）、`TeachingMaterials.vue` DIMENSION_DESCRIPTIONS、`resource-center-business.ts` TEACHING_MATERIAL_DIMENSION_MODULE_MAP。
- **teaching_material CHECK 约束**（DP6 引入的运行时坑）：老库 `teaching_material` 的 dimension_code/module_code CHECK 不含 cognitive-development/cognitive，上传认知教具会被拒。已改 CREATE TABLE（新库）+ 新增 `migrateTeachingMaterialCognitiveConstraint`（老库重建表，照 game_emotion_records 既有 idiom：tableSqlContains 守卫 + PRAGMA foreign_keys 开关 + 显式列拷贝）。
- **DEV_MOCK_ALLOWED_MODULES**（auth.ts:41）原缺 cognitive → dev 旁路下 isFullAccess=false、不展开 cognitive。已补 cognitive → dev 旁路全授权。
- **license 侧无需改**：`license-generator-dist/generate-license.js` 的 ENTITLEMENT_CODES 已含 cognitive，默认授权码已授予 cognitive。

# Relevant Files
- 任务计划（主真源）：docs/plans/2026-07-08-cognitive-development-intake-plan.md
- 改动核心：src/types/physical-equipment.ts、src/features/entitlements/entitlement-catalog.ts、src/utils/training-entry.ts、src/utils/equipment-training-entry.ts、src/utils/equipment-catalog-group.ts、src/utils/resource-center-business.ts、src/utils/training-plan-module.ts、src/database/physical-equipment-parser.ts、src/database/physical-equipment-data.ts、src/database/init.ts、src/stores/auth.ts、src/assets/images/physical-equipment/images.ts + 4 vue（EquipmentMenu/QuickEntry/GameModuleMenu/GameLobby/AssessmentSelect/TeachingMaterials）
- 数据源：G:\SCGP_Rec\认知\（CSV + images/，images 已转 webp）
