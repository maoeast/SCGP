# Current State
- 任务：认知发展（cognitive）模块 P0 游戏包——K03_PATTERN_NEXT 已交付（UAT 通过、已 push），下一步复制其接入节奏推 P0 其余 K01/K04/K05。
- 已完成：K03 全链路落地（registry + PatternNextGame + PatternNextPage + router + cognitive-games-api + GameContainer moduleCode dispatch）+ size 调优（`[0.5,0.85,1.15]`）；type-check / build:web / 实机 UAT 全通过；commit `424724c`（feat）+ `fbeab72`（docs）已 push origin/main。
- 产出物：`PatternNextGame.vue` / `cognitive-games-api.ts` / `PatternNextPage.vue`（新）+ `custom-game-registry.ts` / `router/index.ts` / `GameContainer.vue`（改）；PRD §7 第 0 条决策固化（IEP 级可追踪）。

# Next Action
- 对 P0 第二个游戏 `K01_MEMORY_MATCH`（记忆翻牌，PRD §4）进 Plan 模式，出组件技术方案：翻卡配对玩法 + SVG 卡片 + `actual_params`；接入层（cognitive-games-api + GameContainer dispatch + registry/Page/router 模式）直接复用 K03 已铺好的底座，无需再改容器/API。

# Blockers
- None。

# Key Decisions
- 库表方案 B：cognitive 走 training_session，`source_table='cognitive_game_inline'` 合成源（不建表），actual_params 显式透传 summary_payload；GameContainer 按 moduleCode dispatch 防 emotional 串台——K01/K04/K05 直接复用，不再改容器/API。
- PRD §7 第 0 条定位 = B（IEP 级可追踪）：每个认知游戏 performanceData 必须含 actual_params（本局实际参数）。
- P0 顺序建议：K01（记忆翻牌，纯 SVG）→ K04（哪个不同类，可复用认知器材 webp）→ K05（数感小铺，双模式）。

# Relevant Files
- `src/components/emotional/games/PatternNextGame.vue`（K03 模板，K01 抄骨架换玩法）
- `src/database/cognitive-games-api.ts`（已就绪，K01 直接复用不改）
- `src/views/emotional/games/PatternNextPage.vue` + `src/router/index.ts`（Page/路由模式复用）
- `docs/planning/2026-07-19-cognitive-games-prd.md`（§4 K01/K04/K05 玩法详情）
