# Current State
- 任务：修复 `npm run electron:dev` / `npm run build:electron` 的路由编译故障，并重新产出 Windows 安装包
- 已完成：
  - 修复 `src/router/index.ts` 中的路由守卫与激活跳转逻辑，恢复开发路由限制，清除导致打包失败的语法损坏
  - 清理 `src/database/resource-data.ts` 中 `initResourceData()` 的死代码，去掉构建阶段的 ASI 警告来源
  - 重新执行 `npm run build:electron`，已生成新的安装包和 `win-unpacked` 目录
- 产出物：
  - `src/router/index.ts`
  - `src/database/resource-data.ts`
  - `release/SCGP 星愿能力发展训练系统_1.0.0.exe`
  - `release/win-unpacked/`

# Next Action
- 运行 `release/SCGP 星愿能力发展训练系统_1.0.0.exe`，做一次安装后启动冒烟验证

# Blockers
- None

# Key Decisions
- 开发测试路由继续保留，但仅允许 `import.meta.env.DEV` 下访问：避免生产包误入调试页
- `initResourceData()` 暂不恢复旧资源种子初始化：当前目标是稳定打包，不扩展运行时行为
- 安装包产物保留在 `release/` 本地，不纳入本次源码提交

# Relevant Files
- `.continue-here.md`
- `src/router/index.ts`
- `src/database/resource-data.ts`
- `release/SCGP 星愿能力发展训练系统_1.0.0.exe`
