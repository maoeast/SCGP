# Current State
- 任务：报告导出做实（页眉/头像/HTML 导出/默认格式）+ 报告页性别修复——已提交推送并随 1.1.4 第四次同版本覆盖发布上包
- 已完成：统一分发层 `report-export.ts`（读设置三键 → Word/HTML 路由）+ `export-html.ts`（内联样式单文件 HTML）+ `export-word.ts`（docx 页眉 + 浮动头像，失败静默降级）；`SystemSettings.vue` 三项设置变更即写库 + `saveNow` 刷盘（修 `saveToStorage` 空操作）；19 个 Report.vue 换 `exportReport`；性别修复（CSIRS 硬编码「未知」→ 真实性别、ABC/ATEC/视知觉/SDQ/儿心Ⅱ 补性别栏、Conners 英文归一中文）
- 验证：`npm run verify:core` 全绿（type-check 0 error、node 155/155、jiti 11 项）；`release:verify` 8/8；IP 源复核 latest.yml 的 sha512/size 与本地逐字节一致
- 产出物：提交 `8744922`（报告链）/ `1af5abd`（看板与手册链）/ `ecf1141`（发版整理）/ `39cee49`（发版记录），HEAD = origin/main；`release/scgp-setup-1.1.4.exe` 266,005,883 B（sha512 base64 `/APtbgMw4rs7DZMoE2+0+H4ybfHSJ1o2J5pFKMFmc+iAj0034ITiQX/ic7fE7gRVtaKUmiQ7ZZh3j3IC4LUytA==`）

# Next Action
- HTTPS 更新源修复（用户拍板暂缓，恢复时执行）：服务商控制台续发免费 DV 证书 → 替换 `/etc/nginx/ssl/maohedong.top/www.maohedong.top.pem|.key` → `sudo nginx -t && sudo systemctl reload nginx` → 服务器侧 `curl -s https://maohedong.top/scgp/win/latest.yml`

# Blockers
- HTTPS 主源证书过期（2026-09-20 23:59:59 GMT）：走 https 源的客户端更新 TLS 失败；IP 源 `http://124.220.104.199/scgp/win` 正常

# Dead Ends
- Let's Encrypt HTTP-01 签发失败：海外验证节点把 maohedong.top 解析到 43.174.225.201（DNSPod 拦截页）而非本机，国内解析正常 → DNS 海外/默认线路分流，LE 无法验证
- 服务器无 DNS API 凭据、未装 certbot DNS 插件 → DNS-01 自助签发不可行

# Key Decisions
- 发版拍板：保持 1.1.4 同版本覆盖（已装 1.1.4 的机器收不到自动更新，仅新装/手动下载生效）；证书问题暂缓，发布验证以 IP 源为准
- 报告导出：19 页统一走 `exportReport`（按报告设置分发 Word/HTML），AI 链固定 `exportWordDocument` 不受默认格式影响
- 遗留（不影响验收）：equipment 器材 IEP 导出仍固定 Word 仅获页眉；`docxExporter` 四个无调用方函数待专项清理

# Relevant Files
- `electron/handlers/update.js`（`:19` DEFAULT_UPDATE_URL / `:18` LEGACY_HTTP_IP_UPDATE_URL）
- 服务端：`/etc/nginx/sites-enabled/maohedong.top`、`/etc/nginx/ssl/maohedong.top/`（证书落位点；配置备份 `/root/maohedong.top.bak.20260921`）
- `src/utils/report-export.ts` / `export-html.ts` / `export-word.ts`、`src/views/system/SystemSettings.vue`
- `scripts/release-verify.mjs`、`.thincoder/skills/release-deploy.md`
