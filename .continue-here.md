# Current State
- 任务：评估中心入口页 UI 改版已完成第一轮实现，并按最新反馈移除了快捷筛选 chips，待做 Electron 实机确认。
- 已完成：`src/views/assessment/AssessmentSelect.vue` 改为顶部 `3x2` 分类入口块 + 搜索框 + 双列引导型量表卡片；删除 `全部 / 15 分钟内 / 3-6 岁 / 高频使用` chips；按钮统一主色、分类仅做轻量背景/边框强调；空状态区分“无匹配搜索结果”和“分类下暂无可用量表”；`npm run type-check` 已通过。
- 产出物：`src/views/assessment/AssessmentSelect.vue`

# Next Action
- 启动 Electron 开发环境并打开 `/assessment`，核对新入口页的分类块、搜索框和双列卡片在真实运行时没有溢出、错位或交互异常。

# Blockers
- None

# Key Decisions
- 入口结构固定为 `B2 + T1`：顶部 `3x2` 分类入口块 + 双列引导型量表卡片；原因：解决 6 个分类横向拥挤和 2 卡分类空洞感。
- 只保留搜索框，不再提供快捷筛选 chips；原因：用户明确要求删除。
- 按钮统一主色，分类只做轻量强调；原因：去掉无逻辑的多色按钮。

# Relevant Files
- `src/views/assessment/AssessmentSelect.vue`
- `src/features/assessment/assessment-scale-catalog.ts`
