# Current State
- 任务：开始自理训练模块阶段 1，先把 `task_training` 接入 SCGP 可见训练资源主链。
- 已完成：在 `src/utils/resource-center-business.ts` 中将 `task_training` 识别为可见训练资源展示类型（按现有 `game` 主链显示）；新增 `scripts/tests/resource-center-business.test.mjs`，验证 `task_training` 可见且归入 `life-skills`。
- 产出物：`src/utils/resource-center-business.ts`、`scripts/tests/resource-center-business.test.mjs`

# Next Action（必须原子级——第一步可执行的单一动作）
- 修改 `src/views/resource-center/TrainingResources.vue`，把 `task_training` 加入资源类型列表、类型图标/样式和创建入口显示。

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- `自理训练` 入口继续挂 `life_skills` 授权：SCGP 当前授权只认顶层业务模块代码。
- `task_training` 先复用现有训练资源展示主链的 `game` 显示类型：阶段 1 目标是先打通“可见/可选/可启动”，不额外新开显示主类。
- 计划与记录继续接入 SCGP 现有主链：统一落到 `sys_training_plan / training_records / training_session`，不回退到旧 `train_plan / train_log`。

# Relevant Files（最小集——仅列下一步会用到的）
- `src/views/resource-center/TrainingResources.vue`
- `src/utils/resource-center-business.ts`
- `docs/planning/2026-05-08-self-care-training-module-implementation-plan.md`
