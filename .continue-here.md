# Current State
- 当前任务：SCGP“跨训练入口自定义小游戏扩展”`Phase 0` 实施，已完成 `Step 1-4`，下一步进入 `Step 5` 权限 preflight。
- 已完成到哪一步（具体到函数 / 文件 / 行为）：
- `src/data/custom-game-registry.ts` 已成为当前 5 个情绪小游戏的统一真源；`src/data/emotional-game-catalog.ts`、`src/views/games/emotional-game-catalog.ts` 已退化为 adapter。
- `src/database/schema/emotional-schema.sql`、`src/database/init.ts` 已完成 Phase 0 正式迁移：`game_emotion_records` 新增 `session_group_id / exit_trigger / session_participants`，新增 `game_session_participants`，并替换旧 `G08` 专用 rebuild。
- `src/database/emotional-games-api.ts` 已补 `persistSessionGroup()` 单事务组写入；`src/database/training-session-writer.ts` 已把共享场次摘要写入 `summary_payload.sharedSession`。
- `src/views/emotional/games/GameContainer.vue` 已改为消费 `launchContext`，支持 `difficultyLocked`、`teacher_exit`、`completeGroupGame()`、`abortGroupGame()`；5 个情绪 `*Page.vue` 已统一通过 `src/utils/custom-game-launch.ts` 组装 `launchContext`。
- 当前产出物（代码 / 文件）：`src/data/custom-game-registry.ts`、`src/utils/custom-game-launch.ts`、上述 schema/API/container 相关修改。

# Next Action
- 下一步第一个动作（必须原子级，例如：修改 xx 文件的 xx 函数）：修改 `src/views/emotional/games/GameContainer.vue`，加入权限 preflight 状态机，在挂载子游戏前按 registry 的 `requiredPermissions` 做统一 gate。

# Blocking / Risks
- 阻塞 / 风险：`EnergyBallGame.vue` 和 `VoiceVolumeForestGame.vue` 仍在组件内部自行调用 `getUserMedia()`；Step 5 需要先让容器接管首轮权限，再移除子组件自申请。

# Key Decisions
- 决策 + 原因（只保留影响后续实现的）：
- 保持当前真实链路仍为 `emotional-only`；原因：Phase 0 先收口底座，不把跨入口目标态写成现状。
- `game_code` 约束改为通用模式而非 registry 枚举；原因：后续增加非情绪小游戏时不再重复做 schema rebuild。
- 双人游戏按共享场次处理并走单事务组写入；原因：避免半成功记录与两名学生各自独立落库。

# Relevant Files
- `src/views/emotional/games/GameContainer.vue`
- `src/components/emotional/games/EnergyBallGame.vue`
- `src/components/emotional/games/VoiceVolumeForestGame.vue`
- `electron/preload.mjs`
- `electron/preload.cjs`
- `electron/main.mjs`
- `src/types/electron.d.ts`
- `src/data/custom-game-registry.ts`
