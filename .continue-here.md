# 当前任务：评估质量追踪与提醒

## 任务目标
为17个评估量表添加答题时间追踪和质量控制功能，防止随意快速作答影响评估质量。

## 背景
- 当前系统已有基础时间追踪（`startTime`、`endTime`、`responseTime`）
- 但没有质量检测和提醒机制
- 测试中可以快速胡乱答题，实际应用中需要质量控制

## 已确定的实施方案（轻量级+后门管理）

### Phase 1：基础数据记录（本次实施）

**1. 数据库层面**
各评估表增加字段（17个量表都要加）：
```sql
total_duration INTEGER,      -- 总用时（秒）
avg_response_time REAL,      -- 平均每题用时（秒）
quality_note TEXT            -- 质量备注（'very_fast' / 'fast' / null）
```

涉及表：
- abc_assess
- atec_assess
- brief_assess
- cbcl_assess
- cnbsr2016_assess
- cognitive_self_assess
- conners_psq_assess
- conners_trs_assess
- crt_assess
- csirs_assess
- fine_motor_assess
- gmfm_88_assess
- sdq_assess
- sm_assess
- srs2_assess
- tgmd_3_assess
- weefim_assess

**2. 计算逻辑**
在 `AssessmentContainer.vue` 的 `completeAssessment()` 中：
```typescript
const totalDuration = (Date.now() - state.value.startTime) / 1000  // 秒
const avgResponseTime = totalDuration / Object.keys(state.value.answers).length

let qualityNote = null
if (avgResponseTime < 3) {
  qualityNote = 'very_fast'  // 极端快速
} else if (avgResponseTime < 5) {
  qualityNote = 'fast'        // 偏快
}
```

将质量指标传递给各 Driver 的 `persistAssessment` 方法。

**3. Driver 层面**
- 修改 `PersistContext` 类型，增加可选的质量指标字段
- 17个 Driver 的 `persistAssessment` 方法都要保存质量数据到对应表
- 参考实现：查看 CSIRSDriver.ts:263-325 的 persistAssessment 结构

**4. 报告展示**
在各量表报告组件（如 `abc/Report.vue`、`atec/Report.vue` 等）底部增加：
```html
<el-card class="assessment-info">
  <template #header>
    <h3>📋 评估信息</h3>
  </template>
  <div class="info-items">
    <div class="info-item">
      <span class="label">评估用时：</span>
      <span class="value">{{ formatDuration(assessData.total_duration) }}</span>
    </div>
    <div class="info-item">
      <span class="label">平均每题：</span>
      <span class="value">{{ formatAvgTime(assessData.avg_response_time) }}</span>
    </div>
  </div>
</el-card>
```

格式化函数：
```typescript
function formatDuration(seconds: number): string {
  const mins = Math.floor(seconds / 60)
  const secs = Math.floor(seconds % 60)
  return `${mins}分${secs}秒`
}

function formatAvgTime(seconds: number): string {
  return `${seconds.toFixed(1)}秒`
}
```

**5. 完成提示**
在 `CompleteDialog.vue` 中，仅当 `avgResponseTime < 2` 时显示温和提示：
```html
<el-alert
  v-if="isVeryFastAssessment"
  type="info"
  :closable="false"
  show-icon
>
  提示：本次评估用时较短，如有需要可重新评估以确保准确性。
</el-alert>
```

### Phase 2 & 3（暂不实施）
- 管理后台质量统计看板
- 随机作答模式检测

## 技术要点

1. **数据库迁移**：
   - 使用 SQL ALTER TABLE 为17个表批量增加字段
   - 已有记录的字段值为 NULL（不影响现有数据）

2. **类型定义**：
   - 修改 `@/types/assessment.ts` 中的 `PersistContext` 接口
   - 增加可选字段避免破坏现有代码

3. **向后兼容**：
   - 质量字段都是可选的，不影响已有评估记录
   - 报告组件要处理 `null` 值（旧记录没有时间数据）

4. **验证要点**：
   - 快速答题（<3秒/题）能正确标记 'very_fast'
   - 正常答题（>5秒/题）quality_note 为 null
   - 报告中时间显示正确
   - 旧评估记录不显示时间信息（字段为 NULL）

## 当前代码状态

**已完成**（本会话）：
- ✅ ABC 和 ATEC 评估保存修复（db.exec → db.run）
- ✅ ABC 报告页面语法修复
- ✅ ABC 和 ATEC 报告中心记录创建
- ✅ 报告中心授权过滤（filteredReportList）
- ✅ 9个量表的欢迎内容更新（明确筛查 vs 追踪定位）

**待实施**：
- 评估质量追踪功能（本任务）

## 关键文件

- `src/types/assessment.ts` - 类型定义
- `src/views/assessment/AssessmentContainer.vue` - 评估容器（计算质量指标）
- `src/views/assessment/components/CompleteDialog.vue` - 完成对话框
- `src/strategies/assessment/*Driver.ts` - 17个 Driver（persistAssessment）
- `src/views/assessment/*/Report.vue` - 各量表报告组件
- `src/database/schema.sql` - 数据库 schema

## Git 状态
```
工作区：clean（已完成上述修复）
当前分支：main
最近提交：feat(assessment): 新增 ABC 和 ATEC 孤独症评估量表
```

## 用户要求总结
- 宽松质控（不打扰用户）
- 只记录数据，极端情况温和提示
- 避免"监控感"
- 后台数据供管理用（Phase 2/3 待定）
