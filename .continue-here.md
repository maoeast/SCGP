# Current State
- 任务：`Wave 3` 的 `F04_TRACK_BUILD / 轨道修补匠` 已完成代码接入与真实 UI 验收；`S05_ECHO_PARROT / 动物传声筒` 已通过 `gsd-quick` 完成 registry 起步。
- 已完成：
  - `src/data/custom-game-registry.ts` 已新增 `F04_TRACK_BUILD` 并挂到 `fine-motor`。
  - `src/views/emotional/games/TrackBuildPage.vue` + `src/components/emotional/games/TrackBuildGame.vue` 已实现最小玩法闭环：选轨道件、旋转、点缺口放置、自动完成。
  - `src/router/index.ts` 已接入 `/emotional/games/track-build`。
  - `src/database/emotional-games-api.ts` 与 `src/views/emotional/GameRecordDetail.vue` 已补齐 `F04_TRACK_BUILD` 的摘要和详情指标。
  - `F04_TRACK_BUILD` 已完成真实 UI 验收：启动、旋转、放置、完成落库、详情页指标链路可用。
  - `src/data/custom-game-registry.ts` 已新增 `S05_ECHO_PARROT` 并挂到 `social-communication`，当前定义为单人、麦克风必需、`permissionPolicy: 'all_required'`。
- 产出物：
  - `src/data/custom-game-registry.ts`
  - `src/views/emotional/games/TrackBuildPage.vue`
  - `src/components/emotional/games/TrackBuildGame.vue`
  - `src/router/index.ts`
  - `src/database/emotional-games-api.ts`
  - `src/views/emotional/GameRecordDetail.vue`
  - `.planning/quick/260417-iot-implement-f04-track-build-registry-defin/`
  - `.planning/quick/260417-iwc-implement-f04-track-build-minimal-runtim/`
  - `.planning/quick/260417-jxz-implement-s05-echo-parrot-registry-defin/`

# Next Action
- 继续执行 `gsd-quick` 推进 `S05_ECHO_PARROT / 动物传声筒` 的最小运行链路：页面壳、玩法组件、静态路由，以及训练记录摘要/详情接入。

# Blockers
- None

# Key Decisions
- 下一款新游戏 `S05_ECHO_PARROT` 也必须走 `gsd-quick`：保持 quick artifact / STATE 记录一致。
- `S05_ECHO_PARROT` 先做 registry 起步，不直接跳进语音识别实现：先把麦克风权限和最小运行链路拆开落地。
- 当前 `S05_ECHO_PARROT` 仍是“已注册、运行时未接”的过渡态；不要把它写成已完成可玩。
- `S03_STORY_SEQ` 已知 bug 继续不返修：不回头打断 `Wave 3` 主线。

# Relevant Files
- `src/data/custom-game-registry.ts`
- `docs/planning/2026-04-13-cross-entry-custom-games-expansion-plan.md`
- `src/views/emotional/games/GameContainer.vue`
- `src/views/emotional/games/VoiceVolumeForestPage.vue`
