# Current State
- 任务：AI 聊天智能体子系统（DeepSeek 接入）— 功能完整并真机验证通过，已提交本地
- 已完成：4 Phase（数据层 / Main 代理 / 配置 UI / 流式聊天）+ 会话 `user_id` 隔离 + 历史会话恢复 + admin「全部会话」管理视图 + `setConfig`/`saveAgent` 原子 upsert 修复（UNIQUE 冲突）。真机验证全绿：保存配置 / 测试连接 / 流式问答 / 重启历史恢复 / 多教师隔离 / admin 视图 / 备份迁移 Key。DeepSeek 默认模型 `deepseek-v4-flash`、端点 `/chat/completions`、默认非思考。**已提交本地 main（领先 origin/main 7 提交，未推送）**。

# Next Action
- 待用户决定：① 推送 origin/main（此前指示先不推送）；② 转 A4 资源文件生命周期真机验证（另一条 parked 线，代码已完成待真机）；③ 其他新需求

# Blockers
- 无代码阻塞
- 本地 main 领先 origin/main 7 提交未推送（等用户指示是否推送）

# Key Decisions
- 会话按 `ai_chat_session.user_id` 隔离（每人只看自己），智能体 `ai_agent` 全局共享（管理员维护）；admin 有「全部会话」管理视图；全局预算 + 全局 Key（学校统一采购）
- 旧 NULL 会话迁移归默认管理员(id=1)；`createSession`/`listSessions` 按 user 过滤，`listAllSessions` 不过滤（LEFT JOIN user 带用户名/角色）
- `setConfig`/`saveAgent` 改 `ON CONFLICT … DO UPDATE` 原子 upsert，修 sql.js read-then-write 撞 UNIQUE 的隐患（setConfig 已真机验证）
- Main 进程代理 DeepSeek（绕 CORS），渲染进程只传 API Key【密文】；Key 用 crypto.ts `encryptData` 加密存 system_config，随 `database.sqlite` 备份跨机迁移

# Relevant Files
- `src/database/ai-api.ts`（AIApi：估费 / 三表 CRUD / provider 配置 / 会话隔离）
- `src/database/init.ts`（`initializeAITables`：三表 + `user_id` 列幂等迁移）
- `src/stores/ai.ts`（aiStore：配置 / 测试 / 会话 / 流式 / 隔离 / admin 视图）
- `src/features/ai/components/AiAssistant.vue`（全局悬浮聊天面板 + 历史会话列表）
- `src/views/system/AiAgentConfig.vue`（管理员配置 + 全部会话视图）
- `electron/handlers/ai.mjs`（Main 进程 DeepSeek 代理，流式 SSE）
