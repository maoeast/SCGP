# Current State
- 当前任务：SCGP“跨训练入口自定义小游戏扩展”`Phase 0` 已完成收口，当前处于验证结论归档后的待续阶段。
- 已完成到哪一步（具体到函数 / 文件 / 行为）：
- `src/views/emotional/games/GameContainer.vue` 已接入按 registry `requiredPermissions` 运行的权限 preflight 状态机；未通过前不挂载子游戏，并统一提供 retry / system settings / return gate。
- `src/components/emotional/games/EnergyBallGame.vue`、`src/components/emotional/games/VoiceVolumeForestGame.vue` 已移除首轮自申请，改为消费容器下发的 `cameraStream` / `microphoneStream`；对应 `*Page.vue` 已完成透传。
- `electron/main.mjs`、`electron/preload.mjs`、`electron/preload.cjs`、`src/types/electron.d.ts` 已补媒体权限状态查询与打开系统设置 IPC。
- `electron/main.mjs` 已补主进程日志层 `EPIPE` 防护，完成进度后不再因 broken pipe 弹出主进程异常窗口（commit `8f47ed1`）。
- `src/database/init.ts` 已改为先 `ensureCustomGamePhase0Schema()` 再跑 emotional schema；`src/database/emotional-games-api.ts` 写入前会再次自愈旧库，已消除 `game_emotion_records.session_group_id` 缺列报错。
- `src/views/games/GameLobby.vue`、`src/views/plan/PlanList.vue`、`src/views/resource-center/TeachingMaterials.vue`、`src/views/assessment/cbcl/SocialForm.vue` 及 3 个 archived assessment 页面的 Element Plus radio `label-as-value` 弃用写法已清零。
- `G03_FOREST`（麦克风）与 `G08_ENERGY_BALL`（摄像头）的 `blocked_system` 路径均已在 Electron 手动验证通过；系统拒绝、打开系统设置、重新检测恢复、返回训练列表、跨入口启动与返回链路均正常。
- 当前产出物（代码 / 文件）：`src/views/emotional/games/GameContainer.vue`、`src/components/emotional/games/EnergyBallGame.vue`、`src/components/emotional/games/VoiceVolumeForestGame.vue`、`electron/main.mjs`、`electron/preload.mjs`、`electron/preload.cjs`、`src/database/init.ts`、`src/database/emotional-games-api.ts`。

# Next Action
- 下一步第一个动作（必须原子级，例如：修改 xx 文件的 xx 函数）：先确认是否进入 `Wave 1`；如果继续，则从现有 `Phase 0` 验证结论出发，选择首个新增游戏/入口的实现目标，而不是回头重做 `Phase 0` 规划。

# Blocking / Risks
- 阻塞 / 风险：`type-check:emotional` 仍被仓库既有 TS7016 阻塞（`src/config/CNBSR2016FeedbackConfig.js`、`crypto-js` 类型缺失），与本轮 `Phase 0` 实现无关，但会影响统一类型检查信号。

# Key Decisions
- 保持当前真实链路仍为 `emotional-only`；原因：Phase 0 先收口底座，不把跨入口目标态写成现状。
- 权限首轮申请统一上收至容器；原因：必须让 registry `requiredPermissions` 成为单一 gate，并区分 retryable 与 system-level block。
- 旧数据库在 init 和 persist 两处都自愈 Phase 0 schema；原因：避免已有本地库因缺列直接打断 Step 5 验证。
- `Phase 0` 在手动验证通过后先收口归档，不直接启动 `Wave 1`；原因：先固定已验证底座，再决定后续扩展范围。

# Relevant Files
- `src/views/emotional/games/GameContainer.vue`
- `src/views/emotional/games/VoiceVolumeForestPage.vue`
- `src/components/emotional/games/VoiceVolumeForestGame.vue`
- `electron/main.mjs`
- `electron/preload.mjs`
