# Current State
- 任务：AI 学生级长期记忆 M1（数据层 + 消息状态机）已完成，待提交
- 已完成（工作区未提交）：
  - init.ts：三张新表（ai_student_memory / ai_memory_summary_batch / ai_student_memory_audit）
    含 CHECK/FK/单飞唯一索引；消息表加 delivery_status/completed_at/message_kind；
    会话表加 student_id/memory_watermark；迁移防回灌（历史水位=最大消息 id）
  - ai-api.ts：状态机三 API（createAssistantMessage/updateAssistantChunk/finalizeAssistantMessage）
    + 记忆 CRUD（list/add/confirm/markPriority/delete + 审计）+ 批次两段式
    （createSummaryBatch/markSummarizing/commitSummaryBatch-CAS/failSummaryBatch）
    + bindSessionStudent（库级锁定）+ saveMessage 默认 completed/final（兼容旧调用）
  - stores/ai.ts：3 处 push 补 deliveryStatus/messageKind（user 空、assistant completed/final）
  - 契约测试：新增 ai-memory-data-contract.test.mjs（8 场景）；
    同步 ai-tool-artifact-trend.test.mjs 的 saveMessage 断言（INSERT 新列）
  - 验证：type-check / AI 契约 67 条（剩 1 条既有失败：AiAssistantFloatingButton v-if，
    已在 HEAD 验证与本次无关）/ build:web 全过
- 产出物：4 文件改动（init.ts/ai-api.ts/stores/ai.ts + 1 新测试）

# Next Action
- 用户确认后提交（建议：feat(ai): 学生级长期记忆 M1 数据层与消息状态机（v4.1 设计落地））

# Blockers
- None（M2 总结链路待启动：两段式事务调用方 + 补偿任务 + 脱敏 + 去重）

# Key Decisions
- 迁移防回灌：历史会话水位初始化为现有最大消息 id，默认不补总结（v4.1 §13）
- saveMessage 默认 assistant→completed/final：既有调用零改动兼容
- 绑定锁定走库级 NOT EXISTS 条件 UPDATE（无应用层竞态）
- 批次单飞靠部分唯一索引 WHERE state IN ('pending','summarizing')

# Relevant Files
- src/database/init.ts（三表 + 扩展列 + 防回灌）
- src/database/ai-api.ts（状态机三 API + 记忆 CRUD + 批次两段式 + 绑定）
- src/stores/ai.ts（两条发送路径状态字段）
- scripts/tests/ai-memory-data-contract.test.mjs（新增 M1 契约）
- scripts/tests/ai-tool-artifact-trend.test.mjs（saveMessage 断言同步）
- docs/planning/2026-08-05-ai-student-memory-design.md（v4.1 设计，§6 为 M2 参考）
