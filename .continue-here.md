# Current State
- 任务：DB Worker 处理方式已落地为执行计划（`docs/plans/2026-08-05-db-worker-execution-plan.md`），三个相关 commit 已提交：
  - `b235d1f` 登录背景 Three.js 星空清理（顺带项）
  - `3976298` dev 下 DB Worker 加载失败修复（sql.js 纳入依赖预构建，移除 optimizeDeps.exclude）
  - `b705405` 防抖保存自激循环修复（performAtomicSave 开始接管 isDirty）
- 成果：dev 下 Worker 全链路可用（模块加载 → SQL.js 初始化 ~15ms → schema 建表 → 防抖保存），`Worker 未就绪` 警告消失，WorkerTest 页可跑；主链仍零消费（`getWorkerBridge` 无调用方），生产仍不启动 Worker。
- 工作区干净；`main` 领先 origin 26 个提交（未推送，等用户指示）。

# Next Action
- 按 `docs/plans/2026-08-05-db-worker-execution-plan.md` 执行 **阶段 0：性能 profile（决策门）**——用 devtools BenchmarkRunner + 大数据量临时测量主线程 SQLWrapper 基线，结论写入 `docs/reports/2026-08-05-db-worker-profile.md`，据此决定是否进阶段 1（渐进接入）。

# Blockers
- 无（阶段 0 不依赖任何外部条件）。

# Key Decisions
- 不盲目接主链：无性能痛点数据不投入 2-3 周接入工程；接入前必须先解决双轨 DB 副本持久化冲突（计划 §4.1）。
- 回退开关 `VITE_USE_DB_WORKER` 保留；主线程 SQLWrapper Plan B 始终可用。

# Relevant Files
- `docs/plans/2026-08-05-db-worker-execution-plan.md`（执行计划，新会话必读）
- `src/workers/{db.worker,db-bridge,command-queue,bridge-sql-wrapper}.ts`、`src/database/{init,worker-init,sql-wrapper,sqljs-loader}.ts`
- 测试页 `src/views/devtools/WorkerTest.vue`；性能套件 `tests/performance/benchmarks` + `src/views/devtools/BenchmarkRunner.vue`
