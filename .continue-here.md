---
status: active
last_updated: 2026-04-05
scope: login theme fixes — card opacity for warm-glow/calm-blue, pointer-events on starfield canvas
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**登录页主题系统 bug 修复**

- **本次已完成**:
  - 修复 `Login.vue` 未调用 `applyLoginThemeVariables`，导致 `--login-card-bg-opacity` 等 CSS 变量未写入 DOM 的问题
  - 修复 `StarfieldTunnel.vue` 的 canvas 和容器缺少 `pointer-events: none`，导致从首页退出回到登录页时鼠标无法点击输入框的问题
  - 修复暖光/静蓝主题调卡片透明度视觉无效的问题：根因是表单面板 `login-layout__form` 背景自身几乎不透明（alpha 0.92–0.98），卡片即使变透明也看不到星空背景。修改 `applyLoginThemeVariables` 让表单面板也使用卡片透明度值

- **当前代码现实**:
  - `Login.vue` 新增 watch 监听 theme/opacity/bgImage 变化并调用 `applyLoginThemeVariables`
  - `StarfieldTunnel.vue` 容器和 canvas 均加了 `pointer-events: none`，mousemove 监听加 `{ passive: true }`
  - `login-theme.ts` 的 `applyLoginThemeVariables` 对暖光/静蓝主题动态生成 `--login-form-pane-bg`，alpha 使用卡片透明度值；自定义主题不受影响

## 下一步

**第一个动作**: Electron 运行时手动验证三项修复：
1. 三个主题切换时卡片透明度滑块的视觉效果
2. 从首页退出回到登录页后输入框能否立即点击
3. 自定义主题背景图上传/删除、按钮颜色选择

## 遇到的问题 / 阻塞 / 风险

1. **当前无硬阻塞**
2. **当前风险**:
   - 背景图以 data URL 存储在 system_config，大图片可能导致 DB 行过大（当前限制 4MB）
   - 表单面板从预设的复杂渐变（含 radial-gradient overlay）简化为单线性渐变，视觉差异需要在运行时确认是否可接受
