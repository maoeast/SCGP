# Current State
- 任务：C07「系统设置重置隐私告知入口」已实现并通过 `npm run type-check`（EXIT=0）。
- 已完成：
  - C07 门禁（commit `945424a`）：`sendChat` 首次发送前 `ElMessageBox.confirm` 门禁 + `AIApi.isPrivacyAcked/acknowledgePrivacy`。
  - C07 重置入口（本轮）：`AIApi.resetAllPrivacyAck`（`DELETE FROM system_config WHERE key LIKE 'ai:privacy_ack:user:%'`，返回清除条数）；ai store 导出；`AiAgentConfig.vue`「模型服务配置」card 加「重置全部教师的告知确认」form-item + `resetPrivacyAck` handler（二次确认）。
- 当前 C07 门禁 + 重置闭环已完成；字段脱敏与 tool result 最小化仍未做。

# Next Action
- C07 收尾：tool result 字段级最小化——裁剪 `ai-tool-loop` / `ai-tools` 工具查询返回给 provider 的学生敏感字段（诊断、完整 student 行等，见 PROJECT_CONTEXT §78 外发面盘点），先盘点各工具返回结构再定裁剪口径，不引入通用脱敏框架。

# Blockers
- None

# Key Decisions
- 重置为「全局」语义（清除所有教师 KV），放系统设置「AI 智能体 → 模型服务配置」card；带二次确认；返回清除条数提示。
- 门禁本身仍按「每个登录用户首次」触发；重置不影响触发逻辑，只清记忆态。
- 本批不做字段脱敏、tool result 最小化（留下一步）。

# Relevant Files
- `src/database/ai-api.ts`（`resetAllPrivacyAck`）
- `src/stores/ai.ts`（`resetAllPrivacyAck` store 方法 + 导出）
- `src/views/system/AiAgentConfig.vue`（`resetPrivacyAck` handler + form-item）
- 下一步相关：`src/services/ai-tool-loop.ts`、`src/services/ai-tools.ts`
