# Current State
- 任务：调研 bug —— 老师账号的班级/学生数据隔离缺失（每个老师登录后能看到所有班级、学生、训练记录等数据）
- 已完成：演示数据两批修复 + 客户分发包（均已提交推送 2e5b137/98ccde6/9b52fb8/23bb08a，main 与 origin 同步）；过程产物已清理（zip 保留在 output/SCGP演示数据包-20260806.zip）
- 产出物：scripts/seed-demo-data.mjs（seed/export/import）、scripts/build-demo-dist.mjs、docs/guides/seed-demo-data.md

# Next Action
- 新会话第一步：分析数据隔离现状 —— 教师角色（teacher01~05）登录后各页面（学生管理/班级管理/评估/训练记录/器材/报告）的数据查询链路（StudentAPI/ClassAPI/各训练记录 API）是否按「当前教师 → 分配的班级 → 学生」过滤；已知线索：sys_class_teachers 表存 class_id+teacher_id 分配关系（seed 用 CLASS_TEACHER_MAP），需查前端路由守卫/侧边栏菜单（按角色）与后端查询是否忽略该表
- 交付物预期：根因分析 + 修复方案（数据源加 teacher 维度过滤 or 前端按教师班级过滤），先出方案再改码

# Blockers
- None

# Key Decisions
- （待调研后补充：教师隔离的实现层选择——查询层过滤 vs 菜单/路由层过滤）

# Relevant Files
- src/database/api.ts（StudentAPI/ClassAPI 查询）、src/database/class-api.ts（班级教师分配）
- src/stores/auth.ts（当前用户角色）、src/utils/student-display.ts
- src/views/admin/StudentManagement.vue、src/views/training-records/components/GameRecordsPanel.vue
- scripts/seed-demo-data.mjs（CLASS_TEACHER_MAP 分配关系样例）
