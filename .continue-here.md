# Current State
- 任务：C12 自动更新链 HTTPS 切换已完整闭环（代码 + 服务器部署 + 真机验证 + push），本次会话结束。
- 已完成：`update.js` 默认源切 `https://maohedong.top/scgp/win`，`LEGACY_UPDATE_URLS` 收纳 hzxckj + 旧 http IP 自动迁移；`package.json` publish 切 HTTPS + 版本 1.0.7，契约测试同步；1.0.7 打包部署到服务器 `/home/lighthouse/scgp/win/`（两源 `latest.yml` 均 `version: 1.0.7`），真机 1.0.6→1.0.7 升级通过；新增 `release-deploy` skill + `release:verify` 校验脚本（防 latest.yml 被 exe 内容覆盖）。
- 产出物：commit `d4b86fd`（feat update）+ `64f6604`（chore release skill）+ `27ebeeb`（docs handoff），均已 push `origin/main`。

# Next Action
- 选定并推进下一项：C13（发布终验）/ T03（系统设置-开发者调试加更新地址 UI 入口，需 https-only + dev-only 安全设计）/ AGENTS §3 优先技术债（备份恢复全 schema、资源文件生命周期、资源收藏、评估入口硬编码、命名残留、开发/迁移工具混入主路由）。先读对应代码确认现状再给计划。

# Blockers
- None。C12 外部阻塞均已解除（HTTPS 源用户自有 `maohedong.top`、签名决策不购买、验收机就位并真机通过）。

# Key Decisions
- 不购买 Windows 签名证书；SmartScreen 蓝屏为已知取舍，安装时点「更多信息 → 仍要运行」。
- 更新源 generic 自托管：HTTPS 主源 + HTTP 旧源 IP 给老用户当跳板，代码 `LEGACY_UPDATE_URLS` 自动迁移到 https。
- 服务器部署走 tailscale + `openclaw.pem`（ubuntu 用户免密 sudo）；开发机代理 fake-ip 污染外部探测，外部验证在服务器或干净设备做。

# Relevant Files
- `AGENTS.md`（§3 优先技术债 + §7 高风险主题，选定下一项的入口）
- `electron/handlers/update.js`（T03 扩展更新地址 UI 配置时的核心）
- `.claude/skills/release-deploy/SKILL.md`（发布流程参考）
