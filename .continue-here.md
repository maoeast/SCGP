# Current State
- 任务：A4 资源文件生命周期 — 真机验证（Phase 2+3 合并，用户侧）
- 已完成：A4 Phase 1+2+3 代码全部完成并提交。Phase 3（孤儿 GC）commit `feaef64`（resource-reconcile.ts 4 函数 + ResourceHealthCheck.vue 体检 UI 挂 System.vue 数据备份 tab）；Phase 2 commit `809257d`（备份 v3.0）；Phase 1 commits `30e4ad2`/`ab3b0d7`/`94836a9`。type-check 全绿。
- 产出物：commits 如上；plan `docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md`

# Next Action
- 真机验证（用户侧执行，Phase 2+3 合并）：
  - Phase 2：导出备份 → 清库 + 清空 `userData/resources` → 恢复 → 资源图片/教具文件全部复显；用 2.0 旧备份恢复不报错（仅提示）；备份 `.dat` 体积合理（zip 压缩生效，非散装 base64 膨胀）
  - Phase 3：系统管理 > 数据备份 tab > 资源文件体检 → 报告命中真实孤儿（可硬删资源后人为留文件造孤儿）→ 二次确认清理 → 复检磁盘干净且引用文件完好
- 验证通过后：回填 `docs/reports/2026-07-15-unimplemented-gap.md` A4 状态为 ✅；按需 `git push`

# Blockers
- 真机验证待做（Phase 2+3 合并，单人真机活，无法代跑）
- 本地 main 领先 origin/main 5 提交未推送（用户指示先不推送）

# Key Decisions
- 托管路径规则（全局约束）：`uploaded/`+`teaching-materials/` 可删/进备份；`docs/images/videos/audio/` 预置不删；唯一可写根 `userData/resources`
- Phase 3 dry-run 优先（UI 先展示孤儿报告，二次确认才 purge），不做启动自动清理
- `collectReferencedPaths` 全表扫描 `sys_training_resource`（含 `is_active=0` 软删行）—— 软删文件保留以便恢复，必须算「在用」，否则一软删就被 GC 误清
- 体检 UI 挂 System.vue「数据备份」tab（数据维护中心），抽独立组件 `ResourceHealthCheck.vue`
- `walk-dir` IPC（Phase 2 §4.2）供 Phase 3 `collectDiskPaths` 复用

# Relevant Files
- `src/utils/resource-reconcile.ts`（§5.1：collectReferencedPaths/collectDiskPaths/findOrphans/purgeOrphans）
- `src/views/system/ResourceHealthCheck.vue`（§5.2 体检 UI）
- `src/views/System.vue`（数据备份 tab 挂载点）
- `src/utils/resource-file-refs.ts`、`resource-file-service.ts`（Phase 1 复用 helper）
- `src/utils/backup.ts`、`crypto.ts`（Phase 2）
- `docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md` §5
