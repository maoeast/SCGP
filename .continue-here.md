# Current State
- 任务：AI 学生级长期记忆 M2（总结链路 + 注入）已完成，待提交
- 已完成（工作区未提交）：
  - 新增 src/services/ai-memory.ts 纯函数模块：desensitizeForSummary（姓名→[STUDENT]+敏感字段脱敏）、
    normalizeText/fingerprintOf（FNV-1a）、trigramSimilarity（3-gram Jaccard）、
    buildMemorySummaryPrompt（主体锚定 v1）、parseMemoryFacts（容错解析）
  - stores/ai.ts：
    - finalizeAssistantTurn：两段式（阶段A createSummaryBatch 单飞 + 脱敏输入 ≤4000 字符 →
      阶段B 专用总结接口非流式 → 阶段C CAS 提交 + pending 候选 + 指纹去重 + 3-gram possible_duplicate_of 提示 + 溯源）
    - runMemoryCompensation：扫描 user/completed 消息超水位会话（LIMIT 5 最旧优先）+ failed 批次重试（LIMIT 3）
    - buildMemoryInjection：confirmed+未过期 → priority 排序（关键项 ≤40 不占配额 + 常规 ≤20）→ 转义安全声明注入
    - sendChat 两路径触发 finalizeAssistantTurn（fire-and-forget）；systemPrompt = 守卫 + 记忆注入
    - memoryEnabled 默认关闭（学校级开关）+ setter + 记忆 CRUD 透出
  - 测试：tests/ai-memory.test.ts（5 场景纯函数）+ ai-memory-summary-contract.test.mjs（6 场景）
    + 同步守卫测试断言（baseSystemPrompt）
  - 验证：type-check / AI 契约 73 条（剩 1 条既有失败 AiAssistantFloatingButton 与本次无关）/ build:web 全过
- 产出物：4 文件改动 + 2 新测试

# Next Action
- 用户确认后提交（建议：feat(ai): 学生级长期记忆 M2 总结链路与记忆注入（v4.1 §6/§7 落地））

# Blockers
- None（M3 权限与注入待启动：实时权限过滤 + 确认来源 UI；M4 审核 UI：pending 确认流 + 开关配置入口）

# Key Decisions
- 总结走专用接口（非流式 + 独立提示词），不入对话 tool loop（v4.1 §6.3）
- 记忆注入置于守卫层之后（守卫仍最先，记忆是"参考数据"非指令）
- 开关默认关闭（管理员启用），补偿任务节流（5+3）
- 3-gram 只做 possible_duplicate_of 提示（≥0.8），指纹完全相同才自动去重

# Relevant Files
- src/services/ai-memory.ts（新增纯函数模块）
- src/stores/ai.ts（finalizeAssistantTurn / runMemoryCompensation / buildMemoryInjection / 导出）
- tests/ai-memory.test.ts（新增纯函数单测）
- scripts/tests/ai-memory-summary-contract.test.mjs（新增契约）
- scripts/tests/ai-guard-clause-contract.test.mjs（守卫断言同步）
- docs/planning/2026-08-05-ai-student-memory-design.md（v4.1，§9/§10 为 M3/M4 参考）
