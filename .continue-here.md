# Current State
- 待提交：首页看板「本周异常预警」口径重做（扫描切到统一训练主表 + 修提示依赖死规则 + 补信号 + 条目可点/查看全部；新增 training-anomaly-rules、两份守卫测试、一份专题档）——工作区未提交，等用户定是否提交
- 任务：T24 结构化健康字段（care_instructions 照护要求 与 health_notes 医学状态 职责分离）已提交（9fb0418，8 文件）；同工作区的首页看板「智能特教助理」面板专题（并行会话完成）随后提交（78d2952）
- 已完成（T24）：init.ts 建表+safeAddColumn 幂等迁移（存量不迁移，用户拍板）、api.ts 四处显式列 SQL、student store 类型、AddStudentDialog（医学状态 label+照护要求 textarea 全链）、StudentDetail 双警示条（红=医学状态 role=alert / 琥珀=照护要求 role=note，只填其一只显一段）、student-import 双别名兼容（健康备注→医学状态）+缺列按空、Students.vue 模板 11 列、tests T24 用例+修复改前已失败的 deepEqual 基线
- 验证：npm run verify:core 全绿（type-check 0 error、node 136/136、jiti 8/8 含 student-import 与 assessment-gap-analysis）；advisor 评审 pass（2 🟡 + 3 🔵 全修复）

# Next Action
- 无排期专项；可选下一步：发版打包把 T24 + 看板面板一起带出（注意 1.1.4 已装机器收不到自动更新，需升版本号）

# Blockers
- None

# Dead Ends
- edit 批量调用把目录当默认 path → EISDIR 拒绝；edits 数组每条 entry 必须显式写文件路径
- git 工具 push 传 `ref: main` 报 "'main' does not appear to be a git repository"；须传 `ref: origin main`（remote+branch 空格分隔）

# Key Decisions
- T24 拍板（2026-09-21）：①存量 health_notes 不迁移，原样当医学状态展示；②导入模板列名更新——「健康备注」→「医学状态」+新增「照护要求」，解析层双别名兼容旧文件、缺照护列按空
- T24 AI 透出口径本轮不动（get_student 显式白名单仍不含健康/照护字段，C07 隐私口径）；seed-demo-data 未填充照护要求（列可空不影响运行）
- 异常预警口径（真源 `docs/planning/2026-09-21-首页本周异常预警-判定口径.md`，2026-09-21）：扫描统一主表 `training_session`；四信号 = 低正确率（**仅答对率家族白名单**，器材/情绪游戏是得分率不得参与）/ 提示依赖（hint_count÷question_count > 0.5）/ 器材高辅助（promptLevel ≥ 4）/ 训练中断；已知取舍：器材高辅助在该群体是常态（7 天 76 条），阈值提为 5 或改相对基线待定
- T25 拍板方案 a：Tab 标签计数 = 表格可见行数（游戏+情绪游戏两路）；认知会话只进「最近训练/本月训练」活动条（2026-09-19 口径不动）
- 1.1.4 同版本覆盖发布（当日第三次）：已装 1.1.4 的机器收不到自动更新（electron-updater 同版本语义），修复以下次打包带出

# Relevant Files
- src/database/init.ts（L31 建表列 + L4933 safeAddColumn + T24 注释）
- src/database/api.ts（StudentAPI 四处 SQL）
- src/components/AddStudentDialog.vue（医学状态 label + 照护要求 textarea）
- src/views/StudentDetail.vue（L65-84 双警示条 + care CSS 变量组）
- src/utils/student-import.ts（列别名/校验全链）+ tests/student-import.test.ts
- 看板专题：src/services/assessment-gap-analysis.ts、src/database/dashboard-api.ts（getAssessmentInsights）、src/views/Dashboard.vue、scripts/tests/dashboard-assessment-coverage.test.mjs（详见 docs/CHANGELOG.md 2026-09-21 条目）
- 异常预警口径：src/database/training-anomaly-rules.ts（常量 + evaluateTrainingAnomaly）、tests/training-anomaly-rules.test.ts、scripts/tests/dashboard-anomaly-rules.test.mjs、docs/planning/2026-09-21-首页本周异常预警-判定口径.md
