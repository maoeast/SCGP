# Current State
- 任务：C12/C13 阻塞期间另起 T01 资源/备份覆盖证据闭环与 T02 生产调试信息清理两个独立技术债批次，均已代码级完成且未混入 closeout 主线。
- 已完成：T01 新增备份表覆盖契约、恢复提示契约、资源孤儿体检纯逻辑与测试；T02 新增 `sanitize:production` 产物清理/残留扫描，生产 `build:web` 自动执行，生产 Vite 不加载 Vue DevTools，Electron 生产态默认抑制运行时日志并屏蔽 DevTools 快捷键。
- 产出物：未提交工作区改动；`npm run verify:release` 通过（`verify:core` 78/78 + TS 契约通过，`build:web` sanitizer checked 276 files, sanitized 1）；`git diff --check` 干净。

# Next Action
- 复查当前 T01/T02 dirty diff 边界；如用户确认提交，用精确 `git add` 提交这些已验证文件，不要 `git add .`。

# Blockers
- C12 被外部输入阻塞：客户网络可达的正式 HTTPS 更新目录 URL + 写入凭据、Windows 代码签名证书（`CSC_LINK`/`CSC_KEY_PASSWORD`）、Windows 验收机或隔离虚拟机；三样齐备前 C12 不开工，C13 终验随之等待。
- 完整 electron-builder 安装包验证仍受 C12 签名/更新发布输入阻塞；T01/T02 本身无代码阻塞。

# Key Decisions
- 资源孤儿体检纯计算拆到 `src/utils/resource-reconcile-core.ts`，运行时 `resource-reconcile.ts` 继续负责 DB/IPC 组合并 re-export 类型，避免 Node/jiti 测试穿透 `.sql?raw`。
- 生产调试清理采用“构建后清理 + 残留即失败”，不靠人工注释；HTML/CSS/JS debug 标记块可被清理，未标记的 console/debugger/devtools 残留会让 sanitizer 失败。
- C12/C13 发布与桌面终验状态不因 T01/T02 改为完成；这些批次只证明代码契约覆盖和生产调试门禁已补强。

# Relevant Files
- `package.json`
- `vite.config.ts`
- `electron/main.mjs`
- `scripts/sanitize-production-build.mjs`
- `scripts/tests/production-debug-boundary.test.mjs`
- `scripts/tests/dev-route-production-boundary.test.mjs`
- `scripts/tests/backup-table-coverage.test.mjs`
- `scripts/tests/backup-restore-result.test.mjs`
- `scripts/tests/resource-reconcile.test.mjs`
- `src/utils/resource-reconcile.ts`
- `src/utils/resource-reconcile-core.ts`
