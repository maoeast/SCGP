---
status: active
last_updated: 今天，2026-04-11
scope: AssessmentContainer 重构 —— 已完成全部 12 个量表的持久化逻辑下放，代码精简约 47%，消除了该文件的所有 db.run 裸 SQL
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**本次会话（2026-04-11）已完成 `AssessmentContainer.vue` 的持久化层重构：**

- **重构进度**：12/12 满百迁移
  - Batch 1 (SM, CSIRS) —— 已完成
  - Batch 2 (WeeFIM, Conners PSQ/TRS, FineMotor, CNBS-R2016) —— 已完成
  - Batch 3 (SDQ, SRS2, CBCL, GMFM-88, TGMD-3) —— 已完成
- **容器减负**：`AssessmentContainer.vue` 行数从 1482 缩减至 790行（减幅 -46.7%）
- **彻底消除容器层裸 SQL**：该组件内的所有 `db.run` / `db.all` 均已被移除
- **新增统一下游**：实现并引入了 `PersistContext` / `PersistResult`，通过统一入口 `driver.persistAssessment(context)` 代理之前的 12 个各自为政的 `saveXXX` 方法
- **新建 API 类**：补充了缺失的 `SDQAssessmentAPI` 和 `SRS2AssessmentAPI`，补齐了 CSIRS 的 JSON 解析保存方法

**提交情况**：
- 在本次交接打了一个独立的 refactor commit，将重构工作整洁提交
- 其他不相干的未记录更改（包括由于时空串联留下来的 care_scene immersive work 的脏改动）使用了 stash 暂时封存

## 下一步

**第一个动作**：

本地环境（安装有 Node.js 的实机）运行 `npm run build`，确认本次类型重构及文件修剪未引入任何 TSC 编译级错误。

**紧接着要做的验证**：

在真实应用里执行一次包含这 3 个不同类型的评估流冒烟测试：
1. **纯净单表更新体系**：如 `SM` (社会适应度)，确认可以生成 `assess_id` 并关联 `sys_report_records`
2. **JSON 数据体型**：如 `CSIRS` 或 `SDQ`，确保新的按 JSON stringify 入库格式符合前端图表渲染与后续详情读取预期
3. **复合大表单**：如 `CBCL`，确认跨表单页数据（`socialCompetence` 与行为表单）全部能平滑进入 `CBCLDriver` 和对应新行中。

## 遇到的问题 / 阻塞 / 风险

1. **编译与功能尚未进行实机验证**
   - 当前在纯静态云端完成逻辑拆离，没有跑 TSC，可能有局部的类型严格约束（如 JSON string 接口断言或者个别可选字段传递问题）在构建期才能被拦下。
   - 冒烟测试极为必要，因本次拔掉了所有量表的旧 `save` 方法。

2. **CBCL 的 socialData 保存设计变更**
   - 重构中去掉了向 CBCL `saveCBCLAssessment` 手动传 `socialFormData` 的逻辑，直接利用已由 `handleSocialFormSubmit` 塞进 `CBCLDriver.setSocialData()` 的驱动器内部状态进行落地。如果将来重载流中此处发生时序倒挂可能为空。

## 本次对话做出的关键决策及原因

1. **废弃统一 save 方法上的量表白名单分发（上帝路由）**
   - 之前由一堆 `else if (scale === 'xxx') await saveXxx()` 堆砌的逻辑。
   - 取而代之的是开闭原则：容器只要调用 `driver.persistAssessment()` 并给抛出未实现即可。现在任何新量表的加入，完全不需要改动容器代码。

2. **按真实数据库 Schema 对齐模型**
   - 老旧的 `CSIRSAPI.createAssessment()` API 还停留在老版的细粒度按列计分时代，实际系统已演进为了存 JSON `raw_scores` / `t_scores` 的设计。
   - 没有为了妥协而去使用旧 API 写入非法格式，而是大胆新增 `CSIRSAPI.createAssessmentWithJsonScores` 进行适配修复。

3. **分批合并式重组容器**
   - 执行了 Batch 0 -> 1 -> 2 -> 3 极度细化的渐进重构。确保随时发生意外终端，都能用现有的 "退回旧存库函数 fallback" 兜住未能来得及转换的边缘量表。（最终因为成功完成全部 Batch，回退路由也被拆除丢弃）。
