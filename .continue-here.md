# Current State
- 任务：继续自理训练模块阶段 1，补齐 `task_training` 的平台主链；当前已完成“可见 / 可选 / 可启动”底座补链。
- 已完成：`self-care` 路由壳已接入；`task_training` 已进入统一启动器、首页今日计划启动白名单、训练计划资源选择器、资源中心训练资源主链。
- 产出物：`src/features/self-care/self-care-routes.ts`、`src/views/self-care/TaskList.vue`、`src/utils/training-launch.ts`、`src/database/dashboard-api.ts`、`src/views/plan/PlanList.vue`、`scripts/tests/self-care-routes.test.mjs`、`scripts/tests/training-launch.test.mjs`、`scripts/tests/dashboard-api-resource-types.test.mjs`、`scripts/tests/plan-list-task-training.test.mjs`

# Next Action（必须原子级——第一步可执行的单一动作）
- 修改 `src/features/self-care/task-training-contract.ts`，定义 `task_training` 资源元数据、步骤、执行结果的 TypeScript 类型。

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- `自理训练` 顶层入口继续挂 `life_skills` 授权：当前授权体系只识别现有业务模块代码。
- 阶段 1 只补平台底座主链：不提前实现任务 CRUD、执行页或记录详情页。
- `task_training` 继续复用训练资源的 `game` 展示主链：本阶段目标是“可见 / 可选 / 可启动”。

# Relevant Files（最小集——仅列下一步会用到的）
- `src/features/self-care/task-training-contract.ts`
- `docs/planning/2026-05-08-self-care-training-module-implementation-plan.md`
