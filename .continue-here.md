# Current State
- 上一条主线：游戏 IEP 闭环扩展 Phase 2+3（Tier2 F04/L01/L02/L04 + Tier3 F02/F01/F05）已代码级落地并推送，commit `974a69b`（origin/main，HEAD=`a28889f`）。
- 工作区：干净，`main` 与 `origin/main` 同步，无 stash。
  - 2026-07-08 已确认并丢弃 `wip(shape-match)` stash：基线为 4-21 的 `2a02bad`，其「木块拼图/形状匹配」工作已被主线 5-26 的 `7c1c83a`（独立组件 `WoodBlockPuzzleGame.vue` + `shared/WoodenShapeBlock.vue`）取代，无恢复价值。
- 既有待办（不阻塞新任务）：IEP Phase 2+3 真机 E2E 仍未跑。

# Next Action
- 等待用户新任务。IEP 真机 E2E 为既有待办，可随时由用户在真机执行：`npm run electron:dev`，通关新增 7 游戏，核对 `training_records` + `report_record` + `IEPReport` 专属段 + 缺指标卡隐藏。

# Blockers
- IEP 真机 E2E 未跑（本环境无法驱动交互式 Electron 游戏 + 查 DB）。

# Key Decisions
- IEP Tier2 派生正确率 correct/(correct+wrong)；Tier3 近似指标口径（F02 轨迹精度 / F01 覆盖率 / F05 抑制控制 early_taps）；F05 反应时 `window_response_ms` 数组→均值。
- G07_MONSTER 延后为独立任务（emotional-regulation 入口；启用需改守卫 + G07 专属白名单 + IEPReport 情绪匹配分支 + 新生成器）。
- shape-match WIP 已被主线 `WoodBlockPuzzleGame` 独立组件方案取代，不再恢复。

# Relevant Files
- src/utils/game-performance-normalizer.ts
- src/utils/iep-generator.ts
- 主线 shape-match 落地：src/components/games/hand/WoodBlockPuzzleGame.vue、src/components/games/shared/WoodenShapeBlock.vue
