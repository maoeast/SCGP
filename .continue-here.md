# Current State
- 任务：首页看板移除页头 + 静默自动刷新（3 分钟定时 / 隐藏态跳过 / 焦点唤醒）+ Banner 主标题品牌色——已实现、已验证、已提交
- 已完成：
  - `src/views/Dashboard.vue`：删页头（`首页看板` H1 + 副标题 + 「刷新数据」按钮）及失效的 `RefreshRight` 导入与 `.dashboard-header` 样式，首屏即 Hero Banner；主标题改品牌主色 `var(--scgp-primary)`（#5f89d9，字号/字重不变）
  - 静默自动刷新：`AUTO_REFRESH_INTERVAL_MS = 180_000`；`handleAutoRefreshTick` 在 `visibilityState === 'hidden'` 时跳过；`handleVisibilityChange` 在 visible 时立即刷新；`loadDashboard({ silent })` 静默不翻 `v-loading`；`onUnmounted` 严格 `clearInterval` + `removeEventListener`（布局层无 keep-alive，已实证）
  - 依赖同步（P0）：`scripts/manual/capture-user-manual-screenshots.mjs:2214` 登录就绪闸门 → `.dashboard-hero` 可见；5 处 dashboard 断言 → `学生总数`；`--dry-run` = validated（217 场景 / 21 profile）
  - 测试卫生：`scripts/tests/ai-builtin-agent-presets-contract.test.mjs:141-148` 头像断言适配 `.png`→`.webp` 兜底（该文件已转绿）
  - 文档：`docs/CHANGELOG.md`（2 条新条目）、`PROJECT_CONTEXT.md §3.2`、`README.md`、`docs/planning/2026-08-16-教师-系统首页看板录制包.md`（标注已作废）
- 产出物：提交 `0866c42`（代码 + 截图脚本锚点）→ `1a1d6aa`（测试断言）→ `56bf0c3`（文档）→ 本笔 `chore(docs)`（交接文档）
- 验证证据：`npm run type-check` 0 error；6 组源码/行为断言全过（含隐藏态跳过三态实跑、焦点唤醒两态实跑、silent 遮罩语义实跑）；截图管线 dry-run `status: validated`

# Next Action
- 技术债专项（另开会话，用户已定，勿混入看板上下文）：跑 `npm run verify:core` 复现 6 条红，逐条把断言对齐当前源码语义，复跑至全绿——
  1. `scripts/tests/ai-message-edit-contract.test.mjs:65`（断言找 `<div class="ai-composer">`；`AiAssistant.vue:867` 已改为多属性写法）
  2. `scripts/tests/login-theme-background.test.mjs:29/37/47/124`（断言找 `:background-image="systemConfigStore.activeLoginBackground.image"`；`Login.vue:5` 已包 `getLoginBackgroundUrl(...)`）
  3. `scripts/tests/production-debug-boundary.test.mjs:44`（断言找 `devTools:\s*isDev`；`electron/main.mjs:418` 已是 `devTools: devToolsEnabled`）

# Blockers
- None（上述 6 条红不阻塞看板功能，仅使 `verify:core` 非全绿）

# Dead Ends
- Vue 静态点路径 ref（`ref="obj.prop.value"`）编译为静态字符串 ref，`obj.prop.value` 恒为 null 且无任何警告（本会话三写法实测）；可用 `:ref="obj.prop"` 或函数 ref
- advisor 代码评审单轮 600s 预算 + 1500 行文件 = 超时且无报告（17 轮 / 32 次调用）；把 `paths` 收窄到单文件并声明「只核前表」后才跑完

# Key Decisions
- 静默刷新：定时器与焦点唤醒一律 `silent: true`（不闪遮罩），且定时器在页面隐藏时跳过请求（用户拍板）
- 截图管线登录闸门与看板断言改锚 `.dashboard-hero` + `学生总数`（原 `首页看板` 文本随页头移除而失效）
- 6 条无关陈旧断言先不动、留技术债专项，不进本次看板 Git 历史（用户拍板，防范围蔓延）

# Relevant Files
- 下一步要开的：`scripts/tests/ai-message-edit-contract.test.mjs`、`scripts/tests/login-theme-background.test.mjs`、`scripts/tests/production-debug-boundary.test.mjs`
- 对照源码：`src/features/ai/components/AiAssistant.vue`、`src/views/Login.vue`、`electron/main.mjs`
