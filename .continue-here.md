# Current State
- 任务：下一会话开始重新优化评估中心页面布局，仅处理评估中心入口页，不扩展到具体量表页
- 已完成：
  - `src/database/emotional-api.ts` 的 `getStudentReportPayload()` 已在 payload 层把情绪英文 code、关心方式英文 code、英文兜底分类映射为中文
  - 情绪行为训练模块报告页面与 Word 导出随 payload 自动中文化，本轮修复已验收通过
  - 已执行 `npm run type-check`，通过
- 产出物：
  - `src/database/emotional-api.ts`

# Next Action
- 打开 `src/views/assessment/AssessmentSelect.vue`，梳理当前 header / 筛选区 / 量表卡片区的 DOM 与样式结构，作为评估中心布局优化起点

# Blockers
- None

# Key Decisions
- 情绪报告英文用户可见文案只在 payload 组装层修复，不重构 `src/views/emotional/Report.vue`
- 下一会话只聚焦 `src/views/assessment/AssessmentSelect.vue` 的页面布局优化，不扩展到各量表报告页
- 当前工作区仍有无关本地改动与资源文件变更，继续时不要回退

# Relevant Files
- `src/views/assessment/AssessmentSelect.vue`
- `src/views/assessment/assessment-select-state.ts`
- `src/features/assessment/assessment-scale-catalog.ts`
