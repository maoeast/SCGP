# Current State
- 任务：已完成“游戏训练 -> 社交沟通力”新增双人小游戏“双人表情擂台（S06_EXPRESSION_DUEL）”并接入现有小游戏主链；下一轮要为“表情能量球”和“表情侦探”加入基于 Tone.js 的按表情切换音频反馈能力。
- 已完成：
  - 在 `src/data/custom-game-registry.ts` 注册 `S06_EXPRESSION_DUEL`，归属 `social-communication`，双人、摄像头、共享场次。
  - 在 `src/router/index.ts` 新增 `/emotional/games/expression-duel` 路由并接入 `immersiveShell`。
  - 新增 `src/components/emotional/games/expression-duel.ts`，包含双人脸位排序、相似度计算、对局结果汇总。
  - 新增 `src/components/emotional/games/ExpressionDuelGame.vue` 与 `src/views/emotional/games/ExpressionDuelPage.vue`，实现共享镜头双人对战运行时。
  - 在 `src/database/emotional-games-api.ts` 为 `S06_EXPRESSION_DUEL` 补齐 `accuracy_rate` / `avg_response_time` 派生。
  - 在 `src/views/emotional/GameRecordDetail.vue` 新增 `S06_EXPRESSION_DUEL` 的指标卡与补充记录展示。
  - 顺手收口同域类型问题：`src/composables/useEmotionDetector.ts`、`src/components/emotional/games/ExpressionDetectiveGame.vue`、`src/components/emotional/games/expression-detective/config.ts`、`src/components/emotional/games/EnergyBallGame.vue`、`vite.config.ts`，当前 `npm run type-check` 已恢复通过。
- 产出物：
  - `src/components/emotional/games/ExpressionDuelGame.vue`
  - `src/components/emotional/games/expression-duel.ts`
  - `src/views/emotional/games/ExpressionDuelPage.vue`
  - `scripts/tests/social-expression-duel.test.mjs`
  - `docs/superpowers/plans/2026-05-28-social-expression-duel-implementation-plan.md`

# Next Action
- 读取 `docs/planning/表情音乐音乐效果.md`，然后新增一个可复用的 `src/composables/useExpressionAudio.ts`，先只完成 Tone.js 音频状态机与 `changeExpression()` 的最小可运行实现。

# Blockers
- None

# Key Decisions
- 双人表情擂台按“共享镜头双人模式”落地：原因是当前 `GameContainer` 权限主链只提供单路已授权摄像头流，直接复用现有双人共享场次与落库链最稳。
- 双人游戏继续复用 `GameContainer -> persistSessionGroup()`：原因是现有 `S01_BURGER` 已验证这条双人共享记录链可用，不另开记录体系。
- 下轮音频增强优先做成独立 composable：原因是“表情能量球”和“表情侦探”都要消费同一类表情状态到音频状态切换，避免把 Tone.js 逻辑重复塞进两个游戏组件。

# Relevant Files
- `docs/planning/表情音乐音乐效果.md`
- `src/components/emotional/games/EnergyBallGame.vue`
- `src/components/emotional/games/ExpressionDetectiveGame.vue`
- `src/audio/game-music-controller.ts`
- `src/audio/game-music-engine.ts`
- `src/audio/game-music-profiles.ts`
- `src/views/emotional/games/GameContainer.vue`
