# Current State
- 任务：AI 学生级长期记忆 M5（治理 + 绑定 UI）+ 用户反馈修复（开关入口 + 记忆卡片位置）已完成，待提交
- 已完成（工作区未提交）：
  - M5 治理 SQL：archiveStalePending（pending 30 天→archived）、
    enforceConfirmedQuota（分类配额 + 关键项保护 + 最旧淘汰）、
    purgeInactiveMemories（非有效 365 天 + 每生 500）、
    purgeSummaryBatches（cancelled 30/failed 90/done 180 + 每会话 20 批）
  - stores/ai.ts：runMemoryGovernance 随补偿任务运行 + 导出 + 开关门控
  - AiAssistant.vue：会话绑定学生选择器（memoryEnabled 时显示、已有消息锁定、解绑不开放）
  - **用户反馈修复**：
    1. 系统设置 → AI 智能体：新增「学生长期记忆」开关（管理员可见，立即生效持久化）
    2. 学生详情：AI 记忆从记录 tab 区移除，改为左侧学生信息卡片（学号下方），
       内嵌 StudentMemoryPanel compact 模式（待确认列表 + 确认/拒绝 + 置顶/标记关键/删除），
       卡片头显示「待确认 N · 已确认 M」计数
  - 测试：契约测试更新（permission 9 场景：卡片位置/开关 UI/compact 模式）
  - 验证：type-check / **AI 契约 89 条全过** / build:web 全过
- 产出物：6 文件改动（ai-api/stores/AiAssistant/AiAgentConfig/StudentDetail/StudentMemoryPanel + 测试）

# Next Action
- 用户确认后提交（建议：feat(ai): 学生级长期记忆 M5 治理 + 开关入口 + 记忆卡片移至学生信息区）

# Blockers
- None（v4.1 全部里程碑 M1-M5 + 用户反馈修复完成）

# Key Decisions
- 治理随补偿任务运行（不单独调度），开关关闭时全部跳过
- 配额淘汰只动 normal 优先级（safety_critical/pinned 保护）；淘汰 = 降级 archived 可回溯
- 开关入口在「系统设置 → AI 智能体」的 AI 总开关下方（复用 aiStore.memoryEnabled 双向）
- 记忆卡片内嵌学生信息区（学号下方），compact 模式只列 pending 默认 + 操作按钮

# Relevant Files
- src/database/ai-api.ts（四个治理方法）
- src/stores/ai.ts（runMemoryGovernance + 导出）
- src/features/ai/components/AiAssistant.vue（绑定选择器 + 锁定 + 样式）
- src/views/system/AiAgentConfig.vue（学生长期记忆开关）
- src/views/StudentDetail.vue（记忆卡片移入学生信息区 + 计数）
- src/views/student-detail/components/StudentMemoryPanel.vue（compact 内嵌模式）
- scripts/tests/ai-memory-permission-contract.test.mjs（契约更新）
- docs/planning/2026-08-05-ai-student-memory-design.md（v4.1 设计全文）
