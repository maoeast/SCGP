# Current State
- 任务：继续实现 SCGP 感官统合训练游戏“空中魔法指挥棒（Air Conductor）”，当前已完成阶段 3 的最小运行闭环，并修正骨骼线与镜像视频左右不一致问题。
- 已完成：
  - `src/components/games/pose/AirConductorGame.vue` 已从占位页升级为真实运行态容器，接上校准、倒计时、playing / paused / finishing / done。
  - `src/composables/usePoseTracker.ts` 已接入本地 `public/models/pose_landmarker_lite.task`，可输出肩/肘/腕 Pose 数据并绘制骨骼。
  - `src/composables/usePoseAudio.ts` 已实现指数平滑、抬臂计数、双侧协同秒数、max reach score、离框降级、最小 Tone + 共享音乐联动。
  - `src/components/games/pose/PoseCameraLayer.vue` 已让 `video` 与 `canvas` 同步 `scaleX(-1)`，骨骼线与人物动作现在同侧。
  - `src/database/api.ts` 已把 `handGameStats` 收口到 `training_session.summary_payload`。
  - `scripts/tests/air-conductor-runtime.test.mjs`、`scripts/tests/sensory-hand-games.test.mjs`、`scripts/tests/game-audio-platform.test.mjs` 已覆盖阶段 3 关键口径与镜像回归。
- 产出物：
  - `src/components/games/pose/AirConductorGame.vue`
  - `src/components/games/pose/PoseCameraLayer.vue`
  - `src/components/games/pose/air-conductor-runtime.ts`
  - `src/composables/usePoseAudio.ts`
  - `src/composables/usePoseTracker.ts`
  - `src/types/air-conductor.ts`
  - `public/models/pose_landmarker_lite.task`

# Next Action
- 修改 `src/components/games/pose/AirConductorGame.vue`，新增独立粒子层，并先把左右手腕位置映射为两组七彩音符粒子发射点。

# Blockers
- None

# Key Decisions
- 继续复用现有主链：`GamePlay -> training_records -> training_session -> report_record`，不新增 `air_conductor_sessions` 旁路。
- Pose 画面口径固定为“视频镜像、骨骼 overlay 同步镜像”，避免左右侧再次错位。
- 当前 Air Conductor 页面中文源码已确认正常；后续用测试守住文案，避免把终端编码误判成页面乱码。

# Relevant Files
- `src/components/games/pose/AirConductorGame.vue`
- `src/composables/usePoseAudio.ts`
- `src/components/games/pose/air-conductor-runtime.ts`
- `src/composables/usePoseTracker.ts`
- `scripts/tests/sensory-hand-games.test.mjs`
