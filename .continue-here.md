# Current State
- 任务：C06 AI 附件删除一致性与短期 AI provider Key 交付治理已完成，下一步进入 C07 AI 隐私告知与外发脱敏。
- 已完成：`src/database/ai-api.ts` 会话删除改为 DB 事务；`src/stores/ai.ts` 改为 DB 提交后按剩余引用清理附件；`ai_provider` 新增学校归属、后台 Key 备注、轮换提醒日期；系统设置页可维护这些字段；`verify:core` 通过。
- 产出物：本轮待提交改动；提交后以本次 commit 为准。

# Next Action
- 读取 AI 外发调用面，确认 `sendChat`、tool loop、附件、报告生成工具分别会把哪些学生 / 评估 / 训练 / 附件内容发往 provider。

# Blockers
- None

# Key Decisions
- AI 附件删除顺序固定为：先 DB 事务删除消息与会话，提交后再删除剩余引用数为 0 的物理文件；文件删除失败不回滚 DB。
- AI provider API Key 继续只走 Electron Main `safeStorage`；备份不携带 Key，恢复后需重新配置。
- 短期 Key 交付采用每校独立 provider Key + 官方后台额度限制 + 本地治理元信息；暂不做服务端代理或可离线自动解密的短加密码。

# Relevant Files
- `src/stores/ai.ts`
- `src/services/ai-tool-loop.ts`
- `src/services/ai-tools.ts`
- `src/features/ai/components/AiAssistant.vue`
- `electron/handlers/ai.mjs`
