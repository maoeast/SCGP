# Current State
- 任务：在系统页增加“重新激活 / 更新授权”入口，支持已安装客户在增购模块后输入新激活码并刷新当前机器授权。
- 已完成：
  - 修复生产环境首次启动激活守卫，未正式激活时会先进入激活页。
  - 修复备份恢复时 `report_record -> sm_assess` 错误外键导致的外键校验失败。
  - 真机验收已通过：首次启动进入激活页，历史失败备份可恢复。
- 产出物：
  - `src/router/index.ts`
  - `src/database/init.ts`
  - `src/database/migrate-report-constraints.ts`
  - `src/database/migration/migrate-emotional-foundation.ts`
  - `release/SCGP 星愿能力发展训练系统_1.0.0.exe`

# Next Action
- 修改 `src/views/System.vue` 的激活状态区域，增加“重新激活 / 更新授权”按钮和输入新激活码的交互入口。

# Blockers
- None

# Key Decisions
- 重新激活入口放在系统页：客户增购模块后不需要回到首次启动激活页。
- 重新激活是更新当前机器授权：成功后立即刷新 `allowedModules`，不重装、不清空现有数据。
- `report_record.assess_id` 保留通用引用字段：不再绑定到 `sm_assess` 单表外键。

# Relevant Files
- `src/views/System.vue`
- `src/stores/auth.ts`
- `src/utils/activation-manager.ts`
- `.continue-here.md`
