# Current State
- 任务：评估中心入口页改版后的 Electron 实机确认，仅围绕 `AssessmentSelect` 做复核与必要微调。
- 已完成：`src/views/assessment/AssessmentSelect.vue` 已完成第三轮卡片层级微调；标题与缩写并排，描述句已删除，年龄/题量标签上移到标题下方，维度文案下沉并弱化；`npm run type-check -- --pretty false` 已通过。
- 产出物：`src/views/assessment/AssessmentSelect.vue`

# Next Action
- 在有 X server / `DISPLAY` 的环境执行 `npm run electron:dev`。

# Blockers
- 当前会话环境缺少 X server / `DISPLAY`，无法完成 Electron 可视化复核。

# Key Decisions
- 不恢复搜索框或快捷筛选 chips：用户明确要求，且当前量表数量不需要额外筛选层。
- 卡片信息层级固定为“标题+缩写 / 标签 / 维度弱化”：解决当前最优先的视觉分组混乱问题。
- 本轮只处理 `AssessmentSelect`：避免扩散到其他评估页面或全局样式。

# Relevant Files
- `src/views/assessment/AssessmentSelect.vue`
- `.continue-here.md`
