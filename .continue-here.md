# Current State
- 任务：认知发展模块 Phase 3「综合认知自测·视空间图形匹配绩效题」（cognitive_self）**代码级完成**，未提交。
- 已完成：7 新建 + 11 修改；`npm run type-check` ✅（exit 0，修了 1 处字面量类型陷阱）；`training-route-access` 4/4 ✅。
- 核心能力：评估主链**首个试次级真反应时绩效题**——`isPerformanceTask` flag 分流渲染 `PerformanceTrialBoard`（隔离 14 个既有量表），RT 经 emit→handleAnswer 覆盖既有「伪 RT」写入 `ScaleAnswer.responseTime`。

# Next Action
- 用户真机 E2E：认知 tab → 综合认知自测 → 跑 12 题流程，确认 ①图片渲染 ②计时（RT 非零且合理）③落库 ④报告展示正确率/RT/维度。
- E2E 通过后提交（用户明确要求时）：建议拆 2 提交——数据层+Driver / DB接入+注册路由+报告。

# Blockers
- 真机 E2E 未跑（type-check 过 ≠ SVG 渲染/计时交互可用）。
- 题库（12 题）与常模均为 DRAFT，需专业心理测量审核 + 本地常模采集。
- `entitlement-catalog` 测试预存失败（与本轮无关，未跑；cognitive_self 复用已有 `cognitive` entitlement，未改 entitlement-catalog）。
- `assets/resources/images/raven60/` 版权素材不纳入（继续留工作区）。

# Key Decisions
- **计时自包含在 `PerformanceTrialBoard`**（内部 `useTrialTimer` + `watch(question)` startTrial），`AssessmentContainer` 不新增 watch——改动面最小、既有量表零回归。
- `report_type` 三处双写（init.ts CHECK + migrate CHECK + needsMigration 谓词），漏任一旧库不迁移。
- `BaseDriver.serializeAnswers` 扩 `rt` 字段（向后兼容）；`isPerformanceTask: boolean = false` 显式标注避免字面量类型阻止子类覆写。
- 命名约定：report_type 字面量 `cognitive_self`（下划线），URL/组件目录 `cognitive-self`（连字符）。
- 2 维度 `match_basic`/`match_detail` 各 6 题；4 选 1；MVP 无强制时限（推迟 Phase 3.1）。

# Relevant Files
- 新建：`src/database/{cognitive-self-data,cognitive-self-norms}.ts`、`src/utils/cognitive-match.ts`、`src/strategies/assessment/CognitiveSelfDriver.ts`、`src/views/assessment/composables/useTrialTimer.ts`、`src/views/assessment/components/PerformanceTrialBoard.vue`、`src/views/assessment/cognitive-self/Report.vue`
- 修改：`src/types/assessment.ts`（ScaleDriver.isPerformanceTask?）、`BaseDriver.ts`（flag 默认 + serializeAnswers rt）、`AssessmentContainer.vue`（条件渲染 + handleAnswer 真 RT 覆盖）、`init.ts`（cognitive_self_assess 表 + CHECK）、`migrate-report-constraints.ts`（三处双写）、`api.ts`（CognitiveSelfAssessmentAPI + resolveModuleCode + union）、`strategies/assessment/index.ts`（注册）、`assessment-scale-catalog.ts`（目录条目）、`scale-dimension-mapping.ts`（维度映射）、`report-routes.ts`（case）、`router/index.ts`（路由）
