# Current State
- 当前分支 `main`，领先 `origin/main` 4 个提交，无 stash；工作区仍 dirty，未提交也未推送。
- 上一专题 AI 智能体头像迁移仍完整保留，未与本专题混改：5 PNG 从 `src/assets/ai-agent-avatars/` 移至 `assets/resources/images/ai-agent-avatars/`，`src/features/ai/ai-agent-avatar-assets.ts` 已用 `resolvePresetResourceUrl()` 延迟解析。
- 用户准备的 `assets/resources/images/user-avatars/` 12 张 PNG 已原样复用、未覆盖/删除/重生成：学生 6 张 Student、当前账号 6 张 Teacher。
- 本专题「学生信息」和「个人资料 > 基本信息」头像能力已代码落地：
  - 新增 `src/utils/avatar-presets.ts`，唯一预置头像 catalog 只存 canonical `images/user-avatars/<filename>.png`；显示统一经 `resolvePresetResourceUrl()`，不把环境 URL 写入 DB。
  - 新增 `src/components/common/AvatarPicker.vue`，集中预置选择、Data URL 预览、本地上传 Canvas 压缩（最大 512px / JPEG quality 0.82）、摄像头拍照/重拍/确认、移除与权限/设备错误提示。
  - `AddStudentDialog.vue` 已复用 `AvatarPicker`，学生只显示 6 张 Student 预置；保留新增/编辑回显，继续用 `student.avatar_path` 存 canonical 路径或 Data URL。编辑卡表单为姓名/学号并排、性别/出生日期并排、诊断类型/所属班级并排，保存按钮使用蓝色主操作样式。
  - `StudentAvatar.vue` 的 `resolveStudentAvatarUrl()` 已可解析 canonical 预置路径；`PlanList.vue` 唯一绕过共享显示的原始 `el-avatar` 已改用 `StudentAvatar`。
  - `user.avatar_path TEXT` 已同步 `init.ts` / `sqljs-init.ts` / `schema.sql`，正式 `initDatabase()` 对旧库用 `safeAddColumn(rawDb, 'user', 'avatar_path TEXT')` 幂等迁移。
  - `UserAPI`、`auth.User`、`Profile.vue`、Pinia `updateCurrentUser()` 与 `localStorage.user_info` 均已接上 `avatar_path`；Profile 只显示 6 张 Teacher 预置，保存/重置会同步当前会话。基本信息表单现为「用户名 / 角色」响应式并排、姓名、头像；邮箱不再显示或由该页面写回。
  - `Layout.vue` 顶栏用户菜单已即时显示当前账号头像或姓名首字回退。

# Next Action
- 在 Electron 实机做人工 UAT：学生新增/编辑、两组预置头像、上传、摄像头允许/拒绝/无设备、拍照重拍确认、移除、Profile 保存/重置、顶栏即时刷新、重启和退出再登录后的持久化。
- 用户验收后，按用户指令审阅并拆分提交：上一专题 AI 智能体头像迁移与本专题头像功能应保持独立提交。

# Blockers
- 无代码/构建阻塞。
- 摄像头真实设备权限和 Electron 重启持久化尚未在本轮人工操作，不能标为已完成 UAT。

# Key Decisions
- 预置资源 DB 只保存 canonical `images/user-avatars/...`；Electron 显示为 `resource://images/user-avatars/...`，Web 显示为 `/assets/resources/images/user-avatars/...`。
- 上传与拍照只存 Canvas 压缩后的 Data URL，兼容现有 `student.avatar_path`；不接入 `uploaded/avatars`、资源引用扫描或文件生命周期，避免半套托管链。
- 学生和个人资料各严格限制为自己的 6 张预置头像，二者通过同一个 `AvatarPicker` 状态机实现。
- 未改动 `assets/resources/images/user-avatars/` 原始 12 PNG；安装包通过 `extraResources` 带入，Vite `dist` 不重复含这些图片字节。

# Verification
- `npm run type-check`：通过。
- `node --test scripts/tests/avatar-feature-contract.test.mjs`：3/3 通过；已将测试加入 `test:core:node`。
- `npm run verify:release`：通过（含核心测试与 `build:web`）。
- `npx electron-builder --win`：通过，生成 `release/win-unpacked` 与 `release/scgp-setup-1.0.7.exe`。
- 打包资源核验：`release/win-unpacked/resources/assets/resources/images/user-avatars/` 为 12 张 PNG，名称与 SHA-256 均与源资产一致；`dist` 与这些 PNG 的 SHA-256 交集为 0。

# Relevant Files
- `assets/resources/images/user-avatars/`
- `src/utils/avatar-presets.ts`
- `src/components/common/AvatarPicker.vue`
- `src/components/AddStudentDialog.vue`
- `src/components/student/StudentAvatar.vue`
- `src/views/Profile.vue`
- `src/views/Layout.vue`
- `src/stores/auth.ts`
- `src/database/api.ts`
- `src/database/init.ts`
- `src/database/sqljs-init.ts`
- `src/database/schema.sql`
- `scripts/tests/avatar-feature-contract.test.mjs`
