# Current State
- 当前任务：SCGP cross-entry custom games 的 Wave 1 前期准备已收口，下一会话开始补最小入口适配层。
- 已完成到哪一步（具体到函数 / 文件 / 行为）：
  - `src/data/custom-game-registry.ts` 已确认可承载 `F01_CLOUD_ERASE` 所需字段：`gameCode / moduleCode / trainingEntryCode / entryPath / maxPlayers / requiredPermissions`
  - `src/utils/custom-game-launch.ts` 已确认 `buildCustomGameLaunchContext()` 能稳定生成 `launchEntryCode / launchModuleCode / participantStudentIds / difficultyLocked`
  - `src/views/emotional/games/GameContainer.vue` 已确认权限 gate、完成/中断持久化可复用，但 `getReturnLocation()` 仍会把非 emotional 入口落回 `/emotional/menu`
  - 已锁定 Wave 1 单一目标为 `F01_CLOUD_ERASE / 云朵擦擦乐`，定义见 `docs/planning/2026-04-14-cross-entry-custom-games-wave1-min-target.md`
- 当前产出物（代码 / 文件）：
  - `docs/planning/2026-04-14-cross-entry-custom-games-wave1-min-target.md`
  - `.continue-here.md`

# Next Action
- 下一步第一个动作（必须原子级，例如：修改 xx 文件的 xx 函数）：修改 `src/views/games/GameLobby.vue` 的 custom game 启动分支，让 `fine-motor` 入口先能走 registry-backed custom game，而不是旧的 `/games/play`

# Blocking / Risks
- 阻塞 / 风险：仓库既有 TS7016 仍会污染 `type-check:emotional` 信号（`src/config/CNBSR2016FeedbackConfig.js`、`crypto-js`），不是本轮新增问题

# Key Decisions
- 只做一个 Wave 1 目标：`F01_CLOUD_ERASE`；原因：单人、无媒体权限、最适合先验证真实跨入口
- 不把跨入口目标态写成现状；原因：当前真实运行链路仍是 `emotional-only`
- 先补入口适配层，再写游戏业务；原因：运行时底座基本够用，真正缺口在启动接线和返程

# Relevant Files
- `src/views/games/GameLobby.vue`
- `src/views/emotional/games/GameContainer.vue`
- `src/data/custom-game-registry.ts`
- `src/utils/custom-game-launch.ts`
- `docs/planning/2026-04-14-cross-entry-custom-games-wave1-min-target.md`
