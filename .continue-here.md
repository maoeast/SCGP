# Current State
- 任务：修复打包后教学资料封面图 404 + 调试 DevTools 开关 + teaching_material 表初始化加固
- 已完成：
  - 封面图路径修复：`/assets/resources/images/teaching-materials/...` → `resource://images/teaching-materials/...`（`TeachingMaterials.vue:118`），已 type-check 通过，打包验证通过
  - DevTools 调试开关：`electron/main.mjs` 读取 `package.json` 的 `scgpDebugDevtools` 元数据，生产调试包允许 `Ctrl+Shift+F12` 打开 DevTools
  - DB 初始化加固：`ensureTeachingMaterialSequenceOrder()` 独立迁移 + `seedPresetTeachingMaterials()` 独立于 emotional 链路，增加表存在性检查，更新种子数据为 389 条
  - Store 错误信息改进：`teaching-materials.ts` 所有 catch 分支携带 `error.message` 详情
- 产出物：打包产物 `release/scgp-setup-1.1.0.exe`（已验证封面图正常加载）

# Next Action
- 确认是否提交本轮改动，提交后处理 `.continue-here.md` 更新并等待用户分配新任务

# Blockers
- None

# Key Decisions
- 封面图路径：统一走 `resource://images/` 协议，与项目现有 equipment/physical-equipment 等预置资源路径一致
- DevTools 开关：通过 `package.json` 的 `build.extraMetadata.scgpDebugDevtools` 控制，不污染环境变量

# Relevant Files
- `src/views/resource-center/TeachingMaterials.vue` — 封面图路径
- `electron/main.mjs` — DevTools 开关 + resource:// 协议
- `src/database/init.ts` — teaching_material 初始化加固
- `src/stores/teaching-materials.ts` — 错误信息改进
- `package.json` — scgpDebugDevtools 元数据
