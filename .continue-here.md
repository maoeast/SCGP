# Current State
- 任务：继续自理训练模块阶段 3，补齐任务列表回流后的“当前训练任务”工作区占位与步骤展示
- 已完成：`src/views/self-care/TaskList.vue` 已在读取 `studentId + resourceId` 入口上下文后解析当前任务资源，渲染“当前训练任务”工作区占位，并直接显示 `meta_data.steps[]` 步骤列表；`scripts/tests/self-care-task-pages.test.mjs` 已补充对应页面断言并通过
- 产出物：`src/views/self-care/TaskList.vue`、`scripts/tests/self-care-task-pages.test.mjs`

# Next Action（必须原子级——第一步可执行的单一动作）
- 在开发环境手动走一遍“任务列表 -> 开始训练 -> 选学生 -> 回到 /self-care/tasks”链路，确认当前训练任务工作区与 `meta_data.steps[]` 展示正常

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 自理训练继续挂 `life_skills` 授权：当前路由与入口授权仍以 `TASK_TRAINING_MODULE_CODE` 为准
- `task_training` 步骤结构继续以 `sys_training_resource.meta_data.steps[]` 为一期事实源：本期不恢复旧 `task_step` 表
- 阶段 3 当前只补训练工作区占位与步骤可视化：不伪造尚未实现的正式执行页

# Relevant Files（最小集——仅列下一步会用到的）
- `src/views/self-care/TaskList.vue`
- `scripts/tests/self-care-task-pages.test.mjs`
