# Current State
- 任务：技术债专项——`verify:core` 陈旧契约断言对齐源码语义（已实现、已验证、本笔提交）
- 已完成（仅动测试与文档，未改任何业务源码）：
  - `scripts/tests/ai-message-edit-contract.test.mjs`：composer 容器断言改 `/<div\s+class="ai-composer"/`（class 已与 `:class`/`@drag*`/`@paste` 同处多行标签）；`composer-utility-actions` → `ai-composer-tools`（92a22e2 重构后旧 class 不存在）
  - `scripts/tests/login-theme-background.test.mjs`：静蓝主色期望 ×3 → `#3C9BA6`（65eee05）；`valuesForKey` 收紧为「紧跟 key 的值」，免疫 `init.ts` 主色迁移 SQL 污染（旧写法会把 key 名/旧色混入集合）；`Login.vue` 背景断言放行 `getLoginBackgroundUrl(...)` 包裹（fb1b683）
  - `scripts/tests/production-debug-boundary.test.mjs`：`devTools:\s*isDev` → `let devToolsEnabled = isDev` + `pkg.scgpDebugDevtools === true` + `devTools:\s*devToolsEnabled`（0381d47 开关化）；`devtools-opened` → 「F12 监听整体包在 `devToolsEnabled` 内」
  - 文档：`docs/CHANGELOG.md` 新增技术债条目
- 关键发现：原「6 条红」实为 6 条可见 + **2 条被同测试首条断言短路隐藏**（`composer-utility-actions`、`devtools-opened`），修完可见红后复跑才暴露；两条同样有独立提交背书（92a22e2 / 0381d47）
- 验证证据：`npm run verify:core` 全绿——type-check 0 error；`test:core:node` 132 tests / 132 pass / 0 fail；`test:core:ts` 5/5 passed（日志 `%TEMP%\scgp-debt-verify.log`）

# Next Action
- 无待办技术债，`verify:core` 已全绿。可选后续（均未排期，勿顺手做）：
  1. 这三个测试用「正则匹配 Vue/JS 源码文本」的范式本身脆弱（本次两次因重构转红）——若要根治需改为渲染级/探针式契约，属独立专项
  2. 手册截图专项遗留：重拍 S040/S041/S146 + 新增 CPEP-3 报告×2 / 质量看板×1 / AI 记忆卡×1-2 / 记忆开关×1

# Blockers
- None

# Dead Ends
- Vue 静态点路径 ref（`ref="obj.prop.value"`）编译为静态字符串 ref，静默失效且无警告；用 `:ref="obj.prop"` 或函数 ref
- advisor 单轮评审对 1500 行文件必超时且无报告；把 `paths` 收窄到单文件并声明「只核前表」后才能跑完
- **测试内断言是短路的**：首条红会掩盖同测试后续陈旧断言，不能按「红数 = 陈旧断言数」结账——修红后必须复跑

# Key Decisions
- 定性：4 处源码重构（65eee05 主色、fb1b683 背景 URL 解析、0381d47 DevTools 开关、92a22e2 输入区布局）均有独立提交且语义正确 → 一律**改测试对齐源码**，不动业务源码（用户拍板）
- `valuesForKey` 取「紧跟 key 的值」= 同时收紧与修陈旧：既对齐新色值，又消除迁移 SQL 造成的假值（非单纯放宽）
- 保持既有正则断言范式，本专项不重构测试框架（用户护栏）

# Relevant Files
- 本次改动：`scripts/tests/ai-message-edit-contract.test.mjs`、`scripts/tests/login-theme-background.test.mjs`、`scripts/tests/production-debug-boundary.test.mjs`、`docs/CHANGELOG.md`
- 对照源码：`src/features/ai/components/AiAssistant.vue`、`src/views/Login.vue`、`src/utils/login-theme.ts`、`electron/main.mjs`、`src/database/init.ts`
