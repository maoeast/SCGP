# Current State
- 任务：修复感官统合训练「节奏模仿」游戏全屏显示不全问题
- 已完成：
  - 修复容器链高度约束：`.sensory-shell--rhythm` 添加 `height: 100dvh`，`.game-audio-container--rhythm` 添加 `height: 100%`，`.game-mode-rhythm` / `.rhythm-playground` 添加 `flex: 1; min-height: 0`
  - 紧凑化全部节奏 UI 元素：难度卡片 270→150px，主交互区改为 flex:1 填充剩余空间，鼓垫改为响应式 `min(200px, 28vh)`，阶段指示器/时间线/底部网格全面缩减 padding/gap/min-height
  - type-check 通过，视觉验收通过
- 产出物：
  - `src/components/games/SensoryGameShell.vue`（1 处 CSS）
  - `src/components/games/audio/GameAudio.vue`（~30 处 CSS）

# Next Action
- 无明确下一步任务；等待新指令

# Blockers
- None

# Key Decisions
- 容器链用 `height: 100dvh` 替代 `min-height: 100dvh`：让 flex 子元素获得真实高度约束，`overflow: auto` 生效
- 主交互区用 `flex: 1; min-height: 0` 替代固定 `min-height`：让按钮/鼓垫自适应剩余空间
- 鼓垫用 `min(200px, 28vh)` 替代固定 320px：保证 720p 视口内不溢出

# Relevant Files
- `src/components/games/SensoryGameShell.vue`
- `src/components/games/audio/GameAudio.vue`
- `src/views/games/GamePlay.vue`
