# Current State
- 任务：A4「资源文件生命周期 + 备份收口」—— **Phase 1 代码完成**（type-check ✅，真机待验），Phase 2/3 待续。
- 已完成（Phase 1，本会话）：
  - §3.1 新增 `src/utils/resource-file-refs.ts`（托管/预置前缀 + `normalizeResourceUrl`/`isManagedResourcePath`/`extractResourceFileRefs`）+ `tests/resource-file-refs.test.ts`（`npx jiti` ✅）。
  - §3.2 新增 `src/utils/resource-file-service.ts`（`resolveAbsolutePath`/`deleteManagedFile`/`getManagedRoot` + `purgeAbandonedSceneCandidates`）；`teaching-material-file-manager.ts` 重构为复用 service（对外 API 不变）。
  - §3.3 `hardDeleteResource` 钩入：删前抽引用 → 删 DB 行 → 跨表计数==0 才删物理文件（fire-and-forget，失败 warn）。
  - §3.4 `updateResource` 钩入：仅 cover/metadata 变更时，old−new 引用差，UPDATE 后跨表计数==0 删旧文件。
  - §3.5 AI 候选图清理：两 editor 的 `handleGenerateImages` 开头 `purgeAbandonedSceneCandidates`（保留当前 imageUrl，**regenerate 时机**而非 apply，避免破坏同批次改选）。
  - §3.6 删 link A 死代码：`ResourceUpload.vue` + `electron/main.mjs` `SAVE_ASSET` IPC（preload 无专属绑定，走通用 `invoke`）。
- P0 抽样核对（scgp 活库 558 行 + sic-ads 旧库 61 行）：**零引用落在托管/预置两前缀之外**；823 条 PRESET 全是随包 `images/emotional-scenes/`；`isManagedResourcePath` 规则安全。

# Next Action
- **真机验证 Phase 1**（§3.7）：①硬删有封面/AI 图资源→磁盘文件消失、预置图不动；②编辑替换封面→旧托管文件删、预置不动；③AI 场景图重新生成→上一批未选候选消失；④软删→恢复→文件仍在。
- 真机通过后 → commit Phase 1（`fix(resource): 资源删除/替换即清物理文件，移除死代码写盘链路`）→ 标 A4 Phase1 ✅。
- **Phase 2**（备份纳入物理文件 zip 归档，v3.0）：遇 §7 P1 `fflate` 依赖需用户一句话确认，**停下问**。

# Blockers
- §7 P1：`fflate` 依赖待用户确认（Phase 2 才用，不阻塞当前）。
- 真机验证未跑（Phase 1 收尾 + A6 同样待真机）。
- link B 迁移子任务（见下）未启动。

# Key Decisions / 计划偏离
- §3.6 范围收敛（用户选「分阶段」）：`resource-manager.ts` 有 1 处活引用（`PlanList.vue:1448 handlePreviewResource`→`openWithSystem`），**不删**；`Resources.vue`/`FilePreview.vue`/`stores-resource.ts` 传递性死亡但随 link B 迁移另起子任务。本阶段只删确认全死的 link A。
- §3.5 改 regenerate 时机：计划原「apply 即删其余候选」会破坏候选网格同批次改选（"当前已选/设为场景图" toggle）。
- `hardDeleteResource` 当前**无活 UI 调用方**（仅定义 + docs；计划 §2.3 的 caller 行号实为 `restoreResource`）—— 钩子属预防性 / Phase 3 GC 基础。
- 跨表引用计数（`sys_training_resource` cover/meta LIKE + `teaching_material.file_path` 全等）==0 才删，防误删共享文件；软删 `deleteResource` 不动文件（可恢复语义）。
- 协议弃用 API、`get-app-path` 生产 bug 本计划不动（记 A8/技术债）。
- ⚠️ 工具备忘：Write 工具会把内容里的 `\uXXXX` 解释成字面控制字节——写文件须避免 `\u` 转义，或事后用 perl 修。

# Relevant Files
- 新增：`src/utils/resource-file-refs.ts`、`src/utils/resource-file-service.ts`、`tests/resource-file-refs.test.ts`
- 改：`src/database/resource-api.ts`（hardDelete/updateResource 钩入 + `countResourceFileReferences`/`purgeUnreferencedFiles`）、`src/utils/teaching-material-file-manager.ts`（复用 service）、`EmotionSceneEditor.vue`/`CareExpressionEditor.vue`（候选清理）、`electron/main.mjs`（删 SAVE_ASSET）
- 删：`src/components/ResourceUpload.vue`
- 计划：`docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md`；backlog：`docs/reports/2026-07-15-unimplemented-gap.md`（A4 行已回填 Phase1 状态）

# 待办子任务
- **link B 迁移清理**：扩展 `resource-file-service.ts` 支持 preset/managed 感知 openFile（可能需新 `resolveResourceAbsolutePath` IPC + preload + 类型），迁移 `PlanList.vue handlePreviewResource` 到新 service，再删 `resource-manager.ts` + `Resources.vue` + `FilePreview.vue` + `stores/resource.ts` + router `Resources` 死 import。需真机验 preset/managed 两类资源预览。
