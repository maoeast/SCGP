# Current State
- 任务：下一会话统一系统预置图片物理路径，将 `src/assets/images` 中的数据驱动器材图片收口到 `assets/resources/images`
- 本轮已完成并提交：登录主题背景媒体闭环 `358853a`；用户下拉菜单、退出红态与“AI 聊天记录”文案 `ae05029`
- 验证：`npm run verify:release` 通过（`type-check`、核心 Node 测试 88/88、3 组 TS 契约测试、`build:web`、生产调试残留门禁）；`git diff --check` 通过
- 图片盘点：`src/assets/images` 共 338 文件 / 31.57 MiB，其中 329 张 WebP、7 个 Markdown、2 个 TypeScript 索引；`assets/resources/images` 共 1132 文件 / 328.25 MiB，其中 1128 张图片
- 两棵目录做 SHA-256 交叉检查，字节级重复文件为 0；统一路径的收益是资源模型一致，不是安装包瘦身
- 当前两套加载事实：器材图由两个 `import.meta.glob(..., { eager: true })` 编入 Vite 产物；预置图片由 `package.json build.extraResources` 原样复制并经 `resource://` 解析

# Next Action
- 先给出窄范围实施计划，然后把 329 张 WebP 迁到 `assets/resources/images/equipment/` 与 `assets/resources/images/physical-equipment/`，并将两个图片解析器改为复用 `resolvePresetResourceUrl()` 的确定性路径

# Blockers
- 无 stash、无已知阻塞；登录主题真实 Electron 媒体播放与重启持久化仍是待补手工 UAT，不阻塞图片路径专题

# Key Decisions
- 统一目标只能是 `assets/resources/images`；不要把 1132 个预置资源反向塞入 `src/assets`
- 本专题只收口系统业务预置图片；未来若有组件级 UI 图标/装饰图，仍可保留在 `src/assets`
- 目标结构固定为 `images/equipment/` 与 `images/physical-equipment/`；运行时统一走 `resource://images/...`
- `package.json` 已整体复制 `assets/resources`，预计无需改打包配置；标准器材封面按 `legacyId` 或 `domain + resourceCode` 动态推导，预计无需 DB schema 迁移
- 实施前仍需抽查存量 `cover_image` / `suggestedAssetPath`；旧路径若只属诊断字段，更新生成链与当前参考数据，不为历史文档强造兼容层
- 同步范围至少包括图片解析器、`resource-cover.ts`、`Records.vue`、physical-equipment 路径生成器、图片映射脚本/CSV 和当前有效说明文档
- 这是打包资源改动；除 `type-check` / 契约测试 / `build:web` 外，完成前必须检查 Electron 打包目录中的新文件位置并做器材列表、训练计划、记录页 smoke

# Relevant Files
- src/assets/images/equipment/images.ts
- src/assets/images/physical-equipment/images.ts
- src/utils/preset-resource.ts
- src/utils/resource-cover.ts
- src/views/equipment/Records.vue
- src/database/physical-equipment-parser.ts
- scripts/export-equipment-image-map.mjs
- docs/references/physical-equipment/image-filename-map.csv
- docs/references/physical-equipment/README.md
- package.json
- electron/main.mjs
