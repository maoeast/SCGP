---
status: active
last_updated: 2026-04-08
scope: emotional module — emotion_scene 初始 80 场景 seed canonicalization + 顶层场景图路径收口
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**情绪行为模块当前已完成 `emotion_scene` 初始 80 场景 seed canonicalization，并把顶层场景图路径收口到稳定的预置资源链路。**

- **本次已完成**:
  - `emotion_scene` 当前唯一 canonical seed 已切到：
    - `docs/references/current-emotion-scenes-export.json`
  - 已删除重复旧副本：
    - `docs/references/emotion-scene/current-emotion-scenes-export.json`
  - `emotion_scene` 80 条场景已完成数据清洗：
    - 保留 80 条顶层场景图
    - 删除 `prompts.options[].imageUrl`
    - 删除 `solutions[].imageUrl`
    - 不再要求 539 个嵌套小图标文件
  - `emotion_scene` 顶层 `imageUrl` 已统一改成逻辑预置资源路径：
    - `images/emotional-scenes/<file>`
  - 80 张顶层场景图已移到：
    - `assets/resources/images/emotional-scenes/`
  - 已新增共享预置资源解析层：
    - `src/utils/preset-resource.ts`
  - 以下代码链已接入新路径策略，开发态和打包 Electron 都不再依赖裸 `/assets/scenes/...`：
    - `src/database/emotional-resource-data.ts`
    - `src/views/resource-center/editors/emotional-resource-contract.ts`
    - `src/utils/resource-cover.ts`
    - `src/views/emotional/SceneSelector.vue`
    - `src/components/emotional/engine/SceneSupportPanel.vue`
    - `src/views/resource-center/TrainingResources.vue`
    - `src/views/admin/ResourceManager.vue`
    - `src/views/plan/PlanList.vue`
  - `electron-builder` 已新增 `extraResources`，会把 `assets/resources` 一起带进安装包
  - 已验证：
    - canonical JSON 仍为 80 条
    - 80 条 `emotion_scene.imageUrl` 已全部改为 `images/emotional-scenes/...`
    - 旧 `/assets/scenes/...` 引用已归零
    - 新目录与 JSON 文件名逐条命中，缺失数为 0

- **当前仍未完成但已明确的问题**:
  - `care_scene` 顶层图片路径还没收口到同一套预置资源策略
  - `care_scenes_database.json` 仍指向旧 `/assets/care_scenes/*.jpg`
  - 当前本地 `assets/care_scenes/` 是原始素材目录，且文件名与 `care_scenes_database.json` 并不一一匹配，60/60 现有 JSON 路径都无法直接命中本地文件
  - 之前未完成的 `care_scene` AI 配图端到端联调也仍然未跑完

## 下一步

**第一个动作**: 先处理 `care_scene` 顶层图片债，建立 `care_scenes_database.json -> 本地原始素材` 的文件名映射表，然后把 `care_scene` 也切到：
1. 统一逻辑路径：`images/care-scenes/<file>`
2. 统一预置资源目录：`assets/resources/images/care-scenes/`
3. 再进入运行时验证 `care_scene` 训练页是否能稳定显示顶层场景图

## 遇到的问题 / 阻塞 / 风险

1. **仓库级 type-check 仍被旧问题阻塞**：
   - `src/components/emotional/games/EnergyBallGame.vue`
   - `src/components/emotional/games/VisualSupportOverlay.vue`
   - `src/components/login/StarfieldTunnel.vue`
   - `src/composables/useEmotionDetector.ts`
   这些错误不是本轮引入，但仍然阻塞“本轮代码全仓绿灯”验证
2. **`care_scene` 顶层图片当前仍是未收口债**：
   - JSON 路径与本地素材文件名不匹配
   - 本轮没有把 `assets/care_scenes/` 直接提交为正式结果，避免把未规范化素材冻结进主线
3. **`docs/` 当前只是构建期 seed 来源，不是安装包运行时依赖**
   - `docs/references/current-emotion-scenes-export.json` 的内容会在构建时被打进前端 bundle
   - 真正运行时依赖的是打包进安装包的 `assets/resources`
4. **当前只收口了 `emotion_scene`，没有顺手重写 `care_scene`**
   - 两条资源链当前仍不完全一致
   - 下一轮如果继续做情绪场景资源收口，应优先把 `care_scene` 追平

## 本次对话做出的关键决策及原因

1. **`emotion_scene` 唯一 canonical seed 统一使用 `docs/references/current-emotion-scenes-export.json`**
   - 原因：仓库里此前存在两个同名不同内容的 JSON，容易造成双事实源和后续脚本口径漂移

2. **不再保留 `prompts.options[].imageUrl` 和 `solutions[].imageUrl`**
   - 原因：当前训练 runtime 并不消费这些嵌套图片；继续保留只会引入 539 个无效图标资源和长期维护债

3. **顶层场景图统一改成逻辑预置资源路径，而不是裸 `/assets/...` 绝对路径**
   - 原因：逻辑路径可以通过共享解析层同时兼容开发态与打包 Electron，避免 `file://` 环境下路径失效

4. **预置资源统一走 `assets/resources` + `resource://` / Vite 静态服务链路**
   - 原因：这与现有资源系统设计一致，也让情绪场景图和其他预置资源共用同一套打包/解析规则

5. **本轮不直接提交 `assets/care_scenes/` 原始素材目录**
   - 原因：该目录仍是未规范化本地素材，文件名与 JSON 未对齐；现在提交只会把未完成状态固定为新技术债
