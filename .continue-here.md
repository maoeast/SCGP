---
status: active
last_updated: 2026-04-06
scope: emotional module — new "表情能量球" face-emotion mini-game
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**情绪调节模块新增"表情能量球"小游戏（MediaPipe 面部表情识别）**

- **本次已完成**:
  - 新增 `@mediapipe/tasks-vision` 依赖
  - 创建 `src/types/emotional/face-emotion.ts` — TypeScript 接口（FaceLandmarkPoint、BlendshapeScores、EmotionScores、EmotionBaseline 等）
  - 创建 `src/composables/useEmotionDetector.ts` — 核心 composable，处理 MediaPipe FaceLandmarker 实例化、视频帧检测、情绪分数计算（Happy/Surprised/Angry/Neutral 基于 blendshapes 权重公式）、基线校准
  - 创建 `src/components/emotional/games/VisualSupportOverlay.vue` — SVG 覆盖层，在面部关键点渲染动画视觉引导（嘴角发光点、张嘴虚线圈、眉间箭头、呼吸环）
  - 创建 `src/components/emotional/games/EnergyBallGame.vue` — 完整游戏组件（摄像头 + 检测器 + 覆盖层 + 能量球累积 + 3 关卡 + 无惩罚机制）
  - 创建 `src/views/emotional/games/EnergyBallPage.vue` — 页面包装器（复用 GameContainer）
  - 复制 MediaPipe WASM 文件到 `public/models/wasm/`
  - 下载 `face_landmarker.task` 模型到 `public/models/`（3.6MB）
  - 修改 `src/types/emotional/games.ts` — 新增 `G08_ENERGY_BALL` 游戏代码
  - 修改 `src/data/emotional-game-catalog.ts` — 新增"表情能量球"目录条目
  - 修改 `src/router/index.ts` — 新增 `/emotional/games/energy-ball` 路由
  - 修改 `src/database/schema/emotional-schema.sql` — CHECK 约束加入 `G08_ENERGY_BALL`
  - 修改 `src/database/emotional-games-api.ts` — TITLE_MAP 加入 `G08_ENERGY_BALL` + 运行时 `ensureGameCodeConstraint()` 迁移函数（INSERT 前探测约束，如不支持新 code 则自动重建表）
  - 修改 `src/database/init.ts` — 添加 `migrateEmotionalGameCodeConstraint()` 初始化迁移（作为备份）

- **修复的运行时问题**:
  1. WASM 404 → 复制文件到 `public/models/wasm/`
  2. 摄像头错误静默 → 添加 `cameraError` 状态 + 用户可见错误卡片 + 重试按钮
  3. MediaPipe INFO 日志被误认为错误 → 添加加载状态提示 UI
  4. CHECK constraint violation → 两层修复：schema SQL + 运行时 `ensureGameCodeConstraint()` 在 INSERT 前自动迁移

## 下一步

**第一个动作**: 运行 `npm run electron:dev`，进入表情能量球游戏完整流程验证：
1. 摄像头启动 → 校准（3 秒基线）→ 三关（开心/惊讶/生气）→ 完成保存
2. 重点确认 `ensureGameCodeConstraint()` 在已有数据库上是否正确执行
3. 如保存成功，检查数据库中是否有 `G08_ENERGY_BALL` 记录

## 遇到的问题 / 阻塞 / 风险

1. **运行时迁移尚未验证** — `ensureGameCodeConstraint()` 逻辑已写入但本次对话未完成验证
2. **校准→游戏自动过渡** — `watch(isCalibrating)` 在校准完成后自动调用 `startPlaying()`，但未在运行时验证
3. **MediaPipe 精度** — 当前使用 CPU delegate（XNNPACK），GPU delegate 在 Electron 环境下可能不可用，性能需实测
4. **离线模型** — `face_landmarker.task` 已放入 `public/models/`，但 Electron 打包时需确认资源目录包含此文件
