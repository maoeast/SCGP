# Current State
- 任务：按优先级推进 PRD 未实现项（backlog：`docs/reports/2026-07-15-unimplemented-gap.md`）。
- 已完成：①盘点沉淀 backlog 文档（`4da37ad`）；②P1 首项 A6 资源收藏闭环实装——`resource-api` 激活 favoritesOnly 查询 + `toggleFavorite`/`getFavoriteResourceIds`，`ResourceSelector` 加星标 + 「仅看收藏」+ loadData 注入 userId 回填，`module.ts` 加 `userId`/`isFavorite`；type-check ✅（`f540805`）；③P1 次项 A3 经 `backup.ts` 核验**已闭环**（2026-04-03 `8eb7e6e` 动态全表枚举），gap 报告/AGENTS §3 关于 A3 的描述过时，无需改动（backlog 回填 `55ab017`）。
- 产出物：`docs/reports/2026-07-15-unimplemented-gap.md`（living backlog）；commits `4da37ad`/`f540805`/`55ab017`，main ahead origin/main **3，未 push**。

# Next Action
- 用户侧（原子）：A6 真机验证——Electron 联调，在训练计划/器材快捷/游戏大厅选资源时测：①星标收藏写入 ②「仅看收藏」筛选 ③收藏状态回显。
- 代码侧：继续 backlog 推进，方向待用户定——**A4** 资源文件备份+生命周期（中）/ **P2** 注册表驱动+模块注册（大改先计划）/ 顺手项 **A7**命名+**A8** devtools（小）。

# Blockers
- A6 真机 E2E 未跑（type-check 过 ≠ 运行时收藏/筛选可用），不计「已完成」。
- 下一步方向未定（用户中断了方向选择）。

# Key Decisions
- 收藏 UI 落点 = `ResourceSelector`（当前无面向终端用户的统一资源浏览页，`TrainingResources` 是管理员 CRUD）；`TeachingMaterials` 收藏是教具专用（`teaching_material_favorite`），不冲突。
- `backup.ts` 动态全表枚举（`sqlite_master`）= schema 全覆盖；资源**物理文件**未含在备份内（属 A4）。

# Relevant Files
- 推进总表：`docs/reports/2026-07-15-unimplemented-gap.md`
- A6 改动：`src/database/resource-api.ts`、`src/components/resources/ResourceSelector.vue`、`src/types/module.ts`
- A3 核验：`src/utils/backup.ts`
