# Current State
- 任务：接通情绪模块沉浸式训练的正式进度记录链路，覆盖 `emotion_scene` 与 `care_scene`。
- 已完成：`care_scene` 与 `emotion_scene` 结束页保存已接入 `training_records`、`training_session`、`emotional_training_session`、`emotional_training_detail`、`report_record`；错误尝试与最终答案会记录具体选项；`npm run type-check` 与 `npm run build:web` 均通过。
- 产出物：`src/stores/useTrainingStore.ts`；`src/components/training/TrainingSession.vue`；`src/features/emotional/immersive/compileCareSceneImmersive.ts`；`src/database/api.ts`

# Next Action
- 重启或刷新 Electron 后，从“情绪与场景”选择页重新完成一次训练，确认结果页显示“已保存”且记录出现在“训练记录 -> 情绪调节 -> 游戏训练记录”。

# Blockers
- Electron 热更新可能保留旧 Pinia/路由运行态，尚未完成实机重新验证。

# Key Decisions
- `emotion_scene` 与 `care_scene` 共用 `EmotionalTrainingRecordAPI.persistSession()` 写正式训练记录。
- 从选择页进入的 `emotion_scene` 必须携带 `studentId + resourceId + sceneCode` 才写正式记录；缺上下文时保留旧 prototype 兼容路径。
- 错误点击也作为 detail 明细记录，不只保存最终正确/可接受答案。

# Relevant Files
- `src/stores/useTrainingStore.ts`
- `src/components/training/TrainingSession.vue`
- `src/components/training/ResultStep.vue`
