# Current State
- 任务：继续验证并收尾 SCGP 自理训练 31 条内置任务 seed，确保新库初始化后默认可见且开发环境默认覆盖。
- 已完成：`src/data/self-care-task-seed-inventory.json` 已成为正式内置 seed 数据源；`src/data/self-care-task-seed.ts` 已实现 31 条任务标准化与 seed 模式解析；`src/database/init.ts` 已接入 `upsertSelfCareTaskSeedResources()`，新库初始化后自动写入，已有库按 `legacyId + legacySource + legacyTaskCode` 同步；`scripts/tests/self-care-task-seed.test.mjs` 与 `scripts/tests/self-care-task-init.test.mjs` 已补；窄测试和 `npm run type-check` 已通过。
- 产出物：`src/data/self-care-task-seed-inventory.json`、`src/data/self-care-task-seed.ts`、`src/database/init.ts`、`scripts/tests/self-care-task-seed.test.mjs`、`scripts/tests/self-care-task-init.test.mjs`

# Next Action（必须原子级——第一步可执行的单一动作）
- 在一台空数据库环境启动 SCGP，打开 `/self-care/tasks`，确认 31 条自理任务已自动出现。

# Blockers
- 当前未在真实新库 Electron 环境验证 UI 结果。

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 内置 seed：31 条自理任务改为仓库正式数据源，避免新机器依赖外部 `Self-Care ATS` 项目。
- 开发默认覆盖：开发语义默认 `overwrite`，已有任务命中 seed 时覆盖名称、描述、封面和 `meta_data.steps`。
- 生产保守同步：保留 `missing-only / preserve` 模式，避免正式环境误覆盖本地已编辑任务。

# Relevant Files（最小集——仅列下一步会用到的）
- `src/data/self-care-task-seed.ts`
- `src/data/self-care-task-seed-inventory.json`
- `src/database/init.ts`
- `scripts/tests/self-care-task-seed.test.mjs`
- `scripts/tests/self-care-task-init.test.mjs`
