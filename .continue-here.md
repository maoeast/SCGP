---
status: active
last_updated: 2026-04-09
scope: emotional module — 情绪场景训练全屏沉浸式重构 Phase 6 结果页/奖励层/教师控制已落地；当前转入 Step 1 页面结构与儿童触摸交互的最终视觉打磨；care_scene 仍未开始代码实施
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**情绪场景训练全屏沉浸式重构已从 Phase 1~5 推进到 Phase 6：原型数据层、Pinia 状态机、TTS/SFX 抽象、全屏训练壳、Step 1~4 动态答题流都已在仓库；本轮已补上真正的 Step 5 结果页、全屏奖励动效层、教师控制台，并把 `/emotional/emotion-scene` 切到“隐藏侧边栏与顶部栏”的沉浸式应用壳。此后本轮继续进入 UI 精修：顶部进度改成儿童友好的圆点样式、场景名常驻、提示气泡改为机器人伴学助手风格、Step 1 情绪题改成 3×2 规整卡片布局。最新决定是：情绪卡片保持单击即作答，不能再加“确认选择”这类额外步骤。**

- **本轮已完成**:
  - Phase 6 结果页已落地：
    - `src/components/training/ResultStep.vue`
    - `TrainingSession.vue` 已用 `ResultStep` 替换“训练完成占位”
    - `ResultStep` 会在 `onMounted` 自动执行 `store.saveRecord()`
    - 当前结果页已接入：
      - `store.calculateStars()`
      - 逐颗星星的弹出动画
      - `store.exitTraining()`
      - `store.restartTraining()`
  - 全屏奖励层已落地：
    - `src/components/training/FeedbackOverlay.vue`
    - 答对时会显示纯 CSS 奖励动效
    - 奖励主文案卡片已挪到屏幕中央，避免与顶部提示气泡重叠
  - 教师控制台已落地：
    - `src/components/training/TeacherControlPanel.vue`
    - `TrainingSession.vue` 已接入 `Ctrl + Alt + S`
    - 可触发：
      - `store.forceNext()`
      - `store.forceReset()`
      - `store.forceEnd()`
  - `useTrainingStore` 已补齐 Phase 6 所需运行时能力：
    - `showRewardOverlay`
    - 结果页写库防重（同一轮结果页重复挂载不再重复写入）
    - `restartTraining()`
    - `exitTraining()` 当前已真正返回场景选择页
    - `forceReset()` 当前会清掉当前题答案态、解锁输入并刷新卡片视觉状态
  - 训练页已经切到沉浸式应用壳：
    - `src/router/index.ts`
    - `src/views/Layout.vue`
    - `EmotionSceneTraining` 当前通过 `meta.immersiveShell = true` 隐藏侧边栏、顶部栏和页面留白
    - 当前效果是：训练页吃满整个 Electron 应用窗口，而不是嵌在后台管理壳内
  - 提示气泡已完成两轮 UI 精修：
    - `src/components/training/QuestionStep.vue`
    - 当前为机器人伴学助手式横向布局
    - 左侧状态图标已改成内联 SVG，不再使用 emoji
    - 右侧文案支持内容宽度自适应，减少大段右侧留白
  - `SceneIntroStep.vue` 线索区已补稳：
    - 当前会优先使用 `store.scene.tags`
    - 如无有效 tags，则回退到默认 mock clues：
      - `面部肌肉放松`
      - `图书平铺在腿上`
    - 线索会按 800ms 间隔逐个淡入
  - Step 1 页面结构已开始按最新验收口径重构：
    - `src/components/training/TrainingLayout.vue`
      - 顶部 `Step 1~4` 已改为圆点式进度
      - 场景名当前常驻在顶部中央
    - `src/components/training/QuestionPresenter.vue`
      - 左上角 badge 固定为 `STEP n · 听题并作答`
      - 右上角朗读按钮已改为更突出的大图标样式
      - 当前新增 `speechSynthesis` fallback：没有 Edge TTS endpoint 时，浏览器 / Electron 若支持 Web Speech 仍会尝试播报
    - `src/components/training/OptionBoard.vue`
      - 情绪题区域已改成规整 `3 × 2` 卡片网格
    - `src/components/training/ImageOptionCard.vue`
      - 卡片内已去掉“黄色 / 红色 / 紫色”等颜色文字标签
      - 当前改为通过整张卡片的边框和背景高亮来表达情绪颜色
  - 关于“确认选择”已做出最新修正：
    - 本轮中途曾把 Step 1 改成“先选卡片，再点确认按钮”
    - 用户明确反馈这会增加孩子的负担
    - 当前已回退为：**单击卡片即作答**

- **当前仍未完成**:
  - Step 1 页面结构仍在最后一轮视觉打磨中：
    - 虽然 `3 × 2` 卡片网格、顶部圆点进度、场景常驻、朗读按钮都已进代码
    - 但最新一版还没有完成用户的最终视觉验收
  - Step 2~4 仍未完成同等粒度的视觉验收：
    - 顶部壳和题干区已统一
    - 但是否满足最终儿童触摸体验，还需要继续 live preview
  - TTS 仍未完成仓库内已验证的正式后端对接：
    - `EdgeTTS` 依旧只有前端抽象
    - 当前只是新增了 `Web Speech` fallback 作为过渡兜底
  - `saveRecord()` 仍只写入独立原型库 `training_records`
    - 尚未和当前产品主线训练记录系统收口
  - `care_scene` 沉浸式改造仍未开始代码实施：
    - 当前仍在旧 `EmotionalInteractionEngine` 链路
    - 仅有文档方案与执行计划

## 下一步

**第一个动作**: 重新进入 `emotion_scene` 的 Step 1 页面做一轮真实运行时预览，首先确认“确认按钮已移除、单击卡片即作答”后的最新结构是否满足用户目标分辨率下的触摸体验；如果还有问题，优先继续微调 `TrainingLayout.vue` / `QuestionPresenter.vue` / `OptionBoard.vue` / `ImageOptionCard.vue` 的留白、卡片尺寸和题干区比例。

## 遇到的问题 / 阻塞 / 风险

1. **仓库级全量 `type-check` 仍被历史旧错误阻塞**
   - 位置：
     - `src/components/emotional/games/EnergyBallGame.vue`
     - `src/components/emotional/games/VisualSupportOverlay.vue`
     - `src/composables/useEmotionDetector.ts`
   - 影响：
     - 当前无法用“全仓绿灯”证明本轮新增训练组件完全无 TS 问题
   - 现实：
     - 本轮新增训练文件没有进入当前错误清单

2. **`EdgeTTS` 当前仍没有仓库内已验证的后端端点**
   - 当前处理：
     - `QuestionPresenter.vue` 优先尝试 `EdgeTTS`
     - 无 endpoint 时回退到 `speechSynthesis`
   - 风险：
     - `speechSynthesis` 的声音可用性、音色和时延依赖设备 / Electron 环境，不能等同于正式交付方案

3. **当前“全屏”是“吃满整个应用窗口”，不是训练页自行控制系统级窗口全屏**
   - 当前处理：
     - 通过 `meta.immersiveShell = true` 隐藏应用壳
     - Electron 主窗口本身依赖既有 `fullscreen: true`
   - 风险：
     - 当前没有为训练页单独新增 IPC 去强制切换窗口级全屏

4. **Step 1 页面正在做截图驱动的 live preview 微调**
   - 当前处理：
     - 已按用户截图持续迭代顶部进度、场景名、朗读按钮、提示气泡和卡片布局
   - 风险：
     - 仍可能继续出现“比例不舒服 / 留白不对 / 触摸负担过高”的细节反馈

5. **当前答题 SFX 仍是原型级内置音**
   - 当前处理：
     - `training-feedback-sfx.ts` 继续使用 data URI 短提示音
   - 风险：
     - 未来若要统一资产风格、音量和资源管理，还需要切到正式音频文件

6. **`care_scene` 改造若提前启动，仍会把训练底座问题和模块语义问题混在一起**
   - 当前处理：
     - 继续维持“先稳住 emotion_scene，再进 care_scene”的顺序
   - 风险：
     - 若跨模块并行修改，容易把 UI 底座和语义建模的边界打乱

## 本次对话做出的关键决策及原因

1. **Phase 6 先落真正的结果页、奖励层和教师控制，再继续视觉打磨**
   - 原因：
     - 没有结果页、奖励层和教师干预，整条沉浸式训练链仍不完整
     - 先补功能闭环，再做像素级打磨更稳

2. **结果页必须在挂载时自动保存，并且要防止重复写库**
   - 原因：
     - 用户明确要求 `onMounted` 自动写入成绩
     - 同时必须避免结果页重复挂载时产生多条重复记录

3. **训练页应通过应用壳层隐藏侧边栏与顶部栏来实现沉浸式显示**
   - 原因：
     - 用户要的是整个训练场景吃满显示区域
     - 当前仓库最稳的实现路径是通过 `Layout.vue` 根据路由 meta 隐藏应用壳，而不是额外拆一个新窗口流程

4. **奖励主卡片必须放到页面中央**
   - 原因：
     - 之前奖励卡片和提示气泡发生了叠压
     - 把奖励主文案放在中心区，可以和顶部提示语义层彻底分离

5. **提示气泡要改成“机器人伴学助手”对话框样式**
   - 原因：
     - 旧版 toast 排版割裂、可读性弱
     - 当前改成左图标 + 右文案的对话框样式后，更符合儿童教学场景

6. **提示气泡图标必须改为内联 SVG，不能继续用 emoji**
   - 原因：
     - 运行时已实际复现 emoji 乱码 / 方块问题
     - SVG 更可控，也更适合一致性 UI

7. **情绪颜色不该作为需要孩子阅读的文字标签，而应作为卡片高亮反馈**
   - 原因：
     - “黄色 / 红色 / 紫色”文字会增加额外认知负担
     - 颜色应该服务于视觉反馈，而不是让孩子再读一层文案

8. **Step 1 情绪题不能增加“确认选择”按钮**
   - 原因：
     - 用户明确指出“多一个确认步骤就是额外负担”
     - 因此当前决定保留 `3 × 2` 规整卡片布局和高亮反馈，但恢复为单击即作答

9. **题目朗读应优先使用 `EdgeTTS`，但必须有 `Web Speech` 兜底**
   - 原因：
     - 当前仓库没有已验证可用的 TTS 后端端点
     - 如果完全无兜底，用户会持续看到“暂不可播报”，不符合当前验收目标
