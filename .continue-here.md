# Current State
- 任务：继续排查并收口 `emotional` 能力包在授权展示与入口映射中的残留旧口径，确认除已修复的游戏训练/训练记录菜单外不再有“情绪发展已授权但情绪调节显示未授权”的分叉。
- 已完成：
  - 已定位并修复 `游戏训练`、`训练记录` 菜单页的授权 bug：无显式 `entry/module` 参数时，路由守卫不再错误回退到 `sensory`。
  - `src/router/index.ts` 已改为通过 `resolveRouteModuleCode()` 统一解析菜单页模块判权。
  - 已新增 `src/utils/training-route-access.ts`，把菜单页/详情页的模块解析逻辑抽为可测试函数。
  - 已新增回归测试 `scripts/tests/training-route-access.test.mjs`，覆盖 `games/menu`、`training-records/menu`、`emotional-regulation` 详情页 4 个场景。
  - 本轮验证已通过：`node --test scripts/tests/training-route-access.test.mjs`、`npm run type-check`。
- 产出物：
  - `src/router/index.ts`
  - `src/utils/training-route-access.ts`
  - `scripts/tests/training-route-access.test.mjs`

# Next Action
- 检查 `src/views/System.vue` 与授权发码/展示链，确认 `effectiveEntitlements`、`allowedModules` 和用户可见“能力包授权/授权模块”文案不会再把 `emotional` 与旧模块口径混用。

# Blockers
- None

# Key Decisions
- 菜单级授权不再在缺少显式入口参数时默认回退到 `sensory`：避免只买 `emotional` 的机器被旧模块判权误拦截。
- 将路由模块解析抽到 `src/utils/training-route-access.ts`：便于直接做 Node 回归测试，避免只能靠手点复现。
- 本轮只提交授权 bug 修复相关文件：不混入仓库中既有的大量图片与更新配置脏改动。

# Relevant Files
- `src/router/index.ts`
- `src/utils/training-route-access.ts`
- `scripts/tests/training-route-access.test.mjs`
- `src/views/System.vue`
- `license-generator-dist/generate-license.js`
- `license-generator-dist/renderer.js`
