# Current State
- 任务：统一系统预置图片物理路径（本轮已完成，待用户确认是否提交）
- 已完成：将 `src/assets/images` 下 329 张器材 WebP 迁到 `assets/resources/images/equipment/`（62，扁平 `{category}-{id}.webp`）与 `assets/resources/images/physical-equipment/<domain>/`（267，6 个 domain）；两个 `import.meta.glob(eager)` 解析器改为复用 `resolvePresetResourceUrl()` 的确定性 `resource://images/...` 路径 + 静态 key 清单做占位符回退（签名/占位符逻辑不变）
- 兼容核查：`suggestedAssetPath`（仅 parser 写入 meta_data，运行时无读取方）与器材 `cover_image`（图片走 image 函数、不读 cover_image）均为诊断/seed 字段，未做 DB schema/数据迁移，仅更新生成链
- 同步：`physical-equipment-parser.ts` 的 `getSuggestedAssetPath`、`export-equipment-image-map.mjs` 的 `assetPath`、重生成 `image-filename-map.csv`（168 条）、`equipment-data.ts` 注释、两处 src README + FILE_MAPPING + 4 个 domain README + `docs/references/physical-equipment/README.md`
- 验证：`npm run type-check` 通过；`npm run verify:release` 通过（90.9s，build:web 53s + 生产门禁）；`electron-builder --win` 通过（343.8s）
- 打包核对：`release/win-unpacked/resources/assets/resources/images/equipment/` = 62 webp、`.../physical-equipment/`（6 domain）= 267 webp；videos 仍被排除；dist 已无器材 webp（不再编进 bundle）
- 代码层 smoke（jiti，临时脚本已删）：`getEquipmentImageUrl('tactile',1)` → `resource://images/equipment/tactile-1.webp`；`getPhysicalEquipmentImageUrl(...)` → `resource://images/physical-equipment/emotional-regulation/...webp`；缺失 key → SVG 占位符；SMOKE_OK=true
- `git diff --check` 干净；改动 = 2 个新目录（329 webp）+ 329 D + 14 M，无无关文件；`release/`、`dist/` 已 gitignore

# Next Action
- 待用户决定是否提交（建议 message：`feat: 统一系统预置器材图片到 assets/resources 并改用 resource:// 解析`）。桌面 click-through UAT（器材列表 / 训练计划 / 记录页 真实显示图片）仍需用户在 Electron 实机确认——本轮只到代码层 smoke + 打包产物核对

# Blockers
- 无；桌面 GUI 图片真实渲染的人工 UAT 待用户，不阻塞提交

# Key Decisions
- 路径统一目标固定为 `assets/resources/images/{equipment,physical-equipment}/`，运行时统一走 `resource://images/...`；不反向把预置资源塞回 src/assets
- 去掉 `import.meta.glob(eager)` 后用「确定性 resource:// 路径 + 静态 key 清单（纯字符串 Set，不打包字节）做占位符回退」保留原安全网；新增/删除图片需同步更新两份 key 清单（`images.ts` 内）
- 不做 DB schema/数据迁移：suggestedAssetPath / cover_image 是诊断字段
- 打包配置无需改（extraResources 整体复制 assets/resources；resource:// 解析层 getPresetResourceRoot 已指向该处）
- 这是资源模型统一，不是安装包瘦身（两棵目录无字节级重复；图片仅换位置，总量不变）

# Relevant Files
- src/assets/images/equipment/images.ts（解析器 + key 清单）
- src/assets/images/physical-equipment/images.ts（解析器 + key 清单）
- assets/resources/images/equipment/、assets/resources/images/physical-equipment/<domain>/（迁移后图片）
- src/utils/preset-resource.ts（resolvePresetResourceUrl，未改）
- src/utils/resource-cover.ts（调用方，未改）
- src/views/equipment/Records.vue（调用方，未改）
- src/database/physical-equipment-parser.ts（getSuggestedAssetPath 已改）
- src/database/equipment-data.ts（注释已改）
- scripts/export-equipment-image-map.mjs（assetPath 已改）
- docs/references/physical-equipment/{image-filename-map.csv,README.md}
- package.json（build.extraResources，未改，已核实）、electron/main.mjs（resource:// 解析，未改，已核实）
