# Current State
- 任务：AI 智能体 Phase 2 function calling 骨架（含 list_equipment 训练器材查询）代码完成，待提交
- 已完成：渲染端 tool 循环——新建 `src/services/ai-tools.ts`（7 工具：list_students/get_student/search_students/get_assessment/list_training_sessions/list_equipment/get_ai_usage）+ `ai-tool-loop.ts`（`runToolLoop`，MAX 5 轮非流式）；`ai.mjs` 透传 `tools` + 返回 `toolCalls`、`buildMessages` 支持 tool / assistant+tool_calls；`ai.ts` `sendChat` 按 `supportsToolCalls` 分支 + `toolSteps` ref；`AiAssistant.vue` tool 气泡（🔧 步骤）；`env.d.ts` + `src/types/electron.d.ts` 双声明同步。`npm run type-check` 全绿；Phase 2 主链真机已验（用户确认），`list_equipment` 待真机补验
- 产出物：工作区 dirty（新建 2 + 改 5 + `.continue-here.md`），未提交

# Next Action
- 提交 Phase 2：`git add .continue-here.md 会话启动.md electron/handlers/ai.mjs env.d.ts src/types/electron.d.ts src/stores/ai.ts src/features/ai/components/AiAssistant.vue src/services/ai-tools.ts src/services/ai-tool-loop.ts` 后 commit。建议 message：
  ```
  feat(ai): Phase 2 function calling 骨架 + 训练器材查询工具
  - Done: 渲染端 tool 循环 + ai.mjs 透传 tools/toolCalls + 7 工具（list_equipment 复用 EquipmentAPI）+ tool 气泡；type-check 全绿，主链真机已验
  - Next: Phase 3 图片上传 vision
  - Risk: list_equipment 待真机补验；Phase 3 真机受豆包 vision Key 阻塞
  ```
- 提交后启动 Phase 3（图片上传 vision，详见 plan §Phase 3）

# Blockers
- Phase 3 真机需豆包 vision 模型（火山方舟 Key + 推理接入点 ID `ep-xxx`，未配）
- `list_equipment` 真机补验（代码层 type-check 已过）

# Key Decisions
- tool 执行循环放渲染进程（Main 只透传 tools + 解析 tool_calls，零数据层改造，不破「明文 Key 只在 Main」）
- 首版 tool 循环全非流式（纯问答 `supportsToolCalls=false` 保留流式零回归）；`get_assessment`/`list_equipment` 轻量复用 `ReportAPI.getReportList` / `EquipmentAPI.getEquipment`，不做 18 量表深度分发
- tool 过程不入库（`ai_chat_message` 只存 user/assistant 可见 content）

# Relevant Files
- 提交：`src/services/ai-tools.ts`、`src/services/ai-tool-loop.ts`、`electron/handlers/ai.mjs`、`src/stores/ai.ts`、`src/features/ai/components/AiAssistant.vue`、`env.d.ts`、`src/types/electron.d.ts`
- Phase 3 依据：`C:\Users\maoea\.claude\plans\purring-riding-pie.md`（§Phase 3）
