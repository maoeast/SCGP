# Current State
- 任务：认知发展（cognitive）模块游戏数为 0 → K03_PATTERN_NEXT（模式补全，P0 首样板）已实现 + 静态验证 + 实机 UAT 全通过。
- 已完成：PRD §7 第 0 条决策固化（IEP 级可追踪）+ K03 全链路（registry + PatternNextGame + PatternNextPage + router + cognitive-games-api + GameContainer dispatch）+ size 差异调优（`SIZES = [0.5, 0.85, 1.15]`）。用户实机 UAT 确认 PROGRESSIVE_SIZE 关卡与 size 干扰项差异改善到位。
- 验证：`npm run type-check` ✓、`npm run build:web` ✓、实机 UAT ✓（用户确认）。
- 产出物：3 新文件（`PatternNextGame.vue` / `cognitive-games-api.ts` / `PatternNextPage.vue`）+ 3 修改（`custom-game-registry.ts` / `router/index.ts` / `GameContainer.vue`）+ PRD/handoff 文档；本地领先 origin 1（`cec4a9e`）+ 本次全部改动未提交。

# Next Action
- 待用户决定是否提交/push（本地领先 origin 1 `cec4a9e` + 本次 K03/PRD/size 改动）。
- 提交后：复制 K03 节奏推 P0 其余游戏 K01_MEMORY_MATCH / K04_ODD_ONE_OUT / K05_NUMBER_SENSE（各自进 Plan 模式）。
- IEP 报告消费端（`generateCognitiveReport` 等 4 处）作为后续专题（actual_params 已留好，零迁移接入）。

# Blockers
- 无。UAT 已通过。

# Key Decisions
- §7 第 0 条定位 = B（IEP 级可追踪）。
- 库表方案 B：cognitive 走 training_session，`source_table='cognitive_game_inline'` 合成源（不建表），actual_params 显式透传。
- GameContainer moduleCode dispatch 防 emotional 串台。
- size 三档 `[0.5, 0.85, 1.15]`（UAT 确认差异到位）；非 size 规律候选 size 固定 0.85 是有意设计（避免 size 干扰规律判断）。
- IEP 报告消费端 4 处本次不做（Option 1）。
- entryPath 沿用 `/emotional/games/pattern-next`（方案 A）。

# Relevant Files
- `src/components/emotional/games/PatternNextGame.vue`（K03 主玩法，含 SIZES）
- `src/database/cognitive-games-api.ts`（cognitive 落库）
- `src/views/emotional/games/PatternNextPage.vue` / `GameContainer.vue`（dispatch）
- `src/data/custom-game-registry.ts`（K03 条目）/ `src/router/index.ts`
- `docs/planning/2026-07-19-cognitive-games-prd.md`
