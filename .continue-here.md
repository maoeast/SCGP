# Current State
- 任务：完成 self-care 全屏训练执行页 `TaskExecution.vue` 的信息架构与宽屏布局优化。
- 已完成：`TaskExecution.vue` 已改为全视口流式布局；右栏固定宽度；左舞台占满剩余空间；右栏顶部整合学生信息与“切换”按钮；左上角仅保留眉题和任务名；移除左侧学生/进度副标题与主图重复短标题标签；步骤导航加宽并放宽短标题裁剪；右栏间距压缩且备注区弹性增高；保留原保存、上一步、中断、完成与跳步保存语义。
- 产出物：`src/views/self-care/TaskExecution.vue`

# Next Action（必须原子级——第一步可执行的单一动作）
- 在 27 寸触摸屏或等效宽屏视口打开 self-care 训练执行页，检查右栏顶部学生信息加入后教师备注框实际可用高度。

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 右栏固定宽度、左栏流式填满：解决宽屏两侧大面积留白。
- 学生上下文归属右栏：让右侧成为面向当前学生的记录操作面板。
- 左侧只保留任务标题和主图：步骤进度交给底部导航与图片说明条，减少重复信息。

# Relevant Files（最小集——仅列下一步会用到的）
- `src/views/self-care/TaskExecution.vue`
