# Current State
- 任务：评估入口展示与直链授权重构代码已完成，待做 Electron 授权组合冒烟验证。
- 已完成：新增 `src/features/assessment/assessment-scale-catalog.ts` 统一量表目录；`src/views/assessment/AssessmentSelect.vue` 改为 6 个固定 Tab + 按授权过滤卡片；`src/views/assessment/SelectStudent.vue` 改为复用共享目录；`src/router/index.ts` 为 `/assessment/select-student` 和 `/assessment/unified/:scaleCode/:studentId` 增加量表合法性与授权拦截；`npm run type-check` 已通过。
- 产出物：`src/features/assessment/assessment-scale-catalog.ts`、`src/views/assessment/AssessmentSelect.vue`、`src/views/assessment/SelectStudent.vue`、`src/router/index.ts`

# Next Action
- 启动 Electron 开发环境后，验证仅 `life_skills` 授权时 `/assessment` 只显示 `WeeFIM / S-M`，且未授权量表直链会回退 `/assessment`。

# Blockers
- None

# Key Decisions
- `/assessment` 按跨模块公共评估入口处理：不再使用 `meta.moduleCode = 'sensory'` 做整页拦截。
- 量表展示与授权拆开：`entryTabs` 只用真实 `TRAINING_ENTRY_CODES`，`accessModulesAnyOf` 只用顶层业务模块。
- 本轮不改 `AssessmentContainer` 内部流程、评分逻辑和报告页授权。

# Relevant Files
- `src/features/assessment/assessment-scale-catalog.ts`
- `src/views/assessment/AssessmentSelect.vue`
- `src/views/assessment/SelectStudent.vue`
- `src/router/index.ts`
