---
status: active
last_updated: 2026-04-06
scope: emotional module — care_scene 表达关心训练引导区改版与元数据自动生成
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**情绪行为模块 `care_scene / 表达关心` 训练体验改版**

- **本次已完成**:
  - 将情绪训练共用引擎改为“大场景图常驻 + 右侧任务区切换”布局：
    - 新增 `src/components/emotional/engine/SceneSupportPanel.vue`
    - 修改 `src/components/emotional/engine/EmotionalInteractionEngine.vue`
    - 修改 `src/components/emotional/engine/renderers/SceneIntroRenderer.vue`
  - 将顶部进度从 `1 / N` + 横向进度条改为步骤圆点：
    - 已完成步骤显示绿色勾号
    - 当前步骤使用暖色高亮
  - 为 `care_scene` 新增右侧固定三步引导区：
    - 新增 `src/components/emotional/engine/CareStepGuidePanel.vue`
    - 三步结构为“先观察场景发生了什么 / 想想对方现在的感受 / 想一想怎么说会让对方舒服”
    - 第二步支持 3-5 个动态情绪贴片
    - 下方新增随场景变化的小提示文案区
  - 将 `care_scene` 的“对方 / 听的人”切换为具体名字或角色名：
    - 左侧场景支持区会优先显示 `receiverName`
    - 若未手填，则使用运行时自动推导结果
  - 为 `care_scene` 增加元数据自动生成链路：
    - 新增 `src/features/emotional/care-scene-generated-fields.ts`
    - 自动推导 `receiverName`
    - 自动推导 `emotionChips`
    - 自动推导 `comfortTip`
  - 将自动生成字段接入以下链路：
    - `src/database/emotional-resource-data.ts` 种子资源规范化
    - `src/views/resource-center/editors/emotional-resource-contract.ts` 编辑器规范化
    - `src/features/emotional/adapters/compileCareScene.ts` 训练编译
    - `src/utils/emotional-resource-pack.ts` Excel 导入导出
  - 修改 `src/components/emotional/engine/renderers/CareUtteranceRenderer.vue`：
    - 第三步话术区会消费第二步选中的情绪贴片
    - 在未选情绪贴片时锁住话术选择，强制走“看场景 → 想感受 → 说关心”
  - 修改 `src/components/emotional/engine/renderers/ReceiverPreferenceRenderer.vue`：
    - 反馈文案会带入 `receiverName` 和第二步已选的情绪贴片
  - 修改 `src/components/emotional/CareOptionCard.vue`，将 hover 视觉从偏冷蓝调整为更暖的橙杏色

- **当前尚未完成但已写入工作区**:
  - `care_scene` 新引导区尚未做运行时实机验证
  - 右侧引导区虽然已按 3 步展示，但底层 runtime 仍保留 `receiver_preference` 独立步骤，尚未重构为“第 3 步内反馈闭环”
  - 自动生成字段已接入运行时和导入导出链路，但还没有写“一次性 backfill 现有 care_scene meta_data”的脚本

## 下一步

**第一个动作**: 运行 `npm run electron:dev`，进入任意 `care_scene / 表达关心` 场景做完整 UI 联调：
1. 确认左侧大图在全程训练步骤中常驻显示
2. 确认顶部圆点进度和右侧三步引导区状态切换正确
3. 确认第二步情绪贴片可点击且会锁定/解锁第三步话术选择
4. 确认 `receiverName / emotionChips / comfortTip` 在未手填时能从旧 `care_scene` 资源自动生成并正确展示

## 遇到的问题 / 阻塞 / 风险

1. **运行时尚未验证** — 本轮 UI 和交互改动已写入代码，但还没有在 Electron 界面里走过一遍真实场景
2. **repo 级 type-check 仍被旧问题阻塞** — `npm run type-check` 当前仍报：
   - `src/components/emotional/games/EnergyBallGame.vue`
   - `src/components/emotional/games/VisualSupportOverlay.vue`
   - `src/composables/useEmotionDetector.ts`
   这些错误不是本轮改动引入，但会继续阻塞仓库级绿灯验证
3. **3 步引导与底层 4 段 runtime 仍有语义偏差** — 现在引导区是 3 步，但底层仍有 `scene_intro -> care_utterance -> receiver_preference` 独立完成态；后续最好把 `receiver_preference` 收口成第 3 步的反馈环节
4. **自动生成字段目前是运行时生成优先** — 旧 `care_scene` 资源即使数据库里还没存 `receiverName / emotionChips / comfortTip`，页面仍能显示；但如果要长期稳定、便于人工维护，后续仍建议补一份 backfill 脚本把这些字段回写进 `meta_data`
5. **工作区里当前代码改动尚未提交** — 本轮代码会先以 stash 形式保存，等待下一轮继续联调和决定是否正式提交

## 本次对话做出的关键决策及原因

1. **场景图必须常驻，而不是只在第 1 步显示**
   - 原因：目标用户是特殊儿童，稳定的视觉锚点和持续可见的场景图能明显降低工作记忆负担

2. **`care_scene` 采用固定三步引导，而不是继续只用纯文字说明**
   - 原因：三步卡片 + 当前高亮 + 图标 + 情绪贴片比纯文字更适合低龄和阅读负担较高的儿童

3. **`receiverName / emotionChips / comfortTip` 采用“手工优先，缺失时自动生成”**
   - 原因：现有场景数据已包含足够语义信息，自动生成能立刻提升旧资源可用性，同时保留后续人工精修空间

4. **第二步情绪贴片作为进入第三步的话术前置条件**
   - 原因：把“先理解感受，再表达关心”的训练顺序固化下来，避免孩子直接跳到话术选择

5. **本轮结束时不直接提交代码，而先 stash 代码改动、单独提交 handoff 文档**
   - 原因：本轮 UI 尚未实机联调，不适合在缺少运行时验证的情况下把代码改动直接推到远端主分支
