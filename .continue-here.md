# Current State
- 任务：月龄-年级核对（软提示，不对称阈值）+ 学年中途调班功能 + 业务库跳级数据修复——三项全部完成、评审收敛、待提交
- 已完成：
  ① src/types/class.ts 纯函数组（getAgeAtAcademicYearStart/getExpectedGradeLevel/checkGradeAgeMatch）：义务教育 6 周岁入学、8/31 资格日口径（月份按 birthday.slice(5,7) 取，防负时区漂移）；**阈值不对称（用户 2026-09-19 拍板）：跳级 gap≥+1 即提示"疑似跳级"，留级 gap≤-2 才提示**（留 1 级属特教常态）
  ② StudentDetail.vue 班级 pill 下软提示行（按 current_class_id 反查年级+学年，老师看不到班级时降级无提示）；StudentClassAssignment.vue 分班对话框 el-alert（单个分班时）
  ③ 学年中途调班：assignStudentToClass 同学年已有分班 → UPDATE 现有历史行 + **显式人数对账**（真实 schema 触发器只盖 INSERT 与 UPDATE OF is_current，不盖 UPDATE OF class_id；源班 -1 仅在 is_current===1 时执行——已关闭行重开防幻影扣减）；已关闭行重开清 leave 痕迹；同班重复（current 行）抛"学生已在该班级中"；UI 批量/单个分支均 await、批量按实际成功数提示
  ④ 业务库修复（AppData/Roaming/scgp/database.sqlite，备份 database.sqlite.bak-20260919）：种子按自然年定年级致 11 名 9-1 月出生学生系统性跳 1 级——已按同班号惯例全部下修一级，事务内完成，复验跳级 0 人、人数守恒 19→19
  ⑤ 验证：type-check 0 error；tests/grade-age-check.test.ts 19/19；tests/class-midyear-transfer.test.mts 3/3
- 产出物：6 文件待提交（class.ts / class-api.ts / StudentDetail.vue / StudentClassAssignment.vue / 两个测试）+ .continue-here.md

# Next Action
- 用户新会话第一任务：生成产品演示用学生数据（2026-2027 学年，幼儿园到 9 年级每班至少 3 名，名字符合出生年代新生儿常见名）——先按每班人数/姓名风格与用户确认方案再写库（写库前确认应用已关闭，参考数据修复流程：备份→事务→复验→人数对账）
- 本任务 commit：等用户验收后说一声（精确 add 6 文件，commit 格式 feat(student): 月龄年级核对软提示+学年中途调班）

# Blockers
- None

# Dead Ends
- jiti 加载 class-api.ts 走不通（数据库初始化链）——测试用源码切片（签名行做锚）+ sql.js 内存库复刻真实触发器集
- 同学年调班走 changeStudentClass 的"关旧插新"必撞 UNIQUE(student_id, academic_year)——只能 UPDATE 现有行；changeStudentClass 仅适用跨学年升级
- 新 Date 解析 "YYYY-MM-DD" 是 UTC，负时区下 getMonth 会漂移——月份取值用字符串 slice(5,7)

# Key Decisions
- 月龄-年级核对阈值不对称（用户拍板 2026-09-19）：跳级（年龄不够读高年级）≥1 级即提示，留级（特教常态）≥2 级才提示；年级按 current_class_id 反查（自定义班级名无全局唯一校验，按名不可靠）；学年用班级自身 academicYear
- 同学年内调班 = UPDATE 现有历史行 + 显式人数对账（触发器缺口 API 层补）；审计取舍：同班号调班不留痕（UNIQUE 约束所限），enrollment_date 更新为调班日期
- 种子数据年级口径教训：按自然年定年级会让 9-1 月出生的学生系统性跳级——生成演示数据时必须用 8/31 资格日口径（可用 src/types/class.ts 的 getExpectedGradeLevel 反推每班生日区间）

# Relevant Files
- src/types/class.ts（核对纯函数组，生成数据时可用 getExpectedGradeLevel 校验年龄-年级匹配）
- src/database/class-api.ts（assignStudentToClass 调班分支 L424-468）
- tests/grade-age-check.test.ts + tests/class-midyear-transfer.test.mts
- scripts/seed-demo-data/data.mjs（既有种子数据定义，参考但本次演示数据可能直接写库）
