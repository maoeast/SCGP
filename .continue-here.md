---
status: active
last_updated: 2026-03-27
scope: training-entry hard-cut unification
---

## Current State

This file only keeps the current valid handoff.
Current continuation should follow code reality, not target-state documents.

Already true:

- equipment internal entry model is now strict on route/query level
- `games / equipment / training-records` menus now use the same 6 internal entries in UI and routing:
  - `sensory-integration`
  - `emotional-regulation`
  - `social-communication`
  - `fine-motor`
  - `soothing-aids`
  - `life-skills`
- `training_records` and `equipment_training_records` schema/init path now include `entry_code`
- new game saves now write:
  - `resource_id`
  - `resource_type`
  - `session_type`
  - `entry_code`
- new equipment quick-entry saves now write `entry_code`
- training-record menu/stat panels now count and filter by `entry_code`, not only top-level `module_code`
- runtime system name `生活自理适应综合训练` has been replaced with `星愿能力发展训练系统`
- physical-equipment import script now dedupes by `resourceCode` and can merge duplicate references into canonical rows

## Confirmed Reality

- top-level authorization still stays on real `moduleCode`
- `fine-motor` and `soothing-aids` are still internal entries, not top-level business modules
- current real game-resource coverage is still limited:
  - sensory game resources exist
  - emotional scene / care-scene resources exist
  - `fine-motor / soothing-aids / social-communication / life-skills` do **not** yet have real game-resource coverage in current code/database
- because old records are disposable, current implementation does **not** try to infer old `module_code` into new `entry_code`

## Local DB Note

- local test DB inspection confirmed physical-equipment seed resources had been duplicated `15x`
- current local Electron process likely rewrote the DB after external script writeback
- do not spend time repairing that local DB by hand
- for dev/test, it is acceptable to:
  - close Electron
  - delete local `database.sqlite`
  - let app/script rebuild from clean state

## Completed This Session

- strict equipment entry parsing:
  - `src/utils/equipment-training-entry.ts`
  - `src/views/equipment/SelectStudent.vue`
  - `src/views/equipment/QuickEntry.vue`
  - `src/router/index.ts`
- entry-based resource filtering:
  - `src/components/resources/ResourceSelector.vue`
- entry-based training launch query propagation:
  - `src/utils/training-launch.ts`
- entry-based game flow:
  - `src/views/games/GameModuleMenu.vue`
  - `src/views/games/SelectStudent.vue`
  - `src/views/games/GameLobby.vue`
  - `src/views/games/GamePlay.vue`
- entry-based training-record flow:
  - `src/views/training-records/TrainingRecordsMenu.vue`
  - `src/views/training-records/ModuleTrainingRecords.vue`
  - `src/views/training-records/components/GameRecordsPanel.vue`
  - `src/views/training-records/components/EquipmentRecordsPanel.vue`
- persistence/schema changes:
  - `src/database/init.ts`
  - `src/database/api.ts`
  - `src/database/emotional-api.ts`
- runtime system-name replacement:
  - `src/router/index.ts`
  - `src/stores/systemConfig.ts`
  - `src/database/init.ts`
- duplicate-safe physical-equipment import logic:
  - `scripts/import-physical-equipment-resources.cjs`

## Remaining Work

- decide hard-cut handling for old records:
  - physically delete old game/equipment/training records, or
  - run one explicit reset script for dev/test DB
- verify `entry_code` behavior against a clean rebuilt local DB, not the currently polluted one
- review `src/views/equipment/Records.vue`
  - it is still old student/module-oriented UI
  - not fully entry-based yet
- review whether detail navigation from `ModuleTrainingRecords` should go to:
  - per-entry record detail page
  - or a rebuilt entry-aware equipment/game record detail flow
- decide whether game menu should:
  - keep showing entries with `0` game resources
  - or visually mark them as “暂无资源 / 未落地”
- if the product wants true 6-entry game parity, real resources/content still need to be added for:
  - `social-communication`
  - `fine-motor`
  - `soothing-aids`
  - `life-skills`

## Next Action

Start with:

1. close Electron
2. rebuild or reset local dev DB
3. verify new records actually write `entry_code`
4. continue from `src/views/equipment/Records.vue` and any remaining old module-based record/detail flow

## Next Session Read Order

1. `AGENTS.md`
2. `docs/planning/2026-03-23-scgp-context-bootstrap.md`
3. `README.md`
4. `.continue-here.md`
5. `PROJECT_CONTEXT.md` only if broader background is needed

## Next Session Prompt

```text
先按 AGENTS.md 做最小上下文启动：
1. 读取 AGENTS.md
2. 读取 docs/planning/2026-03-23-scgp-context-bootstrap.md
3. 读取 README.md
4. 读取 .continue-here.md
5. 如确实需要更大背景，再读取 PROJECT_CONTEXT.md

然后严格以代码现状为准继续当前未完成工作，不要把目标态当现状。

当前真实状态：
- 顶层授权模型仍按真实 moduleCode，不变
- games / equipment / training-records 的菜单和路由已经切到 6 个 internal training entries
- training_records / equipment_training_records 已加 entry_code，新写入记录会写 entry_code
- 训练记录菜单/面板已按 entry_code 统计和筛选
- runtime 系统名称已改为“星愿能力发展训练系统”
- physical-equipment 导入脚本已支持按 resourceCode 去重并迁移引用

但当前仍未完成：
- 旧记录的硬切处理还没最终收口，可直接物理删除，不做兼容推断
- 必须在干净本地库上验证 entry_code 新链路，不要继续基于被重复导入污染的本地库做判断
- src/views/equipment/Records.vue 仍是旧 student/module 视角，还没完全 entry-aware
- 当前真实游戏资源仍只覆盖 sensory games + emotional scenes/care scenes，不要把 6 组游戏资源写成已完整交付

优先继续：
- 关闭 Electron 后重建/重置本地 dev DB
- 验证新游戏记录、新器材记录是否正确写入 entry_code
- 检查并改造 src/views/equipment/Records.vue
- 继续清理 remaining old module-based record/detail flow

最后至少跑：
- npm run type-check
```
