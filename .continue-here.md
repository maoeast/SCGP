# Current State
- 任务：首页看板下半部三栏重排（含条目摘要化 + 面板头部精简）已全部落地并推送；手册首页族截图与 docx 内嵌图已同步刷新
- 已完成：① 看板三栏 1.15 : 0.95 : 0.95 + 统一固定高度 520px + 面板内滚动（条目不截断、条目收敛为紧凑摘要、日程底部今日训练进度、头部去说明文字）；② 布局真源档整篇改写 + 两份口径档展示层同步 + `docs/INDEX.md`；③ 手册截图 7 张刷新并批准（S008–S011 布局重拍、S001/S002/S007 数据漂移重拍），三项管路校验全绿；④ docx 就地换图 7 张（封面/封底零改动，`--docx` 校验 217/217）；⑤ 全部提交并 push，工作区 clean
- 产出物：提交 `d8d08f9`（看板三栏）/ `589296c`（截图重拍）/ `74ef431`（docx 换图流程入档）；`origin/main = 74ef431b`；手册基线档 §4.3–4.5 批次留痕

# Next Action
- 先读 `src/database/dashboard-api.ts` 的 `getWeeklyTrainingTrend`（约 217–255 行，仍读旧表 `training_records`）与 `src/views/Dashboard.vue:665-674`（`todayTrainingCount` / `todayProgressRatio`），确认日程面板进度条分子是否切到统一训练主表口径，然后把结论与建议摆给用户（现在可能与异常面板的 `training_session` 口径不一致、少计只写统一主表的入口）

# Blockers
- None

# Dead Ends
- 按**行号**批量替换 `Dashboard.vue` 的大段 CSS：行号漂移导致多/少一个闭括号、还误删了 `.schedule-item {` 选择器，浏览器把后续规则整体吞掉（harness 里表现为自带规则全部失效）——必须用内容锚点 + 改完查括号平衡（depth 0 且 min depth 0）
- 用 `min-height: 100%` 让空态撑满固定高度：父级高度不确定时塌成零高，真实环境空态不可见（S011 采集断言失败暴露）——改 `flex: 1 1 auto` 撑满 + 紧凑横幅用 `margin: auto 0` 居中

# Key Decisions
- 看板下半部形态：三栏同行 + 统一固定高度 520px + 面板内滚动（用户拍板，取代早期「页面自然滚动、不加 max-height」的取舍）
- 条目不截断、明细进「查看全部」弹层与学生详情页；**判定口径不动**，只改展示层
- 手册 docx **禁止整份重新生成**（用户手改封面）；刷图走「就地换 word/media 字节」流程（基线档 §1）

# Relevant Files
- src/views/Dashboard.vue（下半部三栏模板 + 样式 + 进度条计算）
- src/database/dashboard-api.ts（`getWeeklyTrainingTrend` / `todayTaskCount` 取数，唯一待确认口径点）
- docs/planning/2026-09-21-首页看板下半部-布局重排.md（布局唯一真源档）
- docs/user-manual/SCGP-用户手册截图采集与维护基线.md（§1 docx 禁区与就地换图流程；§4.3–4.5 批次留痕）
