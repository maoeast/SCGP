---
status: active
last_updated: 2026-04-08
scope: emotional module — 情绪场景训练全屏沉浸式重构 Phase 3 已完成，等待 Phase 4 UI 骨架
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**情绪场景训练全屏沉浸式重构的 Phase 1~3 已完成到仓库：独立原型数据层、核心 Pinia 状态机、TTS / SFX 抽象层均已落地，当前尚未开始 Phase 4 UI 骨架。**

- **已完成**:
  - Phase 1 新增独立原型 Schema:
    - `src/db/schema.sql`
    - 已兼容真实 `emotion_scene` 所需字段：
      - `scenes.scene_code`
      - `description`
      - `difficulty_level`
      - `scene_domain`
      - `age_range`
      - `ability_level`
      - `tags`
      - `recommended_hint_ceiling`
      - `steps.question_id`
      - `options.option_code`
  - Phase 1 新增真实数据迁移脚本:
    - `src/db/migrateLegacyData.ts`
    - 读取 `docs/references/current-emotion-scenes-export.json`
    - 将 80 条真实 `emotion_scene` 转换写入新关系表
    - 自动生成:
      - Step 1: `emotion`
      - Step 4: `response`
    - 保留真实历史标识:
      - `scene_code`
      - `question_id`
      - `option_code`
  - Phase 1 新增最小 SQL.js Hook:
    - `src/db/useDatabase.ts`
    - 默认初始化空库后执行 Schema + 真实数据迁移
  - Phase 1 新增角色名映射文件:
    - `docs/references/emotion-scene-character-names.json`
    - 当前 80 条已生成映射表
    - 已自动预填一版：
      - `67` 条识别出显式名字
      - `13` 条保留默认 `小朋友`
  - Phase 1 新增辅助脚本:
    - `scripts/refresh-emotion-scene-character-names.mjs`
      - 用于后续重刷 `scene_code -> character_name` 映射
    - `scripts/verify-emotion-scene-phase1.mjs`
      - 用于在内存 SQL.js 中验证 Phase 1 数据迁移
  - `package.json` 已新增命令:
    - `npm run refresh:emotion-scene-names`
    - `npm run verify:emotion-scene-phase1`
  - 已验证:
    - `npm run verify:emotion-scene-phase1` 通过
    - 结果为：
      - `80` scenes
      - `240` clues
      - `320` steps
      - `1040` options
      - `0` hints
    - 路径全部为正斜杠 `/`
    - Step 分布正确：
      - `emotion = 80`
      - `reason = 80`
      - `need = 80`
      - `response = 80`
  - Phase 2 新增核心状态机 Store:
    - `src/stores/useTrainingStore.ts`
    - 当前已接入：
      - `currentStepIndex`（`0=引导, 1-4=答题, 5=结算`）
      - `scene`
      - `steps`
      - `hintLevelPerStep`
      - `answers`
      - `inputLocked`
      - `isTransitioning`
      - `isExitModalVisible`
      - `availableTTSEngine`
    - 当前已实现：
      - `currentStepData`
      - `parsedQuestionText`
      - `loadScene(sceneCode)`
      - `nextStep()`
      - `recordError()`
      - `recordAnswer()`
      - `calculateStars()`
      - `saveRecord()`
      - `toggleExitModal()`
      - `exitTraining()`
      - `forceNext()`
      - `forceReset()`（仅重置当前步骤错误计数）
      - `forceEnd()`
    - 已补充约束：
      - 已加 `watch(currentStepIndex, ...)` 占位，后续步骤切换时需中断 TTS
  - Phase 3 新增 TTS / SFX 抽象层:
    - `src/services/tts/ITTSService.ts`
    - `src/services/tts/EdgeTTSService.ts`
    - `src/services/tts/index.ts`
    - `src/composables/useSound.ts`
    - `package.json`
    - `package-lock.json`
    - 当前已落地：
      - `ITTSService` 标准接口
      - `EdgeTTS` 工厂实现
      - `AbortSignal` 中断控制
      - `audioUrl / audioBase64` 双响应兼容
      - `Howler` 全局音效 Hook 的惰性实例化封装
    - 当前验证现实：
      - 新增代码自身已通过本轮修正
      - 仓库级 `npm run type-check` 仍被历史旧错误阻塞，不属于本轮新增

- **当前仍未完成**:
  - Phase 4 UI 骨架尚未开始：
    - `TrainingSession.vue`
    - `TrainingLayout.vue`
    - `SceneIntroStep.vue`
    - `ExitConfirmDialog.vue`
  - TTS 当前只完成接口与 `EdgeTTS` 抽象，尚未真正接入运行时训练页面
  - `Howler` Hook 已落地，但实际 SFX 资源与触发点尚未接线
  - `hints` 表当前只保留结构，尚未导入具体 hint 文本
  - `saveRecord()` 当前仍写入独立原型库的 `training_records`，尚未与现有产品主线运行时串接

## 下一步

**第一个动作**: 开始 Phase 4，先创建 `TrainingSession.vue` 页面容器和 `TrainingLayout.vue` 骨架，把 `useTrainingStore` 的 `loadScene()`、`parsedQuestionText`、`toggleExitModal()` 接到最小可运行 UI 壳子上。

## 遇到的问题 / 阻塞 / 风险

1. **真实 80 场景数据与 PRD 初版关系表并不一一同构**
   - 原因：
     - 当前真实数据是 `sceneCode + metadata JSON` 结构
     - 历史数据原生只包含 `cause / need` 两类 prompt
     - `emotion / response` 需要迁移时动态补步

2. **角色名自动识别只能覆盖一部分场景**
   - 当前结果：
     - `67` 条自动识别出显式名字
     - `13` 条仍是默认 `小朋友`
   - 原因：
     - 部分题干从一开始就没有显式名字，只有“他/她”或无主语问法
   - 处理策略：
     - 后续由 `docs/references/emotion-scene-character-names.json` 手工补齐

3. **`hints` 真实数据当前不存在具体文本**
   - 当前处理：
     - `hints` 表保留结构
     - 迁移阶段不写入数据
     - 后续前端用 `recommended_hint_ceiling` + 通用 hint 话术兜底

4. **仓库级全量 type-check 仍被旧错误阻塞**
   - 旧问题位置：
     - `src/components/emotional/games/EnergyBallGame.vue`
      - `src/components/emotional/games/VisualSupportOverlay.vue`
      - `src/composables/useEmotionDetector.ts`
   - `StarfieldTunnel.vue` 在本轮未再复现，但前三处旧问题仍阻塞“全仓绿灯”

5. **`EdgeTTS` 当前只有前端抽象，没有仓库内现成可调用的后端合成端点**
   - 当前处理：
     - 服务层先以 `endpoint` 配置方式抽象
     - 保留 `healthcheckUrl`、`headers`、`fetchImpl` 注入点
   - 风险：
     - 真正接入页面前，仍需确认 Electron / 本地服务的实际接口契约

6. **Phase 3 只完成媒体层抽象，尚未接 UI 生命周期**
   - 当前处理：
     - Store 里先加 `watch(currentStepIndex)` 的 TTS 停止占位
   - 风险：
     - 如果 Phase 4/5 直接接页面而不统一收口播放时机，容易重新出现串音或音效重复触发问题

## 本次对话做出的关键决策及原因

1. **Phase 2 的 `currentStepIndex` 保持 PRD 口径**
   - 原因：
     - 明确定义为 `0=引导, 1-4=答题, 5=结算`
     - 避免把四个数据库题步误写成整个训练流程的全部状态

2. **`forceReset()` 只重置当前步骤错误计数，不重置整个训练**
   - 原因：
     - PRD 对教师特权的定义是“重置当前步骤”
     - 如果回退到 intro 并清空所有答案，会破坏训练流程语义

3. **在 Store 内预留步骤切换时的 TTS 中断 watcher**
   - 原因：
     - TTS 真实实现虽在 Phase 3，但步骤切换时中断旧语音是状态核心职责
     - 先占位可避免后续 UI 层各自分散处理

4. **Phase 3 先做服务层抽象，不提前写页面接线**
   - 原因：
     - 当前目标是先把媒体能力的接口、生命周期和中断语义定稳
     - 避免在没有 UI 壳子前把播放逻辑散落到组件中

5. **`EdgeTTSService` 兼容 `audioUrl` 和 `audioBase64` 两类响应**
   - 原因：
     - 当前仓库还没有固定的 EdgeTTS 后端契约
     - 先兼容两种常见返回形态，降低后续 Electron / 本地服务对接成本

6. **`useSound` 采用惰性创建 `Howl` 实例**
   - 原因：
     - 先满足全局音效复用和延迟加载
     - 避免页面未实际播放时就提前创建大量音频实例

7. **放弃 mock `seed.sql`，改用真实 80 场景迁移脚本**
   - 原因：
     - 当前仓库已有真实 `docs/references/current-emotion-scenes-export.json`
     - Phase 1 的价值在于验证“真实数据能否无损塞进新表”

8. **保留 `hints` 表结构，但迁移阶段不强制导入 hint 文本**
   - 原因：
     - 当前真实 `emotion_scene` 只有 `recommendedHintCeiling`
     - 没有 prompt 级别的 hint 文本

9. **Phase 1 数据层走独立 `src/db/` 原型路径，不改动当前主线 `src/database/`**
   - 原因：
     - 当前用户只要求先完成 PRD 的 Phase 1
     - 需要避免误伤现有情绪训练主链和现有产品运行时

10. **引入 `emotion-scene-character-names.json` 作为角色名补齐入口**
   - 原因：
     - 自动抽名不可能覆盖全部 80 条
     - 需要一份稳定、可手工维护的 `scene_code -> character_name` 映射

11. **在迁移时统一把题干归一为 `{name}` 占位符**
   - 原因：
     - 新 UI 和后续 TTS 都要求支持动态角色代入
     - 当前真实题干存在“写死人名 / 他 / 她 / 无主语”混合情况，必须在迁移层收口
