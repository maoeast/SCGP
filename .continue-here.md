---
status: active
last_updated: 2026-04-13
scope: 跨训练入口自定义小游戏扩展 Phase 0 规划已沉淀为实施规格；下一步应按 spec 执行注册表与迁移底座
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**当前续接主线已经切换为“跨训练入口自定义小游戏扩展”的 `Phase 0` 实施准备。**

- **本轮已经落地的文档化变更**
  1. 已确认当前代码现实仍是 `emotional-only` 小游戏链路，不是跨入口统一底座。
  2. 已新增专题计划入口：
     - `docs/planning/2026-04-13-cross-entry-custom-games-expansion-plan.md`
  3. 已新增 `Phase 0` 实施规格：
     - `docs/planning/2026-04-13-cross-entry-custom-games-phase0-implementation-spec.md`
  4. 已把任务卡、状态机、迁移风险、校验脚本和验收标准沉淀为可执行文档入口。

## 遇到的问题 / 阻塞 / 风险

1. **当前主要风险不是运行时，而是结构分散**：
   - 注册表、路由、资源 seed、落库、详情页现在仍是多点散落。
2. **当前迁移风险明确存在**：
   - `game_emotion_records` 仍保留只服务 `G08_ENERGY_BALL` 的临时 rebuild 逻辑。
   - `student_badges` 尚无匹配的正式迁移。
3. **当前不能把以下能力写成已实现事实**：
   - 跨训练入口 custom game 正式链路
   - 双人共享场次事务写入
   - 容器统一权限 preflight
   - `user_exit / teacher_exit` 细分记录

## 下一步要做什么

**接下来的第一个动作：按 implementation spec 开始执行 `Phase 0`，不要先写新游戏。**

1. 先做 `注册表与类型基线`：
   - 新增统一 `custom-game-registry`
   - 让 `emotional-game-catalog` 退化成 adapter
2. 再做 `Schema 正式迁移`：
   - 替换当前 `G08` 专用 rebuild 逻辑
   - 补齐 `student_badges` 迁移
3. 再做 `persistSessionGroup()` 契约与事务边界。
4. 在 `Phase 0` 完成前，不进入 `Wave 1` 新游戏实现。

## 本次对话的关键决策与原因

1. **先沉淀实施规格，再进入代码执行**：
   - 原因：`Phase 0` 横跨 schema、API、容器、权限、路由、记录页，不能只靠对话上下文延续。
2. **以代码现实为准，不把规划写成现状**：
   - 原因：当前真实链路仍是 `emotional-only`，必须先把底座收口。
3. **先做底座，不先做新玩法**：
   - 原因：如果先写 `Wave 1` 新游戏，只会继续扩散旧专线逻辑。
