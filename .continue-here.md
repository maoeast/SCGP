# Current State
- 任务：A4 Phase 3 — 孤儿资源文件 GC（维护动作）
- 已完成：A4 Phase 1+2 代码完成并提交。Phase 2（备份纳入物理文件 zip 归档 v2.0→3.0）commit `809257d`（type-check + crypto/fflate 单测三绿，真机待验）；Phase 1 已提交（`30e4ad2`/`ab3b0d7`/`94836a9`）
- 产出物：commit `809257d`；plan `docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md` §5

# Next Action
- Phase 3 §5.1：新增 `src/utils/resource-reconcile.ts`，实现 `collectReferencedPaths()`（扫 `sys_training_resource` cover_image+meta_data + `teaching_material.file_path`，复用 `resource-file-refs.ts` 的 `extractResourceFileRefs`/`normalizeResourceUrl` 取托管路径集）+ `collectDiskPaths()`（调 `walk-dir` IPC 列 `uploaded`+`teaching-materials`）→ `npm run type-check`

# Blockers
- Phase 2 真机验证待做（导出→清库清资源→恢复→图片恢复可显；2.0 旧备份不报错），建议与 Phase 3 真机合并

# Key Decisions
- 托管路径规则（全局约束）：`uploaded/`+`teaching-materials/` 可删/进备份；`docs/images/videos/audio/` 预置不删；唯一可写根 `userData/resources`
- Phase 3 dry-run 优先（UI 先展示孤儿报告，用户确认才 purge），不做启动自动清理
- `walk-dir` IPC（Phase 2 §4.2 已建）供 Phase 3 `collectDiskPaths` 复用

# Relevant Files
- `src/utils/resource-reconcile.ts`（§5.1 新增）
- `src/utils/resource-file-refs.ts`（复用 extractResourceFileRefs/normalizeResourceUrl/isManagedResourcePath）
- `src/utils/resource-file-service.ts`（deleteManagedFile 复用）
- `docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md` §5
