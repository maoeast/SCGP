# Current State
- 任务：报告导出做实（页眉/头像/HTML 导出/默认格式）+ 报告页性别修复 + 报告设置持久化修复——已提交推送（`8744922` / `1af5abd` / `ecf1141`，HEAD = origin/main）
- 发版：1.1.4 第四次同版本覆盖发布**完成**——重新打包（266,005,883 B）+ `release:verify` 8/8 + 上传（服务器 sha512 与本地逐字节一致）+ 部署 `/home/lighthouse/scgp/win/`；**IP 源验证通过**（latest.yml 全文核对 sha512/size 一致、exe HTTP 200 + MZ 头、blockmap 200）
- 产物：`release/scgp-setup-1.1.4.exe` 266,005,883 B（sha512 base64 `/APtbgMw4rs7DZMoE2+0+H4ybfHSJ1o2J5pFKMFmc+iAj0034ITiQX/ic7fE7gRVtaKUmiQ7ZZh3j3IC4LUytA==`）+ blockmap 279,475 + latest.yml 337（releaseDate 2026-09-21T10:15:51.447Z）

# Next Action
- **HTTPS 主源修复**（用户拍板暂缓）：`https://maohedong.top` 证书 2026-09-20 23:59:59 GMT 过期，走该源的客户端更新会 TLS 失败；两条路径——① 服务商控制台续发免费 DV 证书（DigiCert EE，90 天）后替换 `/etc/nginx/ssl/maohedong.top/*.pem` 并 `nginx -s reload`；② 修 DNS 海外/默认线路（当前海外解析到 43.174.225.201 腾讯拦截页）后用 certbot 签 LE 证书（`certbot.timer` 已就绪，可自动续期）
- 可选：应用默认更新源 `electron/handlers/update.js:19` 是否改走 IP 源（需改代码 + 重新打包，且已装客户端无法远程改配置）

# Blockers
- HTTPS 更新通道不可用（证书过期）：已装客户端若配置为 https 源则更新失败；IP 源（http://124.220.104.199/scgp/win）正常

# Dead Ends
- Let's Encrypt HTTP-01 签发失败：海外验证节点解析 maohedong.top → 43.174.225.201（DNSPod 拦截页），非本机；国内解析正常（124.220.104.199）→ DNS 存在海外/默认线路分流
- 服务器无 DNS API 凭据、未装 certbot DNS 插件 → 无法自助走 DNS-01
- edit 批量调用把目录当默认 path → EISDIR 拒绝；edits 数组每条 entry 必须显式写文件路径
- `db.saveToStorage` 在 SQLWrapper 上不存在（只有 `saveNow`）——`if (db.saveToStorage)` 是静默空操作

# Key Decisions
- 发版拍板（2026-09-21）：保持 1.1.4 同版本覆盖（已装 1.1.4 的机器收不到自动更新，仅新装/手动下载生效）
- 证书拍板（2026-09-21）：暂不处理 HTTPS 证书问题，发布验证以 IP 源为准
- 服务端现状（本次操作留痕）：`/etc/nginx/sites-enabled/maohedong.top` 已加 `www.maohedong.top` 到两处 server_name（原仅 apex），备份在 `/root/maohedong.top.bak.20260921`；证书路径未改

# Relevant Files
- 服务端：`/home/lighthouse/scgp/win/`（发布产物）、`/etc/nginx/sites-enabled/maohedong.top`、`/etc/nginx/ssl/maohedong.top/`
- 应用端更新源：`electron/handlers/update.js:19`（DEFAULT_UPDATE_URL）、`:18`（LEGACY_HTTP_IP_UPDATE_URL）
- 发布校验：`scripts/release-verify.mjs`；发布流程：`.thincoder/skills/release-deploy.md`
- 本轮功能改动：`src/utils/report-export.ts` / `export-html.ts` / `export-word.ts` / `student-display.ts`、`src/views/system/SystemSettings.vue`、19 个 Report.vue
