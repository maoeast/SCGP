# Current State
- 任务：1.1.4 已两次打包发布（同版本覆盖）+ ABC 报告维度表缺陷修复收尾；**下一阶段：学生详情页重构**（用户已定），重构完成后统一重打包发布。
- 已完成：
  - ABC 报告「维度分数详情」三根因修复：①演示数据把维度分写成对象 `{name,rawScore}`（契约是数字）→ 读取期归一化兜底（旧数据免重导）；②报告页硬编码满分 60/48/72/52 实为 GMFM-88 表值 → 改为题库权重派生 38/34/52/34=158；③演示数据含题库里无题目的「生活自理」虚维度且计入总分 → 生成端剔除
  - 同根因收尾：AI 纵向趋势 `assessment-score-normalize.ts` flat-number 兜底（对象取 `rawScore`，脏值记 null，不再 NaN）；新增存量修正脚本（幂等/默认 dry-run/落库前自动备份）
  - 验证：`npm run verify:core` 全绿（type-check 0 error；`test:core:node` 132/132；`test:core:ts` 6/6）；真实库复算——学生 1 → 38/34/48/34（四维和=总分 154）、学生 10013 → 17/21/15/18（44.7/61.8/28.8/52.9%）
  - 发版：1.1.4 两次构建（265,996,179 B → 修后 265,994,189 B / sha512 `eq7xOrhB…`），`release:verify` 8/8、asar `scgpDebugDevtools=false`；同版本覆盖部署 `/home/lighthouse/scgp/win/`（root:root 644），服务器 sha512 与本地逐字节一致、双源均 `version: 1.1.4`
- 产出物：提交 `8ddbf4b`（fix abc）/ `3396cf5`（修正脚本）/ `8bda238`（版本号 1.1.4）/ `6cd3675`（CHANGELOG+交接）；`release/scgp-setup-1.1.4.exe` + `.blockmap` + `latest.yml`
- 存量库已落库（2026-09-20 18:25，应用已关闭时执行）：`npx jiti scripts/fix-legacy-abc-dimension-scores.ts --apply` → 15 行改写、备份 `database.sqlite.bak-20260920102500`（5,136,384 B）；复验 18/18 行「纯数字 + 各维之和 == 总分」，再跑 dry-run = 0 行待修（幂等成立）
- 待办：AI 趋势兜底、修正脚本与本次库修正**都不在**已发布的 1.1.4 包内 → 随下次打包上包

# Next Action
- 读 `src/views/StudentDetail.vue`（37.7KB 单文件）与 `src/views/student-detail/`（assessment-records.ts + components/ 两个面板），梳理现状并输出重构范围清单，等用户确认范围后再动代码。

# Blockers
- None

# Dead Ends
- 用字符串定位改注释：`edit` 新旧串写成同一串会静默无操作，且近似拼写易改错（本次把 `ABCDDriver` 误改成 `ABCDiver`，靠 host 校验才发现）——改用行号/哈希定位（`edit line:` / `hashline_edit`）。
- asar 内按固定路径直查 chunk（`dist/assets/index-<hash>.js`）失败——重建后文件名变化；正确姿势是 `listPackage` 过滤 + 去反斜杠前缀再 `extractFile`。

# Key Decisions
- 存量演示数据取「定向修正脚本」而非重导整包：重导会覆盖库里的真实评估数据；脚本幂等、真实行不动、默认 dry-run。
- 同版本号可重复覆盖发布（1.1.4 已两次），代价是**已装同版本的机器收不到该次修复的自动更新**——修复以「下一次打包」带出。
- 量表满分以题库权重为单一真源（`ABC_SUBSCALE_MAX_SCORES` 派生、勿手写）；演示数据侧用导出常量 + 测试防漂移。

# Relevant Files
- 下一步：`src/views/StudentDetail.vue`、`src/views/student-detail/assessment-records.ts`、`src/views/student-detail/components/StudentMemoryPanel.vue`、`src/views/student-detail/components/AssessmentRecordsPanel.vue`
- 收尾时：`scripts/fix-legacy-abc-dimension-scores.ts`、`.thincoder/skills/release-deploy.md`（打包发布流程）
