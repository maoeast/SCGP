# Current State
- 任务：AI 对话内嵌评估趋势图表（路线 C：工具结果驱动出图）
- 已完成（本轮，工作区未提交）：
  - 路线 C 全链路打通：工具结果含纵向分数 → 自动在 AI 回复下方渲染 echarts 趋势图
  - 四层接入：
    1. 捕获（ai-tools.ts）：ToolResult 加 artifact 字段；get_assessment_trend 在快照≥2 时产 AssessmentTrendArtifact
    2. 传输（ai-tool-loop.ts）：RunToolLoopResult.artifacts 收集本轮所有富产物
    3. 存储（持久化方案 A）：ai_chat_message 加 tool_artifacts TEXT 列（safeAddColumn）；saveMessage/listMessages 往返
    4. 渲染：新建 AiTrendChart.vue（精简 echarts 线图）；AiChatTranscript 在非 pending assistant 回复下方渲染
  - store tool 循环分支把 artifacts 持久化到 assistant 消息，回看历史会话仍可见图
  - type-check + adapter(11场景) + 路由契约(4) + artifact链路契约(10) + build:web 全过
- 产出物：9 文件改动（8 源码 + 1 测试），工作区未提交

# Next Action
- 用户确认后提交（建议 commit message：feat(ai): AI 对话内嵌评估趋势图（路线C：工具结果驱动 echarts 出图））
- 实测验证：在 AI 对话中问「某学生 CSIRS/SRS2 评估趋势」，确认回复下方出现趋势图

# Blockers
- None（功能完成，待用户实测确认后提交）

# Key Decisions
- 持久化方案 A（存 DB）而非 B（仅内存）：评估纵向分析是高价值、需反复回看的数据，丢图会让历史会话失去一半价值
- artifact 与 content 分离：content 仍给模型做文字解读（不变），artifact 只回传 UI 层渲染（不进模型上下文，不增 token）
- 快照≥2 才产 artifact：单点无法成趋势线
- 复用裸 echarts.init（与 AssessmentTrendPage 一致），不引入 vue-echarts 组件依赖
- ToolArtifact 设计为可扩展联合类型（目前仅 assessment_trend），未来 Mermaid 流程图等可新增 kind

# Relevant Files
- src/services/ai-tools.ts（ToolResult.artifact + AssessmentTrendArtifact 类型 + get_assessment_trend 产出）
- src/services/ai-tool-loop.ts（artifacts 收集 + 回传）
- src/database/ai-api.ts（parseToolArtifacts + saveMessage/listMessages 往返）
- src/database/init.ts（tool_artifacts 列）
- src/stores/ai.ts（store 持久化 artifacts 到 assistant 消息）
- src/features/ai/components/AiTrendChart.vue（新增，echarts 趋势图组件）
- src/features/ai/components/AiChatTranscript.vue（渲染接入点）
- src/features/ai/components/AiAssistant.vue（displayMessages 透传 toolArtifacts）
- scripts/tests/ai-tool-artifact-trend.test.mjs（新增，10 场景四层链路契约）
