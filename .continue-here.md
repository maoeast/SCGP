# Current State
- 任务：清理自理训练 `task_training` 中 2 条停用的 E2E 验证数据，并保持 `/self-care/tasks` 只对应 31 条正式任务。
- 已完成：
  - 已用 Electron + Playwright 核对 `#/self-care/tasks`，确认 7 个胶囊分类与 31 条正式任务展示正确。
  - 已物理删除数据库 `sys_training_resource` 中 `572`、`573` 两条停用 E2E 任务。
  - 已同步删除关联 `training_records` 1 条、`training_session` 1 条、`sys_resource_tag_map` 4 条。
  - 已同步删除仅服务于该验证数据的自定义标签 `e2e`、`self-care`。
  - 已复核当前数据库计数：`task_training` 总数 31、启用 31、停用 0。
- 产出物：
  - `.tmp/self-care-tasks-electron-summary.json`
  - `.tmp/self-care-tasks-electron.png`
  - `.tmp/self-care-tasks-first-card.png`
  - `C:\Users\maoea\AppData\Roaming\scgp\database.self-care-e2e-cleanup.2026-05-11T09-36-43-711Z.sqlite`

# Next Action
- 修改 `docs/planning/2026-05-11-self-care-ats-seed-import-analysis.md`，追加“2 条 E2E 验证数据已清理，当前正式 `task_training` 为 31 条”的结论。

# Blockers
- None

# Key Decisions
- E2E 验证数据不再保留在正式库中：避免 `/self-care/tasks` 统计口径混入测试数据。
- 清理采用物理删除关联记录：同时删除资源、训练记录、训练会话和标签映射，避免留下悬挂引用。
- 页面联调口径固定为正式 `task_training` 数据：后续默认应始终对应 31 条正式任务。

# Relevant Files
- `.continue-here.md`
- `docs/planning/2026-05-11-self-care-ats-seed-import-analysis.md`
- `.tmp/self-care-tasks-electron-summary.json`
