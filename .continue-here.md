# Current State
- 任务：AI 智能体 Phase 3 图片上传 vision 代码完成，type-check 全绿，待真机验证 + 提交
- 已完成：① 数据层 init.ts 加 `ai_chat_message.attachments` 列 + ai-api.ts 新增 `AiAttachmentRef`/`saveMessage`+`listMessages` 带 attachments、`parseAttachmentRefs` 防脏数据；② Main `buildMessages` 支持多模态数组 content（单点覆盖流式+非流式）；③ 双声明 aiChat content 扩 `string | 多模态数组`；④ 新建 `src/utils/ai-attachment-manager.ts`（saveAttachment/readAsDataUrl/getFileUrl/deleteAttachment）；⑤ GC `resource-reconcile.collectReferencedPaths` 纳入 `ai_chat_message.attachments`（防附件被当孤儿删——agent 标的致命遗漏 A）；⑥ tool-loop messages 支持数组 content；⑦ store sendChat 多模态（supportsVision/5MB/≥1MB软警告 校验 + 落盘 + 出站拼最近1轮图 + vision+FC 同发）+ deleteSession 删前清附件（遗漏 B）；⑧ UI 图片按钮+缩略图预览+按 supportsVision 禁用+气泡附件展示。`npm run type-check` 全绿
- 产出物：工作区 dirty（改 8 文件 + 新建 ai-attachment-manager.ts + plan），未提交（本地 ahead 3）

# Next Action
- 真机验证（需豆包 vision Key + 推理接入点 ID，未配）：用户已提供官方 curl 示例确认 `doubao-seed-2-1-turbo-260628` 在 `/chat/completions` 支持 `content:[{type:'image_url',image_url:{url}},{type:'text',text}]`（image_url 在前）；真机发图+文确认看图回答 + 带图+tools 确认 vision+FC 共存；附件 GC 回归（findOrphans 不含 AI 附件）+ 删会话清文件
- 验证通过后提交 Phase 3。建议 message：
  ```
  feat(ai): Phase 3 图片上传 vision + 附件生命周期
  - Done: 多模态 content 透传 + 图片落盘(uploaded/ai-attachments) + 最近1轮带图 + vision/FC 同发 + GC 引用扫描防误删 + deleteSession 清附件；type-check 全绿
  - Next: Phase 4 文档上传(PDF/Word) 或 Phase 5 技能包
  - Risk: 真机受豆包 vision Key 阻塞；data URL/vision+FC 真机确认
  ```

# Blockers
- 真机需豆包 vision 模型（火山方舟 Key + 推理接入点 ID `ep-xxx`，未配）
- 代码层 type-check 已过；附件 GC 回归 + deleteSession 清文件 + 真机看图待手动/真机验证

# Key Decisions
- 历史图片：仅带最近 1 轮图（出站前替换最后一个带附件 user 的 content 为多模态数组；本轮用缓存 dataUrl，历史图当轮重读 base64）
- 图+工具：允许 vision+FC 同发（带图也能进 runToolLoop 携 tools）
- 图片上限：5MB 硬上限 + ≥1MB 软警告；canvas 压缩留后
- dataUrl 不持久化（只落 `{rel,fileName,fileType,sizeBytes}` 元信息）
- 附件清理在 store `deleteSession` 层（非 ai-api.ts，避免数据层耦合 IPC）
- 豆包 image_url 格式经用户官方示例确认（chat/completions + image_url 数组），头号 Blocker（vs Responses API）已解除

# Relevant Files
- 新建：`src/utils/ai-attachment-manager.ts`
- 改：`src/database/init.ts`、`src/database/ai-api.ts`、`electron/handlers/ai.mjs`、`src/types/electron.d.ts`、`env.d.ts`、`src/services/ai-tool-loop.ts`、`src/stores/ai.ts`、`src/features/ai/components/AiAssistant.vue`、`src/utils/resource-reconcile.ts`
- Plan：`C:\Users\maoea\.claude\plans\graceful-orbiting-dusk.md`
