# Current State
- 任务：补齐自理训练记录详情跳转，并为 Self-Care ATS 的 31 项旧任务建立正式 seed 落地主线的起步产物
- 已完成：
  - `src/views/training-records/ModuleTrainingRecords.vue` 已为 `resource_type = task_training` 新增详情跳转分支，统一进入 `/self-care/execute/:resourceId/:studentId`
  - `src/views/StudentDetail.vue` 已为 `resource_type = task_training` 新增相同详情跳转分支，避免继续误跳 `/games/report`
  - `scripts/extract-self-care-ats-seeds.mjs` 已可从 `E:\VSC\H5\Self-Care ATS\self-care-ats\src\database\sample-tasks.ts` 抽取 31 条任务与 235 步文本
  - `E:\VSC\H5\SIC-ADS\.tmp\self-care-ats\task-seed-inventory.json` 已生成，确认 `totalTasks = 31`、`totalSteps = 235`
  - 定向测试已补齐并通过：`scripts/tests/module-training-records.test.mjs`、`scripts/tests/student-detail-task-training.test.mjs`、`scripts/tests/self-care-ats-seed-extract.test.mjs`
- 产出物：
  - `src/views/training-records/ModuleTrainingRecords.vue`
  - `src/views/StudentDetail.vue`
  - `scripts/extract-self-care-ats-seeds.mjs`
  - `scripts/tests/module-training-records.test.mjs`
  - `scripts/tests/student-detail-task-training.test.mjs`
  - `scripts/tests/self-care-ats-seed-extract.test.mjs`
  - `.tmp/self-care-ats/task-seed-inventory.json`

# Next Action（必须原子级——第一步可执行的单一动作）
- 新建 `scripts/import-self-care-ats-seeds.mjs`，把 `.tmp/self-care-ats/task-seed-inventory.json` 中的 31 条任务按幂等方式写入 `sys_training_resource`，固定为 `module_code = life_skills`、`resource_type = task_training`

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 自理训练详情统一回到执行页：记录详情入口固定走 `/self-care/execute/:resourceId/:studentId`，不再复用 `/games/report`
- 自理训练真实主键使用 `sys_training_resource.id`：`training_records.task_id = NULL`，详情与执行都只认 `resource_id`
- ATS 旧仓库只作为任务内容种子源：本轮不迁 `student / train_plan / train_log`，只准备把 31 条任务模板落到 `task_training`

# Relevant Files（最小集——仅列下一步会用到的）
- `scripts/extract-self-care-ats-seeds.mjs`
- `.tmp/self-care-ats/task-seed-inventory.json`
- `src/database/resource-api.ts`
- `src/database/init.ts`
- `src/features/self-care/task-training-contract.ts`
- `docs/planning/2026-05-11-self-care-ats-seed-import-analysis.md`
