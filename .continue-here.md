# Current State
- 任务：为 `license-generator-dist` 增加“从 5 个顶层授权模块中选择 1 个或多个输出到许可证 `am`”的发码能力
- 已完成：`license-generator-dist/generate-license.js` 已默认输出 5 个顶层模块的 `am`；runtime 验签/落库/路由拦截已消费 `am`；登录页 DEV 管理员重置入口已改为仅 DEV 组件加载，生产构建不再保留该入口文案
- 产出物：`license-generator-dist/generate-license.js`、`src/views/Login.vue`、`src/components/login/LoginCard.vue`、`src/components/login/DevEmergencyResetButton.vue`

# Next Action
- 修改 `license-generator-dist/generate-license.js` 的命令行解析，新增 `--modules` 参数并校验仅允许 `sensory/emotional/social/cognitive/life_skills`

# Blockers
- None

# Key Decisions
- 顶层授权白名单固定为 `sensory / emotional / social / cognitive / life_skills`：内部 training entry 不能进入许可证顶层授权
- 客户端授权消费链已可用：下一步只改发码工具，不重做 runtime 验签/落库/菜单锁定
- 生产构建不得保留 DEV 管理员重置入口：不能只靠运行时隐藏，必须让构建产物裁剪掉入口 UI/文案

# Relevant Files
- `license-generator-dist/generate-license.js`
- `license-generator-dist/README.md`
- `src/utils/license-manager.ts`
- `src/stores/auth.ts`
