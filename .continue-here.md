# Current State
- 任务：在系统页增加“重新激活 / 更新授权”入口，支持客户增购模块后二次录入激活码，并在当前页立即刷新授权结果
- 已完成：
  - `src/views/System.vue`：在“关于系统”卡片增加“重新激活 / 更新授权”按钮、激活码输入/粘贴/提交入口、授权模块标签展示
  - `src/views/System.vue`：提交新激活码成功后立即重新拉取授权状态，并在页面提示最新可用模块
  - `src/stores/auth.ts`：`checkActivation()` 改为完整覆盖 `expiresAt / trialDays / trialUsed / allowedModules` 的刷新结果；激活码校验前统一 `trim()`
  - 已执行 `npm run type-check`，通过
- 产出物：
  - `src/views/System.vue`
  - `src/stores/auth.ts`
  - `.continue-here.md`

# Next Action
- 在真机或生产包中打开系统页“关于系统”，录入一枚包含新增模块的激活码，确认授权模块标签、菜单锁定状态和当前机器 `allowedModules` 在提交后立即刷新

# Blockers
- None

# Key Decisions
- 重新激活入口放在系统页“关于系统”卡片：客户增购模块后不需要回到首次启动激活页
- 重新授权成功后立即调用当前授权刷新链路：不重装、不清空数据，直接更新当前机器 `allowedModules`
- 系统页直接展示授权模块标签：便于现场验证新授权是否已经生效

# Relevant Files
- `src/views/System.vue`
- `src/stores/auth.ts`
