# Current State
- 任务：学生详情页「上下分层」重构收口并随 1.1.4 第三次发版上包；下一专项为 T24 结构化健康字段
- 已完成：重构（Hero 档案区 + Tab 明细区）+ 删 Hero AI 记忆冗余入口 + StudentMemoryPanel 三卡摘要组 + T25 gameCount 口径对齐（标签数=表格行数）；type-check 0 error、build:web 通过、advisor 三轮评审 pass；1.1.4 打包发布 release:verify 8/8、服务器 sha512 逐字节一致、双源 curl 均 1.1.4、exe URL HTTP 200
- 产出物：提交 `68b0573`（refactor）/ `13ce8e4`（fix scripts）/ `1afaff5`（docs manual）/ `a7c7145` / `13959a5`（handoff）；`release/scgp-setup-1.1.4.exe` 265,994,789 B / sha512 base64 `5TbtMQqLEMG/…`

# Next Action
- T24 首步：读 `src/database/init.ts` 的 student 表结构与迁移挂点 + `src/components/AddStudentDialog.vue` + 学生导出联动点，产出 care_instructions 字段实施方案（表迁移/编辑弹窗/导出/警示条分段）等用户确认后再动代码

# Blockers
- None

# Dead Ends
- edit 批量调用把目录当默认 path → EISDIR 拒绝；edits 数组每条 entry 必须显式写文件路径
- git 工具 push 传 `ref: main` 报 "'main' does not appear to be a git repository"；须传 `ref: origin main`（remote+branch 空格分隔）

# Key Decisions
- T25 拍板方案 a：Tab 标签计数 = 表格可见行数（游戏+情绪游戏两路）；认知会话只进「最近训练/本月训练」活动条（2026-09-19 口径不动）
- 健康警示过渡态：healthNotes 完整透出、不做前端拆分；T24（care_instructions 与 health_notes 职责分离）为用户拍板首选方向，未排期
- 1.1.4 同版本覆盖发布（当日第三次）：已装 1.1.4 的机器收不到自动更新（electron-updater 同版本语义），修复以下次打包带出

# Relevant Files
- src/views/StudentDetail.vue（T24 警示条分段改造点，注意 2026-09-19/20 用户约定注释勿回退）
- src/components/AddStudentDialog.vue（T24 编辑联动）
- src/database/init.ts（student 表结构 + ALTER 迁移挂点）
- src/stores/student.ts（Student 类型/查询列）
