# Current State
- 任务：C11 旧命名与原生开发依赖清理已完成并 push。
- 已完成：`AboutDialog.vue` 文案统一为 `SCGP / 星愿能力发展平台`；`scripts/export-resources.cjs` 从 better-sqlite3 切 sql.js + 补 `--help` 只读分支；`npm uninstall --save-dev better-sqlite3` 移除 17 包（含原生编译树）。验证：`--help` 正常 / `grep better-sqlite3` 零命中 / `verify:release` 全绿（type-check + tests + build:web 40s）/ `git diff --check` 干净。About 文案真机显示待 C13 桌面 UAT。
- 产出物：提交 `ad54913`（feat）+ `f8d7e90`（docs），已 push origin/main。

# Next Action
- 读 `electron/handlers/update.js` + `package.json` 的 `build.publish` / `build.win`，核实当前更新链 HTTP IP 默认值（`http://124.220.104.199/scgp/win`）与代码签名配置现状——为 C12 解锁后的第一步做只读事实核对（不开工，C12 仍处阻塞）。

# Blockers
- C12 被外部输入阻塞：(a) 客户网络可达的正式 HTTPS 更新目录 URL + 写入凭据；(b) Windows 代码签名证书（`CSC_LINK`/`CSC_KEY_PASSWORD`）；(c) Windows 验收机或隔离虚拟机。三样齐备前 C12 不开工，C13 终验随之等待。

# Key Decisions
- About 文案口径定为 `SCGP / 星愿能力发展平台`（与 `package.json` description 一致）；版本继续读 `updateState.currentVersion`，不新增硬编码。
- `export-resources.cjs` 保持 `.cjs`，sql.js wasm 经 `locateFile` 指向 `node_modules/sql.js/dist`（与运行时主线 `export-current-emotion-scenes.mjs` 同范式）；CLI 契约与 `self-care-ats` 历史路径不变。
- better-sqlite3 彻底移除（devDeps + lockfile + 脚本三处），符合「零额外原生依赖」红线；`appId` / localStorage key / 历史 migration 标识符作为兼容事实保留。

# Relevant Files
- `docs/plans/2026-07-18-project-closeout-execution-plan.md`（§C12）
- `electron/handlers/update.js`
- `package.json`
