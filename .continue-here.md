# Current State
- 任务：认知发展（cognitive）模块 P0 游戏包——`K03_PATTERN_NEXT` 与 `K01_MEMORY_MATCH` 均已交付（UAT 通过、已 push），下一步复制接入节奏推 P0 其余 K04/K05。
- 已完成：K03 全链路 + K01 全链路（registry + `MemoryMatchGame` + `MemoryMatchPage` + router，复用 `cognitive-games-api` + GameContainer moduleCode dispatch，未改容器/API）；type-check / build:web / 实机 UAT 全通过。
- 产出物：K01 三档难度（2×2 / 4×3 / 4×4，翻回 / 不翻回 / 相似干扰）+ actual_params（`grid_size` / `pair_count` / `trials` + 强耦 `accuracy_ratio` / `average_response_ms`）+ 纯 SVG 卡面预留 `webp` mode。

# Next Action
- 对 P0 第三个游戏 `K04_ODD_ONE_OUT`（哪个不同类，PRD §4）进 Plan 模式，出组件技术方案；接入层复用 K01/K03 已铺好的认知底座（cognitive-games-api + GameContainer dispatch + registry/Page/router 模式），无需改容器/API。PRD 提示 K04 可复用认知器材 webp 资源做素材。

# Blockers
- None。

# Key Decisions
- 库表方案 B：cognitive 走 training_session，`source_table='cognitive_game_inline'` 合成源（不建表），actual_params 显式透传 summary_payload；GameContainer 按 moduleCode dispatch 防 emotional 串台——K01/K04/K05 直接复用，不再改容器/API。
- PRD §7 第 0 条定位 = B（IEP 级可追踪）：每个认知游戏 performanceData 必须含 actual_params。
- K01 卡面决策：v1 纯 SVG + 预留 `CARD_FRONT_MODE` 字段（webp 物品认知模式留后续）。
- P0 顺序：K03（已交付）→ K01（已交付）→ K04（哪个不同类，可复用认知器材 webp）→ K05（数感小铺，双模式）。

# Relevant Files
- `src/components/emotional/games/MemoryMatchGame.vue`（K01 模板，K04 抄骨架换玩法）/ `PatternNextGame.vue`（K03 模板）
- `src/database/cognitive-games-api.ts`（已就绪，K04 直接复用不改）
- `src/views/emotional/games/MemoryMatchPage.vue` + `src/router/index.ts`（Page / 路由模式复用）
- `docs/planning/2026-07-19-cognitive-games-prd.md`（§4 K04 / K05 玩法详情）
