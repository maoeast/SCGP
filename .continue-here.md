# Current State
- 任务：执行 A4「资源文件生命周期 + 备份收口」，**计划已就绪**，新会话照计划实施（**未开始编码**）。
- 已完成（本会话）：A4 全量调研 + 三方决策 + 落计划文档 `docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md`（含 file:line 证据、3 阶段步骤、风险/前置确认、文件清单）。
- 决策锁定：①范围 = Phase 1+2+3 全做（每阶段独立 commit）；②备份 = 独立 zip 归档（fflate，待用户确认依赖）；③死代码确认无活引用即删。
- 调研关键结论：写盘 4 链路中 2 死（`ResourceUpload`/`SAVE_ASSET`、`Resources.vue`/`ResourceManager`）2 活（`TeachingMaterialFileManager` 健康、AI 场景图直写有孤儿）；唯一正确根 = `userData/resources`；`get-app-path` 生产 bug 坐实（但所在链路 B 已死）；`sys_training_resource` 删/替不删文件 = 主孤儿源；`backup.ts` 完全不碰物理文件。
- 注意：origin/main 已在 `163b474`（与 main 同位，已推送）——交接文档旧的「ahead 3 未 push」表述已过时；A6 仍待真机。

# Next Action
- **执行 A4 Phase 1**（止血：删/替即清文件 + 死代码清理）：
  1. 先做计划 §7 **P0**——抽样核对 DB 实存资源路径全在托管前缀（`uploaded/`、`teaching-materials/`）下，确认 `isManagedResourcePath` 规则成立。
  2. 按计划 §3 实施：新增 `resource-file-refs.ts`/`resource-file-service.ts` → 钩入 `hardDeleteResource`(`resource-api.ts:650`)/`updateResource`(`:530`) → AI 候选清理 → 删死代码。
  3. `npm run type-check` → 回填 backlog A4 状态 → 独立 commit。
- Phase 2/3 按 §4/§5 推进；遇 §7 P1（新增 fflate 依赖）停下问用户。

# Blockers
- §7 P1：`fflate` 依赖需用户一句话确认（Phase 2 才用到，不阻塞 Phase 1）。
- A6 真机 E2E 仍未跑（独立项，不阻塞 A4）。

# Key Decisions
- 托管路径判定：托管（可删/进备份）= `uploaded/`+`teaching-materials/`；预置（永不删）= `docs/images/videos/audio/`。
- 软删 `deleteResource` 不动文件（保留可恢复语义）；仅 `hardDeleteResource` + `updateResource` 替换清文件。
- 备份 zip 走主进程打包 → `encryptBytes`（新增，绕开 JSON-based `encryptData`）→ 作为字段入 `.dat`（v3.0，兼容 2.0）。
- 协议弃用 API（`registerFileProtocol`）本计划不动，记入 A8/技术债。

# Relevant Files
- **计划（首要）**：`docs/plans/2026-07-15-a4-resource-file-lifecycle-plan.md`
- backlog：`docs/reports/2026-07-15-unimplemented-gap.md`（A4 行待回填状态）
- 证据核心：`electron/main.mjs:264-267,495-551,640-700,816-863,1127-1144`、`src/database/resource-api.ts:530-591,601-662`、`src/utils/teaching-material-file-manager.ts`、`src/utils/resource-manager.ts`、`src/utils/backup.ts`、`src/utils/crypto.ts`、`src/router/index.ts:30,1095`
