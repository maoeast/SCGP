# Current State
- 当前任务：收口 `C04_HOURGLASS` 手工验收后遗留的情绪小游戏详情页展示问题。
- 已完成到哪一步（具体到函数 / 文件 / 行为）：
  - 已完成 Electron 手工验收：`#/games/select-student?entry=soothing-aids&module=emotional` -> `魔法沙漏` -> 自然结束 -> 详情页；新记录为 `recordId=21`，`event=timer_end`、`planned_duration_ms=45000`、`progress_ratio=1` 已确认落库。
  - `src/views/emotional/GameRecordDetail.vue` 已去掉用户可见“游戏代码”，改为按注册表显示“训练重点”，覆盖当前已实现小游戏详情页。
  - `npm run type-check:emotional` 已通过。
- 当前产出物（代码 / 文件）：
  - `.continue-here.md`
  - `HANDOFF.md`
  - `src/views/emotional/GameRecordDetail.vue`

# Next Action
- 下一步第一个动作（必须原子级，例如：修改 xx 文件的 xx 函数）
  - 修改 `src/views/emotional/GameRecordDetail.vue` 的 `summaryCards` / `formatResponseTime` 逻辑，让无响应指标的小游戏详情页不再显示 `0ms`。

# Blocking / Risks
- 阻塞 / 风险：详情页时间显示仍存在时区不一致（`开始时间` 与 `落库时间` 口径不同）；修完 `0ms` 后还需继续收口。

# Key Decisions
- 已实现小游戏详情页不再向用户暴露内部 `game_code`；改显示“训练重点”。原因：实现码不是用户语言。
- 当前继续按手工验收发现逐项收口，不做额外重构。原因：本轮目标只收掉真实用户可见问题。
- 下一轮先修 `0ms`，再处理时间显示。原因：`0ms` 是当前详情页最直接的共性文案问题。

# Relevant Files
- `src/views/emotional/GameRecordDetail.vue`
- `src/database/emotional-games-api.ts`
