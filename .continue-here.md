# Current State
- 任务：补齐 `C05_MOOD_METER` 注册表，并为 `life-skills` 入口补齐 `L01_WASH_HANDS` 到 `L05_PACK_BAG` 的自定义小游戏接线。
- 已完成：
  - 已在 `src/data/custom-game-registry.ts` 新增 `C05_MOOD_METER` 注册表定义，`trainingEntryCode` 为 `soothing-aids`，`entryPath` 为 `/emotional/games/mood-meter`。
  - 已在 `src/data/custom-game-registry.ts` 新增 `L01_WASH_HANDS`、`L02_DRESS_UP`、`L03_BRUSH_TEETH`、`L04_SET_TABLE`、`L05_PACK_BAG` 注册表定义。
  - `L01-L05` 全部使用当前代码现实的 `ModuleCode.LIFE_SKILLS + trainingEntryCode: 'life-skills'`，没有引入新的 `daily-living` 真相源。
  - 已为 `L01-L05` metadata 补齐 `cognitiveLoad`，并通过 `npm run type-check`。
- 产出物：
  - `src/data/custom-game-registry.ts`
  - `.continue-here.md`

# Next Action
- 修改 `src/router/index.ts`，为 `C05_MOOD_METER` 和 `L01-L05` 新增对应页面路由。

# Blockers
- None

# Key Decisions
- 生活自理训练入口统一使用 `life-skills`：当前代码、菜单和训练入口底座都以此为准。
- `C05_MOOD_METER` 当前只作为普通 custom game 注册，不做全局悬浮工具。
- 顺序固定为：先补注册表，再补路由/页面，最后补记录摘要。

# Relevant Files
- `src/data/custom-game-registry.ts`
- `src/router/index.ts`
- `src/views/emotional/GameRecordDetail.vue`
- `src/database/emotional-games-api.ts`
