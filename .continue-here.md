# Current State
- 任务：收尾自理训练全屏训练界面的退出/切换确认流，完成左上角“退出”按钮接入与切换弹窗文案调整。
- 已完成：`src/views/self-care/TaskExecution.vue` 新增左上角“退出”按钮；“退出”与“切换”复用同一确认弹窗；确认“退出”返回 `/self-care/tasks`，确认“切换”返回学生选择页；“切换”弹窗标题已改为“确定要切换其他学生训练吗？”；新增退出流配置与最小回归测试，并通过 `node --experimental-strip-types tests/self-care-task-exit-flow.test.ts`、`npm run type-check`。
- 产出物：`src/views/self-care/TaskExecution.vue`、`src/features/self-care/task-exit-flow.ts`、`tests/self-care-task-exit-flow.test.ts`

# Next Action（必须原子级——第一步可执行的单一动作）
- 启动自理训练全屏页，手动点击左上“退出”和右侧“切换”，确认两条确认弹窗与跳转链路符合预期。

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 左上角标题区采用“退出按钮 + 标题”一体浮层：减少双浮层抢视觉。
- 退出/切换共用同一套确认弹窗结构：只按意图切换文案和跳转目标。
- “退出”使用 `router.replace('/self-care/tasks')`，“切换”使用 `router.push('/self-care/tasks/:taskId/select-student')`：避免退出后把训练页留在返回栈。

# Relevant Files（最小集——仅列下一步会用到的）
- `src/views/self-care/TaskExecution.vue`
- `src/features/self-care/task-exit-flow.ts`
- `tests/self-care-task-exit-flow.test.ts`
