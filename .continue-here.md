# Current State
- 任务：游戏训练 IEP 报告闭环扩展（Phase 3：Tier3 近似指标游戏 F02 连线小星座 / F01 云朵擦擦擦 / F05 刺破慢气球），来源 `docs/plans/2026-07-07-game-iep-extension-plan.md` §3。**G07_MONSTER 经用户决定延后**为独立任务。
- 已完成（代码级，**未提交**）：归一化层 `game-performance-normalizer.ts` 新增「数组型反应时」`reactionArrayField`（取数组元素 ms 均值，优先于 `reactionFields`），并在 `GAME_EXTRACTION_RULES` 加 F02/F01/F05 三行；`iep-generator.ts` 的 `generateFineMotorSections` 加 F02（轨迹精度）/F01（擦拭覆盖）/F05（抑制控制，读 `extra.early_taps`）三分支。守卫 / 路由 / 统计卡 / 名字映射 / 通用 summary 均无需改动。本批改动同时含 Phase 2（F04/L01/L02/L04）。
- 验证：`npm run type-check` exit 0；一次性 jiti 断言（Phase 3 共 19 条：F02 精度+null 精度降级、F01 无反应时、F05 数组均值+early_taps 经 extra+空数组/空壳降级、Tier1/2/社交无回归）全过后已删除，未留测试文件。

# Next Action
- 真机 E2E：`npm run electron:dev` 冷启动，分别通关 F02/F01/F05 各一次，核对：①`training_records` 新行（module_code=sensory、task_id=null、raw_data.gameCode 存在）②`report_record` 新行 report_type='iep' ③自动跳 IEPReport 展示专属段（F05 抑制控制段读 early_taps）④F01 无反应时卡隐藏、F05 无正确率卡隐藏、缺指标时隐藏 ⑤可导 Word。
- 通过后提交（Phase 2+3 可合一提交或拆分，由用户定）。
- G07_MONSTER 独立后续任务（需改守卫 + G07 专属白名单 + IEPReport 情绪匹配分支 + 新生成器）。

# Blockers
- Phase 1/2/3 真机 E2E 均未跑（本环境无法驱动交互式 Electron 游戏 + 查 DB）。
- 工作区有无关 stash `wip(shape-match): wooden block ui`，与本任务无关，**未 pop**（待用户另行决定）。

# Key Decisions
- Tier3 三游戏指标偏「近似」（精度/覆盖率/抑制控制），非经典正确率：F02 用 `path_precision_ratio` 作 accuracy（ratio）、F01 用 `cleared_ratio_peak` 作 accuracy（ratio，无反应时）、F05 无 accuracy（核心是抑制控制 `early_taps`）。
- F05 反应时 `window_response_ms` 是数组（无标量均值）→ 归一化层新增 `reactionArrayField` 取数组有效非负元素均值；保持归一化层为唯一提取入口（不在生成器里重复求平均）。F05 的抑制控制口径由生成器读 `extra.early_taps` 给专属段（extra 透传是 Phase 1 既定用法）。
- F02 的 realityFields 含 `completed_constellations`：`path_precision_ratio` 可能为 null（中断局），含此字段保证该情形仍判 hasRealData=true（accuracy 走 null 降级，但游戏确实跑过）。
- 守卫 / 路由 / 统计卡可用性均已在 Phase 1 就绪，本 Phase 未触碰 `GameContainer.vue` / `IEPReport.vue`。
- G07 延后理由：属 emotional-regulation 入口（不在 IEP 放行集合），启用需改守卫并加 G07 专属白名单（否则 Tier4 的 G01/G03/G04/G08/G09 会被一起放进 IEP），还要新增情绪匹配报告分支与生成器；风险更高，与方案文档「二期再议」一致。

# Relevant Files
- `src/utils/game-performance-normalizer.ts`
- `src/utils/iep-generator.ts`
