---
status: active
last_updated: 2026-04-03
scope: login entry runtime render and config persistence have now been manually verified; current follow-up is backup/restore coverage closure, especially verifying that class, class-assignment, plan, report, and training-record data round-trip correctly through the updated backup pipeline
---

> Responsibility: the only current handoff file for unfinished work.
> Read when: continuing work from a previous session.
> Not responsible for: replacing `AGENTS.md` repo rules or `PROJECT_CONTEXT.md` global context.

## Session Closeout (2026-04-03)

Current task status:

- the previously pending login-entry runtime QA gap is now closed:
  - actual login-page render has been manually verified
  - system-settings persistence for login-entry configuration has been manually verified
- the new active work in this conversation was backup/restore coverage closure for current live schema
- backup/restore implementation was updated in:
  - `src/utils/backup.ts`
- system-management backup copy was updated to match current code reality in:
  - `src/views/System.vue`
- current backup export no longer relies on the historical hard-coded table whitelist
- current backup export now discovers live user tables from the active sqlite schema, so the export path should now include current in-use data such as:
  - class and academic-year data
  - student class-history / class-teacher mapping data
  - training plans
  - report records
  - game / equipment / emotional training records
  - unified `training_session`
  - current resource and system-config tables
- current restore flow was also tightened:
  - overwrite restore now clears target tables even when the backup copy for that table is empty
  - restore temporarily disables foreign-key enforcement for the bulk import, then runs `foreign_key_check`
  - class-related derived state is recalculated after restore:
    - `student.current_class_id`
    - `student.current_class_name`
    - `sys_class.current_enrollment`
- backward read compatibility remains in place for old backup payload version `1.0`
- new exports now write backup payload version `2.0`
- verification completed in this conversation:
  - `npm run type-check` passed
- verification not completed in this conversation:
  - no `vite build`
  - no fresh end-to-end manual backup/restore round-trip after the new backup code landed

Next step:

1. manually rerun a real backup/restore round-trip against the current dev database
2. first implementation action:
   - use `系统管理 -> 数据备份` to create a fresh backup file from the post-fix code, restore it into a known dataset, then verify the previously missing domains
3. during that verification, explicitly test:
   - `sys_class` and `sys_academic_year`
   - class-assignment views backed by `student_class_history`
   - training-plan pages
   - reports center data and `report_record` consumers
   - training-record pages and counts
   - any `training_session`-backed or dual-write-dependent read chain that should remain consistent after restore
4. if the round-trip passes:
   - update product-facing backup wording or QA notes if needed

Problems / blockers / risks:

- no active code blocker remains
- important runtime gap:
  - the backup code change has only passed static verification so far
  - the user reported the previous restore gap from manual QA, but the newly patched backup path itself still needs a fresh manual round-trip confirmation
- important implementation boundary:
  - backup now follows current sqlite user tables dynamically instead of a manually maintained allowlist
  - if future local-only transitional tables should stay out of backup, they must be added to the explicit exclude set in `src/utils/backup.ts`
- important compatibility boundary:
  - restore still assumes the current app schema contains every table present in the backup
  - if a future backup file contains tables unknown to an older client, restore now fails fast with a missing-table error instead of silently partial-restoring
- important class-data boundary:
  - class triggers can distort `current_enrollment` during raw replay
  - current code now repairs that derived field after restore; future class-schema changes should preserve this post-restore reconciliation
- local workspace still contains unrelated untracked files:
  - `sql_js_持久化模块标准实现方案_V1.1.md`
  - `tree.txt`
  - treat them as local-only materials, not product work to include in the handoff commit

Key decisions made in this conversation:

- switch backup coverage from a hard-coded table whitelist to live sqlite table discovery
  - reason: the old whitelist had fallen behind current schema and directly caused missing class, plan, report, and training data after restore
- keep explicit exclusions only for transient migration tables
  - reason: the safer default for this repo is “cover current live user tables unless explicitly excluded,” not “remember to manually add every new table forever”
- make overwrite restore clear target tables before replaying records
  - reason: the previous implementation skipped empty backed-up tables, which left stale live data behind during overwrite restores
- disable foreign-key checks during replay, then validate afterward
  - reason: full-schema restore should not depend on perfect insert ordering across dozens of related tables, but it still must fail if the final restored state is inconsistent
- recalculate class-derived state after restore
  - reason: class-management triggers and snapshot fields can drift during raw replay, so restore needs an explicit reconciliation step instead of assuming inserts preserve derived values automatically

## Next Session Read Order

1. `AGENTS.md`
2. `docs/planning/2026-03-23-scgp-context-bootstrap.md`
3. `README.md`
4. `.continue-here.md`
5. `PROJECT_CONTEXT.md`
6. `src/utils/backup.ts`
7. `src/views/System.vue`
8. `src/database/init.ts`
9. `src/database/schema/class-schema.sql`
