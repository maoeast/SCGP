# Current State
- 任务：认知发展（cognitive）模块 P0 游戏包——K03/K01/K04/K05 均已交付，P0 四个游戏全部完成。
- 已完成：
  - K05_NUMBER_SENSE 数感小铺全链路（number-sense-data.ts + NumberSenseGame.vue + NumberSensePage.vue + registry + router），双模式（按数取物 / 多少比较），三级难度，纯点选交互。commit 8746c6e。
  - K04_ODD_ONE_OUT 落库契约 bug 修复：buildPerformanceData 字段对齐 K03 权威契约。commit 8746c6e。
  - 实测发现并修复两个运行时 bug（type-check 查不出，只有实玩暴露）：
    - K04 出题无解：buildMixedCategoryRound 原对每个陪衬项从不同类别各取一个，导致四项全异类、无正确答案。改为锁定单一主类别取全部陪衬项。commit 1f7ef92。
    - K05 L3「拿走」阶段完全无效：进入拿走段 basketCount 未同步减、且 click handler 只实现「加入」路径。补齐「从筐取出」交互 + item--removable 可点视觉。commit a82cf91。
  - type-check ✅  type-check:emotional ✅  build:web ✅。

# Next Action
- P0 实测校准（未完成，需线下真实儿童试跑）：安排 3–5 名不同诊断类型儿童对 P0 四游戏小范围试跑。除锁定难度数值外，还需继续跑 K05 L3 加法段、模式 B 各难度、K01 全链路——本次已证实运行时逻辑 bug 只有实玩才暴露。
- P1 下一步（实测稳定后）：K02_MISSING_ITEM → K09_ECHO_SEQ → K10_STORY_ORDER。

# Blockers
- P0 实测需线下设备 + 真实儿童，代理无法代做；P1 参数应待实测反馈后再定，避免照拍脑袋数字重复返工。

# Key Decisions
- 库表方案 B：cognitive 走 training_session，`source_table='cognitive_game_inline'`，actual_params 显式透传 summary_payload；GameContainer 按 moduleCode dispatch。
- PRD §7 第 0 条定位 = B（IEP 级可追踪）：每个认知游戏 performanceData 必须含 actual_params。
- K05 交互：纯点选（无拖拽），模式切换做进游戏内 HUD 分段控件（不依赖 GameContainer 设置面板）。
- 落库契约权威源：K03_PATTERN_NEXT 的 buildPerformanceData（accuracy_ratio / average_response_ms / 嵌套 actual_params），其余 K 系列组件必须与之对齐。

# Relevant Files
- `docs/planning/2026-07-19-cognitive-games-prd.md`（P1 游戏 PRD §4 K02/K09/K10）
- `src/components/emotional/games/PatternNextGame.vue`（K03，落库契约权威样板）
- `src/data/number-sense-data.ts`（K05 出题器）
- `src/components/emotional/games/NumberSenseGame.vue`（K05 游戏组件）
- `src/database/cognitive-games-api.ts`（K 系列公共 API，只认 accuracy_ratio/average_response_ms/actual_params）
