# Current State
- 任务：产品演示数据 v2 已完成并提交（950286d）——含合并到应用库（A 方案：42 演示学生与真实数据并存）+ 五轮评审收敛；交付终态
- 已完成：
  ① data.mjs：42 学生（8/31 资格日口径生日，预置头像）+ 19 班（12 当前+7 历史）+ 6 诊断画像（含发育迟缓/孤独症 ABC/ATEC）+ 17 量表生成器（判级对齐 driver）+ AI 会话/消息/记忆定义 + 时间轴重锚 2026-2027；ID 区间按 v2 实测扩容
  ② seed-demo-data.mjs：学年 UPSERT+单 active 归一化（seed/import 双路径）+ clearDemoData 全覆盖（训练三表 id OR student_id 双条件、班级 v1 历史宽区间 20001-40000、教师映射同步）+ ID 越界守卫（COMMIT 前抛出）+ 原子写盘 + task_training 块（124 条）+ export/import 补 9 张 v2 表 + --summary 参数真实生效
  ③ build-demo-dist.mjs：隔离 Electron 自建底库（userData=os.tmpdir() 防EBUSY + 直写防旧快照回写）+ 五步流水线
  ④ 应用库合并（A 方案）：v1 残留清理（22 学生/43 班/5 教师）+ v2 seed 合入，终验八项全绿（43 学生=42 演示+刘星/19 演示班/在籍对账 0/单一 active 2026-2027/task_training 124）；合并前备份 database.sqlite.bak-20260920-pre-v2-merge（3.5MB）
  ⑤ 评审：R1 9 项（1🔴+5🟡+3🔵）→ R2 3 残留 → R3/R4/R5 复核 pass，全部闭环
- 提交：950286d（4 文件：data.mjs / seed-demo-data.mjs / build-demo-dist.mjs / seed-demo-data.md）；交接文档本提交收尾

# Next Action
- None（任务闭环）。用户侧可选：启动应用验收合并效果；如需回退应用库，用 database.sqlite.bak-20260920-pre-v2-merge 覆盖（先关应用）

# Blockers
- None

# Dead Ends
- 底库 userData 放项目目录内 → vite watcher EBUSY 崩溃（必须 os.tmpdir()）
- db.saveNow() 落盘会被防抖旧快照覆盖（2.4MB→16KB 回退）→ 必须 db.export()+saveDatabaseAtomic 直写
- **应用运行中向其库写数据会被防抖回写覆盖**（本会话实测：seed exit 0 且复验全绿，几分钟后被回写）→ 写库前必须确认 scgp/electron 进程已退出
- v1 演示库升级 v2 不能直接 seed（v1 班级占 20001-20043 vs v2 清理只认 20001-20019）→ 必须先扩区间一次性清理

# Key Decisions
- 年级-生日口径：getExpectedGradeLevel 反推（expected=4+(age-6)，8/31 资格日）；3 个刻意跳级/留级案例（10030/10033/10039）
- CPEP-3 演示数据走旧记录形态（无 report_snapshot，报告页只读重算）
- AI 演示数据：4 会话×内置智能体专业问答 + 8 条已确认记忆（含 pinned/safety_critical）
- task_training 记录形态对齐 SelfCareTrainingAPI（entry 'life-skills'、session_family 'task_training'、completionBreakdown）
- 学年翻转让 seed/import 都走 UPSERT + 单 active 归一化（INSERT OR IGNORE 在旧库会保留旧 is_active）

# Relevant Files
- scripts/seed-demo-data/data.mjs（42 学生/19 班/画像/AI 数据/17 生成器/扩容后 ID_RANGES）
- scripts/seed-demo-data.mjs（seed 主流程+守卫+原子写+全量 export/import）
- scripts/build-demo-dist.mjs（Electron 自建底库+盘点+校验+打包）
- docs/guides/seed-demo-data.md（v2 文档）
- output/SCGP演示数据包-20260920.zip（客户端分发包，838KB，不入库）
