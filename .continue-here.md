# 当前任务：评估质量追踪 Phase 2/3 + ATEC 完成报错修复（代码完成，待提交）

## 热修复（2026-09-01 用户真机日志反馈）
ATEC 完成评估报错，日志定位两个真 bug，均已修复：
1. **`require is not defined`**（BaseDriver.saveQualityMetrics）：Phase 1 我用了 CJS `require()`，
   渲染进程 ESM 下抛错被 try/catch 吞掉 → 质量指标从未入库。
   修复：改为顶部 ESM import `getDatabase`（BaseDriver.ts，无循环依赖）
2. **`CHECK constraint failed: report_type IN (...)`**：report_record 的 CHECK 约束缺 'abc'/'atec'
   （上一会话加量表漏同步）→ ATEC/ABC 报告中心记录插入失败 → persistAssessment 整体抛错。
   修复三处：init.ts 建表约束、migrate-report-constraints.ts 重建表约束 + needsMigration 检测
   补 'abc'/'atec'；用户旧库下次启动自动触发约束迁移（重建表+复制数据+重建索引+恢复视图）
- 验证：type-check ✓、3 组测试无回归 ✓、needsMigration 检测逻辑（旧库 true/新库 false）与
  新约束接受 abc/atec 插入均已 node:sqlite 实测 ✓
- 注：用户库里已失败的那条 ATEC 评估（atec_assess id=3）主记录在，但 report_record 缺失且质量列为 NULL，
  报告中心不显示该条属预期；重新跑一次 ATEC 即产生完整记录

## 第二轮修复（同日，报告页加载崩溃）
ATEC 持久化已成功后报告页炸：`Report.vue:141 Cannot read properties of undefined (reading '0')`。
- **根因**：atec/Report.vue `loadAssessment` 用 `db.exec(sql, params)` 按 sql.js 原生形态读返回值
  （`rows[0].values[0]`），但 `SQLWrapper.exec(sql)` 无参数无返回值 → assessRows 恒 undefined。
  与上一会话"ABC Report.vue 数据库查询方法"是同款 bug，ATEC 当时没被走到
- **修复**：atec/Report.vue 改 `db.all()`（对象数组，字段名取值），与 abc/Report.vue 修法一致
- **全仓排查同款误用**（`db.exec` 读返回值 + db 来自 getDatabase()=SQLWrapper）：
  - src/views/devtools/ClassSnapshotTestLite.vue：7 处 exec 按原生形态用 → 改 `db = getRawDB()`
    （局部 db 直指原生 sql.js Database，get/run 签名兼容；仅 dev 可达，生产路由折叠）
  - 其余 db.exec 均在迁移文件（db 是 rawDb 原生形态，用法正确）或 SQLTest.vue.backup（死文件）
  - MigrationVerification/SchemaMigration（devtools）已用 getRawDB()，正确
- 验证：type-check ✓、2 组测试 ✓
- **教训已记忆**：SQLWrapper.exec 是 DDL 专用（无参无返回）；查询一律 all/get。视图层拿 getDatabase()
  就当 SQLWrapper 用；需要原生形态显式 getRawDB()

## 第三轮修复（同日，AI 工具不含 ABC/ATEC）
AI 助手回复说 ATEC 不在受支持清单（get_assessment_trend 只覆盖 13 个量表）——加量表时漏同步 AI 工具链。
- **根因**：services/assessment-score-adapters.ts 的 SCORE_ADAPTERS 注册表只有 13 个量表（crt/cognitive_self 明确不支持），abc/atec 无适配器 → enum 无、模型无法取分
- **修复**：
  - assessment-score-adapters.ts：新增 abcAdapter/atecAdapter（flat-number 模式；dimension_scores/subscale_scores 是 {code:rawScore}；scoreNote 带总分区间+维度 code→中文对照；无 API 类故适配器内直接 getDatabase().all 查库）+ 注册进 SCORE_ADAPTERS（15 个）
  - ai-tools.ts：get_assessment_trend description 更新为 15 个量表（enum 自动跟进注册表）
  - 顺带核实：assessment-profile.ts SCALE_DOMAIN_MAP 已有 abc/atec→social（上次配对）；get_student_profile 遍历 SCORE_ADAPTERS 自动包含
- 测试：assessment-score-adapter.test.ts 增至 13 场景（新增 12: ABC/ATEC flat-number 归一化、13: 注册表源码断言——因 adapters import @/ 别名 jiti 不可直接 import，用 fs 读源码断言）
- 验证：type-check ✓、13 场景测试 ✓、quality/random-pattern 测试 ✓
- **教训已存长期记忆**：新增量表同步清单 7 处（catalog/建表+质量列/report_record 约束×3/api 联合类型+resolveModuleCode/AI 适配器+description/SCALE_DOMAIN_MAP/Driver+Report）

## 当前状态
- Phase 1 已提交：`84cb587`
- **Phase 2/3 + 三轮热修复均未提交**（等用户确认）

## Phase 2 已实现：管理后台质量看板
- `src/database/assessment-quality-api.ts`（新）：`AssessmentQualityAPI`
  - `getQualitySummary()`：17 表逐一聚合（总数/已追踪/very_fast/fast/suspicious/平均用时）
  - `getQualityFlags(limit)`：quality_note 非空的明细（学生、时间、用时、标记），按时间倒序
  - `isSuspiciousNote()`：兼容 'suspicious' / 'very_fast+suspicious' 组合标记
  - 纯只读 SELECT；表名硬编码无注入面；组合计数语义已用 SQLite 实测验证
- `src/views/system/AssessmentQualityBoard.vue`（新）：admin-only 看板页
  - 汇总表（17 行）+ 标记明细表（可跳报告页，复用 buildAssessmentReportRoute）
  - 量表名从 assessment-scale-catalog 派生（单一真源）
- 路由：`/system/quality`（router/index.ts，`System` 同级 admin-only，icon chart-bar）

## Phase 3 已实现：随机作答模式检测（仅测验类）
- `src/types/assessment.ts` 新增：
  - `detectRandomPattern(answers, options?)`：三信号检测（纯函数）
    1. rt 漂移：后半/前半均值比 < 0.5 且整体 < 5s/题 →「赶进度」
    2. 滑窗重复：连续 8 题答案模式与上一窗口完全一致占比 > 0.3 →「无脑循环」
    3. 直线作答：≥20 题且同选项占比 ≥ 0.95
    - 守卫：<10 题不判定；无有效 rt 时漂移信号 null
  - `mergeSuspiciousNote()`：组合标记（'very_fast' + suspicious → 'very_fast+suspicious'，幂等）
  - `RANDOM_PATTERN_SCALE_CODES = ['crt', 'cognitive_self']`（仅儿童自答测验类）
- `AssessmentContainer.vue` completeAssessment：量表 ∈ 启用范围时跑检测，
  检出 → qualityNote 追加 '+suspicious' 入库，**不弹窗**（宽松质控，看板是唯一出口）

## 验证
- ✅ `npx jiti tests/assessment-random-pattern.test.ts`（9 组用例）
- ✅ `npx jiti tests/assessment-quality.test.ts`（8 组用例）
- ✅ `npx jiti tests/assessment-score-adapter.test.ts`（无回归）
- ✅ `npm run type-check`
- ✅ report_record 约束迁移检测与新约束插入 node:sqlite 实测
- ⬜ 真机复测：重启应用（触发约束迁移）→ 重跑 ATEC → 完成无报错；看板可看质量数据

## Git 状态
- 待提交：assessment-quality-api.ts（新）、AssessmentQualityBoard.vue（新）、
  router/index.ts、types/assessment.ts、AssessmentContainer.vue、BaseDriver.ts、
  database/init.ts、database/migrate-report-constraints.ts、
  tests/assessment-random-pattern.test.ts（新）
- 未跟踪无关文件（勿提交）：`.agents/`、`thincoder-main.zip`、`docs/references/physical-equipment/aac.html`

## 真机冒烟清单
- 重启应用（自动迁移 report_record 约束）→ 重跑 ATEC → 完成评估无报错
- 报告页看用时卡；旧记录不出现用时卡；快速作答（<2s/题）看温和提示
- admin 登录 → 「评估质量看板」；CRT 乱答（一路选 A）→ 看板出现 suspicious 标记

