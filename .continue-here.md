# Current State
- 任务：激活文件功能——生成端一键导出激活密钥文件（.lic），激活端一键导入；生成端新增纯 HTML 离线版（双击即用）；license-generator-dist 冗余文件清理。
- 已完成：
  - 主程序激活端「导入激活文件」：`src/utils/license-file.ts`（提取逻辑，兼容 .lic 纯激活码与 .txt 摘要文件）；`Activation.vue`（未激活页）与 `System.vue`（更新授权区）均有导入按钮，解析后自动填入激活码，验证链零改动（仍走 LicenseManager 原有 RSA 验签）。
  - HTML 离线生成器：`license-generator-dist/generator.template.html`（模板，内联 Web Crypto 签名，RSASSA-PKCS1-v1_5 + SHA-256，与主程序验证端/Node 生成端完全同算法）；`scripts/build-license-generator-html.mjs`（注入 `.keys/private.pem` 产出 `scgp-license-generator.html`）；产物内嵌私钥不入库（.gitignore 已加）。
  - 生成端可「导出激活文件(.lic)」（Blob + 下载），兼容现有 txt 存档。
  - 目录清理：删除历史示例 txt×3、public-key.pem（主程序已内嵌公钥，generate-license.js 同步逻辑已关闭）、dist/（旧 64MB 打包产物）、.tmp/（审计旧副本，含私钥副本）。
  - 测试 10 项新增全过（`license-file-contract` + `license-generator-html`，后者含 Node webcrypto 与 crypto.sign 签名交叉验证 + 公钥验证 + 主程序解析格式闭环），已挂入 `test:core:node`；`npm run type-check`、`npm run build:web` 通过；CLI --trial 实测正常且不再生成 public-key.pem。

# Next Action
- 等待用户验收/提交。提交建议分组：① 主程序导入（license-file.ts + 两个 Vue）② 生成端（模板+构建脚本+gitignore+README+测试+package.json）。

# Blockers
- HTML 产物未在真实浏览器（Chrome/Edge file:// 环境）实机点击验证——签名算法已由 Node webcrypto 交叉验证等价，但 file:// 下 Web Crypto/下载行为建议人工双击确认一次。
- 既有 3 项测试失败与本次无关（AI 悬浮入口、module-registry social/cognitive、production-debug-boundary，HEAD 上同样失败）。

# Key Decisions
- 激活文件格式不发明新协议：.lic = 纯激活码一行；导入器兼容任意含 SPED- 的文本（含现有 .txt 摘要），提取只做格式校验，签名验证仍走原链。
- HTML 生成器内嵌私钥（与现有 .keys 明文本地存放同等风险），产物不入库；模板入库。
- Electron GUI 版保留不动，HTML 版为新增推荐入口。

# Relevant Files
- `src/utils/license-file.ts`、`src/views/Activation.vue`、`src/views/System.vue`
- `license-generator-dist/generator.template.html`、`scripts/build-license-generator-html.mjs`、`license-generator-dist/README.md`
- `scripts/tests/license-file-contract.test.mjs`、`scripts/tests/license-generator-html.test.mjs`、`package.json`、`.gitignore`
- 产物（不入库）：`license-generator-dist/scgp-license-generator.html`
