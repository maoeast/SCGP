---
status: active
last_updated: 2026-04-03
scope: login entry UI has been repeatedly refined into the current left-brand / right-login shell, and login-page logo configuration is now separated from the global system logo; current follow-up should manually verify Electron runtime behavior, especially layout positioning and system-settings persistence for the new login-logo path
---

> Responsibility: the only current handoff file for unfinished work.
> Read when: continuing work from a previous session.
> Not responsible for: replacing `AGENTS.md` repo rules or `PROJECT_CONTEXT.md` global context.

## Session Closeout (2026-04-03)

Current task status:

- the login entry redesign requested across this conversation is now landed in current code reality:
  - `src/views/Login.vue`
- the login page is now componentized into:
  - `src/components/login/SchoolPanel.vue`
  - `src/components/login/LoginCard.vue`
  - `src/components/login/InputField.vue`
  - `src/components/login/PrimaryButton.vue`
- the current login layout direction is now:
  - two-column shell
  - left brand panel + right login form
  - desktop ratio target is now closer to `45 / 55`
  - mobile / narrow-window fallback remains stacked layout
- the login visual direction has been pushed from the earlier blue-first baseline into a softer lake-blue / teal-leaning education-system entry:
  - restrained and non-marketing
  - no purple bias
  - no animated AI-style background
  - left brand panel now uses explicit positioned blocks for:
    - top-left logo
    - centered title stage
    - lower school / description stage
- a reusable login-theme variable system now exists in:
  - `src/utils/login-theme.ts`
- system config now owns login-entry theme / brand settings through:
  - `src/stores/systemConfig.ts`
  - `src/main.ts`
- system settings can now edit login-entry theme / brand fields in:
  - `src/views/system/SystemSettings.vue`
- new persisted config keys now used by current code reality include:
  - `login_theme_variant`
  - `theme_primary_color`
  - `login_logo_path`
  - `brand_panel_description`
- current configuration boundary is now:
  - global system logo remains `logo_path`
  - login entry logo is now separate via `login_logo_path`
  - login page no longer consumes configurable `brand_panel_title` / `brand_panel_subtitle`
  - login brand text now resolves from:
    - system name
    - school name
    - brand description
- default init / mock config coverage for those keys was added in:
  - `src/database/init.ts`
  - `src/database/sqljs-init.ts`
  - `src/database/mock-data.ts`
- system-settings UI changes landed in this conversation include:
  - basic settings keep the global system logo upload
  - login-page branding / theme now has its own login-logo upload
  - login-page branding / theme removed editable brand-title and brand-subtitle fields
- login-card UI refinements landed in this conversation include:
  - lower emphasis title sizing
  - equalized card padding
  - rectangular primary button with disabled state driven by input completeness
  - only the low-emphasis emergency reset link remains in the footer
- current code now applies login-entry CSS variables through JS `setProperty`:
  - initial application path: `src/main.ts`
  - runtime store application path: `src/stores/systemConfig.ts`
  - preview-time application path: `src/views/system/SystemSettings.vue`
- verification completed in this conversation:
  - `npm run type-check` passed
- verification not completed in this conversation:
  - no `vite build`
  - no manual Electron runtime QA
  - no visual confirmation yet that:
    - login-title / logo / school / description positioning matches the intended screenshots in real Electron window sizes
    - system logo and login logo persist independently after save + relaunch
    - login page preview and actual runtime render stay aligned after changing theme preset / primary color

Next step:

1. manually verify the new login entry flow in a real runtime
2. first implementation action:
   - launch the app, open `系统管理 -> 系统设置`, set different images for system logo and login logo, save, then relaunch and verify the login page in Electron-sized windows
3. during that verification, explicitly test:
   - left/right `45 / 55` visual balance on normal desktop width
   - left brand panel positioning:
     - logo at top-left
     - title stage centered in the intended mid area
     - school / description stage in the intended lower area
   - stacked fallback on narrower widths
   - login success / failure states
   - remembered username behavior
   - system-settings changes to:
     - system logo
     - login logo
     - theme preset
     - primary color
     - brand description
   - full persistence after saving settings and reopening / reloading the app
4. after runtime QA:
   - decide whether `Activation.vue` should stay separate or align visually with the new login-entry theme system

Problems / blockers / risks:

- no active code blocker remains
- important runtime gap:
  - this conversation only completed static verification
  - the new login shell, theme injection, separate login-logo persistence, and settings preview still need manual runtime confirmation
- important scope boundary:
  - current theme system is for the login-entry experience, not a full-site theme engine
  - do not describe it as platform-wide theming
- important product boundary:
  - `Activation.vue` was not migrated in this conversation
  - it may now visually diverge from the new login page until a deliberate follow-up slice decides otherwise
- important config boundary:
  - `SystemSettings.vue` preview temporarily applies login CSS variables while the page is open, then restores the previous theme snapshot on unmount
  - future changes here should preserve that preview/restore behavior
- important compatibility boundary:
  - historical `system_config` rows may still contain `brand_panel_title` / `brand_panel_subtitle`
  - current code intentionally ignores them instead of running a destructive cleanup migration
- local workspace still contains unrelated untracked files:
  - `sql_js_持久化模块标准实现方案_V1.1.md`
  - `tree.txt`
  - treat them as local-only materials, not product work to include in this handoff commit

Key decisions made in this conversation:

- split the login page into dedicated entry components
  - reason: the user explicitly requested componentized structure, and the old single-file login page mixed layout, form controls, and branding too tightly
- keep the login redesign outside the shared backend admin-page shell
  - reason: login is a standalone entry flow, not a backend business page
- use CSS variables plus JS `setProperty` for login theming
  - reason: the user explicitly required runtime primary-color changes and system-settings control
- store login theme and brand fields in existing `system_config`
  - reason: the repo already has a working local config persistence path, so this avoids inventing a second settings store or schema
- centralize theme application in `systemConfigStore` and bootstrap it from `main.ts`
  - reason: theme application should not depend on only the login page being opened first
- soften the palette from hard admin blue toward lake-blue / teal
  - reason: the user explicitly rejected the earlier police / gov-admin feel and asked for a warmer, softer education / rehab direction
- limit the first version to login-entry theming, not platform-wide theming
  - reason: broad theming would expand scope into many unrelated pages and settings consumers
- separate login logo from the global system logo
  - reason: the user explicitly required login-page brand asset management to live under login branding / theme instead of reusing the backend system logo
- remove configurable login brand title / subtitle
  - reason: the user explicitly required a simpler login-brand model; current design now uses system name + school name + brand description only
- switch the left brand panel from flow-based grouping to explicit positioned blocks
  - reason: repeated flow-layout attempts produced visibly broken composition; explicit stages are simpler to tune toward the provided screenshots

## Next Session Read Order

1. `AGENTS.md`
2. `docs/planning/2026-03-23-scgp-context-bootstrap.md`
3. `README.md`
4. `.continue-here.md`
5. `PROJECT_CONTEXT.md`
6. `src/views/Login.vue`
7. `src/views/system/SystemSettings.vue`
8. `src/stores/systemConfig.ts`
9. `src/components/login/SchoolPanel.vue`
