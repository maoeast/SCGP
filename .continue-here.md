---
status: active
last_updated: 2026-04-05
scope: TGMD-3 assessment formal implementation, runtime verification, and UI copy alignment
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作或并提供了快速上下文
> Not responsible for: 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**TGMD-3 已从能力评估页的占位卡片接成正式统一评估链路，并完成本机 runtime 主流程验证。**

- **本次已完成**:
  - 新增 `TGMD-3` 题库与本地常模配置：`src/database/tgmd3-questions.ts`
  - 新增统一驱动：`src/strategies/assessment/Tgmd3Driver.ts`
  - 新增专用报告页：`src/views/assessment/tgmd-3/Report.vue`
  - 接入统一评估入口、学生选择页、统一容器保存逻辑、报告中心、学生评估记录页、报告路由
  - 新增数据库表/API/报告记录迁移支持：`tgmd_3_assess` / `tgmd_3_assess_detail`
  - 新增旧库兼容修复：启动时自动为旧 `tgmd_3_assess` 表补 `domain_feedback` 列，修复真实保存时报错
  - 完成 live runtime 验证：
    - `TGMD-3` 评估保存成功
    - 报告中心可正常打开 `TGMD-3` 报告
    - 学生详情页的评估记录可正常打开 `TGMD-3` 报告
  - 按用户提供的 Excel 更新评估页文案：
    - 技能名称改为中文表格口径
    - `测试任务` 改为 `器材准备`
    - `录入方式` 改为 `场地路线与测试指导语`
    - `观察要点` 改为 `观察要点动作标准`
    - 技能总分按钮仅保留分值
    - 技能总分区新增注释：`每项技能先做1次练习，然后正式测2次，将两次所有动作标准的得分直接相加即为该技能总分。`

- **当前代码现实**:
  - `TGMD-3` 已可从能力评估页进入
  - 可选择学生进入统一评估容器
  - 可保存评估记录并生成报告
  - 报告中心与学生详情评估记录已可打开 `TGMD-3` 报告
  - 当前录入方式是 **13 个技能的总分录入**，不是逐动作标准逐试次点选

## 下一步

**第一个动作**: 运行 `npm run electron:dev`，再做一轮 `TGMD-3` 内容层 live UAT，重点核对：
1. 评估页中的 `器材准备 / 场地路线与测试指导语 / 观察要点动作标准` 是否完全符合 Excel 口径
2. 报告页中的常模等级、反馈文案、IEP 提取是否符合预期
3. 13 个技能总分录入在边界分值下是否仍能稳定保存并正确映射常模等级

## 遇到的问题 / 阻塞 / 风险

1. **已解决问题**:
   - 真实保存时曾报错：`table tgmd_3_assess has no column named domain_feedback`
   - 已通过启动期兼容迁移补列修复：`src/database/init.ts`

2. **当前风险**:
   - `docs/references/TGMD-3/tgmd3_scoring_engine.html` 中位移技能展示标准与满分 `46` 分口径不完全一致，因此当前正式实现选择了“技能总分录入”而不是“逐标准双试次录入”
   - 当前 `TGMD-3` 的技能名、器材、路线、观察标准都已切到 Excel 口径，但仍应注意后续若用户继续提供更高优先级资料，需以新资料为准重新对齐
   - 当前工作区仍存在一批不是本轮新实现的一部分的既有未提交改动：
     - `docs/references/GMFM/*`
     - `docs/references/TGMD-3/`
     - `src/config/feedbackConfig.js`
   - 这些改动在提交或清理前仍需谨慎区分

## 关键决策

1. **TGMD-3 走现有统一评估主链，而不是新建独立页面体系**
   - 原因：仓库当前评估主线是 `AssessmentContainer + ScaleDriver + report_record`

2. **TGMD-3 量表代码使用 `tgmd_3`**
   - 原因：与 `src/config/feedbackConfig.js` 现有配置键保持一致

3. **正式实现采用“13 个技能总分录入”而不是“逐标准双试次录入”**
   - 原因：本地 HTML 资料中的位移技能标准数与 `46` 分满分口径存在冲突，直接做逐标准录入会把不稳定题库固化进正式主链

4. **旧数据库不要求手工清库，而是在启动时自动补齐缺失列**
   - 原因：真实 runtime 已暴露旧 `tgmd_3_assess` 表缺列问题，自动兼容迁移比要求用户重置数据库更稳妥

5. **评估页展示文案以用户提供的 Excel 为当前 UI 文案事实来源**
   - 原因：用户明确要求从表格导入 `技能 / 器材准备 / 场地路线与测试指导语 / 观察要点动作标准`

## 主要修改文件

- `src/database/tgmd3-questions.ts`
- `src/strategies/assessment/Tgmd3Driver.ts`
- `src/views/assessment/tgmd-3/Report.vue`
- `src/database/api.ts`
- `src/database/init.ts`
- `src/database/migrate-report-constraints.ts`
- `src/database/migration/migrate-emotional-foundation.ts`
- `src/views/assessment/AssessmentContainer.vue`
- `src/views/assessment/AssessmentSelect.vue`
- `src/views/assessment/SelectStudent.vue`
- `src/views/Reports.vue`
- `src/views/student-detail/assessment-records.ts`
- `src/features/assessment/report-routes.ts`
- `src/router/index.ts`
- `src/views/assessment/components/QuestionCard.vue`
