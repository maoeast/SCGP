# Current State
- 任务：自理训练模块已打通“选学生 -> 执行页 -> training_records + training_session 写入”的主线闭环，下一会话继续补训练记录详情接入
- 已完成：
  - `src/database/self-care-training-api.ts` 已新增自理训练专用写链，固定把 `TaskTrainingExecutionResult` 写入 `training_records + training_session`
  - `src/views/self-care/TaskExecution.vue` 已新增步骤执行页，按 `meta_data.steps[]` 逐步记录 `completionLevel / errorType / teacherNotes`
  - `src/features/self-care/self-care-routes.ts` 已新增 `/self-care/execute/:taskId/:studentId`
  - `src/views/self-care/SelectStudent.vue` 已从回跳列表页改为直接进入执行页
  - `src/utils/training-launch.ts` 已把 `task_training` 的统一启动入口改为直接进入执行页
  - 定向测试已补齐并通过：`self-care-routes`、`self-care-task-pages`、`self-care-training-api`、`training-launch`
- 产出物：
  - `src/database/self-care-training-api.ts`
  - `src/views/self-care/TaskExecution.vue`
  - `src/features/self-care/self-care-routes.ts`
  - `src/views/self-care/SelectStudent.vue`
  - `src/utils/training-launch.ts`
  - `scripts/tests/self-care-routes.test.mjs`
  - `scripts/tests/self-care-task-pages.test.mjs`
  - `scripts/tests/self-care-training-api.test.mjs`
  - `scripts/tests/training-launch.test.mjs`

# Next Action（必须原子级——第一步可执行的单一动作）
- 修改 `src/views/training-records/ModuleTrainingRecords.vue`，为 `resource_type = task_training` 新增自理训练详情跳转分支，避免继续误跳 `/games/report`

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 自理训练执行主入口固定为 `/self-care/execute/:taskId/:studentId`：训练计划、首页、选学生页都统一走同一执行链，避免继续停在列表壳页
- 自理训练记录固定写入 `training_records + training_session`：不复用旧 `train_log`，也不复用游戏写链的 `sessionFamily = game`
- `task_training` 的真实主键固定为 `sys_training_resource.id`：`training_records.task_id = NULL`，步骤事实源继续只认 `meta_data.steps[]`

# Relevant Files（最小集——仅列下一步会用到的）
- `src/views/training-records/ModuleTrainingRecords.vue`
- `src/views/training-records/components/GameRecordsPanel.vue`
- `src/views/StudentDetail.vue`
- `src/views/self-care/TaskExecution.vue`
- `src/database/self-care-training-api.ts`
