# Current State
- 任务：继续增强 SCGP 感官统合训练游戏“空中魔法指挥棒（Air Conductor）”，当前已完成沉浸式视觉反馈升级到“粒子 + 尾迹 + 波纹 + 左右手分音区/色温节拍轨迹”。
- 已完成：
  - `src/components/games/pose/AirConductorGame.vue` 已挂接独立视觉层，包含手腕发射点、七彩音符粒子、尾迹点、波纹脉冲和左右手分色温节拍轨迹。
  - `src/components/games/pose/air-conductor-runtime.ts` 已新增镜像发射点、音符粒子、尾迹点、波纹脉冲、节拍轨迹段等纯函数与常量。
  - `src/types/air-conductor.ts` 已补齐视觉反馈所需的粒子、尾迹、波纹、节拍轨迹类型。
  - `scripts/tests/air-conductor-runtime.test.mjs` 已覆盖镜像坐标、音符粒子、尾迹、波纹、节拍轨迹方向与左右手音区/色温规则。
  - `scripts/tests/sensory-hand-games.test.mjs` 已覆盖 Air Conductor 视觉层接线存在性断言。
- 产出物：
  - `src/components/games/pose/AirConductorGame.vue`
  - `src/components/games/pose/air-conductor-runtime.ts`
  - `src/types/air-conductor.ts`
  - `scripts/tests/air-conductor-runtime.test.mjs`
  - `scripts/tests/sensory-hand-games.test.mjs`

# Next Action
- 修改 `src/composables/usePoseAudio.ts` 的 `ensureAudioReady()`、`createPentatonicScale()`、`mapHeightToScaleIndex()`、`mapHorizontalToFilterFrequency()` 和 `syncAudioForPose()`，把 Air Conductor 音色改为低刺激五声音阶方案：`sine` 主波、限制音域到 `C3-E5`、滤波上限降到约 `2200Hz`、attack 调到 `0.10-0.12s`，并补充对应运行时测试。

# Blockers
- None

# Key Decisions
- 继续复用现有主链：`GamePlay -> training_records -> training_session -> report_record`，不新增 `air_conductor_sessions` 旁路。
- 视频镜像、骨骼 overlay、粒子层、尾迹层、波纹层、节拍轨迹层必须保持同一镜像坐标系。
- 下一轮音频优化优先走“低刺激、治愈感、五声音阶锁定”方向，不再追求高频电子感。

# Relevant Files
- `src/composables/usePoseAudio.ts`
- `src/components/games/pose/air-conductor-runtime.ts`
- `src/components/games/pose/AirConductorGame.vue`
- `scripts/tests/air-conductor-runtime.test.mjs`
- `scripts/tests/sensory-hand-games.test.mjs`
