# Current State
- 任务：登录页默认主题静蓝主色 #4FB3BF → #3C9BA6（**未提交**）
- 已完成：
  - `src/utils/login-theme.ts`：DEFAULT_LOGIN_PRIMARY_COLOR → #3C9BA6；calm-blue（静蓝）preset 主色与派生色整体
    调整（primary/渐变 #3C9BA6→#2F838C、brand 渐变、badge/layout/brandPanel/formPane rgba 主色、buttonShadow 同步）
  - 数据库种子 ×4：init.ts（两处）、sqljs-init.ts、mock-data.ts 的 theme_primary_color → #3C9BA6
  - `src/database/init.ts`：新增幂等迁移——已有 DB 中 theme_primary_color 仍为旧种子色 #4FB3BF 时自动
    UPDATE 为 #3C9BA6（仅覆盖未自定义过的值，不碰用户改过的）
  - 组件 CSS fallback：InputField / LoginCard / PrimaryButton 的 var(--login-primary, #4FB3BF) 等 → #3C9BA6 系
  - 默认主题预设本身已是 calm-blue（静蓝），未变
- 产出物：type-check ✓、jiti 校验 preset 值 ✓

# Next Action
- dev 目检登录页：整体色调应为静蓝 #3C9BA6（按钮/输入框聚焦/品牌面板/外壳渐变），确认后提交
- 待提交存量（已确认可用）：背景统一改动（src/assets/layout.css、src/views/Layout.vue、TrainingResources.vue 去 stripe）

# Blockers
- None

# Key Decisions
- 主色只动 calm-blue 预设 + 默认值；暖黄/润绿/自定义预设不动
- 已有 DB 用幂等 UPDATE 迁移（条件：值 === '#4FB3BF' 即未被用户自定义），避免覆盖用户配置
- 登录主题背景媒体（login_theme_backgrounds）未动——背景图/视频与主色独立

# Relevant Files
- src/utils/login-theme.ts、src/database/init.ts、src/database/sqljs-init.ts、src/database/mock-data.ts
- src/components/login/InputField.vue、LoginCard.vue、PrimaryButton.vue
