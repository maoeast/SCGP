# Current State
- 任务：重构表情能量球（G08_ENERGY_BALL）页面布局
- 已完成：
  - 三栏水平布局 → Header+Center+Sidebar 模块化布局
  - 中心舞台：260px 能量球 + 任务提示 + 反馈文字 + 320px 进度条，垂直居中为视觉焦点
  - 右侧边栏（280px）：摄像头 + 检测状态 + 4 表情紧凑横排列表（emoji + 标签 + 迷你进度条）
  - 导航按钮移至侧边栏表情卡片下方（margin-top: auto 推底）
  - 删除重复 header（GameContainer 已有工具栏），设置 padding-top: 100px 避让绝对定位工具栏
  - 移除页面四角圆角（GameContainer.vue border-radius: 28px 已删除）
  - 移除自身渐变背景，使用父容器背景
- 产出物：`src/components/emotional/games/EnergyBallGame.vue`、`src/views/emotional/games/GameContainer.vue`

# Next Action
- 启动 `npm run electron:dev`，进入 情绪调节 → 表情能量球 手工验证：
  1. 顶部无重复导航栏，工具栏正常显示
  2. 中心能量球 + 进度条为视觉焦点，右侧侧边栏摄像头+表情卡片正常
  3. 校准后笑一笑 → hold-time 环 → 能量积累 → 30%/60%/100% 分级反馈
  4. 侧边栏底部导航按钮正常

# Blockers
- None

# Key Decisions
- 不新增 header，复用 GameContainer 已有工具栏：避免重复导航
- 导航放侧边栏底部而非独立 footer：视觉更紧凑，低认知负荷

# Relevant Files
- `src/components/emotional/games/EnergyBallGame.vue`
- `src/views/emotional/games/GameContainer.vue`
