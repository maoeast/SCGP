---
status: active
last_updated: 2026-04-11
scope: care_scene 已完成“旧左右分栏移除 + 沉浸式转正式入口 + 正式记录闭环接入”；下一步是做 Electron smoke 和本地 DB 实写核验
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**本次会话（2026-04-11）已完成 `care_scene` 的正式链路切换：**

- 旧的“表达关心”左右分栏 / 模式切换链路已从主路径移除
- `SceneSelector -> /emotional/care-expression` 现在直接进入沉浸式训练，不再提供模式选择
- `/emotional/care-expression/immersive` 仅保留兼容重定向，不再是独立产品路径
- `compileCareSceneImmersive.ts` 已去掉 `deferred` 影子态，改为主库持久化上下文
- `src/database/api.ts` 已新增正式持久化入口，直接写：
  - `training_records`
  - `training_session`
  - `emotional_training_session`
  - `emotional_training_detail`
- 结果页“待接入正式记录 / 预览”文案已移除，按正式功能展示
- `care_scene` 配置缺失现在按 Fail-Fast 直接报错，不再回退旧模式
- 沉浸式 store 已补：
  - `student_id` 必填校验
  - `resource_id` 必填校验
  - 题目级结果收集
  - 切换学生/切换资源/组件卸载时的 `clearSession()` 状态清理
- 仓库级 `npm run type-check` 已重新通过

**本轮删除的旧链路文件（核心）**：

- `src/views/emotional/CareExpressionTraining.vue`
- `src/features/emotional/runtime/useEmotionalTrainingShell.ts`
- `src/composables/useEmotionalSession.ts`
- `src/features/emotional/adapters/compileCareScene.ts`
- `src/features/emotional/adapters/compileEmotionScene.ts`
- `src/features/emotional/adapters/index.ts`
- `src/features/emotional/engine/types.ts`
- `src/components/emotional/engine/*`
- `src/components/emotional/CareOptionCard.vue`
- `src/components/emotional/EmotionSelector.vue`
- `src/components/emotional/ReasoningCard.vue`
- `src/components/emotional/PerspectiveSwitchView.vue`

**当前还未完成 / 还未验证**：

1. **尚未做 Electron 实机 smoke**
   - 还没有在 Electron 中完整走一遍：
     - `care-expression/select`
     - 进入沉浸式训练
     - `intro -> 3 steps -> result`
     - 返回选择页 / 再练一次

2. **尚未做本地 DB 实写核验**
   - 代码路径已经确认写入 `getDatabase()` 主实例
   - 但还没有在本地库中实际确认：
     - `training_records`
     - `training_session`
     - `emotional_training_session`
     - `emotional_training_detail`
     是否都成功生成对应记录

3. **本地 `msedge-tts` 依赖问题仍未处理**
   - `electron/handlers/tts.mjs` 动态导入 `msedge-tts` 仍可能失败
   - 不阻塞本次 `care_scene` 主链切换，但会影响朗读体验

4. **工作区仍是 dirty**
   - 仓库内存在大量非本轮 handoff 文档之外的未提交修改
   - 其中一部分明显是本轮 `care_scene` 转正式相关代码，另一部分看起来是既有脏改动或其他工作线
   - 本轮交接提交时不能把这些内容混进一个笼统的 handoff commit

## 下一步

**第一个动作**：

- 在 Electron 中做一次完整 smoke：
  - 用真实学生进入 `表达关心`
  - 完成 3 个步骤到结果页
  - 然后立即查询本地数据库，确认正式记录是否写入 4 张主表

**紧接着要做的验证**：

1. 查询 `training_records` 中最新 `care_scene` 记录，确认 `student_id` 非空、`module_code = emotional`
2. 查询 `training_session` 中对应记录，确认 `session_family = care_scene`
3. 查询 `emotional_training_session` 与 `emotional_training_detail`，确认 step 明细完整
4. 从训练记录页 / 报告页实机确认新数据能被 UI 正常读取

## 遇到的问题 / 阻塞 / 风险

1. **当前只完成了代码级切换，尚未完成运行时实证**
   - 风险：
     - 代码虽然 type-check 通过，但真实路由切换、结果页保存、返回选择页等仍可能有运行时问题

2. **正式记录现在依赖 `student_id` / `resource_id` 严格存在**
   - 当前处理：
     - 编译阶段与训练页 hydrate 阶段都直接 fail-fast
   - 风险：
     - 如果上游入口漏传学生参数，训练将直接报错，而不会“凑合进入”
     - 这是有意为之，但需要 smoke 确认入口链都已满足约束

3. **工作区存在大量其他未提交改动**
   - 风险：
     - 如果后续直接 `git add .` 或打包大提交，容易把本轮之外的工作一并提交
   - 当前处理：
     - 本轮只更新交接文档，不擅自打包全部脏改动

4. **TTS 依赖仍是已知缺口**
   - 风险：
     - 问题不会阻塞正式落库，但会影响沉浸式播放体验

## 本次对话做出的关键决策及原因

1. **不保留“旧模式 + 新模式”并行**
   - 原因：
     - 用户要求模块统一化，目标是彻底转向沉浸式架构
     - 继续保留旧左右分栏只会让入口、行为和数据口径长期分叉

2. **`/emotional/care-expression` 直接成为唯一正式训练路径**
   - 原因：
     - 这样最符合“点击表达关心直接进入正式沉浸式”的产品预期
     - 兼容旧链接时，仅通过 `/immersive` 做无状态重定向即可

3. **配置缺失时 Fail-Fast，而不是回退旧链路**
   - 原因：
     - 本轮目标是“转正”，不是继续靠旧链路兜底
     - 回退会掩盖资源质量问题，也会让训练数据混入两套实现

4. **正式持久化直接接 `src/database/api.ts`**
   - 原因：
     - 用户明确要求切到正式主库接口
     - `DatabaseAPI` 已统一绑定 `getDatabase()` 主实例，适合接当前产品主线

5. **在 store 内补会话级状态收集与清理**
   - 原因：
     - 沉浸式训练需要在结果页前汇总所有 step 的最终选择、提示次数、响应时间
     - 同时必须保证切换学生 / 退出训练 / 组件卸载时不残留上一个孩子的数据

6. **旧 split-screen 相关组件直接物理删除，而不是先“保留未引用”**
   - 原因：
     - 用户要求目录整洁
     - 保留未引用旧文件会让后续维护者误判仍有双实现需要同步
