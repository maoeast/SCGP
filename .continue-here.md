# Current State
- **v1.1.4 第三次打包发布（2026-09-20 19:42，同版本覆盖）**：本次带上「学生详情页上下分层重构 + AI 记忆面板摘要卡 + gameCount 口径对齐（T25 方案 a）」。产物：`release/scgp-setup-1.1.4.exe` 265,994,789 B / sha512 base64 `5TbtMQqLEMG/…`（hex `e536ed31…`）+ blockmap 278,647 + latest.yml 337（releaseDate 2026-09-20T11:41:16Z）；`release:verify` 8/8 PASS；三件套已落 `/home/lighthouse/scgp/win/`（root:root 644），服务器 sha512 与本地逐字节一致，双源 curl 均 `version: 1.1.4`、exe URL HTTP 200
- 同版本覆盖语义（不变）：已装 1.1.4 的机器收不到本次更新的自动推送；1.1.3 及更老用户直接拿到含全部修复的 1.1.4
- 详情页重构已提交并推送（4 笔：`68b0573` refactor / `13ce8e4` fix scripts / `1afaff5` docs manual / `a7c7145` chore handoff）
- 待办（用户已拍板方向，未做）：**T24** care_instructions 结构化健康字段（表迁移+编辑弹窗+导出联动+警示条分段）；本轮以 healthNotes 完整透出过渡

# Next Action
- **T24**：care_instructions 结构化健康字段专项（用户拍板首选方案）：student 表 ALTER 迁移（sql.js）+ AddStudentDialog 编辑联动 + 导出联动 + StudentDetail.vue 警示条升级「医学状态 + 照护要求」分段。启动前先出方案等确认。

# Blockers
- None

# Dead Ends
- 用字符串定位改注释：`edit` 新旧串写成同一串会静默无操作，且近似拼写易改错（本次把 `ABCDDriver` 误改成 `ABCDiver`，靠 host 校验才发现）——改用行号/哈希定位（`edit line:` / `hashline_edit`）。
- asar 内按固定路径直查 chunk（`dist/assets/index-<hash>.js`）失败——重建后文件名变化；正确姿势是 `listPackage` 过滤 + 去反斜杠前缀再 `extractFile`。

# Key Decisions
- 存量演示数据取「定向修正脚本」而非重导整包：重导会覆盖库里的真实评估数据；脚本幂等、真实行不动、默认 dry-run。
- 同版本号可重复覆盖发布（1.1.4 已两次），代价是**已装同版本的机器收不到该次修复的自动更新**——修复以「下一次打包」带出。
- 量表满分以题库权重为单一真源（`ABC_SUBSCALE_MAX_SCORES` 派生、勿手写）；演示数据侧用导出常量 + 测试防漂移。

# Relevant Files
- 下一步：`src/views/StudentDetail.vue`、`src/views/student-detail/assessment-records.ts`、`src/views/student-detail/components/StudentMemoryPanel.vue`、`src/views/student-detail/components/AssessmentRecordsPanel.vue`
- 收尾时：`scripts/fix-legacy-abc-dimension-scores.ts`、`.thincoder/skills/release-deploy.md`（打包发布流程）
