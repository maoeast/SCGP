# Current State
- 任务：AI 学生记忆无数据排查+修复——已提交（2595d0b，未推送）；根因为批次状态机断点，用户真机验收通过
- 已完成：根因=finalizeAssistantTurn 从不调 markBatchSummarizing，批次卡 pending→commit CAS 恒败→记忆永不写入；修复：①补状态转移（失败释放占位）②recoverStaleActiveBatches(10) 超时自愈+存量卡死批次自动恢复 ③补偿任务接入 AiAssistant 全局挂载（启动一次+5分钟节流+isMounted 防泄漏）④failed 重试改 cancelled 让位保留回溯 ⑤超预算硬截断同样拦总结调用；三轮 advisor 评审收敛（8 项发现全处置）；新增行为测试 ai-memory-batch-state.test.mjs（真 sql.js 驱动状态机 5 用例）
- 产出物：提交 2595d0b（4 文件：stores/ai.ts、database/ai-api.ts、AiAssistant.vue、新测试）；未推送

# Next Action
- 推送 origin/main（用户未明确要求推送，待指示）
- 可选：设计文档 docs/planning/2026-08-05-ai-student-memory-design.md §6.2 补记 pending→summarizing 转移与 10 分钟 stale 恢复策略（r1 评审 #3 遗留的文档同步项，lease_until 仍为死列）

# Blockers
- None

# Dead Ends
- jiti 加载 ai-api.ts 走不通（.sql?raw + import.meta.glob 链）；解法=测试运行时从源码切取状态机方法生成临时模块（签名行做边界锚，勿用 jsdoc 文本做锚）
- SQLite 中 || 拼接任一侧为 NULL 结果为 NULL——存量 failed 行 last_error 可能为 NULL 时拼接标记会丢，行为无影响仅审计展示
- 服务器上 node -e 跑本地相对路径必 ENOENT——本地 size 核对在开发机跑，不要混在 ssh 命令里
- capture 脚本新增场景若种子挂错 fixture 家族（如 AI 记忆挂 assessments）种子静默不执行——必须挂 hasSeed('ai') 对应块
- 手册正文中部插截图占位会使后续已批准编号全部作废——插行必须走 plan+scenarios+capture+approvals 全管线

# Key Decisions
- 批次状态机修复策略：补 markBatchSummarizing 转移而非放宽 commit CAS；超时自愈用 updated_at（10 分钟）而非设计的 lease_until（仍为死列，文档同步项待办）；failed 重试置 cancelled 让位而非删行（保留回溯）
- 行为测试用源码切取+运行时生成临时模块（jiti 加载 ai-api.ts 走不通）

# Relevant Files
- src/stores/ai.ts（finalizeAssistantTurn/runMemoryCompensation，2595d0b）
- src/database/ai-api.ts（批次状态机 API + recoverStaleActiveBatches，2595d0b）
- src/features/ai/components/AiAssistant.vue（补偿任务调度入口，2595d0b）
- scripts/tests/ai-memory-batch-state.test.mjs（行为测试，2595d0b）
- docs/planning/2026-08-05-ai-student-memory-design.md（设计文档，§6.2 待补记状态机转移）
