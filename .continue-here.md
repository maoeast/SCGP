# Current State
- 当前任务：继续推进 cross-entry custom games；`Wave 1` 6 个目标已闭环，下一步进入 `Wave 2` 的 `S01_BURGER / 合作造汉堡`。
- 已完成到哪一步（具体到函数 / 文件 / 行为）：
  - `src/data/custom-game-registry.ts` 已新增 `S04_GIFT_MATCH` 并挂到 `social-communication`。
  - `src/router/index.ts` 已接入 `/emotional/games/gift-match` 路由。
  - `src/views/emotional/games/GiftMatchPage.vue` 与 `src/components/emotional/games/GiftMatchGame.vue` 已实现最小可运行链路，可启动、完成、落库并自动重开下一轮。
  - `src/views/emotional/GameRecordDetail.vue` 已为 `S04_GIFT_MATCH` 补齐中文化指标、礼物名、伙伴名、配对顺序与主题展示，不再暴露内部字段。
  - `npm run type-check:emotional` 已通过；`S04_GIFT_MATCH` UI 手动验证已通过。
- 当前产出物（代码 / 文件）：
  - `src/data/custom-game-registry.ts`
  - `src/router/index.ts`
  - `src/views/emotional/games/GiftMatchPage.vue`
  - `src/components/emotional/games/GiftMatchGame.vue`
  - `src/views/emotional/GameRecordDetail.vue`

# Next Action
- 下一步第一个动作（必须原子级，例如：修改 xx 文件的 xx 函数）
  - 修改 `src/data/custom-game-registry.ts`，新增 `S01_BURGER` 的 registry 定义并挂到 `social-communication`，`maxPlayers` 设为 `2`。

# Blocking / Risks
- 阻塞 / 风险（没有则写 None）
  - `S01_BURGER` 是首个双人共享场次游戏；实现后必须优先实机核对 `session_group_id / game_session_participants / 整组结束语义`，否则容易只跑通单人视角。

# Key Decisions
- `Wave 1` 当前以功能闭环为准，`S04_GIFT_MATCH` 已验收通过；后续直接进入 `Wave 2`。原因：当前 6 个低风险目标都已接入正式链路。
- 下一目标固定为 `S01_BURGER`。原因：它是首个真正验证共享场次与组事务写入的业务游戏。
- UI 细节统一延后到剩余游戏都闭环后再收口。原因：用户明确要求先完成功能，不在单个游戏阶段扩散视觉微调。

# Relevant Files
- `src/data/custom-game-registry.ts`
- `src/views/emotional/games/GameContainer.vue`
- `docs/planning/2026-04-13-cross-entry-custom-games-expansion-plan.md`
