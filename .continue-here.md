# Current State
- 任务：继续自理训练模块阶段 1，补齐 `task_training` 在资源中心界面的可见主链
- 已完成：`src/utils/resource-center-business.ts` 已将 `task_training` 识别为可见训练资源并归到 `life-skills`；`src/views/resource-center/TrainingResources.vue` 已接入 `task_training` 的资源类型显示、默认创建入口、图标与样式；新增 `src/views/resource-center/training-resource-ui.ts` 收口资源中心类型配置；新增 `scripts/tests/training-resource-ui.test.mjs` 覆盖 life_skills 创建入口与图标样式回归
- 产出物：`src/views/resource-center/TrainingResources.vue`、`src/views/resource-center/training-resource-ui.ts`、`scripts/tests/training-resource-ui.test.mjs`

# Next Action（必须原子级——第一步可执行的单一动作）
- 修改 `src/router/index.ts`，新增 `/self-care` 与 `/self-care/tasks` 路由壳，并将路由 `meta.moduleCode` 固定为 `life_skills`

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- `自理训练` 入口继续挂 `life_skills` 授权：当前授权体系只识别现有顶层业务模块代码
- `task_training` 阶段 1 继续复用训练资源的 `game` 展示主链：目标先打通“可见 / 可选 / 可启动”，不新增第三种展示维度
- 自理任务资源创建时强制落到 `module_code = life_skills`：避免资源中心误建到其他模块，保持后续计划与记录主链一致

# Relevant Files（最小集——仅列下一步会用到的）
- `src/router/index.ts`
- `src/views/Layout.vue`
- `docs/planning/2026-05-08-self-care-training-module-implementation-plan.md`
