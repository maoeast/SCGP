# Current State
- 任务：C10 IEP 双主链清理已提交；下一批次 C11 旧命名与原生开发依赖清理待新会话开工。
- 已完成：删除策略试验链 3 文件（`strategies-init.ts` / `SensoryIEPStrategy.ts` / `iep-generator-refactored.ts`）；`module-registry.ts` 去 strategy Map + `register/get/getAll IEPSstrategy` 三方法；`main.ts` 去 `initializeStrategies()` 调用；`types/module.ts` 删 `IEPStrategy`/`IEPResult`/接口两方法/`isIEPSstrategy`；`iep-generator.ts` 去未用 `IEPResult` import + 失效 `@deprecated` 注释；`ModuleDevTools.vue` 删策略列表卡+测试器卡+相关 script/style，保留模块元数据诊断。生产 IEP 主链 `iep-generator.ts` 静态 API 与内部实现未动。
- 产出物：C10 代码提交 + C10 交接文档提交（hash 见 git log）。

# Next Action
- 读 `src/components/AboutDialog.vue` + `scripts/export-resources.cjs` + `package.json` 核实 better-sqlite3 当前用法与 About 文案现状（以源码为准不照搬计划文档），再按 `docs/plans/2026-07-18-project-closeout-execution-plan.md` §C11 推进。

# Blockers
- None

# Key Decisions
- 生产 IEP 唯一主链确认为 `src/utils/iep-generator.ts`（静态 IEPGenerator，`IEPReport.vue`/`Records.vue` 消费）+ `game-performance-normalizer.ts`；策略试验链（refactored 生成器 + ModuleRegistry strategy API + `SensoryIEPStrategy` + `strategies-init` + `IEPStrategy`/`IEPResult` 类型）整体删除，registry 收敛为「模块元数据 + 开发诊断」单一职责。
- 删除 `IEPResult` 类型的强制连带：清理 `iep-generator.ts:21` 未用 import 与失效 `@deprecated 使用 IEPResult 替代` 注释——非生产生成器内部重构，仅为类型删除收尾。
- `ModuleDevTools.vue` 保留模块元数据诊断（C08 已隔离出生产构建），仅删策略区；桌面 UAT 未做，记「代码已实现 / 待桌面 UAT」。

# Relevant Files
- `src/components/AboutDialog.vue`
- `scripts/export-resources.cjs`
- `package.json`
- `package-lock.json`
- `docs/plans/2026-07-18-project-closeout-execution-plan.md`（§C11）
