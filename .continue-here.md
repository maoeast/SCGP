# Current State
- 任务：C11 旧命名与原生开发依赖清理已代码完成并验证通过；下一批次 C12（更新/签名/升级发布门禁）被外部输入阻塞，暂无可立即执行的下一批次。
- 已完成：`AboutDialog.vue` 文案 `感官能力发展系统` → `SCGP / 星愿能力发展平台`（版本仍从 `updateState.currentVersion` 读，无硬编码）；`scripts/export-resources.cjs` 从 `better-sqlite3` 切到 `sql.js`（沿用 `export-current-emotion-scenes.mjs` 的 `locateFile` 范式），补 `--help` 只读分支，修正头注释 `.js`→`.cjs`/`.sql`→`.ts`，CLI 输入输出契约与 `self-care-ats` 历史路径不变；`npm uninstall --save-dev better-sqlite3` 移除 17 包（含原生编译树）。`appId: com.sic.ads`、localStorage key、历史 migration 日志均未动。
- 产出物：C11 代码提交 + C11 交接文档提交（hash 见 git log）。

# Next Action
- **阻塞**：C12 需外部输入——(a) 客户网络可达的正式 HTTPS 更新目录 URL + 写入凭据；(b) Windows 代码签名证书（`CSC_LINK`/`CSC_KEY_PASSWORD`）；(c) 一台 Windows 验收机或隔离虚拟机。三样齐备前 C12 不得开工，C13 终验随之等待。期间如需推进其他独立技术债（备份覆盖收口、资源文件生命周期、资源收藏、评估入口硬编码等），由用户另起批次。

# Blockers
- C12 被外部输入阻塞（见 Next Action），不可凭代码单方面推进。

# Key Decisions
- About 文案口径定为 `SCGP / 星愿能力发展平台`（与 `package.json` description 一致）；版本号继续走 `useUpdateService().updateState.currentVersion`，不引入新硬编码。
- `export-resources.cjs` 保持 `.cjs`（仅修正注释里的扩展名示例），不改为 ESM；sql.js wasm 经 `locateFile` 指向 `node_modules/sql.js/dist`，与运行时主线 `export-current-emotion-scenes.mjs` 同范式。
- better-sqlite3 彻底移除（devDependencies + lockfile + 脚本 require 三处），符合「零额外原生依赖」红线；历史名/路径/appId/migration 标识符作为兼容事实保留。
- About 文案为用户可见改动，按 closeout 协议记「代码已实现 / 待桌面 UAT」；脚本引擎切换与依赖卸载已由 `--help` + grep + 构建充分验证。

# Relevant Files
- `src/components/AboutDialog.vue`
- `scripts/export-resources.cjs`
- `package.json`
- `package-lock.json`
- `docs/plans/2026-07-18-project-closeout-execution-plan.md`（§C11 已完成 / §C12 阻塞）
