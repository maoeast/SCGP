# Current State
- 任务：激活文件功能（生成端一键导出 .lic、激活端一键导入、HTML 离线生成器）——已完成并提交 4 个 commit：`19aa6c2`（激活页导入）、`27ba4c5`（HTML 生成器）、`7e8e744`（目录清理）、`46ed21c`（交接）。
- 产出物：`src/utils/license-file.ts`、`src/views/{Activation,System}.vue` 导入入口、`license-generator-dist/generator.template.html` + `scripts/build-license-generator-html.mjs`、测试 `license-file-contract` / `license-generator-html`（含签名交叉验证，已挂 `test:core:node`）。验证：相关测试全过、type-check、build:web 通过。
- 注意：产物 `license-generator-dist/scgp-license-generator.html`（内嵌私钥）不入库，本地已生成；`dist/`、`.tmp/` 与历史 txt、public-key.pem 已清理，`generate-license.js` 公钥自动同步已关闭。

# Next Action
- 等待用户确认工作区未提交删除 `license-generator-tauri/TAURI-MIGRATION.md`（整个目录已被外部删除）如何处理：提交删除或保留。

# Blockers
- 无（HTML 产物建议人工双击 `license-generator-dist/scgp-license-generator.html` 实测一次浏览器生成/导出）。

# Key Decisions
- 激活文件格式：.lic = 纯激活码一行；导入器兼容任意含 SPED- 文本，提取只做格式校验，签名验证仍走 LicenseManager 原链（RSA RSASSA-PKCS1-v1_5 + SHA-256）。
- HTML 生成器用 Web Crypto 签名（与 Node/验证端同算法），内嵌私钥、产物不入库；模板入库；私钥轮换后重跑构建脚本。

# Relevant Files
- `license-generator-tauri/`（删除待确认）、`src/utils/license-file.ts`、`license-generator-dist/generator.template.html`、`scripts/build-license-generator-html.mjs`
