---
status: active
last_updated: 2026-04-05
scope: login custom theme — background image upload, card opacity, button color picker
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for: 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**登录页主题系统新增"自定义"主题，支持上传背景图、调节卡片透明度、选择按钮颜色。**

- **本次已完成**:
  - `src/utils/login-theme.ts` — `LoginThemeVariant` 扩展为 `'warm-glow' | 'calm-blue' | 'custom'`，新增 `custom` preset；`applyLoginThemeVariables` 为 custom 从 primaryColor 派生按钮渐变和阴影；新增 `--login-card-bg-opacity` / `--login-custom-bg-image` CSS 变量
  - `src/stores/systemConfig.ts` — 新增 `loginCustomBgImage` / `loginCardOpacity` 状态，load/save `login_custom_bg_image` / `login_card_opacity` 两个新 `system_config` key
  - `src/components/login/GalaxyBackground.vue` — 新增 `customBgImage` prop，custom 主题渲染上传图片为 cover 背景，无图片时降级为深色渐变
  - `src/components/login/LoginCard.vue` — 卡片背景透明度改读 `--login-card-bg-opacity` CSS 变量
  - `src/views/Login.vue` — 传递 `customBgImage` 给 GalaxyBackground
  - `src/views/system/SystemSettings.vue` — 主题下拉新增"自定义"选项；选中时显示背景图片上传 + 按钮颜色选择器；移除了登录页品牌与主题的预览区域；卡片透明度滑块对所有三个主题可见（30%–100%）
  - `vue-tsc --build --force` 类型检查通过

- **当前代码现实**:
  - 三个主题（暖光/静蓝/自定义）共用同一套 CSS 变量驱动
  - 自定义主题：用户上传背景图 → data URL 持久化到 system_config → GalaxyBackground 渲染为 cover 背景图
  - 所有主题均可调节卡片透明度
  - 自定义主题的按钮渐变从用户选色实时派生
  - 登录页品牌与主题设置区已移除预览区

## 下一步

**第一个动作**: Electron 运行时手动验证三个主题切换、自定义背景图上传/删除、卡片透明度滑块、按钮颜色选择的视觉效果

## 遇到的问题 / 阻塞 / 风险

1. **当前无硬阻塞**
2. **当前风险**:
   - 背景图以 data URL 存储在 system_config，大图片可能导致 DB 行过大（当前限制 4MB）
   - 需要运行时验证三个主题的视觉效果和切换流畅度
