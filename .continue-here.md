# Current State
- 任务：SCGP 明天继续处理新发现的若干 bug；当前已先修复打包版首次启动时 SQL.js 资源加载失败与误降级问题
- 已完成：
  - `src/database/sqljs-loader.ts` 改为通过 Vite `?url` 资源地址加载 `sql-wasm.js` 与 `sql-wasm.wasm`
  - `src/database/database-loader.ts` 改为通过 Vite `?url` 资源地址解析 wasm，避免 Electron `file://` 下根路径失效
  - `src/database/init.ts` 在 `PROD` 环境禁用 `MockDatabase` 静默降级，初始化失败时直接抛错
  - 已重新执行 `npm run type-check` 与 `npm run build:electron:win`
  - 已推送提交：
    - `6e8819f` `fix(database): load sql.js assets correctly in packaged app`
    - `fba9e46` `chore(assets): refresh emotional scene images and app icon`
- 产出物：
  - `src/database/sqljs-loader.ts`
  - `src/database/database-loader.ts`
  - `src/database/init.ts`
  - 远端 `origin/main@fba9e46`

# Next Action
- 读取用户明天反馈的第一个新 bug 现象，并先在 `main@fba9e46` 基础上稳定复现它

# Blockers
- None

# Key Decisions
- 打包版 `sql.js` 必须走 Vite 产物 URL：Electron `file://` 下不能再使用 `/sql-wasm.js` / `/sql-wasm.wasm`
- 生产环境初始化失败必须直接暴露：不能再静默降级到 `MockDatabase`
- `release/` 仅保留本地构建产物：本轮明确不提交

# Relevant Files
- `src/database/sqljs-loader.ts`
- `src/database/database-loader.ts`
- `src/database/init.ts`
- `PROJECT_CONTEXT.md`
