# Current State
- 任务：补记自理训练 E2E 验证数据清理结论，并收口 `/self-care/tasks` 卡片展示。
- 已完成：
  - `docs/planning/2026-05-11-self-care-ats-seed-import-analysis.md` 已追加“2 条 E2E 验证数据已清理，当前正式 `task_training` 为 31 条”的结论。
  - `src/views/self-care/TaskList.vue` 已移除搜索栏和状态筛选，页面改为直接展示正式 `task_training` 任务全集。
  - `src/views/self-care/TaskList.vue` 已更新说明文案为“生活自理任务训练通过标准化训练任务培养孩子生活自理与生活适应能力”。
  - `src/views/self-care/TaskList.vue` 已完成卡片按钮与标签样式调整：编辑按钮改为弱化次级按钮，开始训练按钮放大，去掉 ID、去掉禁用按钮、去掉“启用中”角标。
  - 已验证 `npm run type-check` 通过。
  - 已验证 `npm run build:web` 通过。
- 产出物：
  - `src/views/self-care/TaskList.vue`
  - `docs/planning/2026-05-11-self-care-ats-seed-import-analysis.md`

# Next Action
- 启动应用并人工检查 `/self-care/tasks` 页面，确认卡片按钮层级、间距和移动端换行效果符合预期。

# Blockers
- None

# Key Decisions
- 页面统计口径只认正式 `task_training` 数据：当前页面应对应 31 条正式任务。
- 不把 E2E 验证数据重新写回正式库：避免污染正式任务统计与验收口径。
- 卡片操作层级固定为“开始训练”主操作 + “编辑”次级操作：不再在卡片底部暴露禁用/启用按钮。

# Relevant Files
- `.continue-here.md`
- `src/views/self-care/TaskList.vue`
- `docs/planning/2026-05-11-self-care-ats-seed-import-analysis.md`
