# Current State
- 任务：C07 隐私告知门禁 + 系统设置重置入口已完成（门禁 / 重置闭环）。
- 已完成：`sendChat` 首次发送前 `ElMessageBox.confirm` 门禁（按 userId 记忆）；`AiAgentConfig`「重置全部教师的告知确认」入口（全局清 KV，二次确认）；两轮 `npm run type-check` EXIT=0。
- 产出物：commit `945424a`（门禁）、`453daaf`（重置入口）

# Next Action
- 盘点 `ai-tools` / `ai-tool-loop` 各工具返回给 provider 的字段结构，标注学生敏感字段（诊断、完整 student 行等）并定裁剪口径；不引入通用脱敏框架。

# Blockers
- None

# Key Decisions
- 隐私确认按「每个登录用户首次」触发，记忆存 `system_config` KV（key=`ai:privacy_ack:user:<userId>`）。
- 重置为「全局」语义（清除所有教师 KV），放系统设置；门禁仍按 per-user 触发。
- 本批不做字段脱敏、tool result 最小化（留下一步）。

# Relevant Files
- `src/services/ai-tools.ts`
- `src/services/ai-tool-loop.ts`
- `src/database/ai-api.ts`
