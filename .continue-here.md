# Current State
- 任务：修复感官训练「声音辨别游戏」IEP 报告准确率错误写成 `0%` 的问题
- 已完成：`src/components/games/audio/GameAudio.vue` 新增 `sessionAccuracy`，将声音类会话准确率改为 `rhythm -> realAccuracy`、`diff/command -> correctTrials / totalTrials`
- 已完成：`src/views/games/IEPReport.vue` 新增 `normalizeLegacyAudioAccuracy()`，打开旧的 `AUDIO_DIFF / AUDIO_COMMAND` 记录时按 `correctTrials / totalTrials` 兜底修正报告准确率
- 已完成：`npm run type-check` 通过
- 已完成：用户已确认本次修复可通过验收
- 产出物：`src/components/games/audio/GameAudio.vue`
- 产出物：`src/views/games/IEPReport.vue`

# Next Action
- 手工回归 `听指令做动作` 路径：新跑一条 `AUDIO_COMMAND` 记录并核对 IEP 报告中的准确率与 `正确/总数` 一致

# Blockers
- None

# Key Decisions
- 声音类准确率口径：`AUDIO_RHYTHM` 继续使用节奏专用 `realAccuracy`；`AUDIO_DIFF / AUDIO_COMMAND` 使用答题正确率，避免非节奏模式被写成 `0`
- 旧报告兼容：不改数据库 schema，不回写历史记录；在报告读取层对 `TaskID 5/6` 做准确率兜底
- 本轮只修准确率写入与报告读取，不扩展到其他游戏或额外重构

# Relevant Files
- `src/components/games/audio/GameAudio.vue`
- `src/views/games/IEPReport.vue`
