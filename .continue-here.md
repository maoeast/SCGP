# Current State
- 任务：评估入口展示与直链授权收口已完成代码实现。
- 已完成：
  - 新增 `src/features/assessment/assessment-scale-catalog.ts`，集中维护 `AssessmentScaleCode`、`AssessmentScaleCatalogItem`、`ASSESSMENT_TABS`、量表展示归属和授权模块口径。
  - `src/views/assessment/AssessmentSelect.vue` 已改为固定 6 个 Tab + 授权过滤卡片布局，不再保留本地硬编码量表列表。
  - `src/views/assessment/SelectStudent.vue` 已改为复用共享目录，页面标题、模块标签和统一评估跳转都来自同一份目录。
  - `src/router/index.ts` 已去掉 `/assessment` 的整页 `meta.moduleCode = 'sensory'`，并为 `/assessment/select-student` 与 `/assessment/unified/:scaleCode/:studentId` 增加量表合法性与授权拦截。
  - 已跑通 `npm run type-check`（使用本机 Node `v24.14.1` PATH）。
- 产出物：
  - `src/features/assessment/assessment-scale-catalog.ts`
  - `src/views/assessment/AssessmentSelect.vue`
  - `src/views/assessment/SelectStudent.vue`
  - `src/router/index.ts`
  - `.continue-here.md`

# Next Action
- 在 Electron 开发环境下按授权组合做手工冒烟，重点验证：
  - 仅 `life_skills` 授权时 `/assessment` 只显示 `WeeFIM / S-M`
  - 仅 `social` 授权时 `SDQ / SRS-2` 可见，`CBCL / Conners-*` 不可见
  - 去掉 `sensory` 授权后 `csirs / tgmd_3 / gmfm_88 / fine_motor / cnbsr2016` 全部消失
  - 直链 `/assessment/select-student?scale=...` 和 `/assessment/unified/:scaleCode/:studentId` 对非法/未授权量表都会回退 `/assessment`

# Blockers
- None

# Key Decisions
- `/assessment` 现已按“跨模块公共评估入口”处理，不再由全局路由守卫一刀切成 `sensory`。
- 量表展示归属与授权归属继续拆开：
  - `entryTabs` 只填真实 `TRAINING_ENTRY_CODES`
  - `accessModulesAnyOf` 只填顶层业务模块
- 本轮只收口入口展示和统一评估直链授权，不扩到 `AssessmentContainer` 内部流程、评分逻辑或报告页改造。

# Relevant Files
- `src/features/assessment/assessment-scale-catalog.ts`
- `src/views/assessment/AssessmentSelect.vue`
- `src/views/assessment/SelectStudent.vue`
- `src/router/index.ts`
