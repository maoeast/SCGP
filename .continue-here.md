# Current State
- 任务：首页看板「训练进度概览」卡片重做（数据源切统一主表 + 结构化展示）已全部落地；手册 S009/S011 重拍 + docx 就地换图完成；工作区待提交（未 commit、未 push）
- 已完成：① 新增纯函数口径模块 `src/database/training-progress-rules.ts`（窗口/本地日分桶/完成率/环比/模块分布）；② `dashboard-api.ts` 删 `getWeeklyTrainingTrend`，新增 `getTrainingProgress(windowDays)` 与 `getTrainingDayDetail(date)`，快照字段 `weeklyTrend` → `trainingProgress`；③ `Dashboard.vue` 卡片升级（窗口切换 + 4 KPI 含环比 + 次数/时长组合图 + 模块分布 + 当日明细弹层 + 空态 + 容器查询双栏），日程进度条分子改 `summary.todayCount`；④ 守卫测试两套（`scripts/tests/training-progress-contract.test.mjs` 5 组 / `tests/training-progress.test.ts` 11 场景）并登记进 `test:core:*`；⑤ 口径档 `docs/planning/2026-09-21-首页训练进度概览-判定口径.md` + INDEX 注册 + 布局档字段同步；⑥ 真渲染 harness 11 项断言全过（7/30 天切换、点柱钻取弹层、空态、模块分布）；⑦ 截图 fixture 新增 `dashboard-trend` 家族 → S009/S011 重拍（`dashboard-trend-20260921`）→ approvals + promote → docx 就地换图（image13/image15，`--docx` 217/217）
- 产出物：真库只读对账（近 30 天 311 条 / 42 名学生；旧口径覆盖 73.1% → 100%）；截图基线档 §4.6 批次留痕

# Next Action
- 跑 `npm run verify:core` 与 `npm run build:web` 收口，然后向用户报告；提交信息建议：`feat(dashboard): 训练进度概览卡片切统一主表并结构化（KPI/组合图/模块分布/日明细）`

# Blockers
- None

# Dead Ends
- 自建 Electron+Playwright harness 时把 `SCGP_TEST_USER_DATA_DIR` 指到仓库 `.tmp/` → 应用加载 dev server 报 `ERR_CONNECTION_RESET`（页面起不来）；把该目录改到 `output/manual-screenshot-capture/runs/<run>/user-data/<account>` 后正常。要跑真渲染 harness，userData 目录必须放在 `output/manual-screenshot-capture/` 下
- 手册 S009/S011 想「卡片 + 看板同框完整入画」不可行：新卡片比旧版高约 250px，1080 视口放不下（卡片 ~610px + 看板 520px + 间距）；两场景锚点仍取看板，卡片顶部在画面之上（基线档 §4.6 已记）

# Key Decisions
- 口径：**统一主表 `training_session` 为唯一事实源**；时间取 `started_at`；窗口 = 最近 N 个**本地自然日**（含今天），分桶在 JS 侧（不用 SQLite `'localtime'`）；时长用 `duration_ms`；完成率 = `completed` 占比（无会话为 null）；环比取上一等长窗口，无上期数据时不显示
- 展示：窗口枚举 7/30 走选项控件；钻取 = 当日明细弹层（训练记录页无日期/学生筛选，直跳落不到「那一天」）；空态用 `hasData`
- 明确不做（本轮）：异常面板 `created_at` 的 8h 偏移（另开条目）、演示数据时间轴整体重锚（另立专题）、legacy `duration` 单位治理

# Relevant Files
- src/database/training-progress-rules.ts（口径单一真源：窗口/分桶/聚合）
- src/database/dashboard-api.ts（`getTrainingProgress` / `getTrainingDayDetail`；快照 `trainingProgress`）
- src/views/Dashboard.vue（卡片模板 + 样式 + 切换/钻取逻辑）
- scripts/tests/training-progress-contract.test.mjs、tests/training-progress.test.ts（守卫）
- docs/planning/2026-09-21-首页训练进度概览-判定口径.md（口径唯一真源档）
- scripts/manual/capture-user-manual-screenshots.mjs（`dashboard-trend` fixture 家族）、user-manual-screenshot-fixtures.mjs（`plans` / `admin-no-schedule` 挂该家族）
- docs/user-manual/SCGP-用户手册截图采集与维护基线.md（§4.6 本轮留痕；§1 docx 就地换图流程）
