# Current State
- 任务：模拟演示数据脚本（面向视频录制与客户演示）——已完成并全链路验证
- 已完成：
  - `scripts/seed-demo-data.mjs` + `scripts/seed-demo-data/data.mjs`：seed / export / import 三合一，
    幂等（固定高段位 id 区间），`--db` 任意路径，`--seed` 固定随机种子可复现
  - 数据规模：22 学生（男15/女7，三大诊断 18 + 自闭 2 + 智力 2）、7 班（大班~初三）、5 教师（admin123）、
    112 次评估（11 个量表，约一半做前测+后测体现进步）、28 个计划、约 450 条训练记录（感官/器材/情绪/游戏，
    同步写 training_session）
  - 验证：临时库全链路（seed→export→import→一致性 13 项 PASS、幂等 PASS）+ 真实应用 UI 文本断言
    （dashboard/学生/报告页/趋势页/训练记录 16 项 PASS）
  - 顺带修复 3 个产品 bug（验证时发现）：assessment-trend-routes.ts 趋势路由 slug 参数化、
    AssessmentTrendPage.vue 性别判断、csirs/Report.vue 同路由参数切换不刷新
- 产出物：上述文件 + docs/guides/seed-demo-data.md 用法文档（未提交）

# Next Action
- 用户确认演示环境目标 DB 路径后执行：
  `node scripts/seed-demo-data.mjs seed --db <演示环境 database.sqlite> --summary`
  （演示数据已在 output/seed-demo-data-test/ 验证，可按需 export/import 到任意环境）
- 待用户确认是否提交：seed 脚本 3 文件 + 3 个产品 bug 修复 + docs/guides/seed-demo-data.md

# Blockers
- None

# Key Decisions
- 评估分数按「诊断画像」生成，level 与各 driver 官方规则严格对应 → 报告文字与症状呼应
- 纵向前测 2026-03 ~ 后测 2026-07，训练记录 2026-04~07，叙事「评估→训练→进步」
- 幂等清理用 BETWEEN 区间（ID_RANGES 是 [start,end] 对，不能当列表 join）
- csirs t_scores/raw_scores 的 key 必须用维度 code（vestibular/tactile/proprioception/learning/executive），
  报告页按 name_en 读取

# Relevant Files
- scripts/seed-demo-data.mjs（CLI：seed/export/import）、scripts/seed-demo-data/data.mjs（数据定义+生成器）
- docs/guides/seed-demo-data.md（用法说明）
- src/features/assessment/assessment-trend-routes.ts、src/views/assessment/components/AssessmentTrendPage.vue、
  src/views/assessment/csirs/Report.vue（3 个 bug 修复）
