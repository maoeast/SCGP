---
status: active
last_updated: 2026-03-27
scope: training-entry hard-cut unification
---

## Current State

This file only keeps the current valid handoff.
Current continuation should follow code reality, not target-state documents.

Already true:

- top-level authorization still stays on real `moduleCode`, unchanged
- `games / equipment / training-records` menus and routing now use the same 6 internal training entries:
  - `sensory-integration`
  - `emotional-regulation`
  - `social-communication`
  - `fine-motor`
  - `soothing-aids`
  - `life-skills`
- `training_records` and `equipment_training_records` schema/init path now include `entry_code`
- new game saves now write:
  - `resource_id`
  - `resource_type`
  - `session_type`
  - `entry_code`
- new equipment quick-entry saves now write `entry_code`
- training-record menu/stat panels now count and filter by `entry_code`, not only top-level `module_code`
- `src/views/equipment/Records.vue` is now entry-aware:
  - route/query aware for `entry / module / recordId`
  - supports entry switch
  - supports per-entry quick-entry jump
  - supports highlighted detail jump from module training records
- `ModuleTrainingRecords -> EquipmentRecords` now carries `recordId`
- `EquipmentRecordsPanel` now exposes a real detail action
- runtime system name `生活自理适应综合训练` has been replaced with `星愿能力发展训练系统`
- physical-equipment import script now dedupes by `resourceCode` and can merge duplicate references into canonical rows
- `SQLWrapper` save loop bug has been fixed:
  - save start now clears the current dirty batch
  - new changes during save mark `pendingSave` instead of spinning a false extra save loop
- `equipment_training_records` schema/init definition has been corrected to reference `sys_training_resource(id)` instead of old `equipment_catalog(id)`

## Confirmed Reality

- `fine-motor` and `soothing-aids` are still internal entries, not top-level business modules
- current real game-resource coverage is still limited:
  - sensory game resources exist
  - emotional scene / care-scene resources exist
  - `fine-motor / soothing-aids / social-communication / life-skills` do **not** yet have real game-resource coverage in current code/database
- because old records are disposable, current implementation does **not** try to infer old `module_code` into new `entry_code`
- local DB verification for game-side `entry_code` was done with a minimal marked record on a rebuilt dev DB
  - this verifies persistence behavior
  - it does **not** mean those missing entry groups now have real game content

## Local DB Note

- the previously polluted local DB was discarded
- local dev DB was rebuilt from scratch under:
  - `C:\Users\maoea\AppData\Roaming\scgp\database.sqlite`
- physical-equipment resources were re-imported into that rebuilt DB
- stale `database.sqlite.tmp` was removed
- current local DB is no longer the old duplicated-import polluted DB
- however, one explicit verification student plus 2 marked verification records were inserted to verify `entry_code` persistence:
  - one game record with `entry_code = emotional-regulation`
  - one equipment record with `entry_code = fine-motor`
- if next session needs a truly blank local DB for manual UI regression:
  - delete those marked verification rows (and optional verification student), or
  - rebuild/reset local DB once more

## Completed This Session

- rebuilt local dev DB after closing Electron
- re-imported physical-equipment resources into clean local DB
- verified clean DB schema includes `entry_code` on both training record tables
- verified DB-level entry-code persistence on clean local DB:
  - game record writes `entry_code = emotional-regulation`
  - equipment record writes `entry_code = fine-motor`
- refactored equipment records flow:
  - `src/views/equipment/Records.vue`
  - `src/views/training-records/ModuleTrainingRecords.vue`
  - `src/views/training-records/components/EquipmentRecordsPanel.vue`
  - `src/views/training-records/components/GameRecordsPanel.vue`
- fixed clean-DB schema tail in:
  - `src/database/init.ts`
- fixed renderer-side repeated save loop in:
  - `src/database/sql-wrapper.ts`
- ran:
  - `npm run type-check`

## Remaining Work

- decide final hard-cut handling for old historical records:
  - physically delete old game/equipment/training records, or
  - provide one explicit dev/test reset path and stop discussing compatibility
- review whether any remaining old module-based record/detail entry points still exist outside the already-fixed equipment records path
- decide UI treatment for game entries with zero real resources:
  - keep visible as-is, or
  - visibly mark as `暂无资源 / 未落地`
- if product expects true 6-entry game parity, real game resources/content still need to be added for:
  - `social-communication`
  - `fine-motor`
  - `soothing-aids`
  - `life-skills`
- optionally harden Electron main-process atomic write path too
  - current user-visible loop came from `SQLWrapper`, now fixed
  - if future logs still show write races, serialize `save-database-atomic` per-db path in main process

## Next Action

Start with:

1. read `AGENTS.md`, bootstrap, `README.md`, `.continue-here.md`
2. confirm whether next step is:
   - hard-delete/reset old disposable records, or
   - continue remaining module-based flow cleanup first
3. if save errors still reproduce after restart, harden Electron main-process atomic write serialization
4. keep all statements aligned with current code and current real resource coverage

## Next Session Read Order

1. `AGENTS.md`
2. `docs/planning/2026-03-23-scgp-context-bootstrap.md`
3. `README.md`
4. `.continue-here.md`
5. `PROJECT_CONTEXT.md` only if broader background is needed

## Next Session Prompt

```text
先按 AGENTS.md 做最小上下文启动：
1. 读取 AGENTS.md
2. 读取 docs/planning/2026-03-23-scgp-context-bootstrap.md
3. 读取 README.md
4. 读取 .continue-here.md
5. 如确实需要更大背景，再读取 PROJECT_CONTEXT.md

然后严格以代码现状为准继续当前未完成工作，不要把目标态当现状。

当前真实状态：
- 顶层授权模型仍按真实 moduleCode，不变
- games / equipment / training-records 的菜单和路由已经切到 6 个 internal training entries
- training_records / equipment_training_records 已加 entry_code，新写入记录会写 entry_code
- 训练记录菜单/面板已按 entry_code 统计和筛选
- src/views/equipment/Records.vue 已改成 entry-aware
- runtime 系统名称已改为“星愿能力发展训练系统”
- physical-equipment 导入脚本已支持按 resourceCode 去重并迁移引用
- clean local dev DB 已重建，并已做过最小 entry_code 写入验证

但当前仍未完成：
- 旧记录的硬切处理还没最终收口，可直接物理删除，不做兼容推断
- 当前真实游戏资源仍只覆盖 sensory games + emotional scenes/care scenes，不要把 6 组游戏资源写成已完整交付
- 仍需检查是否还有 remaining old module-based record/detail flow
- 如登录后仍出现数据库原子写入竞态，再继续收口 Electron main 进程的 save-database-atomic 串行化

本地库注意：
- 当前本地 dev DB 不是旧污染库
- 但里面有 1 个明确标记的验证学生和 2 条验证记录，用于验证 entry_code 写入
- 如果要做“完全空白库”的手工回归，先删掉它们或再重建一次

优先继续：
- 决定并收口旧记录 hard-cut/reset 方案
- 继续清理 remaining old module-based record/detail flow
- 如仍复现保存报错，修 main process atomic write serialization

最后至少跑：
- npm run type-check
```
