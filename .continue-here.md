# Current State
- 任务：收尾 `src/views/self-care/TaskExecution.vue` 的右侧栏信息密度调整，当前已去掉“执行概览”右侧的 `in_progress` 文案。
- 已完成：`TaskExecution.vue` 保持训练标题覆盖在主图左上角；“切换”按钮已接入退出确认弹窗；右侧栏“执行概览”标题行仅保留标题文本；`vue-tsc --build` 已通过。
- 产出物：`src/views/self-care/TaskExecution.vue`

# Next Action（必须原子级——第一步可执行的单一动作）
- 在 `src/views/self-care/TaskExecution.vue` 中检查右侧栏标题行的最终视觉占位，确认没有因为去掉状态文本而产生不必要的空隙。

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 左上角标题覆盖到主图上：符合当前页面的视觉目标。
- “切换”保留为退出确认入口：避免训练中误退出。
- 右侧栏不再显示运行态字符串：减少无意义状态噪声。

# Relevant Files（最小集——仅列下一步会用到的）
- `src/views/self-care/TaskExecution.vue`
