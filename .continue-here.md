# Current State
- 任务：AI 对话内嵌评估趋势图表（路线 C：工具结果驱动出图）
- 已完成（上一轮，已提交 260cbbc）：
  - AI 纵向分析工具 get_assessment_trend（ai-tools.ts）+ 13 量表适配器（assessment-score-adapters.ts/normalize.ts），声明式归一化框架支持 object/flat-number/column 三模式
  - 通用趋势图页 AssessmentTrendPage.vue + catalog 派生 13 个趋势路由（assessment-trend-routes.ts）
  - 13 个 Report.vue 统一「查看历史」按钮跳转趋势页；CSIRS 旧 /history/ 重定向到 /trend/
  - crt/cognitive_self 明确不接入纵向分析（实验性占位常模）
  - type-check + test:core:ts(11场景) + 契约测试(16个) 全过
- 产出物：commit 260cbbc（26 文件 +1603/-24）

# Next Action
- 阅读 AiChatTranscript.vue（src/features/ai/components/）的渲染链路与 toolSteps 机制，设计「工具结果含纵向分数时，在 AI 回复下方自动渲染 echarts 趋势图」的最小接入点

# Blockers
- None（路线 C 方案已与用户确认，不引入新依赖，复用已有 echarts + 趋势页组件逻辑）

# Key Decisions
- 路线 C（工具结果驱动出图）优于 Mermaid（路线 A）：核心场景是评估分数趋势线，echarts 比 Mermaid xychart 更合适，且数据来自 get_assessment_trend 工具、零格式错误风险
- 不引入 mermaid 依赖；项目已装 echarts/vue-echarts，复用现有技术栈
- 图表数据来自工具结果（非 AI 手写），保证 100% 准确；Mermaid 流程图作为后续可选补充（解释训练流程/IEP 决策树等非数据图场景）

# Relevant Files
- src/features/ai/components/AiChatTranscript.vue（消息渲染 + toolSteps，路线 C 的主要接入点）
- src/services/ai-tool-loop.ts（工具循环，工具结果回传链路）
- src/stores/ai.ts（对话状态，工具结果存储位置）
- src/services/ai-tools.ts（get_assessment_trend 工具，返回 LongitudinalScorePayload）
- src/views/assessment/components/AssessmentTrendPage.vue（趋势图 echarts 逻辑，可抽取复用）
