# Current State
- 任务：v1.1.1 打包 + 服务器发布（已完成）
- 发布内容：教师数据隔离（69bae35）+ AI 记忆 v4.2 改绑/解绑（4dc700b）
- 验证：release:verify 全过；服务器两源（https://maohedong.top/scgp/win 与 http://124.220.104.199/scgp/win）均返回 version: 1.1.1
- 已提交推送：2e0bc0c（version bump 1.1.1 + .gitignore 加 release-build/）

# Next Action
- 人工真机验证（可选）：验收机装 1.1.0 → 检查更新应弹 1.1.1 → 下载安装 → 重启后"已是最新版本"

# Blockers
- 本机 release/win-unpacked 与 release-build/win-unpacked 的 app.asar 被未知进程锁定无法删除（疑似 Windows Defender 实时扫描；release/ 与 release-build/ 均已 gitignore，不影响仓库与发布）。下次打包前若仍锁，直接换新输出目录（-c.directories.output=release-build2）即可

# Key Decisions
- 本次打包踩坑与绕行：
  - 首次打包失败：运行中的 electron.exe 锁 release/win-unpacked → taskkill 后仍锁 → 换 -c.directories.output=release-build 绕开，产物拷回 release/ 后 verify
  - SSH：tailscale 路径（100.114.108.86）kex 阶段被服务器关闭（疑似 fail2ban 拉黑 tailscale IP）；openclaw.pem 权限过宽被 OpenSSH 忽略（icacls 改不了原文件 ACL，Access denied）→ 复制到 C:/Users/maoea/.ssh/openclaw-scgp.pem 并 icacls /inheritance:r /grant:r maoea:R 收权 → 公网 IP 124.220.104.199 直连成功
  - 密钥副本 openclaw-scgp.pem 已生效可用；原 E:/Downloads/openclaw.pem ACL 未改动（若后续要改需管理员）
- 上传走公网 22 端口（未走 tailscale）；627MB 单次 scp 成功
- 版本号：package.json + package-lock.json 均 1.1.1

# Relevant Files
- package.json / package-lock.json（version 1.1.1）、.gitignore（release-build/）
- release/scgp-setup-1.1.1.exe（627MB，本地产物）+ latest.yml
- .claude/skills/release-deploy/SKILL.md（流程参考；注意 tailscale 路径本次不可用）
