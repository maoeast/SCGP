# Current State
- 任务：继续自理训练模块阶段 1，收口 `task_training` 平台主链合同常量使用。
- 已完成：`src/features/self-care/task-training-contract.ts` 定义 `task_training` 资源类型、`life_skills` 授权模块、`life-skills` 入口、复用 `game` 展示主链的常量，以及资源元数据、步骤、执行结果 TypeScript 类型。
- 已完成：`src/utils/resource-center-business.ts`、`src/views/resource-center/training-resource-ui.ts`、`src/utils/training-launch.ts`、`src/database/dashboard-api.ts`、`src/views/plan/PlanList.vue`、`src/features/self-care/self-care-routes.ts` 已改为使用 `task-training-contract.ts` 导出的相关合同常量。
- 已完成：资源中心业务、资源中心 UI、训练启动、仪表盘可启动资源类型、计划资源筛选、自理路由的回归测试已补齐合同常量约束。
- 已完成：`docs/planning/2026-05-08-self-care-training-module-implementation-plan.md` 已显式标注 A1 迁移设计和 ATS 到 SCGP 映射清单均已同步到任务计划。
- 最新提交：`44fa45d feat(self-care): 收口 task_training 主链合同常量`，已推送到 `origin/main`。

# Next Action（必须原子级：第一步可执行的单一动作）
- 进入阶段 2：先读取 `src/views/resource-center/TrainingResources.vue` 与 `src/database/resource-api.ts`，确认 `task_training` 任务资源 CRUD 的创建、编辑、保存落点。

# Blockers
- None

# Key Decisions（≤ 3 条，只保留影响后续实现的）
- 自理训练继续挂 `life_skills` 授权：当前授权体系只识别既有业务模块代码。
- `task_training` 继续复用训练资源的 `game` 展示主链：阶段 1 目标仍是可见、可选、可启动，不提前扩展独立展示主链。
- `task_training` 步骤结构以 `meta_data.steps[]` 为一期事实源：不恢复旧 `task_step` 表作为新主链。

# Relevant Files（最小集：仅列下一步会用到的）
- `.continue-here.md`
- `src/features/self-care/task-training-contract.ts`
- `src/utils/resource-center-business.ts`
- `src/views/resource-center/training-resource-ui.ts`
- `src/utils/training-launch.ts`
- `src/database/dashboard-api.ts`
- `src/views/plan/PlanList.vue`
- `src/features/self-care/self-care-routes.ts`
- `docs/planning/2026-05-08-self-care-training-module-implementation-plan.md`
