# Current State
- 任务：资源列表图片/视频一次性加载导致低配机假死 —— 已实现懒加载 + 协议瘦身（**未提交**，8 个文件改动在工作区）
- 已完成：
  - 根因：列表页全量渲染缩略图 + 每个 `resource://` 请求都打 console.log 并做 2 次 fs.access（无缓存），
    低配机上主进程事件循环被 IO+日志占满 → 渲染进程 IPC 排队 → UI 假死
  - `electron/main.mjs`：resource:// 协议去掉成功请求日志；加 30s TTL 路径解析缓存（miss 不缓存，
    避免新导入资源查不到；>500 条惰性清理）
  - 新增 `src/directives/lazy-image.ts`（v-lazy-image，IntersectionObserver，提前 200px 预加载，
    不支持 IO 的环境降级直载），`src/main.ts` 全局注册
  - 接入懒加载：TeachingMaterials.vue（原生 img → v-lazy-image）、ResourceSelector.vue（器材列表 img）；
    el-image 加 lazy：SceneSelector.vue（情绪场景/表达关心）、TaskList.vue（生活自理）、TrainingResources.vue（训练资源）
  - 视频类资源本就不在列表加载（缩略图 jpg + 点击预览），未改动
- 产出物：type-check ✓、build:web ✓（含 sanitize）、node --check electron/main.mjs ✓

# Next Action
- dev 环境实测资源列表滚动加载（教学资料卡片、情绪场景、生活自理、器材选择）确认无破图/无闪烁，
  并观察主进程 Console 不再刷 resource:// 请求日志；实测通过后提交

# Blockers
- 本会话无 TTY/无 Electron GUI，无法截图实测 —— 需用户或后续会话在 dev 环境验证

# Key Decisions
- 原生 `<img>` 用自定义 v-lazy-image 指令；`<el-image>` 用组件自带 lazy 属性（不重复造轮子）
- 协议缓存只缓存"解析成功"的路径，miss 不缓存（保证导入新资源后立即可见）；TTL 30s 兼顾风暴缓解与失效
- 旧版 admin/ResourceManager.vue 路由已 redirect 到 /resource-center，不可达，未改
- 训练计划 PlanList.vue 的封面图同样无懒加载（未在本次范围，用户未要求，可作后续项）

# Relevant Files
- electron/main.mjs（协议缓存，electron/main.mjs:526 附近）
- src/directives/lazy-image.ts（新）
- src/main.ts（注册指令）
- src/views/resource-center/TeachingMaterials.vue、src/views/resource-center/TrainingResources.vue
- src/components/resources/ResourceSelector.vue、src/views/emotional/SceneSelector.vue、src/views/self-care/TaskList.vue
