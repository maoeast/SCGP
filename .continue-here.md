# Current State
- 当前任务：开始实现 SCGP cross-entry custom games Wave 1 最小目标 `F01_CLOUD_ERASE`
- 已完成到哪一步（具体到函数 / 文件 / 行为）：
  - `src/views/games/GameLobby.vue` 已补最小启动适配；`fine-motor` 等有 registry custom game 的入口会走 registry-backed custom game，而不是旧的 `/games/play`
  - `src/views/emotional/games/GameContainer.vue#getReturnLocation()` 已统一返回 `/games/lobby/:studentId`，并保留 `buildReturnQuery()` 中的 `entry / module / studentId / studentName`
  - `npm run type-check` 已通过
- 当前产出物（代码 / 文件）：
  - `src/views/games/GameLobby.vue`
  - `src/views/emotional/games/GameContainer.vue`

# Next Action
- 下一步第一个动作（必须原子级，例如：修改 xx 文件的 xx 函数）：修改 `src/data/custom-game-registry.ts`，新增 `F01_CLOUD_ERASE` 的 registry 定义

# Blocking / Risks
- 工作区已有大量 `.codex/**` 无关改动；后续提交必须继续只 add 业务相关文件

# Key Decisions
- 入口适配层只做 `GameLobby` 启动接线和 `GameContainer` 返程；原因：遵守“不做额外重构”
- custom game 启动 query 统一保留真实 `entry / module`；原因：后续 `F01_CLOUD_ERASE` 需要按真实入口运行与返程
- custom game 结束后统一回 `/games/lobby/:studentId`；原因：先闭环跨入口大厅链路，不保留 emotional 特例

# Relevant Files
- `src/data/custom-game-registry.ts`
- `src/router/index.ts`
- `src/views/emotional/games/WipeSadnessPage.vue`
- `src/components/emotional/games/WipeSadnessGame.vue`
- `docs/planning/2026-04-14-cross-entry-custom-games-wave1-min-target.md`
