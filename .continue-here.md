# Current State
- 任务：AI 智能体 Phase 5 技能包已完成并推送（commit `96a2d18`，origin/main 同步）
- 已完成：Phase 1-5 全完成。Phase 5 = 按 agent 挂载技能，替代全局硬编码 `AI_TOOLS`：
  - 5A 工具维度：`ai_skill`(kind='tool') / `ai_agent_skill` 两表 + `filterTools` 按 agent 过滤 + `dispatchTool` 越权防御 + 旧 agent 全工具回填（不回归）
  - 5B 知识维度：`src/data/skills/` 4 个内置专业角色包（言语治疗师 / 蒙特梭利教师 / 特教老师 / 发育筛查评估），`import.meta.glob` build 时读取 → upsert 入 `ai_skill`(kind='knowledge') → `sendChat` 注入 systemPrompt（120k 字符截断）；UI「挂载技能」分工具 / 知识两组多选
- type-check 全绿；真机验证通过（用户确认）

# Next Action
- 待用户定方向。候选：
  1. `developmental-screening-assessment` 结构修正：其 3 个中文 reference（健康领域 / 社会领域核心经验、观察量表深度分析）在技能**根目录**而非 `references/`，当前 glob `./*/references/*.md` 未覆盖 → 只注入了它的 SKILL.md。建议把 3 个 md 移入 `references/`（统一结构）或扩 glob `./*/*.md`（需与 SKILL.md 去重）。
  2. Phase 5C（plan §不做）：技能库整页 CRUD（查看 / 编辑正文 / 启停 / 自定义知识技能）、references 按需勾选注入。
  3. 其他专题。

# Blockers
- None

# Key Decisions
- 知识技能**默认不挂**（新建 agent 只默认工具；知识手动按需挂，控 token）
- 知识种子用 INSERT OR IGNORE + UPDATE 保留 id 的 upsert（非 OR REPLACE——后者改 id 致绑定 orphan；sql.js FK 默认 OFF 不 CASCADE）
- 源文件是事实源：每次启动 UPDATE 刷新正文，改 SKILL.md 重启即生效（只读 UI 无冲突）
- 工具过滤(5A) 与知识技能(5B) 正交共存于 `ai_skill` 表，kind 区分；5A 保留不回滚
- 注入范围 = SKILL.md 主体 + 全部 references（用户选，接受 token 成本）

# Relevant Files
- `src/data/skills/index.ts`（内置知识技能数据源：glob + frontmatter 解析）
- `src/data/skills/<技能名>/`（SKILL.md + references/*.md）
- `src/database/init.ts`（`TOOL_SKILL_SEED` + `KNOWLEDGE_SKILL_SEED` upsert）
- `src/database/ai-api.ts`（`listToolSkills` / `listKnowledgeSkills` / `getAgentToolCodes` / `getAgentKnowledgePrompt` / `getAgentSkillIds` / `setAgentSkills`）
- `src/services/ai-tools.ts`（`filterTools` + `dispatchTool` 越权防御）
- `src/services/ai-tool-loop.ts`（`runToolLoop` 按 agent tools）
- `src/stores/ai.ts`（`loadAll` toolSkills/knowledgeSkills；`sendChat` 注入 + 工具过滤）
- `src/views/system/AiAgentConfig.vue`（挂载技能 option-group 分组）
- plan：`C:\Users\maoea\.claude\plans\jolly-gathering-book.md`（Phase 5B）
