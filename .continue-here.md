---
status: active
last_updated: 2026-04-06
scope: emotional module — emotion_scene / care_scene 训练布局重构 + AI 场景配图
---

> Responsibility: 当前对话的交接状态文件
> Read when: 继续工作并需要最快恢复当前任务上下文
> Not responsible for 替代 AGENTS.md、PROJECT_CONTEXT.md、HANDOFF.md

---

## 当前任务状态

**情绪行为模块训练体验升级：常驻场景图 + `care_scene` 三步引导 + Gemini AI 场景配图**

- **本次已完成**:
  - 恢复并合并了情绪训练新版双栏布局：
    - `src/components/emotional/engine/EmotionalInteractionEngine.vue`
    - `src/components/emotional/engine/SceneSupportPanel.vue`
    - `src/components/emotional/engine/CareStepGuidePanel.vue`
  - 情绪训练页现在支持：
    - 左侧固定大场景图
    - 场景描述 / 线索 / `care_scene` 视角提示固定在左侧
    - 顶部进度改为步骤圆点，完成态显示绿色勾号
    - 右侧只保留当前题目区
  - `care_scene` 的三步引导区已接回工作树：
    - 第一步：观察场景
    - 第二步：感受词贴片
    - 第三步：选择关心表达
  - `care_scene` 运行时自动生成字段已接入：
    - `receiverName`
    - `emotionChips`
    - `comfortTip`
    - 相关文件：
      - `src/features/emotional/care-scene-generated-fields.ts`
      - `src/database/emotional-resource-data.ts`
      - `src/views/resource-center/editors/emotional-resource-contract.ts`
      - `src/features/emotional/adapters/compileCareScene.ts`
      - `src/utils/emotional-resource-pack.ts`
  - 已接入 Gemini 官方图像生成 API 到资源中心编辑器：
    - 新增 `src/services/scene-image-generation.ts`
    - 在 `electron/main.mjs` 新增 `ai:generate-scene-images` IPC
    - 在以下编辑器中新增 `AI 生成` 按钮和 3 张候选图选择区：
      - `src/views/resource-center/editors/EmotionSceneEditor.vue`
      - `src/views/resource-center/editors/CareExpressionEditor.vue`
  - 保存资源时，AI 生成图会同步写入：
    - `meta_data.imageUrl`
    - `sys_training_resource.cover_image`
    - 相关文件：`src/views/resource-center/TrainingResources.vue`
  - 已完成一次真实联调闭环验证（`emotion_scene`）：
    - 使用 Gemini key 成功生成候选图
    - 选图后保存成功
    - 数据库确认 `cover_image` 与 `meta_data.imageUrl` 已写成 `resource:///uploaded/ai-scenes/...`
    - Electron `resource://` 协议成功从 `~/.config/scgp/resources/uploaded/ai-scenes/...` 读取该图
    - 重新进入 `情绪场景` 训练页后，左侧常驻场景区已正常显示 AI 图

- **当前仍未完成但代码已在工作树中**:
  - `care_scene / 表达关心` 的 AI 配图虽然已接入编辑器，但尚未走完真实“生成 → 选图 → 保存 → 进入训练页显示”的端到端验证
  - `care_scene` 虽然 UI 引导已改成 3 步，但 runtime 底层仍保留 `receiver_preference` 独立步骤，尚未真正收口成“第 3 步内反馈闭环”

## 下一步

**第一个动作**: 运行 `npm run electron:dev`，进入任意 `care_scene / 表达关心` 场景，完成一次真实联调：
1. 在资源中心编辑器里点击 `AI 生成`
2. 选择一张候选图并保存
3. 再进入 `表达关心` 训练页
4. 确认左侧常驻场景区能显示 AI 图
5. 确认右侧三步引导、情绪贴片和角色名展示都正常工作

## 遇到的问题 / 阻塞 / 风险

1. **仓库级 type-check 仍被旧问题阻塞**：
   - `src/components/emotional/games/EnergyBallGame.vue`
   - `src/components/emotional/games/VisualSupportOverlay.vue`
   - `src/composables/useEmotionDetector.ts`
   这些问题不是本轮引入，但会继续阻塞全仓绿灯验证
2. **`care_scene` 端到端尚未实测** — 目前只完成了 `emotion_scene` 的 AI 配图闭环验证，`care_scene` 仍需按同样流程跑一遍
3. **运行时依赖 Gemini key** — 需要通过环境变量提供：
   - `GEMINI_API_KEY`
   - 或 `GOOGLE_API_KEY`
   当前默认模型为 `gemini-3.1-flash-image-preview`
4. **生成图必须人工审核** — 当前模型已能生成“中国校园 / 家庭 / 公共场所、中国人物、写实风格”候选图，但仍有模型偶发失真风险，不适合全自动上线
5. **Node 版本有硬约束** — 本仓库运行 `electron:dev` 必须使用 Node 24；若落回系统 Node 18，Vite 会因 `crypto.hash is not a function` 启动失败

## 本次对话做出的关键决策及原因

1. **AI 配图统一走 Google 官方 Gemini 图像生成 API**
   - 原因：用户明确提到 Gemini / Nano Banana；官方链路已经足够完成文生图，不需要再接额外第三方服务

2. **图像生成调用放在 Electron Main，不放在渲染进程**
   - 原因：避免把 API key 暴露给前端代码；渲染层只通过 IPC 请求候选图

3. **生成结果保存到 userData 资源目录，并统一走 `resource://` 协议**
   - 原因：与当前资源管理体系一致，开发环境和生产环境都能复用同一套资源访问逻辑

4. **不自动覆盖场景图，而是生成 3 张候选图供人工选择**
   - 原因：这是交付亮点，但仍需人工控质；候选图方案更适合儿童训练产品

5. **保存时同时写 `cover_image` 和 `meta_data.imageUrl`**
   - 原因：资源列表 / 场景选择页 / 训练页依赖点不同，双写能避免“封面变了但训练页没变”或反之

6. **恢复上一轮 stash 中的双栏训练布局，并与 AI 配图功能合并**
   - 原因：之前新版布局错误地留在 stash 里，导致训练页回退到旧卡片模式；本轮已把它恢复并重新并入当前工作树
