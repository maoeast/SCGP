# Current State
- 任务：收口 `/self-care/tasks` 卡片交互与筛选区文案，只保留面向训练启动的任务画廊 UI。
- 已完成：
  - `src/views/self-care/TaskList.vue` 已将任务卡改为整卡点击进入训练，交互动画对齐情绪场景选择页卡片。
  - `src/views/self-care/TaskList.vue` 已移除卡片底部“开始训练”按钮，编辑入口固定到卡片右上角。
  - `src/views/self-care/TaskList.vue` 已将禁用任务保留在列表中，但禁用卡片不可进入训练，只保留编辑入口。
  - `src/views/self-care/TaskList.vue` 已移除筛选区顶部标题、说明文案、顶部计数、刷新按钮和“清除分类”提示。
  - `src/views/self-care/TaskList.vue` 已将步骤数量文案改为“7 个步骤 / 8 个步骤”格式。
  - 已完成人工确认：卡片动画、整卡进入训练、禁用态编辑入口、筛选区精简均符合预期。
  - 已验证 `npm run type-check` 通过。
- 产出物：
  - `src/views/self-care/TaskList.vue`

# Next Action
- 先向用户确认是否继续推进 self-care 下一项工作范围（如执行页、记录详情页或其他模块任务）。

# Blockers
- None

# Key Decisions
- `/self-care/tasks` 主交互固定为“整卡点击进入训练 + 右上角编辑入口”：避免主次操作混在卡片底部。
- 已禁用任务继续显示但不可启动训练：只保留编辑入口，避免误启动。
- 分类区只保留胶囊标签本身：不再显示顶部计数、刷新、清除分类或解释性文案。

# Relevant Files
- `.continue-here.md`
- `src/views/self-care/TaskList.vue`
